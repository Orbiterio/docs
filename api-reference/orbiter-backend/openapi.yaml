openapi: 3.1.0
info:
  title: Orbiter API
  version: 0.1.0
  license:
    name: Proprietary
  description: |
    HTTP API for Orbiter. All endpoints are versioned under /v1/.

    This file is the root of the spec — paths are composed from
    per-feature fragments (`<feature>.openapi.yaml`) via $ref so each
    feature's contract lives next to its name. Shared schemas
    (Error, responses, security) stay here so every feature picks
    them up without duplication.

    To bundle into a single deployable file (e.g. for Swagger UI,
    Postman import, or openapi-generator):

        npx @redocly/cli bundle api/openapi.yaml -o api/dist/openapi.yaml
servers:
  - url: http://localhost:8080/v1
    description: Local
  - url: https://orbiter-backend-cprh6jsjpa-uc.a.run.app/v1
    description: Staging (Cloud Run)
tags:
  - name: Account Verification
    description: |
      Identity verification on signup — a new user connects GitHub,
      LinkedIn, or X once; the backend compares the provider name with the
      WorkOS signup name before person resolution/enrichment runs.
  - name: Activities
    description: |
      Contact activity orbits — email and calendar-event feeds sourced from
      Nylas. Cursor-paginated, owner-scoped. Requires a verified WorkOS token.
  - name: Addresses
    description: Postal addresses on persons/companies + private notes. Requires a verified WorkOS access token.
  - name: Admin
    description: Operator-only utilities. Add an auth guard before production use.
  - name: Auth
    description: Sign-up, sign-in, refresh, email verification, OAuth.
  - name: Calendars
    description: Calendar and event management via Nylas.
  - name: Collections
    description: Collections CRUD. Requires a verified WorkOS access token.
  - name: Companies
    description: Company browse and detail. Requires a verified WorkOS access token.
  - name: Company Contacts
    description: The caller's saved companies. Requires a verified WorkOS access token.
  - name: Connections
    description: Orbiter Connect — user-to-user connection requests and established connections. Requires a verified WorkOS access token.
  - name: Copilot
    description: Durable chat state for copilot surfaces — threads, leased runs, checkpointed messages — plus the Outcome Chat intake surface on suggestion requests. Trusted writes additionally require the harness signature headers (X-Copilot-Timestamp, X-Copilot-Signature). Requires a verified WorkOS access token.
  - name: Contacts
    description: The caller's saved contacts (CRM). Add contacts and list with detail. Requires a verified WorkOS access token.
  - name: Dates
    description: Important dates a user tracks about their contacts, and the shared date labels. Requires a verified WorkOS access token.
  - name: Discover
    description: Deep-research records. Requires a verified WorkOS access token.
  - name: EdgeSync
    description: |
      Operator surface over the Edge Sync funnel: schedule a rebuild of a
      user's graph and observe outstanding projection work. Requires a
      verified WorkOS access token.
  - name: EmailAddresses
    description: Email addresses on persons/companies. Requires a verified WorkOS access token.
  - name: EmailFolders
    description: Email folder/label management via Nylas.
  - name: Emails
    description: Email-template CRUD. Requires a verified WorkOS access token.
  - name: EmailSummary
    description: AI-generated email thread summaries.
  - name: EmailThreads
    description: Email thread listing and detail via Nylas.
  - name: EmailWorkspaces
    description: Email workspace profiles grouping connected accounts.
  - name: Experiences
    description: A person's work and education history. Requires a verified WorkOS access token.
  - name: Files
    description: |
      File-ingestion pipeline. Register a GCS object and the worker
      extracts → classifies → chunks → embeds, then completes the file.
      A fundraising deck additionally queues a pitch profile, built as a
      separate stage: `status: completed` does not mean its profile is ready.
  - name: GetStream
    description: Inbound Stream Chat (GetStream) webhooks recording chat messages as comments.
  - name: Imports
    description: |
      Contact imports from external sources (LinkedIn CSV, Apple Contacts
      vCard, Nylas address books). Signed-URL upload, async processing,
      per-row report. Requires a verified WorkOS access token.
  - name: Suggestions
    description: The v2 suggestion feed and batch inspection for Leverage Loops and Outcomes, scoped to an organisation the caller is a member of. Requires a verified WorkOS access token.
  - name: Leverage Loops
    description: Copilot suggestion requests in loop mode with their suggestions, actions, trajectories and follow-up questions. Requires a verified WorkOS access token.
  - name: Links
    description: Web links on persons/companies. Requires a verified WorkOS access token.
  - name: Voice
    description: ElevenLabs speech and transcription proxy. Requires a verified WorkOS access token.
  - name: Memories
    description: |
      Semantic memories about a contact, a company, or the caller themselves,
      read from the mem0 projection of the extraction ledger. Requires a
      verified WorkOS access token.
  - name: MergeSuspects
    description: Suspected duplicate contacts for user review.
  - name: Nodes
    description: Public entity neighborhoods read through Universe.
  - name: Notes
    description: Notes CRUD. Requires a verified WorkOS access token.
  - name: NylasGrants
    description: Nylas OAuth grants — connected email accounts.
  - name: Organizations
    description: Organization creation and WorkOS mirroring. Requires a verified WorkOS access token.
  - name: Outcomes
    description: Investment-thesis building and pitch-to-thesis vector matching (ported from Xano; public endpoints under optional auth).
  - name: Persons
    description: Person browse and detail. Requires a verified WorkOS access token.
  - name: Relationships
    description: The "How I Know <name>" relationship interview and its record. Requires a verified WorkOS access token.
  - name: Phones
    description: Phones on persons/companies + private notes. Requires a verified WorkOS access token.
  - name: Projects
    description: |
      Project work: personal ToDos, project tasks, pipelines and deals. A task
      is either project-scoped or personal, and a personal task is readable
      only by its owner. While the workflow API capability is off, every route
      answers 404 "projects is not available". Requires a verified WorkOS
      access token.
  - name: Teams
    description: Teams listing. Requires a verified WorkOS access token.
  - name: Travels
    description: Travels CRUD. Requires a verified WorkOS access token.
  - name: Users
    description: User CRUD. Requires a verified WorkOS access token.
  - name: Webhooks
    description: |
      Outbound webhook subscriptions, delivery inspection, and
      manual retry of dead deliveries.
  - name: X Connect
    description: |
      Store the caller's X grant (from the command palette) and sync their
      X followers and DMs.
paths:
  /nodes/{id}/graph:
    get:
      operationId: getNodeGraph
      tags:
        - Nodes
      summary: Read an entity's public relationships
      description: |
        Requires a verified, onboarded app user. Reads the attributed Universe
        subgraph API using the backend's existing consumer credentials. Expands
        breadth-first, excluding hidden nodes and geographic/expertise edges.
        Missing graph entities return empty arrays; upstream failures return 500.
        At most 64 neighborhoods are read, with at most 200 neighbors per read
        and four times the node limit in edges. Truncated is true when any
        bound omits results. No embeddings or private user graphs are returned.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Canonical entity UUID, or an alias resolved by Universe.
          schema:
            type: string
            format: uuid
        - name: hops
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 4
            default: 1
        - name: limit
          in: query
          description: Maximum number of displayed nodes, including the root.
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 200
      responses:
        '200':
          description: Bounded neighborhood with directed relationships.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/Internal'
  /sign-up:
    post:
      tags:
        - Auth
      summary: Sign up
      description: |
        Creates a WorkOS identity, mints the local users.users row, and
        fans out provisioning side-effects (Knock identify, personal org,
        graph node) via Pub/Sub. WorkOS emails a verification code and the
        response carries a `pending_authentication_token`: the client posts
        that token plus the code to /verify-email to verify and log in (no
        second sign-in round-trip). If verification isn't required, `tokens`
        is returned directly and the user is already logged in.
      operationId: signUp
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignUpRequest'
      responses:
        '201':
          description: User created (verification pending, or logged in).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignUpResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: |
            Email already registered; or, with an invitation_token, the
            invitation is no longer pending / the account already signed in.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /sign-in:
    post:
      tags:
        - Auth
      summary: Sign in
      description: |
        Authenticates against WorkOS. Returns an access/refresh token pair
        on success. If the user's email isn't verified, returns 403 with a
        pending_authentication_token the frontend uses to drive the
        verify-email flow. If the user belongs to multiple organizations,
        returns 403 with the organization list to drive the select-org flow.
      operationId: signIn
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignInRequest'
      responses:
        '200':
          description: Authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokensResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid credentials.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Email verification or organization selection required.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/EmailVerificationRequiredResponseEnvelope'
                  - $ref: '#/components/schemas/OrganizationSelectionRequiredResponseEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /sign-in/refresh:
    post:
      tags:
        - Auth
      summary: Refresh tokens
      description: Exchange a refresh token for a new access/refresh pair
      operationId: refreshSession
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
      responses:
        '200':
          description: New token pair.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokensResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Refresh token invalid or expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /sign-in/select-organization:
    post:
      tags:
        - Auth
      summary: Select an organization
      description: |
        Submits the chosen organization id together with the
        pending_authentication_token returned by /sign-in for a multi-org
        user. On success returns the same AuthTokens shape as /sign-in.
      operationId: selectOrganization
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SelectOrganizationRequest'
      responses:
        '200':
          description: Authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokensResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid or expired pending token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /sign-out:
    post:
      tags:
        - Auth
      summary: Sign out
      description: |
        Revokes the WorkOS session identified by the access token's `sid`
        claim, so its refresh token can no longer mint new access tokens.
        Best-effort and idempotent; the frontend discards its stored tokens
        and redirects to login on the 204.
      operationId: signOut
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Session revoked (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /verify-email:
    post:
      tags:
        - Auth
      summary: Verify email
      description: |
        Submits the code from the verification email together with the
        pending_authentication_token returned by /sign-in. On success
        returns the same AuthTokens shape as /sign-in.
      operationId: verifyEmail
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyEmailRequest'
      responses:
        '200':
          description: Verified and authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokensResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid or expired verification code.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /send-verification-email:
    post:
      tags:
        - Auth
      summary: Send verification email
      description: (Re)send the verification email
      operationId: sendVerificationEmail
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendVerificationEmailRequest'
      responses:
        '200':
          description: Email dispatched (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /forgot-password:
    post:
      tags:
        - Auth
      summary: Request password reset
      description: |
        Triggers WorkOS to create a one-time reset token and email the
        user a reset link. Always returns 200, even for an unregistered
        email, so the endpoint can't be used to probe which addresses
        exist. The emailed link points at the frontend reset page with a
        `?token=` query param, which is submitted to /reset-password.
      operationId: forgotPassword
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ForgotPasswordRequest'
      responses:
        '200':
          description: Reset email dispatched if the address is registered (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /reset-password:
    post:
      tags:
        - Auth
      summary: Reset password
      description: |
        Submits the token from the reset email together with the new
        password. WorkOS sets the password (and verifies the email if it
        wasn't). No session is returned — the client signs in afterward.
      operationId: resetPassword
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordRequest'
      responses:
        '200':
          description: Password updated (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid or expired reset token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /oauth/{provider}/start:
    get:
      tags:
        - Auth
      summary: Start OAuth sign-in
      description: |
        Returns the WorkOS authorization URL the browser should navigate to.
        WorkOS redirects back to the frontend /auth/callback route with
        state + code, which the frontend forwards to /oauth/callback.
      operationId: oauthStart
      security: []
      parameters:
        - in: path
          name: provider
          required: true
          schema:
            type: string
            enum:
              - google
              - github
          description: OAuth provider (must be present in OAUTH_ALLOWED_PROVIDERS).
      responses:
        '200':
          description: Authorization URL.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthStartResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /oauth/callback:
    get:
      tags:
        - Auth
      summary: Complete OAuth sign-in
      description: |
        The frontend /auth/callback page forwards the WorkOS `code` and
        `state` here. We verify state, exchange the code for a session, and
        return the standard AuthTokens envelope.
      operationId: oauthCallback
      security: []
      parameters:
        - in: query
          name: code
          required: true
          schema:
            type: string
        - in: query
          name: state
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokensResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: State mismatch or code exchange failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: |
            User belongs to multiple organizations and must choose one.
            Redeem the returned pending_authentication_token via
            /sign-in/select-organization to finish authentication.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationSelectionRequiredResponseEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /account-verification/oauth/{provider}/start:
    post:
      tags:
        - Account Verification
      summary: Start a provider verification
      description: |
        Mints a single-use OAuth transaction bound to the authenticated
        user and returns the provider consent URL for a full-page
        redirect. `return_to` must exactly match an allowlisted
        same-origin frontend path.

        Rejected with 409 when the account is already verified or under
        support review, and with 400 when the per-user rate limit
        (10 starts per trailing hour) trips.
      operationId: startAccountVerification
      security:
        - bearerAuth: []
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum:
              - github
              - linkedin
              - x
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartVerificationRequest'
      responses:
        '200':
          description: Provider consent URL for a full-page redirect.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StartVerificationEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /account-verification/oauth/{provider}/callback:
    get:
      tags:
        - Account Verification
      summary: Provider authorization-code callback
      description: |
        Public endpoint the provider redirects the browser to. Consumes
        the single-use transaction, exchanges the code, compares the
        provider name with the WorkOS signup name, and ALWAYS answers
        with a 302 to the frontend return path carrying only a safe
        `?result=` code (`processing`, `ready`, `support_review_required`,
        `provider_cancelled`, `provider_unavailable`,
        `insufficient_provider_data`). No PII, token, or provider error
        detail ever appears in the redirect.
      operationId: accountVerificationCallback
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum:
              - github
              - linkedin
              - x
        - name: code
          in: query
          required: false
          schema:
            type: string
        - name: state
          in: query
          required: false
          schema:
            type: string
        - name: error
          in: query
          required: false
          schema:
            type: string
      responses:
        '302':
          description: Redirect to the frontend with a safe result code.
  /account-verification/status:
    get:
      tags:
        - Account Verification
      summary: Read the caller's verification status
      description: |
        Serves the onboarding gate UI. `status` mirrors the user's
        onboarding_status; `safe_reason` is the latest attempt's safe
        failure code (never a provider error body); `retryable` tells the
        UI whether offering the provider chooser again makes sense.
      operationId: getAccountVerificationStatus
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The caller's verification state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /account-verification/finalize:
    post:
      tags:
        - Account Verification
      summary: Re-enqueue missing finalization work
      description: |
        Retry seam for `person_setup_failed` / stuck
        `verified_pending_person`: re-enqueues the idempotent person
        finalizer. Never re-runs a completed setup, never repeats OAuth,
        and never accepts identity fields from the browser. 409 when no
        passed verification exists.
      operationId: finalizeAccountVerification
      security:
        - bearerAuth: []
      responses:
        '202':
          description: Finalization is queued (or already complete).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FinalizeEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /account-verification/reviews/{review_id}/decision:
    post:
      tags:
        - Account Verification
        - Admin
      summary: Decide a support review (privileged)
      description: |
        Operator-only (X-Admin-Token). An approval is an audited override
        of the automatic name rule: the verification becomes a pass and
        the same idempotent finalizer runs. A rejection leaves the
        mandatory gate in place. Replayed decisions 409.
      operationId: decideAccountVerificationReview
      security:
        - adminToken: []
      parameters:
        - name: review_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewDecisionRequest'
      responses:
        '200':
          description: The decided review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewDecisionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /users:
    get:
      tags:
        - Users
      summary: Get all users
      description: List users
      operationId: listUsers
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
            default: 0
        - in: query
          name: query
          description: |
            People-picker search: case-insensitive match on the linked master
            person's full name or the username. When present, the response is
            SearchUsersResponseEnvelope (active users with a linked master
            person only) and `offset` is ignored.
          schema:
            type: string
      responses:
        '200':
          description: One page of users; search rows when `query` is present.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/ListUsersResponseEnvelope'
                  - $ref: '#/components/schemas/SearchUsersResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /users/me:
    get:
      tags:
        - Users
      summary: Get my profile
      description: |
        Returns the current user with the JWTs the frontend needs and
        their own person. The id comes from the session token, never
        the URL.
      operationId: getMe
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated authenticated user's composed profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /users/me/auth:
    get:
      tags:
        - Auth
      summary: Get my sign-in methods
      description: |
        Reports what the caller's account can sign in with, so a settings
        screen can offer "set password" vs "change password" without
        probing for a 401.

        has_password is tracked locally — WorkOS's user object carries no
        such flag — and is stamped on password sign-up, password sign-in,
        reset confirm, and change. It can therefore read false for an
        account that does have a password but has not authenticated with
        it since the field was introduced; the next password sign-in
        corrects it. oauth_providers comes live from WorkOS's linked
        identities, using the same short tokens as
        /v1/oauth/{provider}/start.
      operationId: getMyAuthMethods
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The caller's available sign-in methods.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthCapabilitiesResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /users/me/sessions:
    get:
      tags:
        - Auth
      summary: List my active sessions
      description: |
        Returns the caller's active WorkOS sessions, newest first. The
        session backing this request has is_current true, so a settings
        screen can render it as "this device". Sessions live in WorkOS;
        nothing is stored locally.
      operationId: listMySessions
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The caller's active sessions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionListResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /users/me/sessions/{sessionId}:
    delete:
      tags:
        - Auth
      summary: Revoke one of my sessions
      description: |
        Revokes one of the caller's own WorkOS sessions, invalidating its
        refresh token. The session's current access token stays valid
        until it expires (verification is stateless), so the device is
        signed out within minutes, not instantly. A session id that does
        not belong to the caller returns 404. Revoking the is_current
        session is allowed and acts as a sign-out for this device.
      operationId: revokeMySession
      security:
        - bearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          description: WorkOS session id (the "session_..." id from GET /v1/users/me/sessions).
          schema:
            type: string
      responses:
        '200':
          description: Session revoked.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /users/me/password:
    post:
      tags:
        - Auth
      summary: Change password
      description: |
        Changes the authenticated caller's password. The account comes
        from the session token, so this can only ever target the caller's
        own password. current_password must be correct — a wrong value
        returns 401 and the stored password is left untouched.

        Distinct from /reset-password, which is the locked-out flow driven
        by an emailed token. Existing sessions (including this one) stay
        valid after a successful change.
      operationId: changeMyPassword
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '200':
          description: Password updated (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          description: |
            Malformed body, confirm_password mismatch, new password same as
            current, or WorkOS rejected the new password as too weak.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing/invalid session, or current_password is incorrect.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /users/{id}:
    parameters:
      - $ref: '#/components/parameters/UserID'
    get:
      tags:
        - Users
      summary: Get one user
      description: Get a user by id
      operationId: getUser
      security:
        - bearerAuth: []
      responses:
        '200':
          description: User.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Users
      summary: Delete your own user account
      description: |
        Self-service only — `id` must be the authenticated caller's own user
        id; deleting anyone else's account returns 403.

        **Irreversible, and it revokes sign-in**: the WorkOS account is
        destroyed along with the local row (cascading user_settings,
        user_roles, user_consumption_level, and organization_memberships)
        and the user's graph. There is no undo and no re-login.

        WorkOS goes first; if that fails the request returns 500 and nothing
        local is touched. An access token issued before the delete stays
        syntactically valid until it expires — verification is stateless —
        but its user row is gone, so authorized reads fail on lookup.
      operationId: deleteUser
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Deleted (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /users/{id}/jwts/regenerate:
    get:
      tags:
        - Admin
      summary: Regenerate vendor JWTs
      description: |
        Re-signs the Tiptap collab/AI and Knock JWTs for a user after a
        secret rotation. Operator-only (X-Admin-Token).
      operationId: regenerateJWTs
      security:
        - adminToken: []
      parameters:
        - $ref: '#/components/parameters/UserID'
      responses:
        '200':
          description: New JWTs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JWTsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /admin/provisioning-events/{id}/retry:
    post:
      tags:
        - Admin
      summary: Retry a provisioning event
      description: |
        Resets the row's status and republishes the message on the
        user-provisioning-events topic. Already-succeeded rows return 400.
        Operator-only (X-Admin-Token).
      operationId: retryProvisioningEvent
      security:
        - adminToken: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Republished attempt.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProvisioningEventResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /admin/users/external-id:
    post:
      tags:
        - Admin
      summary: Set a WorkOS external id
      description: |
        Sets external_id on the WorkOS user so access tokens carry the
        urn:orbiter:user_id claim. Backfill / repair tool.

        Operator-only (X-Admin-Token): repointing a WorkOS account at an
        arbitrary local row is an account-takeover primitive, so an end-user
        bearer token must never reach this.
      operationId: setUserExternalID
      security:
        - adminToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetExternalIDRequest'
      responses:
        '200':
          description: external_id updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /admin/users/{id}:
    delete:
      tags:
        - Admin
      summary: Delete any user
      description: |
        Hard-deletes an arbitrary user by local id, bypassing the
        self-service rule on `DELETE /users/{id}`. Same teardown as that
        route — including **destroying the WorkOS account** — just on
        someone else's id. Irreversible.

        Order: WorkOS first — if that fails the request aborts with 500 and
        nothing local is touched, because deleting locally while the remote
        account lives just means the next sign-in re-creates the user. An
        already-deleted WorkOS account is treated as success so an operator
        can still clear a local remnant. Then the local row (cascading
        user_settings, user_roles, user_consumption_level, and
        organization_memberships), then the FalkorDB graph best-effort — a
        graph failure does not fail the delete.

        Operator-only (X-Admin-Token): an end-user bearer token must never
        reach this — it deletes accounts other than the caller's.
      operationId: adminDeleteUser
      security:
        - adminToken: []
      parameters:
        - $ref: '#/components/parameters/UserID'
      responses:
        '200':
          description: Deleted (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /admin/persons/projection-sync:
    post:
      operationId: adminPersonsProjectionSync
      tags:
        - Admin
      summary: Queue a package re-apply for persons
      description: |
        Operator backfill (X-Admin-Token). Queues a projection sync for each held person named, so the worker overwrites each row from its latest enrichment package: the tool for a package field the backend only recently started to store. Ids not held are reported as unknown and skipped. Capped at 500 ids per call.
      security:
        - adminToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminProjectionSyncRequest'
      responses:
        '202':
          description: Syncs queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminProjectionSyncEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /users/webhooks/workos:
    post:
      tags:
        - Users
      summary: 'Webhook: WorkOS users'
      description: |
        Receives WorkOS `user.*` events. `user.created` provisions the
        local user row and fires the first-auth fan-out (Knock identify,
        personal org, user graph), so invited users exist locally before
        their first sign-in. Other event types return 200 with action
        "ignored". Verified via the `WorkOS-Signature` HMAC header, not
        a bearer token. Idempotent — an already-provisioned user returns
        action "existing".
      operationId: workosUserWebhook
      security: []
      parameters:
        - in: header
          name: WorkOS-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Raw WorkOS event envelope ({ event, data }).
      responses:
        '200':
          description: Event processed (or ignored).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserWebhookResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /connection-requests:
    get:
      tags:
        - Connections
      summary: List pending connection requests
      description: |
        The caller's pending connection requests, both directions (not yet
        accepted, not declined, not blocked). Clients split incoming vs
        outgoing on user_from_id. Ports Xano GET /orbiter-connect-requests.
      operationId: listConnectionRequests
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Pending requests involving the caller.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionRequestsResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Connections
      summary: Send a connection request
      description: |
        Creates a pending request from the verified session user to
        user_to_id — there is deliberately no user_from_id field, identity
        never comes from the body. 409 when the users are already connected
        or a pending request exists in either direction; declined requests
        don't block a retry. Ports Xano POST /orbiter-connect-requests.
      operationId: createConnectionRequest
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateConnectionRequestRequest'
      responses:
        '201':
          description: Request created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionRequestResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /connection-requests/{id}:
    patch:
      tags:
        - Connections
      summary: Accept or decline a connection request
      description: |
        Only the recipient may respond, and only while the request is
        pending. Accepting stamps connected_at and writes the bidirectional
        users.connection_joins pair in one transaction. Ports Xano PUT
        /orbiter-connect-requests/{id}.
      operationId: updateConnectionRequest
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: Connection request id.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateConnectionRequestRequest'
      responses:
        '200':
          description: The updated request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionRequestResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations:
    get:
      tags:
        - Organizations
      summary: Get all organizations
      description: |
        Every organization the authenticated user belongs to, each enriched with the
        authenticated user's role, membership status and WorkOS membership id. Flat
        list (no pagination) — a single user's organization count is
        always small. Sorted by name.
      operationId: listOrganizations
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's organizations.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListOrganizationsResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Organizations
      summary: Create a organization
      description: |
        Creates a team organization owned by the authenticated user:
        the org row, an owner membership, and the claimed domains (at
        least one, required) are written in one transaction, then
        mirrored to WorkOS (org + admin membership), a per-org FalkorDB
        graph is provisioned, and DNS ownership verification starts for
        each domain — all best-effort: an outage does not fail the
        request; `workos_organization_id` stays null until a later sync
        backfills it. Each returned organization_domain carries the
        `verification_token` to publish as a DNS TXT record; domains
        stay `pending` until WorkOS confirms via webhook. The org starts
        **unactivated**: it cannot be selected at sign-in, cannot mint an
        org-scoped session, and cannot take members or invitations until
        its first domain verifies (`activated` flips to true then).
        Personal organizations cannot be created here; they are
        provisioned automatically at sign-up.
      operationId: createOrganization
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrganizationRequest'
      responses:
        '201':
          description: Created organization.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}:
    get:
      tags:
        - Organizations
      summary: Get one organization
      description: |
        Returns the organization, its claimed domains, and all
        memberships. The authenticated user must be a member.
      operationId: getOrganization
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The organization with memberships.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationDetailResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Organizations
      summary: Update a organization
      description: |
        Edits the organization's name and/or claimed domain, then mirrors
        the change to WorkOS and the org graph (best-effort). Omitted
        fields are unchanged; `domain: ""` clears the claimed domains.
        Only owner/admin members may edit. `is_personal` is immutable.
      operationId: updateOrganization
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrganizationRequest'
      responses:
        '200':
          description: Updated organization.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Organizations
      summary: Delete a organization
      description: |
        Deletes the organization everywhere: WorkOS first (remote
        failure aborts), then the per-org graph and each member's
        :OrgMembership node, then the local row (memberships and domains
        cascade). Only owner/admin members may delete; personal
        organizations cannot be deleted.
      operationId: deleteOrganization
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/soft-delete:
    post:
      tags:
        - Organizations
      summary: Soft-delete an organization
      description: |
        Marks the organization deleted (sets `deleted_at`) without tearing
        anything down. The row, its WorkOS/graph mirrors, and all org-scoped
        data survive — a reaper hard-deletes past the retention window.
        Soft-deleted orgs disappear from all read endpoints. Only owner/admin
        members may delete; personal organizations cannot be deleted.
      operationId: softDeleteOrganization
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Soft-deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/domains/verify:
    post:
      tags:
        - Organizations
      summary: Verify the organization's domains
      description: |
        (Re)starts DNS ownership verification for every unverified claimed
        domain and returns each domain with its `verification_token` — the
        value the owner publishes as a DNS TXT record. WorkOS polls DNS and
        reports the outcome via the organization_domain webhooks; poll the
        organization until `status` flips to `verified`. Owner/admin only.
        409 when the organization has not been mirrored to WorkOS yet.
      operationId: verifyOrganizationDomains
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The organization's domains with verification tokens.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListOrganizationDomainsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/graph-stats:
    get:
      operationId: getOrgGraphStats
      tags:
        - Organizations
      summary: Get the organization graph's aggregate counts
      description: |
        Totals only. The relationships behind them are not exposed here and there is no per-edge listing: what a member knows stays theirs, and an administrator seeing the totals is not the same as seeing who knows whom.
        Owners and admins only. A member without the role and a non-member are refused identically, so the response cannot be used to probe membership. An organization whose graph has not been written yet reports its member count with the graph figures at zero.
      security:
        - bearerAuth: []
      parameters:
        - name: organization_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Aggregate counts
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OrgGraphStats'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /organizations/{organization_id}/invitations:
    get:
      tags:
        - Organizations
      summary: List invitations
      description: |
        Returns the organization's invitations, newest first, optionally
        filtered by `?status=`. Only owner/admin members may list them —
        pending invitations expose email addresses, so a plain member gets
        403.
      operationId: listOrganizationInvitations
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: status
          required: false
          description: |
            Filter by lifecycle state (pending, accepted, expired).
            Omit to return every state. `revoked` is accepted but always
            empty — revoked invitations are deleted, not stored.
          schema:
            type: string
      responses:
        '200':
          description: The organization's invitations.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListInvitationsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Organizations
      summary: Invite a member
      description: |
        Invites an email address to join the organization. Unlike adding a
        membership this grants no access on its own: WorkOS emails the
        invitation and hosts acceptance, and the invitee becomes a member
        only once they accept (which arrives back as an `invitation.*`
        webhook). The address does not need to be an Orbiter user yet.

        The WorkOS call is not best-effort — an invitation nobody receives
        is not an invitation — so a delivery failure returns 5xx and
        records nothing. A second invitation while one is still pending is
        409, as is an address WorkOS refuses (already a member). The
        inviter comes from the session, never the body. Personal
        organizations take no invitations (403).
      operationId: inviteOrganizationMember
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InviteMemberRequest'
      responses:
        '201':
          description: The created invitation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/invitations/{invitation_id}:
    delete:
      tags:
        - Organizations
      summary: Delete an invitation
      description: |
        Revokes a pending invitation's emailed link in WorkOS and deletes
        the record entirely — it no longer appears in the invitation
        list. For a live invitation WorkOS is revoked first and a remote
        failure aborts, so the record never disappears while its link
        still works. Any never-accepted invitation (pending, lapsed, or
        expired) can be deleted; an accepted one gives 409 because it is
        membership history. One belonging to another organization gives
        404. Only owner/admin members may delete. The pending membership
        WorkOS minted alongside the invitation is removed too (WorkOS
        does not reliably cascade it), best-effort.
      operationId: deleteOrganizationInvitation
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: invitation_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The invitation was revoked and deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/invitations/{invitation_id}/resend:
    post:
      tags:
        - Organizations
      summary: Resend an invitation
      description: |
        Re-sends a pending invitation's email and extends its expiry. The
        status stays pending; only `expires_at` moves. An invitation that
        is no longer pending, or has no WorkOS record to resend, gives
        409. Only owner/admin members may resend, and personal
        organizations take no invitations (403).
      operationId: resendOrganizationInvitation
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: invitation_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The resent invitation, with its extended expiry.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /invitations/by-token/{invitation_token}:
    get:
      tags:
        - Organizations
      summary: Preview an invitation by token
      description: |
        Resolves the token from an invitation email to what the accept
        page shows before the invitee signs in: the organization they
        would join, the invited address, the invitation state (pending,
        accepted, expired, revoked) and its expiry. Public — the invitee
        has no session yet; possession of the emailed token is the
        authorization. `organization` is null when the invitation
        carries no organization, and its `id` is null when the
        organization is not mirrored locally yet.
      operationId: getInvitationPreview
      security: []
      parameters:
        - in: path
          name: invitation_token
          required: true
          schema:
            type: string
          description: The token from the invitation email's accept link.
      responses:
        '200':
          description: What the accept page shows for this invitation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationPreviewEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /invitations/by-token/{invitation_token}/accept:
    post:
      tags:
        - Organizations
      summary: Accept an invitation by token
      description: |
        Accepts the emailed invitation on behalf of the signed-in caller.
        The caller's verified email must match the invited address (403
        otherwise), and only a pending invitation can be accepted (409
        otherwise). WorkOS hosts acceptance: it activates the WorkOS
        membership, and the local membership follows via the
        organization_membership webhooks. Responds with the settled
        invitation in the same shape as the preview endpoint.
      operationId: acceptInvitation
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: invitation_token
          required: true
          schema:
            type: string
          description: The token from the invitation email's accept link.
      responses:
        '200':
          description: The invitation after acceptance.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationPreviewEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/organization-memberships:
    get:
      tags:
        - Organizations
      summary: Get all members
      description: |
        Returns every membership for the organization, each row enriched
        with the member's person summary (`person`, null
        when the user isn't linked to a person). Pending memberships are
        excluded: WorkOS mints one when an invitation is sent, and an
        invitee is not a member until they accept (see the invitations
        endpoints). The authenticated user must be a
        member, else 403 — rosters are not enumerable by non-members.
      operationId: listOrganizationMemberships
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The organization's membership roster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMembershipsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Organizations
      summary: Add a member
      description: |
        Adds a user to the organization, then mirrors the membership to
        WorkOS and upserts the membership graph edges — both best-effort:
        the mirror only runs when the member has a WorkOS identity and
        the org has a WorkOS id, and a vendor outage does not fail the
        request (`workos_membership_id` stays null until a later sync).
        Only owner/admin members may add members.
      operationId: addOrganizationMembership
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddMembershipRequest'
      responses:
        '201':
          description: Created membership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/organization-memberships/{membership_id}:
    put:
      tags:
        - Organizations
      summary: Update a membership
      description: |
        Updates the membership's role and/or status (omitted fields are
        unchanged). A role change is best-effort mirrored to WorkOS and the
        membership graph edge is re-upserted. Only owner/admin members may
        edit memberships. An edit that would leave the organization with no
        active owner/admin is rejected with 409 — assign another admin first.
      operationId: updateOrganizationMembership
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: membership_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMembershipRequest'
      responses:
        '200':
          description: Updated membership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Organizations
      summary: Remove a member
      description: |
        Deletes the membership everywhere: WorkOS first (a remote failure
        aborts so we never orphan a live WorkOS membership), then the
        member's :Member and :OrgMembership graph nodes (best-effort), then
        the local row. Owner/admin members may remove any member; any member
        may remove their own membership (leave the organization). The
        organization's last active owner/admin cannot be removed — assign
        another admin first (409).
      operationId: deleteOrganizationMembership
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: membership_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Removed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/organization-memberships/{membership_id}/role:
    patch:
      tags:
        - Organizations
      summary: Change a member's role
      description: |
        Updates only the membership's role, best-effort syncs it to WorkOS,
        and re-upserts the membership graph edge. Only owner/admin members
        may change roles. Demoting the organization's last active owner/admin
        is rejected with 409 — assign another admin first.
      operationId: updateOrganizationMembershipRole
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: membership_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMembershipRoleRequest'
      responses:
        '200':
          description: Updated membership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/{organization_id}/organization-memberships/{membership_id}/status:
    patch:
      tags:
        - Organizations
      summary: Change a member's status
      description: |
        Deactivates or reactivates a membership. WorkOS is called first and
        a vendor failure aborts the request — the local status is never
        flipped while WorkOS still disagrees. WorkOS emits an
        organization_membership.updated event on success. Only owner/admin
        members may change status. Deactivating the organization's last
        active owner/admin is rejected with 409 — assign another admin first.
      operationId: setOrganizationMembershipStatus
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: organization_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: membership_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetMembershipStatusRequest'
      responses:
        '200':
          description: Updated membership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/backfill/workos:
    post:
      tags:
        - Organizations
      summary: 'Backfill: WorkOS orgs'
      description: |
        Re-runs the full sync chain (WorkOS org + per-org graph +
        membership + membership edges) for organizations where
        `workos_organization_id` is null. Idempotent; safe to repeat
        until `scanned` is 0.
      operationId: backfillOrganizationsWorkOS
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        '200':
          description: Backfill counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackfillResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/backfill/graphs:
    post:
      tags:
        - Organizations
      summary: 'Backfill: org graphs'
      description: |
        Provisions the per-org graph (:Organization node, master mirror,
        membership edges) for organizations that have a WorkOS id but a
        null `graph_id`. Idempotent; safe to repeat until `scanned` is 0.
      operationId: backfillOrganizationGraphs
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        '200':
          description: Backfill counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackfillResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/backfill/companies:
    post:
      tags:
        - Organizations
      summary: 'Backfill: company links'
      description: |
        Links organizations with no `company_id` to a master
        company by matching their claimed domain, then re-syncs the
        per-org graph so the :MasterOrganization mirror lands. `failed`
        includes orgs whose domain matches no company. Idempotent.
      operationId: backfillOrganizationMasterCompanies
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        '200':
          description: Backfill counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackfillResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/backfill/external-ids:
    post:
      tags:
        - Organizations
      summary: 'Backfill: external ids'
      description: |
        Scans WorkOS organizations and stamps each one missing an
        `external_id` with its local org id (matched on
        `workos_organization_id`). `failed` counts WorkOS orgs with no
        local row. Idempotent.
      operationId: backfillOrganizationExternalIDs
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        '200':
          description: Backfill counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackfillResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/webhooks/workos-memberships:
    post:
      tags:
        - Organizations
      summary: 'Webhook: WorkOS memberships'
      description: |
        Receives WorkOS `organization_membership.created/updated/deleted`
        events and syncs the local membership table and graphs. Verified
        via the `WorkOS-Signature` HMAC header, not a bearer token.
        Idempotent under WorkOS retries (upsert keyed on
        `workos_membership_id`). A member not yet provisioned locally
        returns 500 on purpose so WorkOS retries after the user webhook
        lands. Unknown event types return 200 with action "ignored".
        WorkOS mints a `pending` membership when an invitation is sent;
        it is recorded verbatim, and the members list filters it out
        until acceptance flips it to active.
      operationId: workosMembershipWebhook
      security: []
      parameters:
        - in: header
          name: WorkOS-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Raw WorkOS event envelope ({ event, data }).
      responses:
        '200':
          description: Event processed (or ignored).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipWebhookResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/webhooks/workos-organizations:
    post:
      tags:
        - Organizations
      summary: 'Webhook: WorkOS organizations'
      description: |
        Receives WorkOS `organization.created/updated/deleted` events and
        syncs the local organizations table and graph. Verified via the
        `WorkOS-Signature` HMAC header (its own signing secret, distinct
        from the membership endpoint), not a bearer token. Idempotent under
        WorkOS retries (upsert keyed on `workos_organization_id`).
        `organization.updated` for an unknown org is skipped, not created,
        so out-of-order delivery can't resurrect a deleted org. Unknown
        event types return 200 with action "ignored".
      operationId: workosOrganizationWebhook
      security: []
      parameters:
        - in: header
          name: WorkOS-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Raw WorkOS event envelope ({ event, data }).
      responses:
        '200':
          description: Event processed (or ignored).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationWebhookResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/webhooks/workos-invitations:
    post:
      tags:
        - Organizations
      summary: 'Webhook: WorkOS invitations'
      description: |
        Receives WorkOS `invitation.created/accepted/revoked/resent`
        events and settles the local invitation row. This is the only path
        by which an accepted invitation becomes visible to us — WorkOS
        hosts acceptance. The resulting membership arrives separately as
        `organization_membership.created`.

        Verified via the `WorkOS-Signature` HMAC header using its own
        signing secret, distinct from the membership and organization
        endpoints. Idempotent under retries (keyed on the WorkOS
        invitation id). A revocation deletes the local row (action
        "deleted") — revoked invitations keep no record — and sweeps the
        invite-minted pending membership, since WorkOS does not reliably
        cascade that delete. An invitation
        first seen here is adopted when its organization is known and
        WorkOS still reports a live state (a stale pending snapshot is
        checked against the WorkOS API first); one for an unknown
        organization returns 200 with action "ignored", as do revoked
        adoptions, unknown event types, and states. An invitee whose
        local user row has not landed yet is still recorded as accepted,
        with `accepted_user_id` left null.
      operationId: workosInvitationWebhook
      security: []
      parameters:
        - in: header
          name: WorkOS-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Raw WorkOS event envelope ({ event, data }).
      responses:
        '200':
          description: Event processed (or ignored).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationWebhookResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /organizations/webhooks/workos-domains:
    post:
      tags:
        - Organizations
      summary: 'Webhook: WorkOS organization domains'
      description: |
        Receives WorkOS `organization_domain.*` events (verified,
        verification_failed, created, updated) and syncs the local domain's
        verification status. The org's **first** verified domain also
        activates the org: memberships deferred at creation are mirrored
        to WorkOS, making the org selectable at sign-in. Verified via the
        `WorkOS-Signature` HMAC header (its own signing secret), not a
        bearer token. Idempotent under WorkOS retries. Events for domains
        this environment doesn't hold return 200 with action
        "skipped_not_found"; unknown event types return 200 with action
        "ignored".
      operationId: workosDomainWebhook
      security: []
      parameters:
        - in: header
          name: WorkOS-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Raw WorkOS event envelope ({ event, data }).
      responses:
        '200':
          description: Event processed (or ignored).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DomainWebhookResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /getstream/webhooks/message-new:
    post:
      tags:
        - GetStream
      summary: 'Webhook: message.new'
      description: |
        Receives GetStream (Stream Chat) `message.new` events and records each
        message as a comment on the entity named by the channel id. Verified via
        the `X-Signature` HMAC header (HMAC-SHA256 of the raw body under the app
        API secret), not a bearer token.

        The channel id is `<kind>-<id>`: `collection-<uuid>`, `note-<uuid>`,
        `email-template-<uuid>`, or `email-thread-<thread_id>`. The message
        author (`message.user.id`) is the local `users.id` UUID; an
        unprovisioned author is recorded with a null `user_id`.

        Idempotent under GetStream retries (keyed on `message.id` /
        `getstream_message_id`). Non-`message.new` events, unknown channel
        prefixes, and unroutable ids return 200 (acked, no-op) so GetStream
        does not retry a permanently-unroutable event.
      operationId: getstreamMessageNew
      security: []
      parameters:
        - in: header
          name: X-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: GetStream event envelope ({ type, channel_id, message }).
              properties:
                type:
                  type: string
                  example: message.new
                channel_id:
                  type: string
                  example: collection-018f9c2e-7b3a-7e21-9c0a-1f2e3d4c5b6a
                message:
                  type: object
                  properties:
                    id:
                      type: string
                    text:
                      type: string
                    user:
                      type: object
                      properties:
                        id:
                          type: string
      responses:
        '200':
          description: Event recorded, acked, or ignored.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /getstream/webhooks/message-updated:
    post:
      tags:
        - GetStream
      summary: 'Webhook: message.updated'
      description: |
        Receives GetStream `message.updated` events and edits the comment
        carrying `message.id` (keyed on `getstream_message_id`) in the table
        named by the channel. HMAC-verified via `X-Signature`, no bearer auth.
        No-op (still 200) when no comment matches.
      operationId: getstreamMessageUpdated
      security: []
      parameters:
        - in: header
          name: X-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: GetStream event envelope ({ type, channel_id, message }).
      responses:
        '200':
          description: Comment updated, acked, or ignored.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /getstream/webhooks/message-deleted:
    post:
      tags:
        - GetStream
      summary: 'Webhook: message.deleted'
      description: |
        Receives GetStream `message.deleted` events and deletes the comment
        carrying `message.id` (keyed on `getstream_message_id`) in the table
        named by the channel. HMAC-verified via `X-Signature`, no bearer auth.
        No-op (still 200) when no comment matches.
      operationId: getstreamMessageDeleted
      security: []
      parameters:
        - in: header
          name: X-Signature
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: GetStream event envelope ({ type, channel_id, message }).
      responses:
        '200':
          description: Comment deleted, acked, or ignored.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /notes:
    get:
      tags:
        - Notes
      summary: Get all notes
      description: |
        Returns the authenticated user's notes, optionally filtered to
        those mentioning a person and/or company (AND when both are set;
        omit both for all notes). Ordered by updated_at then created_at,
        descending.
      operationId: listNotes
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: my_person_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: my_company_id
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Matching notes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNotesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Notes
      summary: Create a note
      description: |
        Creates a note owned by the authenticated user. The owner is
        taken from the verified session (urn:orbiter:user_id claim), not
        the body.
      operationId: createNote
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNoteRequest'
      responses:
        '201':
          description: Created note.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoteResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /notes/mentions:
    get:
      tags:
        - Notes
      summary: Search @mention candidates
      description: |
        Returns every person and company the caller can @mention — not just
        their contacts — optionally fuzzy-filtered by ?query= on name. The
        caller's own contacts sort first; contact_id / company_contact_id is
        null for anyone they have not saved. Capped at 25 of each.
      operationId: getNoteMentions
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: query
          required: false
          schema:
            type: string
          description: Case-insensitive name filter for both persons and companies.
      responses:
        '200':
          description: The authenticated user's mentionable persons and companies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MentionsResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /notes/persons:
    get:
      tags:
        - Notes
      summary: Get contacts with notes
      description: |
        Cursor-paginated, newest-first. Returns each contact referenced by
        ≥1 note (via note_person_mentions) with a {id, name, avatar} person
        card. Ports the Xano GET /my-persons/notes. meta carries total and
        nextCursor (empty on the last page).
      operationId: listNotePersons
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - in: query
          name: cursor
          required: false
          schema:
            type: string
          description: Opaque keyset cursor from a prior page's meta.nextCursor.
      responses:
        '200':
          description: One page of mentioned contacts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMentionedPersonsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /notes/companies:
    get:
      tags:
        - Notes
      summary: Get company contacts with notes
      description: |
        Cursor-paginated, newest-first. Returns each of the caller's company
        contacts referenced by >=1 note with a {id, name, logo} company
        card. The company mirror of GET /notes/persons. meta carries total
        and nextCursor (empty on the last page).
      operationId: listNoteCompanies
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - in: query
          name: cursor
          required: false
          schema:
            type: string
          description: Opaque keyset cursor from a prior page's meta.nextCursor.
      responses:
        '200':
          description: One page of mentioned company contacts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMentionedCompaniesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /notes/{note_id}:
    get:
      tags:
        - Notes
      summary: Get one note
      description: Get a note by id
      operationId: getNote
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: note_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The note.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoteResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - Notes
      summary: Update a note
      operationId: updateNote
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: note_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateNoteRequest'
      responses:
        '200':
          description: Updated note.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoteResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Notes
      summary: Delete a note
      operationId: deleteNote
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: note_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /notes/{note_id}/collaborators:
    get:
      tags:
        - Notes
      summary: Get all collaborators
      description: List a note's collaborators
      operationId: listNoteCollaborators
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: note_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Collaborators.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollaboratorsResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Notes
      summary: Add a collaborator
      description: Add a collaborator to a note
      operationId: addNoteCollaborator
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: note_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCollaboratorRequest'
      responses:
        '201':
          description: Added collaborator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollaboratorResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /notes/{note_id}/collaborators/{user_id}:
    patch:
      tags:
        - Notes
      summary: Update a collaborator
      description: Update a collaborator's access mode
      operationId: updateNoteCollaboratorMode
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: note_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: user_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCollaboratorModeRequest'
      responses:
        '200':
          description: Updated collaborator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollaboratorResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Notes
      summary: Remove a collaborator
      description: Remove a collaborator from a note
      operationId: removeNoteCollaborator
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: note_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: user_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Removed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-templates:
    get:
      tags:
        - Emails
      summary: Get all email templates
      description: |
        Returns one keyset page of email templates the authenticated user owns
        OR collaborates on (via email_collaborators), deduplicated and newest
        first — for infinite scroll. Pass the previous page's `next_cursor`
        back as `cursor` to fetch the next page; an empty `next_cursor` means
        no more results.
      operationId: listEmailTemplates
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: cursor
          required: false
          description: Opaque cursor from a previous response's next_cursor.
          schema:
            type: string
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 200
            default: 50
      responses:
        '200':
          description: A page of the authenticated user's email templates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEmailTemplatesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Emails
      summary: Create a email template
      description: |
        Creates a template owned by the authenticated user. The owner is
        taken from the verified session (urn:orbiter:user_id claim), not the
        body.
      operationId: createEmailTemplate
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmailTemplateRequest'
      responses:
        '201':
          description: Created email template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailTemplateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-templates/{template_id}:
    patch:
      tags:
        - Emails
      summary: Update a email template
      description: |
        Full replace of the template's editable fields, scoped to the owner.
        404 when the template is unknown or not the authenticated user's.
      operationId: updateEmailTemplate
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: template_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEmailTemplateRequest'
      responses:
        '200':
          description: Updated email template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailTemplateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Emails
      summary: Delete a email template
      description: |
        Deletes a template owned by the authenticated user. 404 when unknown or not the
        authenticated user's.
      operationId: deleteEmailTemplate
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: template_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-drafts:
    get:
      tags:
        - Emails
      summary: Get all email drafts
      description: List the authenticated user's email drafts
      operationId: listEmailDrafts
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's drafts with a total count in meta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEmailDraftsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Emails
      summary: Create a email draft
      description: |
        Creates an outbound email draft for the authenticated user and stores
        any attachments to GCS. Recipients (to/cc/bcc) are resolved to display
        names from persons where known. The owner is taken from the
        verified session (urn:orbiter:user_id claim), not the body.

        Sent as multipart/form-data: repeated `to` / `cc` / `bcc` text fields
        for addresses and repeated `attachments` file parts. The total request
        body is capped at 25 MB.

        NOTE: the Nylas sync is not yet wired — the draft is returned with
        status `draft` and a null `nylas_draft_id` until the Nylas integration
        lands.
      operationId: createEmailDraft
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CreateEmailDraftRequest'
      responses:
        '201':
          description: Created email draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailDraftEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-collaborators:
    get:
      tags:
        - Emails
      summary: Get all email collaborators
      description: |
        Returns the collaborators on a template and/or thread, enriched with
        each collaborator's display name and avatar (from persons). At
        least one of email_template_id / email_thread_id is required.
      operationId: listEmailCollaborators
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: email_template_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: email_thread_id
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The collaborators.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEmailCollaboratorsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Emails
      summary: Share with a user
      description: |
        Adds a collaborator to a template and/or thread, then notifies the
        collaborator via Knock. The sharer is taken from the verified session
        (urn:orbiter:user_id claim), not the body. At least one of
        email_template_id / email_thread_id is required.
      operationId: createEmailCollaborator
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmailCollaboratorRequest'
      responses:
        '201':
          description: Created collaborator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailCollaboratorEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /email-collaborators/{collaborator_id}:
    patch:
      tags:
        - Emails
      summary: Update a collaborator
      description: Update a collaborator's access mode
      operationId: updateEmailCollaborator
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collaborator_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEmailCollaboratorRequest'
      responses:
        '200':
          description: Updated collaborator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailCollaboratorEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Emails
      summary: Remove a collaborator
      operationId: deleteEmailCollaborator
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collaborator_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Removed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-drafts/{draft_id}:
    get:
      tags:
        - Emails
      summary: Get one email draft
      description: Get a single email draft
      operationId: getEmailDraft
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: draft_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Draft found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailDraftEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Emails
      summary: Update a email draft
      description: Update an email draft
      operationId: updateEmailDraft
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: draft_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEmailDraftRequest'
      responses:
        '200':
          description: Draft updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailDraftEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Emails
      summary: Delete a email draft
      description: Delete an email draft
      operationId: deleteEmailDraft
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: draft_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Draft deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-drafts/{draft_id}/attachments:
    get:
      tags:
        - Emails
      summary: Get all draft attachments
      description: List a draft's attachments
      operationId: listEmailDraftAttachments
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: draft_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Attachments listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DraftAttachmentListEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Emails
      summary: Add attachments
      description: |
        Uploads one or more attachments to the draft and stores them to GCS.
        Sent as multipart/form-data with repeated `attachments` file parts
        (total request body capped at 25 MB). Returns the draft's full,
        current attachment set so the client learns every new id.
      operationId: addEmailDraftAttachments
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: draft_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AddDraftAttachmentsRequest'
      responses:
        '201':
          description: Attachments added.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DraftAttachmentListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-drafts/{draft_id}/attachments/{attachment_id}:
    delete:
      tags:
        - Emails
      summary: Remove an attachment
      description: Detaches the attachment and deletes its underlying GCS blob.
      operationId: deleteEmailDraftAttachment
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: draft_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: attachment_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Attachment removed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-folders:
    get:
      tags:
        - EmailFolders
      summary: Get all email folders
      description: |
        Returns folders grouped by Nylas grant. Optionally scoped to a single
        grant (grant_id) or to all grants in a workspace (email_workspace_id).
      operationId: listEmailFolders
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: grant_id
          required: false
          schema:
            type: string
        - in: query
          name: email_workspace_id
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Folders grouped by grant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListFoldersEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - EmailFolders
      summary: Create a email folder
      description: Create an email folder
      operationId: createEmailFolder
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateFolderRequest'
      responses:
        '201':
          description: Folder created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NylasFolderEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-folders/{folder_id}:
    put:
      tags:
        - EmailFolders
      summary: Update a email folder
      description: Update an email folder
      operationId: updateEmailFolder
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: folder_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateFolderRequest'
      responses:
        '200':
          description: Folder updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NylasFolderEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - EmailFolders
      summary: Delete a email folder
      description: grant_id is required in the JSON body to identify the Nylas grant.
      operationId: deleteEmailFolder
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: folder_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteFolderRequest'
      responses:
        '200':
          description: Folder deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-threads:
    get:
      tags:
        - EmailThreads
      summary: Get all email threads
      description: |
        Returns threads grouped by Nylas grant. Optionally scoped to a single
        grant (grant_id) or workspace (email_workspace_id). Supports Nylas-native
        filtering via page_token and folder_id.
      operationId: listEmailThreads
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: grant_id
          required: false
          schema:
            type: string
        - in: query
          name: email_workspace_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: page_token
          required: false
          schema:
            type: string
        - in: query
          name: folder_id
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Threads grouped by grant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListThreadsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-threads/widget:
    get:
      tags:
        - EmailThreads
      summary: Get the emails widget
      description: |
        Opinionated emails widget: unread threads from the last 7 days across
        every widget-enabled grant (show_in_email_widget = true), grouped by
        grant with enriched senders. A fixed server-side query — no params.
      operationId: getEmailThreadsWidget
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Widget thread groups.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListThreadsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-threads/{thread_id}:
    get:
      tags:
        - EmailThreads
      summary: Get one email thread
      description: |
        Returns the full thread detail including all messages (enriched with
        persons data) and the latest AI summary if one exists.
        grant_id is required to identify the Nylas mailbox.
      operationId: getEmailThread
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: thread_id
          required: true
          schema:
            type: string
        - in: query
          name: grant_id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Thread detail with messages.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreadDetailEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - EmailThreads
      summary: Update a email thread
      description: grant_id is required in the body to identify the Nylas grant.
      operationId: updateEmailThread
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: thread_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateThreadRequest'
      responses:
        '200':
          description: Thread updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NylasThreadEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-threads/{thread_id}/attachments/{attachment_id}/download:
    get:
      tags:
        - EmailThreads
      summary: Download an attachment
      description: |
        Streams the raw bytes of a Nylas message attachment. grant_id and
        message_id are required query params. The response Content-Type is
        set from Nylas metadata; falls back to application/octet-stream.
      operationId: downloadEmailThreadAttachment
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: thread_id
          required: true
          schema:
            type: string
        - in: path
          name: attachment_id
          required: true
          schema:
            type: string
        - in: query
          name: grant_id
          required: true
          schema:
            type: string
        - in: query
          name: message_id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Raw attachment bytes.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-threads/{thread_id}/snooze:
    put:
      tags:
        - EmailThreads
      summary: Snooze a thread
      description: |
        Moves the thread to a snooze folder and records the snooze until time.
        A background worker resurfaces the thread when the snooze expires.
      operationId: snoozeEmailThread
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: thread_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SnoozeThreadRequest'
      responses:
        '200':
          description: Thread snoozed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SnoozedThreadEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - EmailThreads
      summary: Unsnooze a thread
      description: |
        Cancels an active snooze, restoring the thread to its original folders.
        grant_id is required in the JSON body.
      operationId: unsnoozeEmailThread
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: thread_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnsnoozeThreadRequest'
      responses:
        '200':
          description: Thread unsnoozed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-thread-summary:
    post:
      tags:
        - EmailSummary
      summary: Summarize a thread (AI)
      description: Generate AI summary for a thread
      operationId: generateEmailThreadSummary
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateSummaryRequest'
      responses:
        '200':
          description: AI summary generated or returned from cache.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailAISummaryEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email/send:
    post:
      tags:
        - Emails
      summary: Send an email
      description: |
        Sends an email immediately through the user's Nylas grant. Accepts the
        same multipart/form-data shape as create-draft (grant_id, to/cc/bcc
        address fields, subject, body, reply_to_message_id, attachments). At
        least one `to` address is required. Total body capped at 25 MB.
      operationId: sendEmail
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SendEmailRequest'
      responses:
        '200':
          description: Email sent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /nylas-grants:
    get:
      tags:
        - NylasGrants
      summary: Get all nylas grants
      description: Returns all Nylas OAuth grants belonging to the authenticated user.
      operationId: listNylasGrants
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's Nylas grants.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNylasGrantsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /x-connect:
    get:
      tags:
        - X Connect
      summary: Read the caller's X connection
      description: |
        `connected` is false when the caller has no stored grant; the other
        fields are then defaults. `current_run` is the most recent sync.
      operationId: getXConnection
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The caller's X connection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XConnectionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - X Connect
      summary: Turn DM sync on or off
      description: Applies from the next sync. 404 when X is not connected.
      operationId: updateXConnection
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateXConnectionRequest'
      responses:
        '200':
          description: The updated connection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XConnectionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - X Connect
      summary: Disconnect X
      description: |
        Revokes the grant at X (best effort) and deletes it. 404 when X is
        not connected.
      operationId: deleteXConnection
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Disconnected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /x-connect/sync:
    post:
      tags:
        - X Connect
      summary: Sync X followers and DMs
      description: |
        Queues a sync on the worker. A sync already in flight is returned
        as-is instead of stacking another. 400 when X is not connected or the
        user must reconnect.
      operationId: syncXConnection
      security:
        - bearerAuth: []
      responses:
        '202':
          description: The queued (or already running) sync.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XSyncRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /x-connect/oauth/start:
    post:
      tags:
        - X Connect
      summary: Start connecting X
      description: |
        Mints a single-use OAuth transaction bound to the authenticated user
        and returns X's consent URL, requesting followers, DMs, and a refresh
        token. `return_to` must exactly match an allowlisted frontend path.
        Answers 400 once the per-user limit (10 starts per trailing hour)
        trips, and 404 when X connect is not configured on the server.
      operationId: startXConnect
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartXConnectRequest'
      responses:
        '200':
          description: X consent URL, to open in a popup.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StartXConnectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /x-connect/oauth/callback:
    get:
      tags:
        - X Connect
      summary: X authorization-code callback
      description: |
        Public endpoint X redirects the browser to. Consumes the single-use
        transaction, exchanges the code, stores the sealed grant, and queues
        the first sync. ALWAYS answers with a 302 to the frontend return path
        carrying only `?result=connected|cancelled|error`; no token or X
        error detail appears in the redirect.
      operationId: xConnectCallback
      security: []
      parameters:
        - name: code
          in: query
          required: false
          schema:
            type: string
        - name: state
          in: query
          required: false
          schema:
            type: string
        - name: error
          in: query
          required: false
          schema:
            type: string
      responses:
        '302':
          description: Redirect to the frontend with a safe result code.
  /x-connect/webhooks/activity:
    get:
      tags:
        - X Connect
      summary: X webhook challenge (CRC)
      description: |
        Public. X calls it to prove we own the webhook. The answer is X's own
        shape, `{"response_token": "sha256=..."}` signed with the app's client
        secret, not the API envelope. 404 while Activity subscriptions are not
        configured.
      operationId: xConnectActivityCRC
      security: []
      parameters:
        - name: crc_token
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: The challenge answer.
          content:
            application/json:
              schema:
                type: object
                required:
                  - response_token
                properties:
                  response_token:
                    type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags:
        - X Connect
      summary: X Activity delivery
      description: |
        Public. X posts one follow or unfollow event, signed over the raw body
        in `X-Twitter-Webhooks-Signature-OAuth2`. The event is recorded and
        applied to every connected account it concerns before the 200, so a
        redelivery changes nothing. The body is X's Activity event, not ours.
      operationId: xConnectActivityDelivery
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: An X Activity event (event_uuid, event_type, filter, payload).
      responses:
        '200':
          description: Acknowledged.
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /admin/x-connect/activity/webhook:
    post:
      tags:
        - Admin
      summary: Register the X Activity webhook
      description: |
        Operator (X-Admin-Token). Makes sure X holds a webhook for this
        environment's delivery URL, registering one when it does not, and
        remembers its id for subscriptions. Registering runs X's CRC, so the
        delivery route must already be reachable. 404 when the app token or
        URL is not configured.
      operationId: adminXConnectRegisterActivityWebhook
      security:
        - adminToken: []
      responses:
        '200':
          description: The webhook X holds for this environment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityWebhookEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /admin/x-connect/activity/subscriptions:
    get:
      tags:
        - Admin
      summary: List the X Activity subscriptions the app holds
      description: Operator (X-Admin-Token). What X reports, not the local records.
      operationId: adminXConnectListActivitySubscriptions
      security:
        - adminToken: []
      responses:
        '200':
          description: Every subscription X holds for the app.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivitySubscriptionsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Admin
      summary: Subscribe every connection that has no subscriptions
      description: |
        Operator (X-Admin-Token). The rollout step for accounts connected
        before subscriptions existed, and the retry for a connect whose
        subscribe failed. Each account gets follow and unfollow, inbound and
        outbound, billed per delivered event. 404 while the kill switch is off.
      operationId: adminXConnectBackfillActivitySubscriptions
      security:
        - adminToken: []
      responses:
        '200':
          description: How many connections were subscribed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityBackfillEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Admin
      summary: Tear down every X Activity subscription
      description: |
        Operator (X-Admin-Token). The kill switch that stops billing: deletes
        every subscription X holds for the app, then the local records.
      operationId: adminXConnectTeardownActivitySubscriptions
      security:
        - adminToken: []
      responses:
        '200':
          description: How many subscriptions were deleted at X.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityTeardownEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /nylas-grants/token-exchange:
    post:
      tags:
        - NylasGrants
      summary: Exchange a Nylas OAuth token
      description: |
        Accepts an OAuth authorization code from the Nylas consent flow and
        exchanges it for a grant, storing the connected account.
      operationId: exchangeNylasGrantToken
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenExchangeRequest'
      responses:
        '201':
          description: Grant created from token exchange.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NylasGrantEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /nylas-grants/{grant_id}:
    get:
      tags:
        - NylasGrants
      summary: Get one nylas grant
      description: Get a Nylas grant by id
      operationId: getNylasGrant
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: grant_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The Nylas grant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NylasGrantEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - NylasGrants
      summary: Update a nylas grant
      operationId: updateNylasGrant
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: grant_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateNylasGrantRequest'
      responses:
        '200':
          description: Updated grant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NylasGrantEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - NylasGrants
      summary: Delete a nylas grant
      operationId: deleteNylasGrant
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: grant_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-workspaces:
    get:
      tags:
        - EmailWorkspaces
      summary: Get all email workspaces
      description: Returns all email workspaces belonging to the authenticated user.
      operationId: listEmailWorkspaces
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's email workspaces.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEmailWorkspacesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - EmailWorkspaces
      summary: Create a email workspace
      description: Creates an email workspace owned by the authenticated user.
      operationId: createEmailWorkspace
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmailWorkspaceRequest'
      responses:
        '201':
          description: Created email workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailWorkspaceEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-workspaces/reorder:
    patch:
      tags:
        - EmailWorkspaces
      summary: Reorder email workspaces
      description: Updates the display order of the authenticated user's email workspaces.
      operationId: reorderEmailWorkspaces
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderEmailWorkspacesRequest'
      responses:
        '200':
          description: Workspaces reordered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-workspaces/{workspace_id}:
    put:
      tags:
        - EmailWorkspaces
      summary: Update a email workspace
      description: Update an email workspace
      operationId: updateEmailWorkspace
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: workspace_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEmailWorkspaceRequest'
      responses:
        '200':
          description: Updated email workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailWorkspaceEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - EmailWorkspaces
      summary: Delete a email workspace
      description: Delete an email workspace
      operationId: deleteEmailWorkspace
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: workspace_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-workspaces/{workspace_id}/grants:
    post:
      tags:
        - EmailWorkspaces
      summary: Attach a grant
      description: Links an existing Nylas grant to the specified email workspace.
      operationId: addGrantToEmailWorkspace
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: workspace_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddGrantRequest'
      responses:
        '201':
          description: Grant attached to workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailWorkspaceEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /email-workspaces/{workspace_id}/grants/{grant_id}:
    delete:
      tags:
        - EmailWorkspaces
      summary: Detach a grant
      description: Removes a Nylas grant from the specified email workspace.
      operationId: removeGrantFromEmailWorkspace
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: workspace_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: grant_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Grant detached.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /calendars:
    get:
      tags:
        - Calendars
      summary: Get all calendars
      description: Returns all calendars from the authenticated user's connected Nylas grants.
      operationId: listCalendars
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's calendars.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCalendarsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendars/{calendar_id}:
    patch:
      tags:
        - Calendars
      summary: Update a calendar
      description: Partial update of a calendar's local settings (e.g. visibility).
      operationId: updateCalendar
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: calendar_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCalendarRequest'
      responses:
        '200':
          description: Updated calendar.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar-events:
    get:
      tags:
        - Calendars
      summary: Get all calendar events
      description: |
        Returns the next 14 days of events across the authenticated user's active calendars,
        grouped by calendar with enriched participants. The range is fixed
        server-side — no query params.
      operationId: listCalendarEvents
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Calendar events in the requested range.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCalendarEventsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar-events/sync-upcoming:
    post:
      tags:
        - Calendars
      summary: Refresh upcoming meetings for the authenticated user
      description: Fetches all pages of recurring occurrences through 18 months. Replaces each successful calendar snapshot atomically, retains failed snapshots, and throttles repeated successful refreshes to once per minute. Does not create completed activity records.
      operationId: syncUpcomingCalendarEvents
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Refresh counts; failed calendars may still have older cached meetings.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - error
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - calendars
                      - updated
                      - failed
                      - through_at
                      - reconnect_required
                    properties:
                      reconnect_required:
                        type: boolean
                      calendars:
                        type: integer
                      updated:
                        type: integer
                      failed:
                        type: integer
                      through_at:
                        type: string
                        format: date-time
                  error:
                    type: 'null'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar-events/widget:
    get:
      tags:
        - Calendars
      summary: Get the events widget
      description: |
        Opinionated events widget: events for the next 14 days across every
        widget-enabled calendar (event_widget_preference = true), grouped by
        calendar with enriched participants. A fixed server-side query — no
        params.
      operationId: getCalendarEventsWidget
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Widget calendar-event groups.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCalendarEventsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar-events/send-rsvp:
    post:
      tags:
        - Calendars
      summary: Send an RSVP
      description: Sends an RSVP response (accept, decline, tentative) for an event.
      operationId: sendCalendarEventRsvp
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendRsvpRequest'
      responses:
        '200':
          description: RSVP sent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/calendars:
    get:
      tags:
        - Calendars
      summary: List mirror calendars
      description: |
        The caller's calendars with account metadata (email account, provider,
        grant expiry) for the account selector and calendar list.
      operationId: listMirrorCalendars
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Calendars with account metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMirrorCalendarsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/grants:
    get:
      tags:
        - Calendars
      summary: List connected calendar accounts
      description: Connected Nylas grants with provider + expiry status.
      operationId: listCalendarGrants
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Connected grants.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListGrantsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/events:
    get:
      tags:
        - Calendars
      summary: List mirror events in a range
      description: |
        Normalized events from the server-side mirror for [from, to). The
        envelope meta carries `cursor` (change-log position for SSE resume)
        and `coverage` (verified-sync windows per calendar); ranges outside
        coverage are not yet synced and should not be read as "empty".
      operationId: listMirrorEvents
      security:
        - bearerAuth: []
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date-time
          description: Range start (RFC3339). Defaults to now-30d.
        - name: to
          in: query
          schema:
            type: string
            format: date-time
          description: Range end (RFC3339). Defaults to now+60d.
        - name: calendar_ids
          in: query
          schema:
            type: string
          description: Comma-separated mirror calendar UUIDs; omit for all.
      responses:
        '200':
          description: Events plus cursor + coverage meta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMirrorEventsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Calendars
      summary: Create an event
      description: |
        Durable write: enqueues a command, waits up to ~8s for the provider
        call, then returns 201 with the event or 202 with a pending command.
        Send `Idempotency-Key` to make retries safe. `notify=true` sends
        provider invitations.
      operationId: createMirrorEvent
      security:
        - bearerAuth: []
      parameters:
        - name: Idempotency-Key
          in: header
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMirrorEventRequest'
      responses:
        '201':
          description: Event created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MirrorEventEnvelope'
        '202':
          description: Write still in flight; poll /calendar/commands/{id}.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommandPendingEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/events/{event_id}:
    patch:
      tags:
        - Calendars
      summary: Update an event
      description: |
        Durable write; `scope` selects recurring-edit semantics
        (`this` default, `following`, `all`). Same 201/202 contract as create.
      operationId: updateMirrorEvent
      security:
        - bearerAuth: []
      parameters:
        - name: event_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: scope
          in: query
          schema:
            type: string
            enum:
              - this
              - following
              - all
        - name: Idempotency-Key
          in: header
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMirrorEventRequest'
      responses:
        '201':
          description: Event updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MirrorEventEnvelope'
        '202':
          description: Write still in flight.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommandPendingEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Calendars
      summary: Delete an event
      description: |
        Durable write; same `scope` semantics as PATCH. Completed deletes
        return 200 with a null data payload; in-flight deletes return 202.
      operationId: deleteMirrorEvent
      security:
        - bearerAuth: []
      parameters:
        - name: event_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: scope
          in: query
          schema:
            type: string
            enum:
              - this
              - following
              - all
        - name: Idempotency-Key
          in: header
          schema:
            type: string
      responses:
        '200':
          description: Event deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '202':
          description: Delete still in flight.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommandPendingEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/events/{event_id}/rsvp:
    post:
      tags:
        - Calendars
      summary: RSVP to an event
      description: Owner-bound RSVP via the durable command path.
      operationId: rsvpMirrorEvent
      security:
        - bearerAuth: []
      parameters:
        - name: event_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RsvpMirrorRequest'
      responses:
        '200':
          description: RSVP applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MirrorEventEnvelope'
        '202':
          description: RSVP still in flight.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommandPendingEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/availability:
    post:
      tags:
        - Calendars
      summary: Compute busy blocks over the mirror
      description: |
        Merged busy intervals across the given calendars (all, when omitted).
        Meta `coverage` marks which windows are verified-synced — treat
        uncovered ranges as unknown, not free.
      operationId: calendarAvailability
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AvailabilityRequest'
      responses:
        '200':
          description: Busy blocks plus coverage meta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailabilityEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/window:
    post:
      tags:
        - Calendars
      summary: Request sync for an additional window
      description: |
        Enqueues reconciliation so [from, to) becomes covered on every owned
        calendar. Returns 202; poll range meta `coverage` for completion.
      operationId: requestCalendarWindow
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WindowRequest'
      responses:
        '202':
          description: Window queued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WindowQueuedEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/commands/{command_id}:
    get:
      tags:
        - Calendars
      summary: Poll a durable command
      description: |
        After a 202, poll here: `done` returns the command result payload,
        other statuses return {command_id, status, error?}.
      operationId: getCalendarCommand
      security:
        - bearerAuth: []
      parameters:
        - name: command_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Command result or status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommandStatusEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /calendar/stream:
    get:
      tags:
        - Calendars
      summary: Server-sent event changes stream
      description: |
        SSE stream of mirror change events (`op`: upsert/delete) with
        monotonically increasing `seq` cursors and immutable revision
        payloads. Pass `cursor` to resume; a cursor older than the retained
        log gets 410 — refetch a range snapshot and reconnect.
      operationId: streamCalendarChanges
      security:
        - bearerAuth: []
      parameters:
        - name: cursor
          in: query
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: text/event-stream of change events.
          content:
            text/event-stream:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '410':
          description: Cursor expired; refetch snapshot and reconnect.
        '500':
          $ref: '#/components/responses/Internal'
  /webhooks/nylas:
    post:
      tags:
        - Calendars
      summary: Nylas webhook ingress
      description: |
        Public, signature-verified Nylas webhook. Validates
        `x-nylas-signature`, admits durable queue work only; all provider I/O
        happens in the worker drain.
      operationId: nylasWebhook
      security: []
      responses:
        '200':
          description: Event admitted.
        '401':
          description: Bad signature.
        '503':
          description: Webhook secret not configured.
    get:
      tags:
        - Calendars
      summary: Nylas webhook challenge
      description: Echoes `?challenge=` for Nylas webhook verification.
      operationId: nylasWebhookChallenge
      security: []
      parameters:
        - name: challenge
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Challenge echoed.
  /contacts/imports:
    post:
      tags:
        - Imports
      summary: Register a contact import
      description: |
        Creates the import job and starts asynchronous processing.
        File sources (linkedin_csv, vcard) POST multipart/form-data with an
        `input_source` field and a `file` part, streamed straight into
        storage. The nylas source POSTs JSON with nylas_grant_id — one of
        the caller's connected email accounts — and the worker syncs the
        grant's full address book (looping the Nylas next_cursor).
      operationId: registerContactImport
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterImportRequest'
          multipart/form-data:
            schema:
              type: object
              required:
                - input_source
                - file
              properties:
                input_source:
                  type: string
                  description: 'File source format: linkedin_csv or vcard. Must precede the file part.'
                file:
                  type: string
                  format: binary
                  description: The raw export file.
      responses:
        '201':
          description: Created import job (status pending).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    get:
      tags:
        - Imports
      summary: List the caller's contact imports
      operationId: listContactImports
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor from next_cursor.
      responses:
        '200':
          description: One page of import jobs, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListImportsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /contacts/imports/{import_id}:
    get:
      tags:
        - Imports
      summary: Get one contact import (status + report counters)
      operationId: getContactImport
      security:
        - bearerAuth: []
      parameters:
        - name: import_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The import job.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /contacts/imports/{import_id}/rows:
    get:
      tags:
        - Imports
      summary: List an import's per-row report
      description: |
        Returns the staged rows with their outcome (pending, imported,
        merged, skipped, failed) — the detail behind the job's counters.
      operationId: listContactImportRows
      security:
        - bearerAuth: []
      parameters:
        - name: import_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          schema:
            type: string
          description: 'Filter by row outcome: pending, imported, merged, skipped, failed.'
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor from next_cursor.
      responses:
        '200':
          description: One page of report rows ordered by record index.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListImportRowsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /log-and-parse/runs:
    post:
      tags:
        - Imports
      summary: Start a mailbox log-and-parse run
      description: |
        Kicks off a per-user parse of all the caller's connected email
        accounts. Sweeps messages + calendar events once, discovers the
        people the user actually knows, and writes one email_account_import
        row per eligible person. Runs asynchronously; poll the run for status.
      operationId: startLogParseRun
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartRunRequest'
      responses:
        '202':
          description: Run accepted and queued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogParseRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    get:
      tags:
        - Imports
      summary: List a connected account's log-and-parse runs
      description: |
        Returns the caller's runs for one connected email account, newest
        first — the account's parse history. grant_id is required.
      operationId: listLogParseRuns
      security:
        - bearerAuth: []
      parameters:
        - name: grant_id
          in: query
          required: true
          description: The Nylas grant id to filter runs by.
          schema:
            type: string
      responses:
        '200':
          description: The account's runs, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListLogParseRunsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /log-and-parse/runs/{run_id}:
    get:
      tags:
        - Imports
      summary: Get a log-and-parse run
      operationId: getLogParseRun
      security:
        - bearerAuth: []
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The run and its counters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogParseRunEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /refresh-activities:
    post:
      tags:
        - Imports
      summary: Refresh the caller's activity feed from the last 7 days
      description: |
        Sweeps every connected email account for the last 7 days of messages
        and calendar events, resolves each counterpart to a master person, and
        writes email + calendar activities for them.

        Unlike log-and-parse (which only ever promotes people who are NOT yet
        contacts), this keeps an existing contact's feed current. A known
        person who is not yet a contact is promoted; an address no master
        person owns is skipped.

        Runs asynchronously; poll the returned run for status. Idempotent: a
        repeat call writes no duplicate activities, and while one refresh is in
        flight the same run is returned rather than a second being queued.
      operationId: startActivityRefresh
      security:
        - bearerAuth: []
      responses:
        '202':
          description: Refresh accepted and queued, or the run already in flight.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityRefreshRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    get:
      tags:
        - Imports
      summary: List the caller's activity refreshes
      description: The caller's recent refreshes, newest first.
      operationId: listActivityRefreshRuns
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The caller's refreshes, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListActivityRefreshRunsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /refresh-activities/{run_id}:
    get:
      tags:
        - Imports
      summary: Get an activity refresh run
      operationId: getActivityRefreshRun
      security:
        - bearerAuth: []
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The refresh and its counters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityRefreshRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /contacts/{contacts_id}/memories:
    get:
      tags:
        - Memories
      summary: List a contact's memories
      description: |
        Returns the caller's mem0 memories about one contact — the content of the
        relationship extracted from email/calendar/notes. Scoped to the caller.
      operationId: listContactMemories
      security:
        - bearerAuth: []
      parameters:
        - name: contacts_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The contact's memories.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMemoriesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /contacts/{contacts_id}/memories/search:
    post:
      tags:
        - Memories
      summary: Search a contact's memories
      description: Semantic search within the caller's memories about one contact.
      operationId: searchContactMemories
      security:
        - bearerAuth: []
      parameters:
        - name: contacts_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchMemoriesRequest'
      responses:
        '200':
          description: Matching memories, ranked.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMemoriesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /me/memories:
    get:
      tags:
        - Memories
      summary: List the caller's own memories
      description: The caller's user-scoped memories (goals, preferences).
      operationId: listUserMemories
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The caller's own memories.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMemoriesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /travels:
    get:
      tags:
        - Travels
      summary: Get all travels
      description: |
        Returns all travels visible to the authenticated user — owned,
        directly shared to them (travel_user_join.shared_to_user), or shared to
        a team they're an accepted member of (travel_team_join) — split into
        active_travels (end date today or later, or unset) and archived_travels
        (end date before today). Not paginated.
      operationId: listTravels
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's travels, grouped into active and archived.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListTravelsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Travels
      summary: Create a travel
      description: |
        Creates a travel owned by the authenticated user. The owner is
        taken from the verified session (urn:orbiter:user_id claim), not
        the body.
      operationId: createTravel
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTravelRequest'
      responses:
        '201':
          description: Created travel.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TravelEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /travels/{travel_id}:
    get:
      tags:
        - Travels
      summary: Get one travel
      description: |
        Returns the travel only if it is visible to the authenticated user —
        owned, directly shared to them, or shared to a team they're an accepted
        member of. Returns 404 (not 403) when the travel is unknown or not
        visible, so existence isn't leaked.
      operationId: getTravel
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: travel_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The travel.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TravelEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Travels
      summary: Update a travel
      description: |
        Full replace of the user-editable fields, scoped to the owner. Omitted
        fields are reset, and an omitted/empty contact_ids or team_ids clears
        that relation — clients must send the full intended state.
      operationId: updateTravel
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: travel_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTravelRequest'
      responses:
        '200':
          description: Updated travel.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TravelEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Travels
      summary: Delete a travel
      operationId: deleteTravel
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: travel_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /activities:
    get:
      tags:
        - Activities
      summary: Get the activity feed
      description: |
        Returns the authenticated user's contact activity feed, grouped by day
        (newest-first), one card per contact at their last-touch day. Walks back
        over a 9-day window from the time_range anchor. When show_empty_orbits is
        off it stops early at the oldest non-empty day (up to 9); when on it emits
        all 9 calendar days, empty days included.
      operationId: getActivityFeed
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: time_range
          required: false
          description: Upper bound of the walk. "now" (default), an RFC3339 timestamp, or a YYYY-MM-DD date.
          schema:
            type: string
            default: now
        - in: query
          name: timezone
          required: false
          description: |
            IANA timezone the feed is computed in: the YYYY-MM-DD day keys, the
            parsing of a YYYY-MM-DD time_range, and the 9-day window floor are
            all resolved in this zone. Send the zone the client draws its day
            rings in. When absent, or when the name does not resolve to a
            zone, the user's stored timezone applies (working timezone unless
            they opted into their primary timezone, then primary, then working,
            then UTC). "Local" and names over 64 characters are rejected
            with 400.
          schema:
            type: string
          example: Africa/Nairobi
      responses:
        '200':
          description: The day-grouped activity feed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityFeedEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /teams:
    get:
      tags:
        - Teams
      summary: Get all teams
      description: |
        Returns a page of the teams the authenticated user is an accepted
        member of, newest first. Each item carries the team's total member
        count and the authenticated user's own membership. `with_favourites=false` excludes
        teams the authenticated user has favourited; `query` is a case-insensitive title
        substring search.
      operationId: listTeams
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: query
          required: false
          schema:
            type: string
            maxLength: 255
          description: Case-insensitive team-title substring filter.
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
          description: Page size (per_page).
        - in: query
          name: with_favourites
          required: false
          schema:
            type: boolean
            default: true
          description: When false, excludes the authenticated user's favourited teams.
      responses:
        '200':
          description: A page of the authenticated user's teams.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListTeamsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Teams
      summary: Create a team
      description: |
        Creates a team owned by the authenticated user. The owner is taken
        from the verified session (urn:orbiter:user_id claim), not the body.
        The creator is added as an accepted admin; each id in `user_ids` is
        added as a pending invite (deduped). Returns the created team.
      operationId: createTeam
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTeamRequest'
      responses:
        '201':
          description: Created team.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /teams/{team_id}:
    get:
      tags:
        - Teams
      summary: Get one team
      description: |
        Returns a single team. The authenticated user must be a member of the team (any
        status); a non-member or an unknown team returns 404 so a team's
        existence isn't leaked to outsiders.
      operationId: getTeam
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The team.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Teams
      summary: Update a team
      description: |
        Full replace of a team's editable fields. The authenticated user must be an accepted
        admin member of the team (the creator, or a promoted admin); an unknown
        team or a non-admin authenticated user returns 404.
      operationId: updateTeam
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTeamRequest'
      responses:
        '200':
          description: Updated team.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Teams
      summary: Delete a team
      description: |
        Deletes a team and (via ON DELETE CASCADE) its members, questions, and
        question nodes. The authenticated user must be an accepted admin member; an unknown
        team or a non-admin authenticated user returns 404.
      operationId: deleteTeam
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /teams/{team_id}/members:
    get:
      tags:
        - Teams
      summary: Get all team members
      description: |
        Paginated list of a team's members, newest first. The authenticated user must be a
        member of the team; non-members get 404. (person profile
        enrichment is not included yet.)
      operationId: listTeamMembers
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: A page of the team's members.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListTeamMembersResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Teams
      summary: Add team members
      description: |
        Invites users to a team as pending members (is_admin=false,
        status=pending). The authenticated user must be an accepted admin; users already in
        the team are skipped. Returns the members actually added.
      operationId: addTeamMembers
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddTeamMembersRequest'
      responses:
        '201':
          description: The members added (already-members omitted).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddTeamMembersResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /teams/{team_id}/members/{member_id}:
    patch:
      tags:
        - Teams
      summary: Update a team member
      description: |
        Partial update of a membership. `is_admin` may only be changed by a team
        admin; `is_favourite` only by the member themselves (their own row). At
        least one field is required.
      operationId: patchTeamMember
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: member_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchTeamMemberRequest'
      responses:
        '200':
          description: The updated membership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamMemberEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Teams
      summary: Remove a team member
      description: |
        Removes a member from a team. A team admin may remove anyone; a member
        may remove themselves (leave). Others get 403.
      operationId: removeTeamMember
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: member_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Removed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /teams/{team_id}/members/{member_id}/invitation:
    put:
      tags:
        - Teams
      summary: Respond to an invitation
      description: |
        The invitee accepts or rejects their own pending invitation. Only the
        invitee may respond (others get 404); an already-answered invite gives 409.
      operationId: respondTeamInvitation
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: member_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RespondInvitationRequest'
      responses:
        '200':
          description: The updated membership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamMemberEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /team-contains:
    get:
      tags:
        - Teams
      summary: Get teams shared with a user
      description: |
        Returns the teams the authenticated user and the queried `user_id` are
        BOTH accepted members of (the intersection), newest first. Each item
        carries the team's member count and the authenticated user's own membership. Empty
        when they share no teams.
      operationId: listSharedTeams
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: user_id
          required: true
          schema:
            type: string
            format: uuid
          description: The other user to intersect the authenticated user's teams with.
      responses:
        '200':
          description: The teams the authenticated user and the queried user share.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SharedTeamsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /team-favourites:
    get:
      tags:
        - Teams
      summary: Get all team favourites
      description: |
        Returns the teams the authenticated user has favourited (accepted
        membership with is_favourite=true), in the authenticated user's custom order
        (membership position ascending). Optionally filtered by a case-insensitive
        title substring. Unpaginated. Each item is the same shape as a GET /teams
        item — every one is, by definition, a favourite.
      operationId: listFavouriteTeams
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: query
          required: false
          schema:
            type: string
          description: Case-insensitive team-title substring filter.
      responses:
        '200':
          description: The authenticated user's favourited teams.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FavouriteTeamsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - Teams
      summary: Toggle a team favourite
      description: |
        Sets the authenticated authenticated user's `is_favourite` flag on their own
        membership in the team named by `team_id`, and maintains the contiguous
        position order their favourites list relies on: favouriting appends the
        team to the end of their favourites (keeping its place if already a
        favourite); unfavouriting clears its slot and re-compacts the rest.
        Returns the updated membership. 404 when the authenticated user is not a member.
      operationId: patchTeamFavourite
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchTeamFavouriteRequest'
      responses:
        '200':
          description: The updated membership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamMemberEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /team-favourites/reorder:
    patch:
      tags:
        - Teams
      summary: Reorder favourite teams
      description: |
        Assigns the authenticated user's favourited teams the order given by `team_ids` (first
        → position 0, second → 1, …). Every id must be one of the authenticated user's
        favourited teams; a non-matching id rejects the whole request (400) and
        nothing changes (applied atomically). Duplicate ids are rejected.
      operationId: reorderFavouriteTeams
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderFavouritesRequest'
      responses:
        '200':
          description: Reorder applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReorderFavouritesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /team-questions:
    post:
      tags:
        - Teams
      summary: Ask a team question
      description: |
        Records a question asked against a team. The asker is taken from the
        verified session, not the body. The authenticated user must be an accepted member of
        the team (non-members get 404, pending invitees 403). `team_question_id`
        threads the new question under an existing parent question in the same
        team. `llm_response` starts empty — it is filled later by the
        semantic-query pipeline.
      operationId: createTeamQuestion
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTeamQuestionRequest'
      responses:
        '201':
          description: The created team question.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamQuestionResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /team-questions/{team_question_id}:
    put:
      tags:
        - Teams
      summary: Update a team question
      description: |
        Edits only the question text of a team question. Only the asker may edit
        their own question (others get 403); an unknown question returns 404.
      operationId: updateTeamQuestion
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: team_question_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTeamQuestionRequest'
      responses:
        '200':
          description: The updated team question.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamQuestionResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /orbiter-connect-requests:
    get:
      tags:
        - Connections
      summary: List pending connection requests
      description: |
        Returns every unresolved connection request the authenticated user is
        on either side of, newest first. A request is unresolved when it has
        not been accepted, declined, or cleared.

        Both directions come back in one list — split them on
        `user_from_id`: equal to the authenticated user's id means the request
        was sent by them, otherwise it was received. Unpaginated; pending
        requests are inherently bounded. `meta.total` carries the count.
      operationId: listOrbiterConnectRequests
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's pending requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListConnectRequestsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Connections
      summary: Ask another user to connect
      description: |
        Creates a pending connection request from the authenticated user to
        `user_id` and returns `status: "pending"` (201).

        If `user_id` already has a pending request out to the authenticated
        user, the two are connected immediately instead: that request is
        accepted, both join rows are written, and the response is
        `status: "connected"` (200).

        A request the recipient previously declined — or that either side
        cleared — is revived by asking again, so the client may offer connect
        again after a rejection. A request the recipient *blocked* is not
        revivable and 403s.

        The requester always comes from the session, never the body.
      operationId: sendOrbiterConnectRequest
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendConnectRequest'
      responses:
        '200':
          description: The two users were connected immediately (reciprocal request existed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendConnectRequestEnvelope'
        '201':
          description: A pending request was created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendConnectRequestEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: |
            This user blocked the authenticated user; the request cannot be sent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: |
            Already connected, or a live request to this user already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /orbiter-connect-requests/{orbiter_connect_request_id}:
    put:
      tags:
        - Connections
      summary: Respond to a connection request
      description: |
        Applies one action to a pending request. Exactly one of `accepted`,
        `declined`, `blocked`, `cleared` must be true — several or none is a
        400.

        `accepted` stamps the request and writes both connection join rows in
        one transaction, then puts each user in the other's contacts and
        notifies the original requester. `declined` marks it declined;
        `blocked` additionally sets `from_blocked`. `cleared` dismisses the
        request from the pending list without accepting or declining.

        Accept, decline, and block are the recipient's alone (403 otherwise);
        either party may clear. An already-accepted request 409s. A request
        the authenticated user is not party to 404s.
      operationId: updateOrbiterConnectRequest
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: orbiter_connect_request_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateConnectRequest'
      responses:
        '200':
          description: The updated request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectRequestEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Only the recipient may accept, decline, or block.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The request was already accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /orbiter-connections:
    get:
      tags:
        - Connections
      summary: List established connections
      description: |
        Returns the authenticated user's established Orbiter connections,
        newest first, each with the connected-to person's card. Unpaginated;
        `meta.total` carries the count.
      operationId: listOrbiterConnections
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's connections.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListConnectionsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /orbiter-connections/{orbiter_connection_join_id}:
    delete:
      tags:
        - Connections
      summary: Remove a connection
      description: |
        Disconnects the pair. The path id is the authenticated user's own join
        row; someone else's row 404s. Both directions are removed, along with
        the spent request rows so the pair can connect again later.
      operationId: deleteOrbiterConnection
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: orbiter_connection_join_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Removed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-questions:
    get:
      tags:
        - Collections
      summary: Get all collection questions
      description: |
        Returns the authenticated user's own questions on a collection (each with its LLM
        response and processing flag). Scoped to the authenticated user — you only ever see
        your own Q&A.
      operationId: listCollectionQuestions
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The authenticated user's questions on the collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionQuestionsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Collections
      summary: Ask a collection question
      description: |
        Saves a question about a collection and queues its answer. The question is
        answered over the collection's members (people and companies, newest first,
        capped) by a background worker; the response is the question with
        `processing: true` and no answer yet. Poll `GET /collection-questions` until
        `processing` is false, then read the answer nodes from
        `GET /collection-question-nodes`. The caller must own or be a member of the
        collection, and the collection must have members to answer over.
      operationId: askCollectionQuestion
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AskCollectionQuestionRequest'
      responses:
        '201':
          description: The saved question, still processing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionQuestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-questions/{collection_question_id}:
    put:
      tags:
        - Collections
      summary: Update a collection question
      description: |
        Edits a question's text. Only the asker may edit it; an unknown question
        or one the authenticated user didn't ask returns 404.
      operationId: updateCollectionQuestion
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_question_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCollectionQuestionRequest'
      responses:
        '200':
          description: The updated question.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionQuestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Collections
      summary: Delete a collection question
      description: |
        Deletes a question. Only the asker may delete it; an unknown question or
        one the authenticated user didn't ask returns 404.
      operationId: deleteCollectionQuestion
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_question_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Question deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-question-nodes:
    get:
      tags:
        - Collections
      summary: Get a question's answer nodes
      description: |
        Returns the members the model named as the answer to a question, in the
        order it named them, each hydrated into the shared node shape (person or
        company card, targeting, the asker's relationship paths). Only the asker may
        read them; a question that is not theirs returns 404. `paths_processing` is
        true on a node until the asker's paths to it are stored.
      operationId: listCollectionQuestionNodes
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: collection_question_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The question's answer nodes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionQuestionNodesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-reactions:
    post:
      tags:
        - Collections
      summary: React to a comment
      description: |
        Sets the authenticated user's reaction on a comment to an emoji. One reaction per
        user per comment — re-reacting replaces the emoji (the row is kept). The
        authenticated user must have access to the comment's collection.
      operationId: createCommentReaction
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCommentReactionRequest'
      responses:
        '201':
          description: The reaction (created or updated).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionReactionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-reactions/{collection_reaction_id}:
    delete:
      tags:
        - Collections
      summary: Remove a reaction
      description: |
        Deletes one of the authenticated user's reactions by id. Only the owner may delete
        it; an unknown reaction or one the authenticated user didn't make returns 404.
      operationId: deleteCommentReaction
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_reaction_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Reaction removed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-contains:
    get:
      tags:
        - Collections
      summary: Get collections containing an entity
      description: |
        Returns the authenticated user's own collections that pin a given person or company,
        each with its total node count and the id of the node that links the
        entity. Provide exactly one of person_id / company_id.
        Owner-scoped — shared collections are not included.
      operationId: listCollectionsContaining
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: company_id
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The authenticated user's collections that contain the entity.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionsContainingResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-comments:
    get:
      tags:
        - Collections
      summary: Get all collection comments
      description: |
        Returns a collection's comments, newest first, each enriched with its
        author (person name + avatar) and the reactions on it. The authenticated user
        must have access to the collection.
      operationId: listCollectionComments
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The collection's comments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionCommentsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Collections
      summary: Comment on a collection
      description: |
        Adds a comment to a collection, authored by the authenticated user. The authenticated user must
        have access to the collection.
      operationId: createCollectionComment
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCollectionCommentRequest'
      responses:
        '201':
          description: The created comment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionCommentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-comments/{collection_comment_id}:
    put:
      tags:
        - Collections
      summary: Update a collection comment
      description: |
        Edits a comment's text. Only the comment's author may edit it; an unknown
        comment or one the authenticated user didn't write returns 404.
      operationId: updateCollectionComment
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_comment_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCollectionCommentRequest'
      responses:
        '200':
          description: The updated comment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionCommentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Collections
      summary: Delete a collection comment
      description: |
        Deletes a comment. Only the comment's author may delete it; an unknown
        comment or one the authenticated user didn't write returns 404.
      operationId: deleteCollectionComment
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_comment_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Comment deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-favourites:
    get:
      tags:
        - Collections
      summary: Get all collection favourites
      description: |
        Returns a page of the collections the authenticated user has favourited, in their
        chosen favourite order (collection_favourite.position). Each item carries
        its node count, the teams it's shared through, and the authenticated user's own
        questions — the same shape as GET /collections. `query` is a
        case-insensitive title substring search.
      operationId: listCollectionFavourites
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: query
          required: false
          schema:
            type: string
            maxLength: 255
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: A page of the authenticated user's favourite collections.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Collections
      summary: Favourite a collection
      description: |
        Adds a collection to the authenticated user's favourites (idempotent — re-favouriting
        returns the existing entry). A new favourite is appended to the end of
        the list. The authenticated user must have access to the collection.
      operationId: addCollectionFavourite
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCollectionFavouriteRequest'
      responses:
        '201':
          description: The favourite (existing or new).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionFavouriteEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Collections
      summary: Unfavourite a collection
      description: |
        Removes a collection from the authenticated user's favourites and re-compacts the
        remaining positions to a dense 0,1,2… order. Idempotent — succeeds even
        if the collection wasn't favourited.
      operationId: removeCollectionFavourite
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Unfavourited (or was not a favourite).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-favourites/reorder:
    patch:
      tags:
        - Collections
      summary: Reorder favourites
      description: |
        Sets the authenticated user's favourites to the submitted order — each collection's
        position becomes its index in collection_ids. Every id must already be
        one of the authenticated user's favourites; otherwise the whole reorder is rejected.
      operationId: reorderCollectionFavourites
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderCollectionFavouritesRequest'
      responses:
        '200':
          description: Favourites reordered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /collections:
    get:
      tags:
        - Collections
      summary: Get all collections
      description: |
        Returns a page of the collections the authenticated user can see —
        ones they own or ones shared with them via a collection join — in the
        authenticated user's saved sidebar order first, then latest activity
        (updated_at, created_at, id) for collections with no saved position.
        Each item carries its node count, the teams it's
        shared through (an array; a collection can be shared through several),
        and the authenticated user's own questions on it. `with_favourites=false` excludes
        the authenticated user's favourited collections; `query` is a case-insensitive title
        substring search.
      operationId: listCollections
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: query
          required: false
          schema:
            type: string
            maxLength: 255
          description: Case-insensitive collection-title substring filter.
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
          description: Page size (per_page).
        - in: query
          name: with_favourites
          required: false
          schema:
            type: boolean
            default: true
          description: When false, excludes the authenticated user's favourited collections.
      responses:
        '200':
          description: A page of the authenticated user's collections.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Collections
      summary: Create a collection
      description: |
        Creates a collection owned by the authenticated user. The owner is
        taken from the verified session (urn:orbiter:user_id claim), not the
        body. When `team_id` is given, every other member of that team is added
        to the collection (the creator is the owner, not a member row), and each
        newly-added member receives a notification. Returns the created
        collection.
      operationId: createCollection
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCollectionRequest'
      responses:
        '201':
          description: Created collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /collections/reorder:
    patch:
      tags:
        - Collections
      summary: Reorder collections in the sidebar
      description: |
        Persists the authenticated user's sidebar order for the submitted
        collections. Covers owned and shared collections, independent of
        favourites (Starred keeps its own ordering). A partial list is allowed:
        the submitted collections are placed in the submitted order within
        their existing slots, and every collection not sent keeps its slot.
        An unknown or inaccessible id rejects the whole reorder.
      operationId: reorderCollections
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderCollectionsRequest'
      responses:
        '200':
          description: Collections reordered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /collections/{collection_id}:
    get:
      tags:
        - Collections
      summary: Get one collection
      description: |
        Returns a single collection the authenticated user can see (owns or is shared with),
        enriched with its node count, the teams it's shared through, and the
        authenticated user's own questions on it. An unknown or inaccessible collection
        returns 404 (existence isn't leaked).
      operationId: getCollection
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionListItemEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Collections
      summary: Update a collection
      description: |
        Full replace of a collection's editable fields. Only the collection's
        owner may update it — a member who was shared the collection gets 403,
        and an unknown or inaccessible collection returns 404 (existence isn't
        leaked).
      operationId: updateCollection
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCollectionRequest'
      responses:
        '200':
          description: The updated collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Collections
      summary: Delete a collection
      description: |
        Deletes a collection and all of its contents (members, nodes,
        questions, comments, reactions, favourites cascade; node ratings and
        assignments are cleared too). Only the owner may delete — a member gets
        403, and an unknown or inaccessible collection returns 404.
      operationId: deleteCollection
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Collection deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-members:
    get:
      tags:
        - Collections
      summary: Get all collection members
      description: |
        Returns the distinct teams an existing collection is shared through
        (collection_join.team_id). The authenticated user must have access to the collection;
        an unknown or inaccessible one returns 404.
      operationId: listCollectionTeams
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The teams the collection is shared with.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionTeamsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Collections
      summary: Share a collection with a team
      description: |
        Invites a team's other members to an existing collection (idempotent —
        members already on it are skipped). The inviter is the authenticated user;
        newly-added members get a best-effort notification. The authenticated user must have
        access to the collection. Returns the count newly invited.
      operationId: shareCollectionWithTeam
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShareCollectionWithTeamRequest'
      responses:
        '200':
          description: Team members invited.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShareCollectionWithTeamResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Collections
      summary: Un-share a collection
      description: |
        Removes every membership a collection gained through a team (direct
        invites are untouched). The authenticated user must have access to the collection.
        Idempotent — removing a team that wasn't sharing it still succeeds.
      operationId: unshareCollectionTeam
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: team_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Team un-shared (or was not sharing it).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-nodes:
    get:
      tags:
        - Collections
      summary: Get all collection nodes
      description: |
        Returns a page of the nodes pinned to a collection, newest first, each
        in the shared node shape every feature serves: `entity_type` names the
        pinned entity's table, `node_uuid` is its row id (the master graph's node
        id), and exactly one entity object is non-null. The authenticated user
        must have access to the collection; an unknown or inaccessible one
        returns 404.
      operationId: listCollectionNodes
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: collection_id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: A page of enriched collection nodes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCollectionNodesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Collections
      summary: Add nodes to a collection
      description: |
        Bulk-pins entities to a collection as nodes. Each `nodes` ref becomes one
        node; an entity already pinned to the collection is skipped (idempotent).
        A ref naming a kind or an entity the backend does not hold is a 400.
        Anyone with access to the collection (owner or member) may add. Returns
        the count of nodes newly added.
      operationId: addCollectionNodes
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCollectionNodesRequest'
      responses:
        '201':
          description: Nodes added (count of new ones).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddCollectionNodesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-nodes/move:
    post:
      tags:
        - Collections
      summary: Move nodes
      description: |
        Moves the listed nodes into collection_id. The authenticated user must have access to
        the target collection and to every node's source collection. A node whose
        entity is already pinned to the target is merged (its source row is
        removed) rather than duplicated. Returns how many nodes were moved vs
        merged. Unknown ids are skipped; 404 when the target/a source is
        inaccessible or no nodes were found.
      operationId: moveCollectionNodes
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveCollectionNodesRequest'
      responses:
        '200':
          description: Move outcome (moved + merged counts).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MoveCollectionNodesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-nodes/{collection_node_id}/assign:
    post:
      tags:
        - Collections
      summary: Assign a node
      description: |
        Assigns a collection node to a user, in a labelled context. The recipient
        (user_id) and label come from the body; the assigner is the session
        authenticated user. The authenticated user must have access to the node's collection. Idempotent:
        re-assigning the same (node, user) refreshes the label/assigner.
      operationId: assignCollectionNode
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_node_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignCollectionNodeRequest'
      responses:
        '201':
          description: Assignment created (or refreshed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionNodeAssignmentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-nodes/{collection_node_id}/ratings:
    post:
      tags:
        - Collections
      summary: Rate a node
      description: |
        Records a 1-5 rating for the node on behalf of user_id (the session
        authenticated user is the authoriser), then recomputes the node's aggregate score
        (collection_node.rating = mean rounded to 1dp) and returns the new
        average + distinct-rater count. The authenticated user must have access to the node's
        collection. Idempotent: re-rating the same (node, user) replaces the
        prior score.
      operationId: rateCollectionNode
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: collection_node_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RateCollectionNodeRequest'
      responses:
        '201':
          description: Rating recorded; node aggregate returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionNodeRatingSummaryEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /collection-nodes/{node_id}:
    delete:
      tags:
        - Collections
      summary: Remove a node
      description: |
        Deletes a single collection node (and its rating/assignment rows). The
        authenticated user must have access to the node's collection; an unknown or
        inaccessible node returns 404.
      operationId: deleteCollectionNode
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: node_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Node deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /phones/contacts/{contact_id}/primary:
    put:
      tags:
        - Phones
      operationId: setContactPrimaryPhone
      summary: Set a contact's primary phone
      description: Atomically makes a linked phone the caller-owned contact's only primary.
      security:
        - bearerAuth: []
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone_id
              properties:
                phone_id:
                  type: string
                  format: uuid
      responses:
        '200':
          description: Selected primary phone, with the caller's note.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PhoneEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /phones:
    get:
      tags:
        - Phones
      summary: List phones for a person or company
      description: |
        Returns every caller-visible phone for one master person or company
        (exactly one of person_id / company_id), newest first, each with the
        caller's private note. Phones are read through the caller's contact
        join rows; when the caller has no contact for the person yet, the
        fallback is the person's public phones plus the caller's own.
        Unpaginated — phones per contact are bounded; meta carries total.
      operationId: listPhones
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: company_id
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The phones with the caller's notes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListPhonesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Phones
      summary: Add a phone to a person or company
      description: |
        Creates a phone on one master person or company (exactly one of
        person_id / company_id), auto-creating the caller's contact when
        absent, then joins the phone to it and stores the optional private
        note. The creator comes from the session, not the body.
      operationId: createPhone
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePhoneRequest'
      responses:
        '201':
          description: Created phone with the caller's note.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PhoneEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /phones/{phone_id}:
    put:
      tags:
        - Phones
      summary: Update a phone and the caller's note
      description: |
        Edits the phone's fields only when the caller created it; the number
        is normalized to E.164 with its country calling code when parseable.
        Regardless of ownership the caller's private note is upserted when
        phone_note is set and deleted when it is null. Non-owners get a 200
        with data null (only their note changed).
      operationId: updatePhone
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: phone_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePhoneRequest'
      responses:
        '200':
          description: Updated phone, or data null when the caller is not the creator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PhoneOrNullEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Phones
      summary: Delete or unlink a phone
      description: |
        Removes the caller's note and contact join rows for the phone. The
        phone record itself is hard-deleted only when the caller created it;
        public/enrichment phones are just unlinked from the caller.
      operationId: deletePhone
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: phone_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /links:
    get:
      tags:
        - Links
      summary: List links for a person or company
      description: |
        Returns every caller-visible link for one master person or company
        (exactly one of person_id / company_id), newest first. Links are
        read through the caller's contact join rows; when the caller has no
        contact for the person yet, the fallback is the person's public
        links plus the caller's own. Unpaginated — links per contact are
        bounded; meta carries total.
      operationId: listLinks
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: company_id
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The links.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListLinksEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Links
      summary: Add a link to a person or company
      description: |
        Attaches a link to one master person or company (exactly one of
        person_id / company_id), auto-creating the caller's contact when
        absent. The URL is normalized (https, trailing slash stripped) and
        an existing link with the same URL on the target is reused instead
        of duplicated. Domain, label and social_profile are derived
        server-side. The creator comes from the session, not the body.
      operationId: createLink
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLinkRequest'
      responses:
        '201':
          description: Created (or reused) link.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LinkEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /links/{link_id}:
    put:
      tags:
        - Links
      summary: Update a link
      description: |
        Edits a link the caller created: the URL is normalized and domain
        metadata re-derived. A LinkedIn person-profile URL (/in/, /pub/)
        clears the link's company owner; a company-profile URL (/company/,
        /school/, /showcase/) clears the person owner. Links created by
        others or by enrichment return 404 — the lookup is owner-scoped.
      operationId: updateLink
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: link_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateLinkRequest'
      responses:
        '200':
          description: Updated link.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LinkEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Links
      summary: Delete or unlink a link
      description: |
        Removes the caller's contact join rows for the link. The link
        record itself is hard-deleted only when the caller created it;
        public/enrichment links are just unlinked from the caller.
      operationId: deleteLink
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: link_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /voice/speech:
    post:
      tags:
        - Voice
      summary: Synthesize speech
      description: |
        Streams an mp3 rendering of `text` in the ElevenLabs voice
        `voice_id` (eleven_multilingual_v2, mp3_44100_128). The success body
        is raw `audio/mpeg`, not an envelope, and is flushed as it is
        produced so playback can start early. Validation and upstream
        failures before any audio is sent return the usual error envelope.
        Text is trimmed and capped at 5000 characters.
      operationId: synthesizeSpeech
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpeechRequest'
      responses:
        '200':
          description: The spoken audio, streamed.
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /voice/transcriptions:
    post:
      tags:
        - Voice
      summary: Transcribe a recorded clip
      description: |
        Sends one recorded clip (webm/opus or mp4, under 25MB) to ElevenLabs
        Scribe (`scribe_v1`) and returns the trimmed transcript.
      operationId: transcribeSpeech
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: The transcript.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TranscriptionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /email-addresses/contacts/{contact_id}/primary:
    put:
      operationId: setContactPrimaryEmail
      tags:
        - EmailAddresses
      security:
        - bearerAuth: []
      summary: Set the primary email for an owned contact
      description: |
        Atomically attaches or creates the address and makes its contact join
        the only primary. Other contacts are unchanged. Repeating the same
        address is safe. Contact-scoped reads return the primary first.
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - address
              properties:
                address:
                  type: string
                  format: email
      responses:
        '200':
          description: Saved primary email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailAddressEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-addresses:
    get:
      tags:
        - EmailAddresses
      summary: List email addresses for a person or company
      description: |
        Returns every caller-visible email for one master person or company
        (exactly one of person_id / company_id), contact primary first, then newest. Emails are
        read through the caller's contact join rows (contact_emails /
        company_emails); when the caller has no contact for the person yet,
        the fallback is the person's public emails plus the caller's own
        from email_addresses. Unpaginated — meta carries total.
      operationId: listEmailAddresses
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: company_id
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The email addresses.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEmailAddressesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - EmailAddresses
      summary: Add an email address to a person or company
      description: |
        Attaches an email to one master person or company (exactly one of
        person_id / company_id). When the caller has no contact for the
        target yet, one is created first (the copy-to-my flow), then the
        email lands in the relevant contact join table. The address is
        stored lowercase and an existing email with the same address on the
        target is reused instead of duplicated. The creator comes from the
        session, not the body.
      operationId: createEmailAddress
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmailAddressRequest'
      responses:
        '201':
          description: Created (or reused) email address.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailAddressEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /email-addresses/{email_address_id}:
    put:
      tags:
        - EmailAddresses
      summary: Update an email address
      description: |
        Edits an email the caller created (address re-normalized to
        lowercase). Emails created by others or by enrichment return 404 —
        the edit is owner-scoped.
      operationId: updateEmailAddress
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: email_address_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEmailAddressRequest'
      responses:
        '200':
          description: Updated email address.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailAddressEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - EmailAddresses
      summary: Delete or unlink an email address
      description: |
        Removes the caller's contact join rows for the email. The email
        record itself is hard-deleted only when the caller created it;
        public/enrichment emails are just unlinked from the caller.
      operationId: deleteEmailAddress
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: email_address_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /addresses/contacts/{contact_id}/primary:
    put:
      tags:
        - Addresses
      operationId: setContactPrimaryAddress
      summary: Set a contact's primary address
      description: Atomically makes a linked address the caller-owned contact's only primary.
      security:
        - bearerAuth: []
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - address_id
              properties:
                address_id:
                  type: string
                  format: uuid
      responses:
        '200':
          description: Selected primary address, with the caller's note.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddressEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /addresses:
    get:
      tags:
        - Addresses
      summary: List addresses for a person or company
      description: |
        Returns every caller-visible address for one master person or
        company (exactly one of person_id / company_id), newest first, each
        with the caller's private note. Addresses are read through the
        caller's contact join rows; when the caller has no contact for the
        person yet, the fallback is the person's public addresses plus the
        caller's own. Unpaginated — meta carries total.
      operationId: listAddresses
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: company_id
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The addresses with the caller's notes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAddressesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Addresses
      summary: Add an address to a person or company
      description: |
        Attaches an address to one master person or company (exactly one of
        person_id / company_id), auto-creating the caller's contact when
        absent. An existing address with the same formatted_address on the
        target is reused instead of duplicated; the optional private note
        is stored either way. Geocoding / component resolution is an
        enrichment concern and not performed here. The creator comes from
        the session, not the body.
      operationId: createAddress
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAddressRequest'
      responses:
        '201':
          description: Created (or reused) address with the caller's note.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddressEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /addresses/{address_id}:
    put:
      tags:
        - Addresses
      summary: Update an address and the caller's note
      description: |
        Edits the address only when the caller created it: the structural
        components are stored and formatted_address is rebuilt from them
        (comma-joined non-empty parts). Regardless of ownership the
        caller's private note is upserted when address_note is set and
        deleted when it is null. Non-owners get a 200 with data null (only
        their note changed).
      operationId: updateAddress
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: address_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAddressRequest'
      responses:
        '200':
          description: Updated address, or data null when the caller is not the creator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddressOrNullEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Addresses
      summary: Delete or unlink an address
      description: |
        Removes the caller's note and contact join rows for the address.
        The address record itself is hard-deleted only when the caller
        created it; public/enrichment addresses are just unlinked.
      operationId: deleteAddress
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: address_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /dates:
    get:
      tags:
        - Dates
      summary: List important dates for a person
      description: |
        Returns the caller's important dates for one person, newest first.

        Unlike phones/emails/addresses/links these are NOT shared records:
        a date belongs to the caller's contact and is private to them, so a
        caller with no contact for the person gets an empty list — there is
        no public/enrichment fallback. Unpaginated; meta carries total.
      operationId: listDates
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The dates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListDatesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Dates
      summary: Add an important date to a person
      description: |
        Attaches an important date to a person, auto-creating the caller's
        contact when absent. The label is resolved from `date_descriptor_id`
        when given, otherwise found-or-created from `date_descriptor` (the
        sheet's picker is name-driven, so a just-created label works before
        the descriptor list refetches). A blank note is stored as null.
      operationId: createDate
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDateRequest'
      responses:
        '201':
          description: Created date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /dates/{date_id}:
    put:
      tags:
        - Dates
      summary: Update an important date
      description: |
        Edits a date on the caller's own contact. Dates on another user's
        contact are invisible to the caller and return 404, not 403.
      operationId: updateDate
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: date_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDateRequest'
      responses:
        '200':
          description: Updated date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Dates
      summary: Delete an important date
      description: |
        Deletes a date on the caller's own contact. Dates on another user's
        contact return 404.
      operationId: deleteDate
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: date_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /date-descriptors:
    get:
      tags:
        - Dates
      summary: List date labels
      description: |
        The shared label vocabulary for important dates ("Birthday",
        "Anniversary", ...), alphabetically. Filtered by a case-insensitive
        substring match when `query` is given.
      operationId: listDateDescriptors
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The descriptors.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListDateDescriptorsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Dates
      summary: Add a date label
      description: |
        Adds a label to the shared vocabulary. Names are unique
        case-insensitively — an existing match is returned instead of
        erroring, so "birthday" resolves to the seeded "Birthday".
      operationId: createDateDescriptor
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDateDescriptorRequest'
      responses:
        '201':
          description: Created (or reused) descriptor.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DateDescriptorEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /relationships/{person_id}:
    get:
      tags:
        - Relationships
      summary: Get the caller's relationship record for a person
      description: |
        Returns the caller's "How I Know" record for the person: the running
        or finished interview transcript, the reflection, and the nuggets.
        `data` is null when no interview has started yet; that is a normal
        state, not a 404.
      operationId: getRelationship
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PersonID'
      responses:
        '200':
          description: The record, or null data when none exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RelationshipOrNullEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Relationships
      summary: Upsert the caller's relationship record for a person
      description: |
        Creates or updates the record keyed by (caller, person). Partial
        semantics: a field absent from the body is left untouched; a present
        field replaces the stored value. `relationship_nuggets: null` clears
        the nuggets. The caller's contact for the person, when they hold one,
        is stamped on the row. Returns the full record.
      operationId: upsertRelationship
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PersonID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpsertRelationshipRequest'
      responses:
        '200':
          description: The full record after the write.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RelationshipEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /relationships/{person_id}/interview:
    post:
      tags:
        - Relationships
      summary: Run one turn of the relationship interview
      description: |
        Pure compute: nothing is persisted. The frontend sends the running
        transcript (or the literal `(no prior turns yet)` for the opening
        turn) and the previous turn's summary, and saves the result through
        the PUT afterwards.

        The interviewer asks one plain-text question per turn, skipping what
        the person's card and the caller's contact note already say. It never
        closes before 8 exchanges and always closes by 14; in between it
        closes as soon as every coverage area is answered. On `ready: true`,
        `summary` is the closing reflection (two to four short paragraphs in
        the second person), `next_question` is empty, and `nuggets` is
        complete.
      operationId: relationshipInterviewTurn
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PersonID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InterviewRequest'
      responses:
        '200':
          description: The next question, or the closing reflection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InterviewTurnEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /work-experiences:
    get:
      tags:
        - Experiences
      summary: List a person's work history
      description: |
        Returns a master person's work history (shared enrichment data, the
        same for every caller), newest/current first. `person_id` is
        required. Each entry embeds its linked company (employer) resolved
        via company_id, or null when unmatched. Unpaginated — meta carries
        total.
      operationId: listWorkExperiences
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The person's work history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListWorkExperiencesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /education-experiences:
    get:
      tags:
        - Experiences
      summary: List a person's education history
      description: |
        Returns a master person's education history (shared enrichment data),
        newest first. `person_id` is required. Each entry embeds its linked
        company (the school) resolved via company_id, or null when unmatched.
        Unpaginated — meta carries total.
      operationId: listEducationExperiences
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: person_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The person's education history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEducationExperiencesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /discover:
    get:
      tags:
        - Discover
      summary: Get all discover
      description: |
        Returns a page of the authenticated user's own, non-archived discover records,
        newest-activity first. Each item carries its answer-node count and its
        follow-up questions. `query` is a case-insensitive substring search
        over the research text.
      operationId: listDiscover
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: query
          required: false
          schema:
            type: string
            maxLength: 255
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: A page of the authenticated user's discover records.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListDiscoverEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Discover
      summary: Create a discover research query
      description: |
        Runs one research query and returns the created record with its
        synthesised `answer`. The record's owner is the authenticated user;
        it is never taken from the body.

        Supply either `query` (natural language, searched as a vector KNN over
        the master graph) or `selected_nodes` (entity ids the caller already
        picked, which replace the search as the answer's context). Sending
        neither returns 400.

        Set `discover_id` to file the new record as a follow-up question under
        an existing discover; the caller must own that discover (403 otherwise)
        and the question inherits its team scope when `team_ids` is omitted.

        Connection paths are computed asynchronously. The record comes back with
        `processing: true` and its nodes with `paths_processing: true`; poll
        `GET /discover/{discover_id}` and the node endpoints until they clear.
        An unanswerable question still creates the record, with a fallback
        `answer` rather than an error.
      operationId: createDiscover
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDiscoverRequest'
      responses:
        '201':
          description: The created discover record.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscoverDetailEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /discover/{discover_id}:
    get:
      tags:
        - Discover
      summary: Get one discover
      description: |
        Returns one discover record by id with its follow-up
        questions. Auth-gated but not
        owner-scoped: any signed-in authenticated user can fetch any record by id. Unknown
        ids return 404.
      operationId: getDiscover
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: discover_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The discover record with its questions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscoverDetailEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Discover
      summary: Update a discover
      description: |
        Replaces a discover's `query` and `archived` flag and stamps the
        authenticated user as the record's owner. `answer` / `processing`
        are left untouched. Auth-gated but not
        owner-scoped: any signed-in authenticated user can update any record by id. Unknown
        ids return 404.
      operationId: updateDiscover
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: discover_id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiscoverUpdateRequest'
      responses:
        '200':
          description: The updated discover record.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscoverRecordEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Discover
      summary: Delete a discover
      description: |
        Deletes a discover record and its entire answer graph (questions and
        context / response nodes cascade). Auth-gated but not owner-scoped: any
        signed-in authenticated user can delete any record by id. Unknown ids return 404.
      operationId: deleteDiscover
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: discover_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /discover/{discover_id}/candidates:
    get:
      tags:
        - Discover
      summary: List a discover's Deep Discover candidates
      description: |
        Returns every entity the Deep Discover web lane found for this
        discover, kept or not: what the engines returned, its anchor, the
        relevance score and reason, and where it landed (`status`, and after
        the fan-in, `bucket`). Empty when the request did not set
        `deep_research` or the lane has not run yet. Auth-gated but not
        owner-scoped. Unknown ids return 404.
      operationId: listDiscoverDeepCandidates
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: discover_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The discover's candidates, kept first, best score first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeepCandidateListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /discover-context-nodes/{context_node_id}/paths:
    get:
      tags:
        - Discover
      summary: Get direct context-to-response relationships
      description: |
        Owner-scoped. Resolves the context card's entity UUID and all response
        nodes in the same answer (including follow-up answers), then reads
        direct hops from the caller's saved path tables. Missing pairs are
        looked up through Universe with max_hops=1. Does not overwrite network
        introduction paths. Funding-round context cards are excluded.
      operationId: getDiscoverContextPaths
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: context_node_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: One-hop paths with direction, edge description and resolved endpoints. Empty if no direct relationships exist.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - error
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/NodePath'
                  error:
                    type: 'null'
                  meta:
                    type: object
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /discover-context-nodes:
    get:
      tags:
        - Discover
      summary: Get all discover context nodes
      description: |
        Returns a page of a discover's context graph nodes, oldest first. Each
        node resolves its entity polymorphically (`entity_type` names the
        table, `node_uuid` the row) and is enriched with that entity plus the
        authenticated user's own contact records (`my_person` = contacts,
        `my_company` = company contacts). `discover_id` may be a follow-up
        question's id (questions are child discover records). Auth-gated but
        not owner-scoped.
      operationId: listDiscoverContextNodes
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: discover_id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: A page of the discover's context nodes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NodesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /discover-response-nodes:
    get:
      tags:
        - Discover
      summary: Get all discover response nodes
      description: |
        Returns a page of a discover's answer (response) graph nodes, oldest
        first — same shape and enrichment as the context nodes; only the kind
        differs. `discover_id` may be a follow-up question's id. Auth-gated
        but not owner-scoped.
      operationId: listDiscoverNodes
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: discover_id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: page
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: A page of the discover's answer nodes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NodesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /leverage-loops:
    get:
      operationId: listLeverageLoops
      tags:
        - Leverage Loops
      summary: List the caller's leverage loops
      description: Newest activity first. Excludes archived loops unless `status=archived`. Each item embeds its resolved person and non-archived suggestions.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
        - name: status
          in: query
          description: Filter to one status (draft, submitted, processing, suggestion, failed, archived). Empty excludes archived.
          schema:
            type: string
        - name: query
          in: query
          description: Case-insensitive substring match on request_panel_title.
          schema:
            type: string
      responses:
        '200':
          description: One page of leverage loops.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListLeverageLoopsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      operationId: createLeverageLoop
      tags:
        - Leverage Loops
      summary: Create a draft leverage loop
      description: The owner is the authenticated user. The loop starts in status `draft`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLeverageLoopRequest'
      responses:
        '201':
          description: The created loop.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /leverage-loops/{loop_id}:
    parameters:
      - $ref: '#/components/parameters/LoopID'
    get:
      operationId: getLeverageLoop
      tags:
        - Leverage Loops
      summary: Get one leverage loop
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The loop with its suggestions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateLeverageLoop
      tags:
        - Leverage Loops
      summary: Update a leverage loop
      description: 'Partial update; absent fields are unchanged. Archive with `status: archived`. A client may only move a loop to `archived`, or between `draft` and `submitted` while it is still a draft; the pull''s own states (`processing`, `suggestion`, `failed`) are written by dispatch and the worker, and a PATCH to one of them returns 400. A status change that loses a race with a dispatch returns 409.'
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateLeverageLoopRequest'
      responses:
        '200':
          description: The updated loop.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
    delete:
      operationId: deleteLeverageLoop
      tags:
        - Leverage Loops
      summary: Delete a leverage loop and everything under it
      description: A loop whose suggestions were stored (status `suggestion`, `batch_id` set) cannot be deleted, since the batch keeps it; archive it instead.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /leverage-loops/{loop_id}/dispatch:
    parameters:
      - $ref: '#/components/parameters/LoopID'
    post:
      operationId: dispatchLeverageLoop
      tags:
        - Leverage Loops
      summary: Submit a draft loop for suggestion generation
      description: 'Moves the loop to `processing`, records usage and queues it for the worker, which files a Universe leverage loop over the caller''s contacts, polls it, stores the delivered package as a batch and walks the caller''s paths to each suggestion. The loop then reads `suggestion` with `batch_id` set (read it via `GET /suggestion-batches/{batch_id}`), or `failed` with a `failure_reason`. A failed loop may be dispatched again without a second usage charge. Universe keeps one job per loop, so this re-checks that job: it recovers a loop that timed out here but finished later in Universe, while one Universe itself failed stays failed (create a new loop instead). Any other loop past draft/submitted returns 409. A loop with no `person_id`, or a caller with no contacts, returns 400.'
      security:
        - bearerAuth: []
      responses:
        '202':
          description: The loop in its processing state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /leverage-loops/{loop_id}/suggestions:
    parameters:
      - $ref: '#/components/parameters/LoopID'
    get:
      operationId: listLeverageLoopSuggestions
      tags:
        - Leverage Loops
      summary: List a loop's suggestions
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
        - name: archived
          in: query
          description: '`true` returns the archived suggestions instead of the live ones.'
          schema:
            type: boolean
            default: false
        - name: query
          in: query
          description: Case-insensitive substring match on header_text.
          schema:
            type: string
      responses:
        '200':
          description: One page of suggestions, newest activity first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListLeverageLoopSuggestionsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /leverage-loop-suggestions/{suggestion_id}:
    parameters:
      - $ref: '#/components/parameters/SuggestionID'
    get:
      operationId: getLeverageLoopSuggestion
      tags:
        - Leverage Loops
      summary: Get one suggestion
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The suggestion with its person / company resolved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopSuggestionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateLeverageLoopSuggestion
      tags:
        - Leverage Loops
      summary: Archive or restore a suggestion
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - archived
              properties:
                archived:
                  type: boolean
      responses:
        '200':
          description: The updated suggestion.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopSuggestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteLeverageLoopSuggestion
      tags:
        - Leverage Loops
      summary: Delete a suggestion and its actions, trajectories and questions
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /leverage-loop-suggestions/{suggestion_id}/actions:
    parameters:
      - $ref: '#/components/parameters/SuggestionID'
    get:
      operationId: listLeverageLoopActions
      tags:
        - Leverage Loops
      summary: List a suggestion's actions
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: One page of live actions, oldest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListLeverageLoopActionsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /leverage-loop-suggestions/{suggestion_id}/trajectories:
    parameters:
      - $ref: '#/components/parameters/SuggestionID'
    get:
      operationId: listLeverageLoopTrajectories
      tags:
        - Leverage Loops
      summary: List a suggestion's trajectories with their nodes
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: One page of trajectories, oldest first, nodes embedded and hydrated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListLeverageLoopTrajectoriesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /leverage-loop-suggestions/{suggestion_id}/questions:
    parameters:
      - $ref: '#/components/parameters/SuggestionID'
    get:
      operationId: listLeverageLoopQuestions
      tags:
        - Leverage Loops
      summary: List a suggestion's follow-up questions
      description: Every question, oldest first (not paged); rebuild the thread from parent_id.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The questions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopQuestionListEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createLeverageLoopQuestion
      tags:
        - Leverage Loops
      summary: Ask a follow-up question on a suggestion
      description: 'Stores the question with `processing: true`; the answer is produced asynchronously. selected_nodes must name existing entities.'
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLeverageLoopQuestionRequest'
      responses:
        '201':
          description: The created question.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeverageLoopQuestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /leverage-loop-questions/{question_id}/nodes:
    get:
      operationId: listLeverageLoopQuestionNodes
      tags:
        - Leverage Loops
      summary: List the nodes pinned to a question
      security:
        - bearerAuth: []
      parameters:
        - name: question_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: One page of hydrated nodes, oldest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSuggestionNodesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /suggestion-batches:
    get:
      operationId: listSuggestionFeed
      tags:
        - Suggestions
      summary: List an organisation's live suggestions in one scope
      description: 'The v2 suggestion feed: every suggestion from batches with status `complete` in the organisation, of one `suggestion_type` and optionally one beneficiary, newest batch first (`batch_seq` desc) and rank order inside a batch. Dismissed suggestions are hidden; ready and placeholder suggestions are returned alike, placeholders with a null `why` and empty arrays. The caller must be a current member of the organisation. Nodes on the page are hydrated for the caller. Keyset-paginated: pass `limit` and the previous page''s `meta.nextCursor`.'
      security:
        - bearerAuth: []
      parameters:
        - name: organization_id
          in: query
          required: true
          description: The local organisation whose suggestions to list.
          schema:
            type: string
            format: uuid
        - name: suggestion_type
          in: query
          required: true
          description: One of `outcome`, `leverage_loop`, `leverage_outcome`, `serendipity`.
          schema:
            type: string
        - name: beneficiary_contact_id
          in: query
          required: false
          description: Narrow to one beneficiary's scope (a person entity id). Omit for every scope of the type.
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: One page of suggestions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSuggestionFeedEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /suggestion-batches/{batch_id}:
    parameters:
      - $ref: '#/components/parameters/BatchID'
    get:
      operationId: getSuggestionBatch
      tags:
        - Suggestions
      summary: Inspect one stored batch of any status
      description: One batch with every suggestion, dismissed ones included, for a member of its organisation. Partial and failed batches are served as stored and never presented as complete.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The batch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestionBatchEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /suggestion-batches/{batch_id}/suggestions/{suggestion_id}:
    parameters:
      - $ref: '#/components/parameters/BatchID'
      - $ref: '#/components/parameters/SuggestionID'
    patch:
      operationId: updateFeedSuggestion
      tags:
        - Suggestions
      summary: Dismiss or restore one suggestion of a batch
      description: Sets or clears `dismissed_at` on a single suggestion, scoped by its batch. Dismissed suggestions drop out of the live feed but stay on the batch detail read. Only a member of the batch's organisation can write.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateFeedSuggestionRequest'
      responses:
        '200':
          description: The flag was written.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /copilot/threads/ensure:
    post:
      operationId: ensureCopilotThread
      tags:
        - Copilot
      summary: Find or create the caller's thread for a surface key
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnsureThreadRequest'
      responses:
        '200':
          description: The existing thread replayed for the same surface key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnsureThreadEnvelope'
        '201':
          description: The newly created thread.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnsureThreadEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/threads:
    get:
      operationId: listCopilotThreads
      tags:
        - Copilot
      summary: List the caller's threads, newest activity first
      security:
        - bearerAuth: []
      parameters:
        - name: surface
          in: query
          schema:
            type: string
            enum:
              - outcome
        - name: archived
          in: query
          schema:
            type: boolean
            default: false
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
        - name: cursor
          in: query
          schema:
            type: string
      responses:
        '200':
          description: One page of threads.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreadListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /copilot/threads/{id}:
    patch:
      operationId: updateCopilotThread
      tags:
        - Copilot
      summary: Rename, archive, or restore a thread
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateThreadRequest-2'
      responses:
        '200':
          description: The updated thread.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreadEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
    delete:
      operationId: archiveCopilotThread
      tags:
        - Copilot
      summary: Soft-archive a thread; repeat archive succeeds
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Archived; the request row is unaffected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/threads/{id}/messages:
    get:
      operationId: listCopilotMessages
      tags:
        - Copilot
      summary: Page the thread transcript under a pinned read window
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        '200':
          description: One ascending page plus the pinned transcript version.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageListEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
    post:
      operationId: checkpointCopilotMessages
      tags:
        - Copilot
      summary: Commit a durable assistant checkpoint batch under the run's lease
      description: Trusted write; requires the harness signature headers.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/SignatureTimestamp'
        - $ref: '#/components/parameters/Signature'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckpointRequest'
      responses:
        '200':
          description: Per-action counts and the new transcript version.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckpointResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/threads/{id}/runs:
    post:
      operationId: startCopilotRun
      tags:
        - Copilot
      summary: Accept a user turn or retry the latest attempt
      description: Trusted write; requires the harness signature headers.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/SignatureTimestamp'
        - $ref: '#/components/parameters/Signature'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartRunRequest-2'
      responses:
        '200':
          description: Identical replay of a terminal run's acquisition.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunAcquisitionEnvelope'
        '201':
          description: The acquired run lease and accepted input.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunAcquisitionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/threads/{id}/runs/{run_id}:
    patch:
      operationId: updateCopilotRun
      tags:
        - Copilot
      summary: Renew, finish, or pin execution metadata on a run
      description: Trusted write; requires the harness signature headers.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            maxLength: 128
        - $ref: '#/components/parameters/SignatureTimestamp'
        - $ref: '#/components/parameters/Signature'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRunRequest'
      responses:
        '200':
          description: The run id, status and lease expiry.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunPatchEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/outcomes/classes:
    get:
      operationId: listOutcomeClasses
      tags:
        - Copilot
      summary: Return the pinned outcome-intake class registry
      security:
        - bearerAuth: []
      parameters:
        - name: registry_version
          in: query
          schema:
            type: string
            default: outcome-intake-v1
      responses:
        '200':
          description: The registry version with its dispatchable classes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/outcomes/drafts:
    post:
      operationId: createOutcomeDraft
      tags:
        - Copilot
      summary: Create the unclassified draft bound to a conversation
      description: Trusted write; requires the harness signature headers.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SignatureTimestamp'
        - $ref: '#/components/parameters/Signature'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDraftRequest'
      responses:
        '200':
          description: Identical receipt replay.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateDraftEnvelope'
        '201':
          description: The created draft bound to the conversation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateDraftEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/outcomes/drafts/{id}/context:
    patch:
      operationId: patchOutcomeContext
      tags:
        - Copilot
      summary: Apply one typed intake change under the version guard
      description: Trusted write; requires the harness signature headers.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/SignatureTimestamp'
        - $ref: '#/components/parameters/Signature'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchContextRequest'
      responses:
        '200':
          description: The updated request plus committed checkpoint coordinates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatchContextEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/outcomes/dispatch:
    post:
      operationId: submitOutcomeRequest
      tags:
        - Copilot
      summary: Explicit user submission; durable only, no generation
      description: Trusted write; requires the harness signature headers.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SignatureTimestamp'
        - $ref: '#/components/parameters/Signature'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DispatchRequest'
      responses:
        '200':
          description: The public submission receipt.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmissionReceiptEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /copilot/outcomes/requests:
    get:
      operationId: listOutcomeRequests
      tags:
        - Copilot
      summary: List the caller's outcome-chat requests
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: query
          in: query
          schema:
            type: string
            maxLength: 200
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
        - name: cursor
          in: query
          schema:
            type: string
      responses:
        '200':
          description: One page of requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutcomeRequestListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /copilot/outcomes/requests/{id}:
    get:
      operationId: getOutcomeRequest
      tags:
        - Copilot
      summary: Get one outcome request with derived intake fields
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The request view.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutcomeRequestEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /edge-sync/rebuild:
    post:
      tags:
        - EdgeSync
      summary: Rebuild the caller's graph
      description: |
        Schedules a full rebuild of the authenticated user's graph: the relay
        drops it and replays it from Postgres. The owner is taken from the
        verified session, never the body, so a caller cannot schedule work
        against another user's graph.

        Returns 202 — the rebuild happens on the relay's next pass.
      operationId: rebuildUserGraph
      security:
        - bearerAuth: []
      responses:
        '202':
          description: The rebuild was scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RebuildEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /edge-sync/backfill:
    post:
      tags:
        - EdgeSync
      summary: Schedule a rebuild for every user
      description: |
        Operator endpoint. Schedules a rebuild for every user whose graph can
        be rooted, and returns how many were queued. Backs the migration off
        the old `:Profile` shape and the periodic backstop.

        Users with no linked person are skipped: there is no `node_uuid` to
        root their graph at.
      operationId: backfillUserGraphs
      security:
        - bearerAuth: []
      responses:
        '202':
          description: The sweep was scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackfillEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /edge-sync/status:
    get:
      tags:
        - EdgeSync
      summary: Get funnel status
      description: |
        Reports outstanding funnel work. A pending count that climbs without
        falling means the relay is not draining.
      operationId: getEdgeSyncStatus
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current funnel status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusEnvelope-2'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /files:
    get:
      tags:
        - Files
      summary: Retrieve the caller's files by filter
      description: |
        One endpoint for every retrieval. Filters combine, and the response shape
        depends on which are present:

        - `q` — text search. Without `semantic`, matches `file_name` /
          `display_name` with a case-insensitive substring match; costs no
          embedding call, so it is safe for type-ahead. With `semantic=true` the
          query is embedded and files are ranked by how close their nearest chunk
          sits to it. Returns one row PER FILE either way, so a document with
          many matching passages does not crowd out the rest. Ranked, so no
          cursor is returned.
        - `ids` — batch read of specific files, for polling upload progress in
          one request instead of one per file. Ids the caller cannot see are
          omitted rather than erroring.
        - `contact_id` / `company_contact_id` — that one contact's files.
        - `scope` — `mine` (the caller's uploads), `contacts` (every file across
          all the caller's contacts), or `all` (default, both). Ignored when a
          contact is pinned.

        Every variant is scoped to the caller: files they own, or files linked to
        a contact they own. Soft-deleted files are never returned.
      operationId: listUserFiles
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          schema:
            type: string
          description: Search text. Matched against file names unless `semantic=true`.
        - in: query
          name: semantic
          schema:
            type: boolean
            default: false
          description: |
            Rank by meaning instead of matching names. Costs a live embedding
            call, so it is opt-in rather than the default.
        - in: query
          name: ids
          schema:
            type: string
          description: Comma-separated file ids. Use for upload-progress polling.
        - in: query
          name: scope
          schema:
            type: string
            default: all
          description: One of `mine`, `contacts`, `all`. Ignored when a contact is pinned.
        - in: query
          name: contact_id
          schema:
            type: string
            format: uuid
        - in: query
          name: company_contact_id
          schema:
            type: string
            format: uuid
        - in: query
          name: classification
          schema:
            type: string
          description: Narrow to one LLM-assigned file type, e.g. `pitch_deck`.
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - in: query
          name: cursor
          schema:
            type: string
          description: Opaque cursor from a prior page. Not used by `q` or `ids`.
      responses:
        '200':
          description: |
            A page of files, or ranked search hits when `q` is present.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/ListFilesResponseEnvelope'
                  - $ref: '#/components/schemas/SearchFilesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /files/upload:
    post:
      tags:
        - Files
      summary: Upload one or many files
      description: |
        Stores each file in GCS, records its row, and publishes `files.uploaded`
        per file, so every uploaded file independently runs the whole pipeline
        (extract → classify → chunk → embed). Returns immediately with
        `status=pending` for each; poll `GET /v1/files?ids=…` for progress.

        IMPORTANT — part ordering: send every text field BEFORE the file parts.
        Each file streams straight to GCS as it is reached, so `contact_id` and
        `company_contact_id` must already be known and verified by then.

        Byte-transfer progress is a client-side concern (`XMLHttpRequest.upload.
        onprogress`); the server cannot report it sooner than the client knows it.

        This route does not accept pre-extracted markdown — it is asynchronous
        and the worker extracts every file. Use the deprecated
        `/ingestion/files/upload-file-markdown` if you must supply markdown.

        Files are independent: one failure neither fails the request nor undoes
        its siblings. `201` when all succeeded, `207` when some failed (per-file
        `error` in the array), and an error envelope only when none succeeded.
        Max body size 100 MB across all files.
      operationId: uploadFiles
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadFilesRequest'
      responses:
        '201':
          description: Every file was stored and queued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadFilesEnvelope'
        '207':
          description: |
            Some files were stored and queued; others failed. Inspect each
            element's `error`. `meta.uploaded` / `meta.failed` carry the counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadFilesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: A supplied contact_id / company_contact_id is not the caller's.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /files/{id}:
    get:
      tags:
        - Files
      summary: Get one of the caller's files
      description: |
        Returns a file the caller owns or that is linked to one of their
        contacts; anything else is a 404 rather than a 403, so the endpoint
        reveals nothing about other users' files.

        `include_markdown` is opt-in because markdown is the whole extracted
        document and routinely megabytes.
      operationId: getUserFile
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: include_markdown
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: The file, with markdown when requested.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/FileResponseEnvelope'
                  - $ref: '#/components/schemas/UploadFileResponseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Files
      summary: Delete one of the caller's files
      description: |
        Soft delete. The row, its GCS blob, and its embeddings all survive, so
        the file stays restorable and a restore costs no re-extraction. The
        deletion timestamp is cascaded onto the file's contact links in the same
        transaction, so the file disappears from every contact listing at once.

        Returns 200 with a null payload, per the house convention that deletes
        are not 204.
      operationId: deleteUserFile
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: File soft-deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /files/{id}/preview:
    get:
      tags:
        - Files
      summary: Get a link to the file's preview image
      description: |
        Returns a short-lived signed URL for the file's preview image — a
        downscaled render of page one, produced when the file was processed.
        Available for PDFs and image uploads; other formats have no preview
        yet and return 404.

        The link is returned instead of the image bytes so the client fetches
        the object straight from storage. Files whose preview has not been
        produced (text formats, or anything uploaded before previews existed)
        return 404. `expires_at` is null when the preview is hosted elsewhere
        and we did not sign it.
      operationId: getUserFilePreview
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: A link to the preview image.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilePreviewEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /files/{id}/rename:
    post:
      tags:
        - Files
      summary: Rename one of the caller's files
      description: |
        Sets `display_name`. The upload-time `file_name` is never overwritten:
        the extraction worker owns it, and other consumers (activities, email
        attachments, the Xano `files.completed` webhook) read it as the original
        name. Clients should render `display_name ?? file_name`.
      operationId: renameUserFile
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RenameFileRequest'
      responses:
        '200':
          description: The renamed file.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /ingestion/files:
    post:
      tags:
        - Files
      summary: Register an uploaded file
      description: |
        Records the file row and publishes `files.uploaded` to Pub/Sub.
        The worker takes over from there (extract → classify → optional
        pitch-profile). Returns immediately with the file's id and
        `status=pending`.
      operationId: createFile
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateFileRequest'
      responses:
        '201':
          description: File row created and queued for processing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
    get:
      tags:
        - Files
      summary: Get all files
      description: List files (cursor-paginated)
      operationId: listFiles
      security: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - in: query
          name: cursor
          schema:
            type: string
          description: Opaque pagination cursor returned by a prior listing.
      responses:
        '200':
          description: One page of files.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListFilesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /ingestion/files/upload-file-markdown:
    post:
      tags:
        - Files
      summary: Upload a file (markdown)
      description: |
        Streams the uploaded file to GCS, records the file row, persists the
        user-supplied `markdown`, and publishes `files.uploaded`. Because the
        markdown is already provided, the worker SKIPS its extraction step.
        Returns the file with `status=pending`. Max body size 100 MB. Requires
        a verified WorkOS session.

        Use this when the frontend has already run extraction (e.g. LlamaParse
        on a server function) and just needs the file persisted + ingested.

        IMPORTANT — part ordering: the `file` part is streamed straight to GCS
        and must be sent **last**, after all text fields, so the server can
        process it in a single pass without buffering it in memory.
      operationId: uploadFileMarkdown
      deprecated: true
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadFileMarkdownRequest'
      responses:
        '201':
          description: File uploaded, row created, and queued for processing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadFileResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /ingestion/files/upload-file-raw:
    post:
      tags:
        - Files
      summary: Upload a file (raw)
      description: |
        Synchronous extract-then-upload path. The server extracts the file's
        markdown inline (local extractor or LlamaParse by mime type), uploads
        the file to GCS, persists the row WITH the extracted markdown, and
        returns the file object — `markdown` included — with `status=pending`.
        It then publishes `files.uploaded` so the worker runs the rest of the
        pipeline (classification, embeddings) and SKIPS re-extraction.

        Because extraction runs on the request path, the call BLOCKS until
        extraction completes (seconds to minutes for large documents) — set
        client/proxy timeouts accordingly. If extraction fails, nothing is
        persisted and the request errors. Any `markdown` form field is ignored.
        Max body size 100 MB. Requires a verified WorkOS session.

        IMPORTANT — part ordering: send the `file` part **last**, after all
        text fields, so they are known when the file part is reached.
      operationId: uploadFileRaw
      deprecated: true
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadFileRawRequest'
      responses:
        '201':
          description: File uploaded, row created, and queued for processing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadFileResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /ingestion/files/{id}:
    get:
      tags:
        - Files
      summary: Get one file
      description: Get a file by id
      operationId: getFile
      security: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: File.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileResponseEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /ingestion/files/{id}/retry:
    post:
      tags:
        - Files
      summary: Retry a failed file
      description: |
        Resets status to `pending` and republishes the `files.uploaded`
        event so the worker pipeline runs again. Use this to recover
        files dead-lettered by Pub/Sub after repeated transient errors.

        Only allowed from `failed` or `pending` status — in-flight
        statuses (`extracting`, `embedding`, `classified`) return 409
        to prevent double-processing.
      operationId: retryFile
      security: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Retry accepted; file re-queued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileResponseEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: File is in an in-flight status that cannot be retried.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /webhooks/event-types:
    get:
      tags:
        - Webhooks
      summary: Get all event types
      description: |
        Returns the closed set of event types the platform currently
        publishes (e.g. `pitch_profile.completed`, `files.completed`).
        Use this to populate the event-type picker in your UI.
      operationId: listWebhookEventTypes
      security: []
      responses:
        '200':
          description: Event types.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventTypesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /webhooks/subscriptions:
    post:
      tags:
        - Webhooks
      summary: Create a subscription
      description: |
        Server generates the signing secret and returns it **exactly
        once** in this response — clients must persist it now or
        rotate. Subsequent GETs never re-disclose the secret.
      operationId: createWebhookSubscription
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSubscriptionRequest'
      responses:
        '201':
          description: Subscription created; secret returned once.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateSubscriptionWithSecretResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
    get:
      tags:
        - Webhooks
      summary: Get all subscriptions
      description: List subscriptions
      operationId: listWebhookSubscriptions
      security: []
      responses:
        '200':
          description: Subscriptions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSubscriptionsResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /webhooks/subscriptions/{id}:
    parameters:
      - $ref: '#/components/parameters/SubscriptionID'
    get:
      tags:
        - Webhooks
      summary: Get one subscription
      description: Get a subscription
      operationId: getWebhookSubscription
      security: []
      responses:
        '200':
          description: Subscription.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponseEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - Webhooks
      summary: Update a subscription
      description: Update a subscription's URL or enabled flag
      operationId: updateWebhookSubscription
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSubscriptionRequest'
      responses:
        '200':
          description: Updated subscription.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Webhooks
      summary: Delete a subscription
      operationId: deleteWebhookSubscription
      security: []
      responses:
        '200':
          description: Deleted (no body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /webhooks/deliveries:
    get:
      tags:
        - Webhooks
      summary: Get all dead deliveries
      description: |
        Today only `status=dead` is supported — surfacing the dead pile
        is the only listing affordance v2.0 promises. Other filters
        will follow once the operator UI grows.
      operationId: listWebhookDeliveries
      security: []
      parameters:
        - in: query
          name: status
          required: true
          schema:
            type: string
            enum:
              - dead
          description: Only `dead` is accepted today.
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        '200':
          description: One page of dead deliveries.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListDeliveriesResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /webhooks/deliveries/{id}/retry:
    post:
      tags:
        - Webhooks
      summary: Retry a delivery
      description: |
        Resets the delivery's status and republishes onto the
        deliveries topic. The actual retry is asynchronous, hence
        the 202 response.
      operationId: retryWebhookDelivery
      security: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Retry accepted; delivery requeued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryResponseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /persons:
    get:
      operationId: listPersons
      tags:
        - Persons
      summary: Get all persons
      description: Browse all persons with the authenticated user's contact + connection state
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
        - name: cursor
          in: query
          schema:
            type: string
        - name: query
          in: query
          description: Case-insensitive search across the professional display name and stored full name parts.
          schema:
            type: string
        - name: node_uuid
          in: query
          description: Exact match on the person's legacy `graph_node_id`. Scheduled for removal — a person's graph node id is now their own id.
          schema:
            type: string
        - name: sort
          in: query
          description: Sort order. `created_at` is newest-first; `name` is professional display name A-Z.
          schema:
            type: string
            enum:
              - created_at
              - name
            default: created_at
        - name: created_after
          in: query
          description: Only persons created at or after this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
        - name: created_before
          in: query
          description: Only persons created at or before this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated master-person list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Person-2'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
                      total:
                        type: integer
                      next_cursor:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      operationId: createPerson
      tags:
        - Persons
      summary: Save the New Contact form as a person and contact
      description: |
        Saves the New Contact form and adds the person as the caller's
        contact in one request.

        With `person_id` (the form was prefilled by `POST /persons/url-lookup`)
        that person is reused. A supplied `full_name` fills an unnamed person
        without replacing an existing name. Saving a person that still has
        no name fails validation on `full_name` before adding a contact.
        Without `person_id`, `full_name` is required and the
        typed identity (name, emails, phones, title, company name, profile
        URL) is sent to the enrichment provider's person get-add, which
        resolves or creates the canonical person; the row is then persisted
        locally under the provider's id, stamped `source = web_app`. A
        provider decline fails validation on `full_name`; an unconfigured
        provider fails with 500.

        The employer resolves from `company_id`, else `company_domain`, else
        an exact case-insensitive `company_name` match, else the typed name
        is resolved-or-created with the enrichment provider's company get-add
        and written under the provider's id (a provider decline leaves the
        employer unset); employer and title are written only into empty
        columns. Emails, phones, and the profile link attach as public
        rows (deduped) and are copied into the contact's join tables. A
        non-blank `note` is stored on the contact.

        Duplicate emails or phones inside one request collapse silently.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePersonRequest'
      responses:
        '200':
          description: The person already existed; the contact and details were reconciled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatePersonEnvelope'
        '201':
          description: The person row was persisted on this call.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatePersonEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /persons/url-lookup:
    post:
      operationId: lookupPerson
      tags:
        - Persons
      summary: Look up a person by profile URL
      description: |
        Resolve a person from a social-profile URL (LinkedIn, IMDB, X, Instagram, ...). A LinkedIn URL is always sent to the enrichment provider's get-add, local hit or miss; a held person is overwritten with the provider's latest package. On a miss the person is resolved-or-created there. Once a name is available it is persisted locally and returned with status created while enrichment completes in the background. An unresolved name returns pending_enrichment with found false and person null; no unnamed local person is created. A held unnamed person also stays pending until its name resolves. When the provider's package names a current employer, that company is ensured locally first and returned as company. Other profile domains return pending_enrichment on a miss.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonLookupRequest'
      responses:
        '200':
          description: Lookup result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PersonLookup'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /person-financials:
    get:
      operationId: getPersonFinancials
      tags:
        - Persons
      summary: Get a person's investor profile and investment history
      description: |
        Powers the person sheet's Financials tab. Returns the person's investor
        profile plus their investment history split three ways.

        `exits` is DERIVED, not stored: a round counts as an exit when the funded
        company was acquired or went public, and it appears in `exits` instead of
        `angel_investments` / `institutional_investments`. All three arrays are
        always present. Amounts inside `money_raised` are in **millions**.
      security:
        - bearerAuth: []
      parameters:
        - name: person_id
          in: query
          required: true
          description: The person whose financials to return.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The person's investor profile and investments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonFinancialsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /persons/{person_id}:
    get:
      operationId: getPerson
      tags:
        - Persons
      summary: Get one person
      description: Get a person's full detail record
      security:
        - bearerAuth: []
      parameters:
        - name: person_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Person detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PersonDetail'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /contacts:
    get:
      operationId: listContacts
      tags:
        - Contacts
      summary: Get all contacts
      description: List the authenticated user's contacts, fully enriched
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
        - name: cursor
          in: query
          schema:
            type: string
        - name: query
          in: query
          description: Case-insensitive name filter.
          schema:
            type: string
        - name: sort
          in: query
          description: Sort order. `created_at` is newest-first; `name` is person full name A-Z.
          schema:
            type: string
            enum:
              - created_at
              - name
            default: created_at
        - name: created_after
          in: query
          description: Only contacts created at or after this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
        - name: created_before
          in: query
          description: Only contacts created at or before this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated contact list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ContactListItem'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
                      total:
                        type: integer
                      next_cursor:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      operationId: addContact
      tags:
        - Contacts
      summary: Add a contact
      description: Add a person as a contact (copy-to-my). A person without a stored name fails validation on full_name before any contact or copied details are written.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - person_id
              properties:
                person_id:
                  type: string
                  format: uuid
      responses:
        '200':
          description: Contact already existed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AddContactResult'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '201':
          description: Contact created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AddContactResult'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /contacts/clone:
    post:
      operationId: cloneContacts
      tags:
        - Contacts
      summary: Clone contacts from a donor
      description: |
        Duplicates the contact rows of a randomly chosen donor (one of the users holding the most contacts) into the authenticated user's account. Only the contacts rows are cloned; existing contacts are left untouched and donor data is not deleted. Idempotent — contacts the authenticated user already holds are skipped.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Contacts cloned
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CloneContactsResult'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /contacts/merge:
    post:
      operationId: mergeContacts
      tags:
        - Contacts
      summary: Merge two contacts
      description: |
        Fold the loser contact into the winner (survivor picked by the caller):
        the loser's detail records move onto the winner, the loser is tombstoned
        and hidden from queries, and the user's graph node is repointed.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - winner_contact_id
                - loser_contact_id
              properties:
                winner_contact_id:
                  type: string
                  format: uuid
                loser_contact_id:
                  type: string
                  format: uuid
      responses:
        '200':
          description: Contacts merged.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /contacts/table:
    get:
      operationId: listContactTableRows
      tags:
        - Contacts
      summary: List contacts as table rows
      description: |
        The caller's live contacts by default as flat, denormalized rows for Contacts
        View, keyset-paged on (created_at DESC, id DESC). There is no sort or
        query param: sorting and text/column filtering happen client-side.
        Optional team_id or organization_id selects one shared network; they are
        mutually exclusive. Team scope requires accepted membership and returns
        accepted members' visible contacts. Organization scope requires active
        membership in a non-personal organization and returns visible contacts
        assigned to that organization or contributed through its claimed email
        domains. Shared scopes deduplicate by person, preferring the caller's
        contact. Notes, custom fields, interaction details and private addresses
        belonging to other users are excluded. can_edit is false for shared rows. meta.total
        is the live-contact count observed at that page's read; meta.nextCursor
        is an opaque cursor carrying the frozen page-1 upper bound plus the
        last returned tuple; meta.fieldDefsVersion is the caller's monotonic
        custom-field schema revision (it changes on any definition change,
        including deletions). Merged-away contacts are never returned.
        Optional collection_id loads people directly in an accessible collection and
        people whose current company belongs to it. Existing contacts retain their
        saved details. Other people have contact_id null and can_edit false, and
        expose only public enrichment details. No contact records are created.
        When combined with team_id or organization_id, results intersect that scope.
      security:
        - bearerAuth: []
      parameters:
        - name: collection_id
          in: query
          description: Collection owned by or shared with the caller.
          schema:
            type: string
            format: uuid
        - name: team_id
          in: query
          description: Accepted team membership required. Omit organization_id.
          schema:
            type: string
            format: uuid
        - name: organization_id
          in: query
          description: Active non-personal organization membership required. Omit team_id.
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 500
            maximum: 1000
        - name: cursor
          in: query
          description: Opaque keyset cursor from a previous page's meta.nextCursor. Empty on the first call.
          schema:
            type: string
      responses:
        '200':
          description: One page of contact table rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ContactTableRow'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      requestId:
                        type: string
                      total:
                        type: integer
                      nextCursor:
                        type: string
                      fieldDefsVersion:
                        type: integer
                        format: int64
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /contacts/{contact_id}:
    get:
      operationId: getContact
      tags:
        - Contacts
      summary: Get one contact
      description: Get a single contact with person summary
      security:
        - bearerAuth: []
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Contact detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ContactWithSummary'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /contacts/{contact_id}/visibility:
    patch:
      operationId: setContactVisibility
      tags:
        - Contacts
      summary: Set a contact's visibility
      description: |
        Controls whether a contact may reach shared surfaces. `private` keeps it out of the graph of any workspace the holder belongs to, and withdraws it from one it was already contributed to. An administrator cannot override the holder's choice.
        The owner comes from the session, so a caller can only change a contact they hold. Someone else's contact reads as not found rather than forbidden, which avoids confirming that it exists.
      security:
        - bearerAuth: []
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - visibility
              properties:
                visibility:
                  type: string
                  description: visible or private
      responses:
        '200':
          description: The contact's visibility after the change
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ContactVisibility'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /companies:
    get:
      operationId: listCompanies
      tags:
        - Companies
      summary: Get all companies
      description: Browse all companies with the authenticated user's company state
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
        - name: cursor
          in: query
          schema:
            type: string
        - name: query
          in: query
          description: Case-insensitive company-name filter.
          schema:
            type: string
        - name: node_uuid
          in: query
          description: Exact match on the company's graph node id, which IS the company's own id. Lets graph surfaces resolve a node to its company.
          schema:
            type: string
            format: uuid
        - name: sort
          in: query
          description: Sort order. `created_at` is newest-first; `name` is company name A-Z.
          schema:
            type: string
            enum:
              - created_at
              - name
            default: created_at
        - name: created_after
          in: query
          description: Only companies created at or after this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
        - name: created_before
          in: query
          description: Only companies created at or before this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated master-company list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Company'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
                      total:
                        type: integer
                      next_cursor:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      operationId: createCompany
      tags:
        - Companies
      summary: Create a company
      description: |
        Resolve a company from its website or LinkedIn/Crunchbase company page and add it to the authenticated user's contacts. A company we do not already hold is created through the enrichment provider and written here under the id it assigns. The form's phone is stored as a public number on the company and linked to the contact; its note is stored on the contact. Idempotent: repeating the call returns the same company with company_created and contact_added false.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompanyCreateRequest'
      responses:
        '200':
          description: Company already existed; contact ensured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompanyCreateEnvelope'
        '201':
          description: Company created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompanyCreateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /companies/brand-search:
    get:
      operationId: searchCompanyBrands
      tags:
        - Companies
      summary: Search company names using Logo.dev
      description: Returns name, domain and logo previews. Searching does not resolve IDs or create companies.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          required: true
          schema:
            type: string
            maxLength: 200
          description: Company name. Fewer than two characters returns an empty list.
      responses:
        '200':
          description: Ranked company candidates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CompanyBrand'
                  error:
                    type: 'null'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /companies/url-lookup:
    post:
      operationId: lookupCompany
      tags:
        - Companies
      summary: Look up a company by website domain
      description: |
        Normalize a website URL or domain and call Logo.dev Describe first. A matching result returns immediately without database or Universe calls. Otherwise try the existing Universe lookup with an eight-second deadline. Reject mismatched domains before persisting or prefilling them. When Universe cannot supply a matching name, make one Firecrawl scrape of the original website with a five-second deadline. Website previews use its own branding and declared logo or favicon; the name and logo can be returned independently. A homepage logo or wordmark label corroborated by the copyright owner or visible site copy takes precedence over the copyright name; explicit organization and site names remain preferred. A different final or canonical domain is rejected. Preview ids are null until Add Company resolves the canonical entity. Website-published company profile URLs accompany the preview for Add Company. Exhausted lookups return manual_entry so the caller preserves the original domain and asks for a name instead of polling indefinitely.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompanyLookupRequest'
      responses:
        '200':
          description: Lookup result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompanyLookup'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /companies/{company_id}:
    get:
      operationId: getCompany
      tags:
        - Companies
      summary: Get one company
      description: Get a company's full detail record
      security:
        - bearerAuth: []
      parameters:
        - name: company_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Company detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompanyDetail'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /companies/{company_id}/founders:
    get:
      operationId: listCompanyFounders
      tags:
        - Companies
      summary: List a company's founders
      description: |
        Powers the company sheet's founders section. Returns the people the
        enrichment provider reports as the company's founders in the order they were linked, each with the lean person
        card nested under `person`. The list is empty until the company's
        package has been synced, and `meta.total` is its length.
      security:
        - bearerAuth: []
      parameters:
        - name: company_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The company's founders; empty when none are known.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCompanyFoundersEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /companies/{company_id}/people:
    get:
      operationId: listCompanyPeople
      tags:
        - Companies
      summary: List known current company people, including founders
      description: |
        Universe company relationships, independent of the caller's contacts.
        Includes current WORKS_AT, OFFICER_OF and BOARD_MEMBER_OF relationships
        and all FOUNDED relationships. Ended employment and board roles are
        excluded. Roles describe this company, even when a founder now works
        elsewhere. Missing local person profiles are held so cards can open;
        no contacts or enrichment jobs are created.
        Pages scan up to 200 graph neighbours. A page may be empty and still
        have next_cursor; continue until next_cursor is empty.
      security:
        - bearerAuth: []
      parameters:
        - name: company_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque next_cursor from the previous page.
      responses:
        '200':
          description: Known company people and the next graph page cursor.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - error
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/CompanyPeoplePage'
                  error:
                    type: 'null'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /company-financials:
    get:
      operationId: getCompanyFinancials
      tags:
        - Companies
      summary: Get a company's financial data and funding rounds
      description: |
        Powers the company sheet's Financials tab. Returns the company's
        financial record plus every funding round with its investors and, for
        institutional investors, the named partners at the investing firm.

        `financial_data` is null until the company has a financial record;
        `funding` is always an array. Monetary amounts inside `money_raised` are
        denominated in **millions** (1 = $1M, 0.5 = $500K). Note that
        `funding_total_value` / `funding_total_value_usd` on `financial_data` are
        in **whole units**, not millions — the two scales differ.
      security:
        - bearerAuth: []
      parameters:
        - name: company_id
          in: query
          required: true
          description: The company whose financials to return.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The company's financial data and funding rounds.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompanyFinancialsEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /company-contacts:
    get:
      operationId: listCompanyContacts
      tags:
        - Company Contacts
      summary: Get all company contacts
      description: List the authenticated user's saved companies (company contacts)
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
        - name: cursor
          in: query
          schema:
            type: string
        - name: query
          in: query
          description: Case-insensitive company-name filter.
          schema:
            type: string
        - name: sort
          in: query
          description: Sort order. `created_at` is newest-first; `name` is company name A-Z.
          schema:
            type: string
            enum:
              - created_at
              - name
            default: created_at
        - name: created_after
          in: query
          description: Only company contacts created at or after this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
        - name: created_before
          in: query
          description: Only company contacts created at or before this RFC3339 timestamp (inclusive).
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated company-contact list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CompanyContactListItem'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
                      total:
                        type: integer
                      next_cursor:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      operationId: addCompanyContact
      tags:
        - Company Contacts
      summary: Add a company to contacts
      description: |
        Creates the caller's company contact and copies the company's public detail records (emails, links, addresses, phones) into the contact join tables. Idempotent: re-adding returns 200 with contact_added=false.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompanyAddContactRequest'
      responses:
        '200':
          description: Company contact already existed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompanyAddContact'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '201':
          description: Company contact created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompanyAddContact'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /company-contacts/clone:
    post:
      operationId: cloneCompanyContacts
      tags:
        - Company Contacts
      summary: Clone company contacts from a donor
      description: |
        Duplicates the company-contact rows of a randomly chosen donor (the user holding the most company contacts, ties broken randomly) into the authenticated user's account. Only the company_contacts rows are cloned; existing contacts are left untouched and donor data is not deleted. Idempotent — companies the authenticated user already holds are skipped.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Company contacts cloned
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CloneCompanyContactsResult'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /merge-suspects:
    get:
      operationId: listMergeSuspects
      tags:
        - MergeSuspects
      summary: Get all merge suspects
      description: List the authenticated user's active merge suspect pairs
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of suspected duplicate contact pairs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MergeSuspect'
                  error:
                    type:
                      - object
                      - 'null'
                  meta:
                    type: object
                    properties:
                      request_id:
                        type: string
                      total:
                        type: integer
        '401':
          $ref: '#/components/responses/Unauthorized'
  /merge-suspects/backfill:
    post:
      operationId: backfillMergeSuspects
      tags:
        - MergeSuspects
      summary: Backfill merge-suspect vectors
      description: |
        Enqueue a one-shot pass that vectorizes the authenticated user's
        contacts and runs duplicate detection. Async; returns 202.
      security:
        - bearerAuth: []
      responses:
        '202':
          description: Backfill enqueued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /merge-suspects/{id}/dismiss:
    post:
      operationId: dismissMergeSuspect
      tags:
        - MergeSuspects
      summary: Dismiss a merge suspect
      description: Mark a suspect pair do-not-merge so detection never resurfaces it.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Suspect dismissed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/Internal'
  /outcomes/investment-thesis/build:
    post:
      tags:
        - Outcomes
      summary: Build (or return the cached) investment thesis for an investor
      description: |
        Builds the six-dimension investment thesis for exactly one owner —
        a master person XOR a master company (Xano BIGINT ids, kept through
        the persons rename). Returns the existing thesis without re-running
        the LLM when it is still fresh (`cached: true`); `force` rebuilds
        regardless. Public today: the route runs under optional auth.
      operationId: buildInvestmentThesis
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BuildThesisRequest'
      responses:
        '200':
          description: The built or cached thesis summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BuildThesisEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /outcomes/investment-thesis/freshness:
    get:
      tags:
        - Outcomes
      summary: Check whether an owner's thesis is fresh
      description: |
        Reports whether the owner's thesis exists, is ready, and is inside
        the freshness TTL. Exactly one of the two query params is required.
        `reason` on a stale answer is one of `stale`, `no_thesis_yet`, or
        `not_ready` (a build in flight or failed).
      operationId: getInvestmentThesisFreshness
      security: []
      parameters:
        - in: query
          name: master_person_id
          required: false
          schema:
            type: integer
            format: int64
            minimum: 1
          description: Owner person id (Xano BIGINT). XOR with master_company_id.
        - in: query
          name: master_company_id
          required: false
          schema:
            type: integer
            format: int64
            minimum: 1
          description: Owner company id (Xano BIGINT). XOR with master_person_id.
      responses:
        '200':
          description: The freshness verdict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThesisFreshnessEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/Internal'
  /outcomes/vector-search/investment-thesis-match:
    post:
      tags:
        - Outcomes
      summary: Rank investment theses against a pitch profile
      description: |
        Scores every completed thesis against the pitch profile's six
        narrative dimensions by cosine similarity and returns the top
        `top_n` (clamped to [1, 50]; default 10). Filters narrow the
        candidate pool before ranking; `total_evaluated` counts the
        pre-filter universe.
      operationId: matchInvestmentTheses
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThesisMatchRequest'
      responses:
        '200':
          description: Ranked matches, best composite score first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThesisMatchEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /outcomes/vector-search/fund-thesis-match:
    post:
      tags:
        - Outcomes
      summary: Rank investment theses against a fund profile
      description: |
        Scores every completed thesis against a fund deck's six fund-native
        narrative dimensions (gp_track_record, fund_thesis, sourcing_edge,
        performance_momentum, fund_economics, portfolio_strategy), each
        mapped onto the thesis dimension that answers the same question
        about an investor, and returns the top `top_n` by weighted
        composite. Weights favour thesis alignment (fund_thesis 0.45,
        gp_track_record 0.20, performance_momentum 0.15, sourcing_edge
        0.10, portfolio_strategy 0.07, fund_economics 0.03). Filters narrow
        the candidate pool before ranking; `investor_type` is how a caller
        restricts results to investor kinds that commit to funds.
      operationId: matchFundTheses
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FundThesisMatchRequest'
      responses:
        '200':
          description: Ranked matches, best composite score first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FundThesisMatchEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /projects:
    get:
      tags:
        - Projects
      summary: List the projects the caller can reach
      description: |
        Returns root projects only: one row per tree the caller reaches
        through any route, with the strongest role of those routes in
        `rights`. Subprojects are reached through `GET /projects/{id}`.

        The access rule runs before the count and the page, so `total` counts
        the matching roots the caller can reach and never the ones they
        cannot. A caller who reaches nothing gets an empty `items` and a
        `total` of `0`, not 403.

        Paging is keyset on creation time, newest first, so a project created
        while a client pages does not shift the pages it has still to read.
        `next_cursor` is empty on the last page. A cursor is bound to the
        caller and the filters that produced it; replayed under any other
        combination it is 400 with `details: [{field: "cursor", issue:
        "filter_mismatch"}]` rather than a page that skips or repeats rows.
      operationId: listProjects
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ProjectStatus'
        - $ref: '#/components/parameters/ProjectSearch'
        - $ref: '#/components/parameters/ProjectIncludeArchived'
        - $ref: '#/components/parameters/ProjectLimit'
        - $ref: '#/components/parameters/ProjectCursor'
      responses:
        '200':
          description: One page of reachable root projects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Projects
      summary: Create a root project or a subproject
      description: |
        `parent_project_id` decides what is created. Absent or `null` makes a
        root: it requires `owning_organization_id`, an organization the
        caller is an active member of, and a `timezone` that is a
        region-qualified IANA name, or `UTC`. A value makes a subproject of that project, inheriting its
        root; the root-only settings are then rejected with 400 rather than
        stored where nothing would read them.

        The caller becomes the new root's owner and so its admin. Handing
        ownership over is `PUT /projects/{root_id}/owner`, under root-admin
        rules, so no body field here can name another owner. Creating a
        subproject needs editor access to its root.

        Nesting is capped at ten levels, the root counting as one. An
        eleventh is 409 `nesting_too_deep`, checked under the root lock, so
        two creates racing for the last level cannot both take it.

        The parent has to be somewhere work can still be added: a parent that
        is archived, complete, or anywhere under an archived project is 409
        `invalid_state`. A parent archive suppresses the branch under it
        without stamping a row there, so the descendants carry no timestamp of
        their own to refuse on.

        `Idempotency-Key` is required. A retry of the same key with the same
        body returns the first execution's project with 200 instead of 201;
        the same key with a different body is 409 `idempotency_conflict`.
      operationId: createProject
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectRequest'
      responses:
        '200':
          description: |
            The project an earlier request with this `Idempotency-Key`
            already created. Nothing was written.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '201':
          description: The created project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}:
    get:
      tags:
        - Projects
      summary: Get one project
      description: |
        Returns the project if the caller may read it, with `rights` set to
        their effective role on its root (`admin`, `editor` or `viewer`) and
        `root_settings` resolved: a root's own settings, and on a subproject
        the ones it inherits from its root.

        Returns 404 (not 403) when the project is unknown, not visible, or
        the capability is off, so existence is never leaked. 403 `account is
        not active` is reached only by a caller who does have a route to it.
      operationId: getProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - Projects
      summary: Update a project
      description: |
        Edits the caller-editable content of one project. Requires editor
        access to its root.

        Patch semantics: an omitted key preserves the stored value, a key
        present with `null` clears it. Unknown fields are rejected with 400
        rather than ignored; the body must name at least one patchable
        field, and one carrying only `expected_version` is 400
        `VALIDATION_FAILED`. `expected_version` is required and must equal
        the project's current `version`; a mismatch is 409 `version_conflict`
        and writes nothing. A patch whose values all equal the stored ones is
        a 200 that writes nothing and leaves `version` where it was.

        `status` moves between `planned`, `active`, `on_hold` and `canceled`.
        `completed` is refused with 409 `invalid_state`: completing a project
        has to check its descendants, tasks and deals, which is `POST
        /projects/{id}/complete`. A project that is already completed refuses
        any status change here until it is reopened.

        `timezone` is a root-only setting and is refused on a subproject with
        409 `invalid_state`; a subproject reads it from its root. Neither
        `title`, `priority`, `status` nor `timezone` is nullable.

        A project under an archived project is 409 `invalid_state` as well:
        the parent archive suppressed the branch without stamping a row in it.
        A patch that asks for the values already stored still answers 200,
        because it writes nothing.

        Hierarchy, archiving, sharing, ownership and agent settings are not
        writable here. Each is its own command, so a PATCH cannot bypass
        their rules. `last_progress_at` does not move: an edit is not
        progress.
      operationId: updateProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectRequest'
      responses:
        '200':
          description: The project as stored after the patch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/move:
    post:
      tags:
        - Projects
      summary: Move a project under another parent
      description: |
        Reparents one subproject inside its own root. Requires admin access to
        that root: the shape of a tree is configuration, not work inside it.

        `parent_project_id` is required and not nullable. Clearing a parent
        would turn the project into a root of its own, which is a cross-root
        change; omitting it or sending `null` is 400 with `details: [{field:
        "parent_project_id", issue: "required"}]`.

        Refused with 409 and one of: `invalid_state` when the project is a
        root, when it is archived, or when the destination is archived,
        complete, or anywhere under an archived project; `cross_root_move`
        when the destination belongs to another root, which V1 does not
        support;
        `project_cycle` when the destination is the project itself or sits
        inside its own subtree; `nesting_too_deep` when the moved subtree's
        deepest level would land past ten. Nothing is written in any of them.

        A move that names the parent the project already has is a 200 that
        writes nothing and leaves `version` where it was. Descendants are not
        rewritten: they travel with the project through `parent_project_id`.
      operationId: moveProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveProjectRequest'
      responses:
        '200':
          description: The project in its new place.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectLifecycleConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/archive:
    post:
      tags:
        - Projects
      summary: Archive a project
      description: |
        Puts one project aside. Requires admin access to its root.

        Stamps `archived_at` on that project and on nothing else. Its
        descendants are suppressed through it and keep their own archive
        state, so a subproject archived earlier still carries its own
        timestamp, and reopening the parent does not bring it back.

        Archiving cancels the undispatched action runs, agent runs and
        follow-ups of the whole subtree, so nothing still fires for work that
        has been put aside. Runs already dispatched and notifications already
        being delivered are left as they are: their outcome has to be
        reconciled, not overwritten.

        A project that is already archived is 409 `invalid_state`.
      operationId: archiveProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectNoteRequest'
      responses:
        '200':
          description: The archived project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectLifecycleConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/reopen:
    post:
      tags:
        - Projects
      summary: Reopen an archived or completed project
      description: |
        Brings one project back into the work. Requires admin access to its
        root, and it is the one command that may run inside an archived root,
        because reopening the root is how a tree gets out of one.

        Clears that project's `archived_at` and `completed_at`, and nothing
        else. A project that was completed becomes `active`; one that was only
        archived keeps the status it had. Descendants are untouched: a
        subproject archived on its own stays archived, because the parent
        never put it aside. `last_progress_at` does not move, since reopening
        is a decision rather than evidence of advancement.

        A project that is neither archived nor complete is 409
        `invalid_state`.
      operationId: reopenProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectNoteRequest'
      responses:
        '200':
          description: The reopened project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectLifecycleConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/complete:
    post:
      tags:
        - Projects
      summary: Complete a project
      description: |
        Closes one project. Requires admin access to its root. Completion is
        an explicit command and is never derived from a count, which is why
        `PATCH /projects/{id}` refuses `status: "completed"`.

        Every non-archived project under it has to be `completed` or
        `canceled`, every accepted task `done` or `canceled`, and every deal
        in a `done` or `canceled` stage. Unaccepted `suggested` tasks are not
        committed work and never block. Archived work does not block either:
        putting it aside is how it stops counting.

        When something is left the answer is 409 `blockers_open` and nothing
        is written. `error.details` then carries `open_projects`, `open_tasks`
        and `open_deals` beside `reason`, each an `issue` holding a decimal
        count, zeros included, so the client can say what is outstanding
        rather than retry blind.

        On success `status` becomes `completed`, `completed_at` is stamped and
        `last_progress_at` moves with it: delivery is the advancement that
        column records. The subtree's undispatched action runs, agent runs and
        follow-ups are cancelled. A project that is already complete is 409
        `invalid_state`, and so is an archived one.
      operationId: completeProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectNoteRequest'
      responses:
        '200':
          description: The completed project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectLifecycleConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{root_id}/organization-access/{org_id}:
    put:
      tags:
        - Projects
      summary: Share a project root with an organization
      description: |
        Grants one organization the `viewer` or `editor` role on one root, or
        moves the role it already holds. Requires admin on the root.

        Active members of a granted organization receive that role. Nothing
        is granted implicitly: the owning organization and a linked team
        reach the root through their own routes, and neither is written here.
        An explicit grant to the owning organization is accepted and is how
        its ordinary members are given access, since only its owners and
        admins reach a root without one.

        `admin` is not grantable: root admin comes from ownership, the owning
        organization or a linked team. A role outside `viewer`/`editor` is
        400 with `issue: "unknown_value"`, and an unknown or soft-deleted
        organization is 400 with `issue: "unknown"`.

        The grant, and the root's `access_version` bump, are one statement,
        so the two can never disagree. `version` moves with it: a permission
        change invalidates the view a concurrent admin was editing from.
        Re-granting the role an organization already holds changes nothing
        and leaves both counters where they were.
      operationId: grantProjectOrganizationAccess
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/GrantedOrganizationId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GrantOrganizationAccessRequest'
      responses:
        '200':
          description: The root's counters after the grant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RootAccessEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectConflict'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Projects
      summary: Stop sharing a project root with an organization
      description: |
        Removes one organization's grant on one root. Requires admin on the
        root. The route is gone for every subsequent authorization check;
        any other route that organization's members hold is untouched, so a
        member who is also on the linked team keeps that access.

        No grant for that organization is 404. Deleting the organization
        itself removes the grant too, without deleting the work.

        The request carries a body: `expected_version` is required here as on
        every other command, because revoking off a stale view is exactly the
        race it exists for.
      operationId: revokeProjectOrganizationAccess
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/GrantedOrganizationId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RevokeOrganizationAccessRequest'
      responses:
        '200':
          description: The root's counters after the revocation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RootAccessEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{root_id}/team:
    put:
      tags:
        - Projects
      summary: Link or clear the root's team
      description: |
        Links the root to a team, or clears the link with `team_id: null`.
        Requires admin on the root.

        Accepted team members are editors and accepted team admins are
        admins, independently of organization membership, so this one field
        moves a whole audience at once. Clearing it removes every route the
        team conferred and touches no work.

        `team_id` must be present: omitting it is 400 with
        `issue: "required"`, because clearing is too much to infer from a
        missing key. An unknown team is 400 with `issue: "unknown"`.
        Re-linking the team the root already has changes nothing.
      operationId: setProjectTeam
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetProjectTeamRequest'
      responses:
        '200':
          description: The root's counters after the change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RootAccessEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{root_id}/owner:
    put:
      tags:
        - Projects
      summary: Transfer root ownership
      description: |
        Hands the root to another user. Requires admin on the root.

        The new owner must be an active account and an active member of the
        root's owning organization, which is exactly the route that makes a
        root owner an admin; anyone else is 400 with
        `issue: "not_eligible"`. A root whose owning organization has been
        deleted has no eligible owner and is 409 `invalid_state`: it is held
        for administrative recovery rather than handed on.

        Background assistance does not travel with the root. The delegation
        was the outgoing owner's, so the transfer switches it off, clears the
        review schedule and cancels the root's queued background reviews. A
        review already running is left alone: it has an outcome to
        reconcile, and cancelling it would claim a side effect never
        happened. The interval and budget survive as settings the new owner
        can re-enable with.

        Transferring to the current owner changes nothing.
      operationId: setProjectOwner
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetProjectOwnerRequest'
      responses:
        '200':
          description: The root's counters after the transfer.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RootAccessEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{root_id}/pipelines:
    get:
      tags:
        - Projects
      summary: List a root's pipelines
      description: |
        Returns every pipeline configured on the root, in board order.
        Requires viewer access to the root: a pipeline's stages are what the
        board's columns are, so anyone who may see the board may see them.

        The list is whole rather than paged. A root holds a handful of
        pipelines and a board needs all of them at once, so there is no
        `next_cursor`: a page would let a client render half a configuration.

        Returns 404 when the root is unknown, not visible, or the capability
        is off. A subproject id answers 404 too: pipelines hang off the root.
      operationId: listPipelines
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/PipelineIncludeArchived'
      responses:
        '200':
          description: The root's pipelines, in board order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Projects
      summary: Create a pipeline
      description: |
        Configures one new pipeline on the root. Requires **admin** access:
        a pipeline's stages are the rules every deal in the root is judged
        by, so configuring one is not an editor's act.

        The pipeline starts with no stages. Adding them, copying an existing
        pipeline and creating one from a starter template are separate
        commands, so a create never half-applies a template.

        It is appended to the end of the root's board order; `position` is
        not a field a client may set, because moving a pipeline is its own
        command and two clients writing ranks through a generic create could
        not be ordered.

        `Idempotency-Key` is required: without one a dropped response would
        leave the client unable to retry without risking a second pipeline.
        A retry of the same key with the same body returns the first
        execution's result; the same key with a different body is 409
        `idempotency_conflict`.
      operationId: createPipeline
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePipelineRequest'
      responses:
        '200':
          description: The result of the first execution under this key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineEnvelope'
        '201':
          description: |
            The new pipeline. A replay of an earlier idempotency key returns
            200 with that execution's result, not 201.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PipelineConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/activity:
    get:
      tags:
        - Projects
      summary: List one project's activity
      description: |
        Returns the history written about this project: what happened to the
        tasks in it and to the project itself. A subproject's history is
        reached through that subproject, not through its parent.

        Reading needs viewer access to the project's root, and the access rule
        runs before the count and the page. A project the caller cannot read
        answers 404 exactly as an id that names nothing does, so existence is
        never leaked, and no total is drawn from rows they cannot see.

        Private history never appears here. A personal ToDo's rows are written
        for their owner alone and carry no project at all, so a task that was
        once a ToDo shows only what has happened to it since it was placed.
        Rows written for admins, which is how sharing changes are recorded,
        appear only to an admin; a viewer or editor sees everything else.

        `task_id` and `deal_id` narrow the feed to one thing's history. Paging
        is keyset and newest first, so a row written while a client pages does
        not shift the pages it has still to read. `next_cursor` is empty on the
        last page. A cursor is bound to the caller, the project, the filters
        and the audience that produced it; replayed under any other combination
        it is 400 with `details: [{field: "cursor", issue: "filter_mismatch"}]`
        rather than a page that skips or repeats rows.
      operationId: listProjectActivity
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: The project whose history to list.
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/ActivityTaskId'
        - $ref: '#/components/parameters/ActivityDealId'
        - $ref: '#/components/parameters/ActivityLimit'
        - $ref: '#/components/parameters/ActivityCursor'
      responses:
        '200':
          description: One page of the project's history, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/suggestions:
    get:
      tags:
        - Projects
      summary: List a project's suggestions
      description: |
        Returns the project's proposals, newest first, keyset paged. Requires
        viewer access to the project's root.

        **The page is personal.** Each row carries `my_state`: the calling
        user's own response and nobody else's. A proposal this caller
        dismissed, or snoozed until later, is simply not in their page; nobody
        else's page changes, and no field anywhere says who hid what. That is
        why there is no "dismissed by" list: a private response that showed up
        on somebody else's screen would stop being private.

        The response is joined **by intent**, not by suggestion id. A
        dismissal therefore follows an idea across a regenerated proposal: the
        agent rewording the same suggestion produces a different row with the
        same `intent_key`, and it stays hidden.

        `include_hidden=true` asks for the caller's own hidden rows back, which
        is what a "show dismissed" toggle sends. It can only ever widen the
        page to this caller's own responses.

        `total` counts exactly what the page would show under the same
        filters, hidden rows included or excluded alike.
      operationId: listSuggestions
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/SuggestionStatus'
        - $ref: '#/components/parameters/ActivityTaskId'
        - $ref: '#/components/parameters/ActivityDealId'
        - $ref: '#/components/parameters/IncludeHidden'
        - $ref: '#/components/parameters/SuggestionLimit'
        - $ref: '#/components/parameters/SuggestionCursor'
      responses:
        '200':
          description: One page of the project's suggestions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestionListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /project-suggestions/{id}:
    get:
      tags:
        - Projects
      summary: Get one suggestion
      description: |
        Returns one proposal with the caller's own response, if they may read
        its scope.
      operationId: getSuggestion
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SuggestionId'
      responses:
        '200':
          description: The suggestion.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /project-suggestions/{id}/my-state:
    put:
      tags:
        - Projects
      summary: Save, snooze or dismiss a suggestion for yourself
      description: |
        Records the calling user's private answer to an idea. Requires viewer
        access; reading a proposal is enough to have an opinion about it.

        **This changes nothing for anybody else.** It accepts no work, creates
        no task, moves no deal, and does not close the proposal. Dismissing is
        "not for me", not "not for anyone": withdrawing a proposal for every
        reader is `POST /project-suggestions/{id}/withdraw`, and it needs
        root-admin access.

        The response is stored against the **intent**, so it survives the
        agent rewording the same idea: a regenerated proposal carrying the
        same `intent_key` arrives already dismissed for this caller. Sending
        `undecided` clears the answer.

        A `snoozed` disposition requires `snoozed_until`, and no other
        disposition accepts one: a snooze with no wake time is a dismissal
        wearing a softer word, and the client should send that instead.

        There is no `expected_version`. A response is the caller's own state,
        nobody races them for it, and a stale view of somebody else's proposal
        cannot make their answer wrong.
      operationId: setSuggestionMyState
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SuggestionId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuggestionMyStateRequest'
      responses:
        '200':
          description: The suggestion, carrying the caller's new response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SuggestionConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{root_id}/agent-settings:
    put:
      tags:
        - Projects
      summary: Switch background assistance on or off
      description: |
        Sets whether an agent may review this root on its own schedule.

        **Turning it on is the root owner's call, not an admin's.** Every other
        configuration route here needs admin access and this one needs it too,
        but enabling also requires that the caller *is* the owner. An admin can
        already do anything a person can do; this is agreeing that something
        else may act on the root's behalf and spend its money, and that is the
        owner's decision. Turning it off needs only admin, because stopping
        something is never the decision that needs guarding.

        A root that enables assistance and names nothing gets a twenty-four
        hour cycle and the deployment's daily budget. The first review is
        scheduled a full cycle out: switching assistance on is not a request
        for a review, and a root that turned it on to read the settings should
        not be charged for one.

        Turning it off cancels the background reviews this root had **queued**,
        because they were queued on the strength of a setting that no longer
        holds. A review already running has a lease and an outcome to record
        and is left alone.

        Only a root carries these settings; a child project is 400
        `root_id: not_a_root`.
      operationId: setAgentSettings
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentSettingsRequest'
      responses:
        '200':
          description: The root, with its new settings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: |
            The caller may configure this root but is not its owner, so they
            cannot switch background assistance on.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ProjectConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/agent-runs:
    post:
      tags:
        - Projects
      summary: Ask the agent to review a project
      description: |
        Queues one bounded review. Requires **editor** access: a review costs
        the project money, which a viewer cannot spend.

        Asking is not doing. The response is the run, queued, with `202`; the
        worker is what runs it, and the row is where the answer appears. A
        request that blocked on the review would be a very slow route that
        still had to handle the worker crashing.

        `task_id` or `deal_id` narrows the review to one row of the same
        project; naming both is refused, because a review naming two subjects
        would have to guess which one bounded it. Neither is required.

        What the review is allowed to do is written onto the row when it is
        queued: how much it may look at, how many times it may ask the model,
        and the day's budget. They are the deployment's and the root's
        decision, never the caller's, and they are frozen on the job so a
        default changing overnight cannot make yesterday's request a bigger
        one.

        `Idempotency-Key` is **required**. A review costs money, so a dropped
        response must not leave a client choosing between a lost request and a
        second one.
      operationId: requestProjectAgentRun
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestAgentRunRequest'
      responses:
        '200':
          description: The review this key already queued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunEnvelope'
        '202':
          description: The queued review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/AgentRunConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /me/todo-agent-runs:
    post:
      tags:
        - Projects
      summary: Ask the agent to review your own ToDo
      description: |
        Queues one bounded review of the caller's own ToDo. It is always the
        caller's own: a personal review is requested by and acts for its owner
        alone, which the schema enforces with a CHECK and the service refuses
        first, with a message rather than a constraint name. There is no
        automatic background review of a personal ToDo, by design.

        `deal_id` is refused here: a ToDo holds no deals. Everything else works
        as the project route does, and the day's budget is the deployment's,
        because a ToDo carries no budget column of its own.

        At most one queued or running personal review per owner, whatever
        asked for it.
      operationId: requestTodoAgentRun
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestAgentRunRequest'
      responses:
        '200':
          description: The review this key already queued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunEnvelope'
        '202':
          description: The queued review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/AgentRunConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /project-agent-runs/{id}:
    get:
      tags:
        - Projects
      summary: Read one agent review
      description: |
        Returns one review: what it was allowed to do, what it has spent so
        far, and how it ended. Requires viewer access to its scope; a personal
        review is readable by its owner alone.

        `status` is where the answer is. `queued` and `running` mean it is
        still happening; `succeeded`, `partial` and `failed` mean it is over.
        `budget_exhausted` means the scope had spent its day before this run
        could start, which is not a failure: trying again tomorrow is exactly
        what should happen.

        `error_code` names the reason a run did not produce anything, and
        `model_unconfigured` is the one a deployment with no model gives.

        The context fingerprint, the resource versions and the evidence
        manifest are deliberately not here: they describe what the agent looked
        at, and some of that is private to one person.
      operationId: getAgentRun
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AgentRunId'
      responses:
        '200':
          description: The review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /project-actions:
    post:
      tags:
        - Projects
      summary: Authorize one action
      description: |
        Authorizes one exact side effect and queues it. The response is the
        action, not its result: the worker is what carries it out, so `status`
        comes back `queued` and a client must never render that as finished.

        An authorization is for one exact thing: this action, this target, this
        content, these versions. The content is hashed onto the row, so editing
        the text afterwards needs a fresh authorization rather than riding on
        the yes given to the old words. It expires after 24 hours if nothing
        has started it.

        `expected_versions` names the rows the caller judged the action
        against, keyed by name (`deal`, `pipeline_configuration`). The worker
        re-reads them before acting and refuses on any that moved, which is how
        an agent racing a person loses rather than wins by being later.

        Only an action this release can carry out is accepted; anything else is
        `ACTION_NOT_EXECUTABLE`. A suggestion on its own never authorizes
        anything: naming one in `suggestion_id` records where the idea came
        from and grants nothing.

        Requires editor access to the project, or the caller's own ToDo when
        `personal` is set. `Idempotency-Key` is required, because a dropped
        response must be retryable without risking the side effect twice.
      operationId: authorizeAction
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthorizeActionRequest'
      responses:
        '202':
          description: The authorization, queued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActionRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /project-actions/{id}:
    get:
      tags:
        - Projects
      summary: Read one authorized action
      description: |
        Returns what became of one authorized action. Requires viewer access to
        its scope; a personal action is readable by its owner alone.

        `status` is where the answer is, and none of it is softened. `queued`
        and `running` mean it has not finished. `succeeded` and `failed` mean
        it is over. **`unknown` means nobody can say whether the side effect
        happened**: the provider was asked and never answered conclusively, so
        the action stopped rather than being tried again. It stays there until
        a person settles it.

        `results` carries what the attempt produced, each with the evidence
        that says how much it is worth: `provider_acknowledged` is the provider
        saying so, `domain_committed` is our own transaction saying so, and
        `user_reported` is one person's account of it.

        `input`, `expected_versions`, `results` and `last_error` are the
        private half and are returned **only to the person who authorized the
        action**. Everybody else who can read the scope sees that an authorized
        action of this type took place and how it ended, which is all the
        shared timeline says too.
      operationId: getActionRun
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ActionRunId'
      responses:
        '200':
          description: The action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActionRunEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /project-actions/{id}/cancel:
    post:
      tags:
        - Projects
      summary: Cancel an action that has not started
      description: |
        Stops an authorized action before anything happens. Only the person who
        authorized it may cancel it.

        An attempt already under way keeps going: a worker holds its lease, and
        the honest end is whatever that attempt reports. Cancelling one of
        those answers `409 INVALID_STATE` rather than telling a client the
        action stopped while a worker is mid-send.
      operationId: cancelActionRun
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ActionRunId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelActionRequest'
      responses:
        '200':
          description: The cancelled action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActionRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /project-actions/{id}/retry:
    post:
      tags:
        - Projects
      summary: Try a failed action again
      description: |
        Puts a **failed** action back on a fresh 24-hour authorization window.
        The content is unchanged, because the yes was for that content; the
        window is fresh, because the original yes was for a moment that has
        passed. Only the person who authorized the action may retry it.

        Failed is the only status this accepts, and that is the whole safety
        argument: failed means nothing happened, either because the domain
        refused or because the provider refused before acting. An **unknown**
        action might have happened, so it is settled through
        `resolve-unknown` instead and never silently tried again. Anything
        else answers `409 INVALID_STATE`.
      operationId: retryActionRun
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ActionRunId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RetryActionRequest'
      responses:
        '200':
          description: The action, queued again.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActionRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /project-actions/{id}/resolve-unknown:
    post:
      tags:
        - Projects
      summary: Settle an action nobody could account for
      description: |
        Ends an action stuck in `unknown`. Only the person who authorized it
        may settle it, and only an `unknown` action can be settled this way.

        Give exactly one of two answers.

        **A provider receipt.** Send `provider_result_id` with the `grant_id`
        it belongs to. It is checked against the provider before anything is
        recorded, and evidence the provider will not confirm is refused rather
        than quietly downgraded: you said you had a receipt, and you have not.
        A confirmed receipt records `provider_acknowledged` evidence.

        **Your own account.** Send `happened` as true or false. It is recorded
        as `user_reported` evidence, which is exactly what it is. **A user
        report never satisfies an `action_succeeded` stage criterion**; a
        criterion that wants a person's say-so is `user_confirmed`, a
        different check with a different audit trail.
      operationId: resolveUnknownActionRun
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ActionRunId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveUnknownRequest'
      responses:
        '200':
          description: The settled action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActionRunEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{root_id}/my-preferences:
    get:
      tags:
        - Projects
      summary: Read your own reminder settings for a project
      description: |
        Your own settings, and only ever your own. Nobody reads anybody else's,
        and changing them writes nothing to the project's history: a timeline
        naming who muted what would publish the thing the setting exists to
        keep quiet.

        A scope you have never set anything in answers with the defaults rather
        than 404, with `stored: false` so a client can show "using defaults".
        Requires read access to the root.
      operationId: getProjectPreferences
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
      responses:
        '200':
          description: Your settings for this project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferencesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Projects
      summary: Change your own reminder settings for a project
      description: |
        A patch: a field you leave out is unchanged, and an explicit `null`
        clears that setting. Quiet hours are one setting in two fields, so both
        ends move together or neither does; sending one half is refused rather
        than half-applied, and sending both as `null` clears the window.

        The row is created the first time you change anything, so there is no
        create route and no 404 to handle.
      operationId: setProjectPreferences
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RootProjectId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetPreferencesRequest'
      responses:
        '200':
          description: Your settings, as they now stand.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferencesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /me/todo-preferences:
    get:
      tags:
        - Projects
      summary: Read your own reminder settings for your ToDo
      description: |
        The same settings for your personal ToDo. The route takes no user, so
        there is no id anybody could substitute for somebody else's.
      operationId: getTodoPreferences
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Your settings for your ToDo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferencesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/Internal'
    put:
      tags:
        - Projects
      summary: Change your own reminder settings for your ToDo
      description: |
        The same patch rules as the project route.
      operationId: setTodoPreferences
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetPreferencesRequest'
      responses:
        '200':
          description: Your settings, as they now stand.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferencesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/Internal'
  /project-suggestions/{id}/adopt:
    post:
      tags:
        - Projects
      summary: Adopt a suggestion
      description: |
        Turns a proposal into something real. Requires **editor** access to the
        project's root.

        `adoption_type` is required and has no default. "Adopt" could mean four
        different things, and a client that had to guess which one the server
        picked would be accepting work it never chose:

        - `accepted_task` accepts the Suggested task the proposal came with,
          through exactly the write `POST /tasks/{id}/accept` uses. The
          proposal must carry one.
        - `created_task` writes new work from the `task` body the caller
          reviewed, already accepted, and links it. Refused when the proposal
          already carries a task: accept that one instead.
        - `linked_task` points the proposal at an existing accepted task of the
          same project. **Nothing is written to that task**: linking says the
          idea is already being handled, and editing somebody's task to say so
          would be a change nobody asked for.
        - `applied_change` and `queued_action` perform the action a change
          request describes. Every descriptor in the action registry is
          advisory in this release, so both answer 409
          `action_not_executable`. That refusal reads the registry rather than
          being hard-coded: when a descriptor becomes executable it stops.

        Each type takes exactly one of `task` and `task_id`, and carrying the
        other is refused rather than ignored.

        Two people cannot both adopt. `expected_version` and the proposal's
        open status settle it: the second gets 409 and no second task.
      operationId: adoptSuggestion
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SuggestionId'
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdoptSuggestionRequest'
      responses:
        '200':
          description: The adopted suggestion, naming what it became.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/AdoptSuggestionConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /project-suggestions/{id}/resolve:
    post:
      tags:
        - Projects
      summary: Close a suggestion that no longer applies
      description: |
        Closes an open proposal that has been overtaken: the work was done
        another way, or the situation moved on. Requires **editor** access;
        noticing that advice has gone stale is not an administrative act.

        It closes the row for every reader, with `resolution_reason: stale`.
        It adopts nothing and creates nothing.

        A proposal that has already left the open set is 409 `invalid_state`.
      operationId: resolveSuggestion
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SuggestionId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloseSuggestionRequest'
      responses:
        '200':
          description: The closed suggestion.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SuggestionConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /project-suggestions/{id}/withdraw:
    post:
      tags:
        - Projects
      summary: Withdraw a suggestion for everyone
      description: |
        Takes an open proposal away from every reader. Requires **admin**
        access to the project's root, with `resolution_reason: withdrawn`.

        This is the shared half of the pair. One reader deciding an idea is
        not for them is `PUT …/my-state`; this is the root deciding nobody
        should be offered it. Do not present a personal dismissal as this.

        A proposal that has already left the open set is 409 `invalid_state`.
      operationId: withdrawSuggestion
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SuggestionId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloseSuggestionRequest'
      responses:
        '200':
          description: The withdrawn suggestion.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SuggestionConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/deals:
    get:
      tags:
        - Projects
      summary: List a project's deals
      description: |
        Returns the project's opportunities, newest first, keyset paged by
        `(created_at, id)`. Requires viewer access to the project's root.

        `pipeline_id` and `stage_id` narrow the list. A filter naming
        something outside this project's root is 400 `not_in_project` rather
        than an empty page: a page drawn over an empty set looks exactly like
        a project with no deals.

        The cursor is bound to the project and to every filter it was produced
        under, so replaying one under a different filter is 400 rather than a
        page that silently skips or repeats rows.
      operationId: listDeals
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/DealPipelineFilter'
        - $ref: '#/components/parameters/DealStageFilter'
        - $ref: '#/components/parameters/DealIncludeArchived'
        - $ref: '#/components/parameters/ProjectLimit'
        - $ref: '#/components/parameters/ProjectCursor'
      responses:
        '200':
          description: One page of the project's deals.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Projects
      summary: Open a deal
      description: |
        Opens one opportunity in a project's pipeline. Requires **editor**
        access: a deal is work in a project, while the pipeline it sits in is
        configuration and needs admin.

        There is no stage field. A new deal lands in its pipeline's default
        column, and moving it is its own command.

        The pipeline must be ready to hold deals: exactly one live default
        stage, and at least one live stage for each outcome. A pipeline that
        is still being configured answers 409 `pipeline_not_ready`, which is
        the first moment an incomplete configuration would actually hurt.

        `target` names the canonical person or company, and is resolved
        through the nodes service before the command runs: an id that resolves
        to nothing is 400 `not_found`, and one whose kind is not what the body
        claimed is 400 `wrong_kind`. The reference confers no access of its
        own.

        The owner defaults to the caller and must be an eligible editor of the
        root. Ownership alone grants nobody access, so an owner who could not
        already reach the work would hold a deal they cannot open.

        `money` is optional and paired: a value without a currency is
        meaningless and a currency without a value is noise. The value is a
        string, never a number, with at most 16 integer and 4 fraction digits,
        so nothing passes through a float.

        Another live deal in the same pipeline with the same target does not
        refuse the create: the response carries a `duplicate_target` warning,
        because one company can legitimately be several opportunities.

        `Idempotency-Key` is required.
      operationId: createDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDealRequest'
      responses:
        '200':
          description: The result of the first execution under this key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '201':
          description: |
            The new deal. A replay of an earlier idempotency key returns 200
            with that execution's result, not 201.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /projects/{id}/tasks:
    get:
      tags:
        - Projects
      summary: List one project's tasks
      description: |
        Returns the tasks whose `project_id` is this project. A subproject's
        tasks are reached through that subproject: this is the project asked
        for, not its subtree.

        Reading needs viewer access to the project's root, and the access rule
        runs before the count and the page. A project the caller cannot read
        answers 404 exactly as an id that names nothing does, so existence is
        never leaked, and no total is drawn from rows they cannot see.

        `filter` selects one section, and `total` counts that section alone.
        `suggested` is the unaccepted proposals: a section of its own, never
        part of the accepted workload `active` counts. Archived rows are left
        out unless `include_archived` asks for them.

        Paging is keyset rather than offset, so a task written while a client
        pages does not shift the pages it has still to read. `next_cursor` is
        empty on the last page. A cursor is bound to the caller, the project,
        the filters and the sort that produced it; replayed under any other
        combination it is 400 with `details: [{field: "cursor", issue:
        "filter_mismatch"}]` rather than a page that skips or repeats rows.
      operationId: listProjectTasks
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: The project whose tasks to list.
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/TaskFilter'
        - $ref: '#/components/parameters/TaskSort'
        - $ref: '#/components/parameters/TaskSearch'
        - $ref: '#/components/parameters/TaskIncludeArchived'
        - $ref: '#/components/parameters/TaskLimit'
        - $ref: '#/components/parameters/TaskCursor'
      responses:
        '200':
          description: One page of the project's tasks.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Projects
      summary: Create a task in a project
      description: |
        Creates one task inside this project. It needs editor access to the
        project's root: a viewer is 403, and a caller with no route at all is
        404, the same answer an unknown project gives.

        The project comes from the path, so the body cannot name one, and
        `deal_id` and `work_key` are not accepted on this route yet. An
        unknown key is rejected with 400 rather than ignored.

        The result has `project_id` and `root_project_id` set,
        `personal_user_id` null, `status: "todo"` and a set `accepted_at`: a
        direct human create is committed work, not an AI proposal. No assignee
        is created, because assignment is its own versioned command, so a new
        task shows up here and not yet in `GET /tasks`.

        A project that is archived, complete, or anywhere under an archived
        project is 409 `invalid_state`: finished or suppressed work takes no
        new tasks.

        `Idempotency-Key` is required. A retry of the same key with the same
        body returns the first execution's task with 200 instead of 201; the
        same key with a different body is 409 `idempotency_conflict`.
      operationId: createProjectTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: The project the task goes in.
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskDraftRequest'
      responses:
        '200':
          description: |
            The task an earlier request with this `Idempotency-Key` already
            created. Nothing was written.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '201':
          description: The created task.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/TaskCreateConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}:
    get:
      tags:
        - Projects
      summary: Get one deal
      description: |
        Returns the deal if the caller may read its root. Returns 404 when it
        is unknown, not visible, or the capability is off.
      operationId: getDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
      responses:
        '200':
          description: The deal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - Projects
      summary: Update a deal
      description: |
        Edits the caller-editable content of one deal. Requires editor access
        to its root.

        Stage, outcome and board position are **not** writable here. Where a
        deal sits is the move command's, whether it is won or lost is derived
        from that stage's category, and its place in the column is the reorder
        command's, so a generic edit can neither close a deal nor rearrange a
        board.

        Patch semantics: an omitted key preserves the stored value, a key
        present with `null` clears it, and the body must name at least one
        patchable field. `expected_version` is required.

        `target` is replaced whole or left alone, and is re-resolved when
        sent: changing what a deal is about is explicit. `owner_user_id` is
        not nullable, because a deal always has someone accountable for it,
        and a new owner must be an eligible editor of the root. `money` is
        set as a pair or cleared as a pair.
      operationId: updateDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDealRequest'
      responses:
        '200':
          description: The updated deal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/move:
    post:
      tags:
        - Projects
      summary: Move a deal to another stage
      description: |
        Moves one card to another column of its own pipeline. Requires editor
        access to the deal's root.

        A drag and an agent run this same command. There is no second path with
        looser rules, so a card cannot reach a stage the criteria refuse
        whichever end of the product asked for it.

        Three things are asserted, not one. `expected_version` is the deal's,
        so two people moving one card cannot both win.
        `expected_configuration_version` is the pipeline's, so a client that
        dragged under rules that have since changed is refused with 409
        `configuration_conflict` rather than judged by rules it never saw. And
        the destination stage is read under the same root lock, so it cannot be
        archived out from under the move.

        The destination's criteria are evaluated before the write. Each answers
        met, unmet or **unknown**, and unknown blocks: a criterion whose
        evidence could not be read is not one that passed. A refused move is
        409 `criteria_unmet` and carries one `details` entry per gap, keyed
        `criteria.<criterion_id>` with the state as its issue, so a client can
        show what is missing rather than only that something is.

        The outcome is derived from the destination's category, never sent: a
        `done` stage wins the deal, a `canceled` stage loses it, and everything
        else leaves it open. A losing stage requires `lost_reason`, and any
        other stage refuses one. Moving a closed deal is refused: reopen it
        first.
      operationId: moveDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveDealRequest'
      responses:
        '200':
          description: The deal, in its new column.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealMoveConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/reopen:
    post:
      tags:
        - Projects
      summary: Reopen a closed deal
      description: |
        Brings a won or lost deal back into play. Requires editor access to the
        deal's root.

        It is an explicit command rather than a move with a nicer name, because
        reopening clears a closure somebody recorded. The destination's
        criteria still have to be met, and the destination cannot itself be a
        stage that closes the deal. Reopening a deal that is already open is
        409 `invalid_state`.

        Both versions are asserted exactly as they are on a move, and the
        history row records the outcome the deal is leaving.
      operationId: reopenDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReopenDealRequest'
      responses:
        '200':
          description: The deal, open again.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealMoveConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/progress:
    post:
      tags:
        - Projects
      summary: Record progress on a deal
      description: |
        Records real advancement. Requires editor access to the deal's root.

        This is the **only** deal write that moves `last_progress_at`. A
        rename, a value, a stakeholder or a rank change is not advancement, and
        treating it as such would make every staleness scan lie.

        `next_action` and `blocked_reason` ride along, because progress is when
        a person says what happens next and what is in the way. A closed or
        archived deal takes no progress: reopen it first.
      operationId: recordDealProgress
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordDealProgressRequest'
      responses:
        '200':
          description: The deal, with its progress time moved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/reorder:
    post:
      tags:
        - Projects
      summary: Reorder a deal within its column
      description: |
        Drops one card somewhere else in the column it is already in. Requires
        editor access to the deal's root.

        `after_deal_id` names the card this one goes below. `null` or an
        omitted key puts it at the top of the column: a card dragged to the top
        has no card above it to name. An index is deliberately not accepted; an
        index is a statement about the whole column, which a second drag
        invalidates, while a neighbour stays true under concurrent edits
        elsewhere in the column.

        Moving a card to **another** column is `POST /deals/{id}/move`. That
        one passes the destination's criteria, and a reorder must never become
        the way around them.

        Ranks are re-spaced automatically when a column runs out of room
        between two neighbours. The rebalance writes only the positions, so the
        cards it moves aside keep their `version` and a client holding one of
        them has not gone stale. Every reorder bumps the pipeline's
        `board_version`.
      operationId: reorderDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderDealRequest'
      responses:
        '200':
          description: The deal, in its new place.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealBoardConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/archive:
    post:
      tags:
        - Projects
      summary: Archive a deal
      description: |
        Puts one card away. Requires editor access to the deal's root.

        The deal stays readable at its own id and keeps its whole history; it
        leaves every column, count and page a live card appears in. There is no
        unarchive in this release.

        Archiving is not closing. A deal that was won or lost is closed by
        `POST /deals/{id}/move` into a terminal stage, which records the
        outcome; archiving a deal that never closed leaves it with no outcome
        at all, which is the right record of work that was simply dropped.

        It bumps the pipeline's `board_version`.
      operationId: archiveDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchiveDealRequest'
      responses:
        '200':
          description: The archived deal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealBoardConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/attest:
    post:
      tags:
        - Projects
      summary: Confirm a human-confirmation criterion
      description: |
        Records that a person has confirmed one criterion on this deal.
        Requires editor access to the deal's root.

        A human confirmation is the one kind of evidence a model can never
        manufacture, so the record names the person who gave it. An agent
        actor is refused outright: it may prepare the work behind a
        confirmation but can never give one.

        Only a criterion a **live stage of this deal's pipeline** gates on can
        be attested, and only one of the `user_confirmed` kind. Anything else
        would leave a record nobody can act on, and is 400 `not_found` or
        `not_confirmable`.

        The confirmation is kept after the criterion it names is edited away:
        what somebody confirmed is a fact about them, not about the current
        configuration.
      operationId: attestCriterion
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttestCriterionRequest'
      responses:
        '200':
          description: The deal, with the confirmation recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DealConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/stakeholders:
    get:
      tags:
        - Projects
      summary: List a deal's stakeholders
      description: |
        Returns the people and companies attached to the deal, oldest first, so
        a reader sees the relationship as it was built. Requires viewer access
        to the deal's root.

        Removed links are history and come back only when asked for. The list
        is whole rather than paged: a deal holds a handful and a card needs all
        of them at once.
      operationId: listStakeholders
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/StakeholderIncludeRemoved'
      responses:
        '200':
          description: The deal's stakeholders.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StakeholderListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Projects
      summary: Add a stakeholder to a deal
      description: |
        Attaches one person or company to the deal in one role. Requires
        **editor** access: a stakeholder is work on a deal.

        The target is resolved through the nodes service, exactly as a deal's
        own target is: an id that resolves to nothing is 400 `not_found`, and
        one whose kind is not what the body claimed is 400 `wrong_kind`. The
        reference confers no access, creates no graph edge, and one entity in
        two roles is two links.

        Adding a target that is already on the deal in that role is 409
        `stakeholder_exists`. A removed link frees the slot, so the same person
        can be added again later in the same role.

        `Idempotency-Key` is required.
      operationId: addStakeholder
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddStakeholderRequest'
      responses:
        '200':
          description: The result of the first execution under this key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StakeholderEnvelope'
        '201':
          description: |
            The new link. A replay of an earlier idempotency key returns 200
            with that execution's result, not 201.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StakeholderEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/StakeholderConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /deals/{id}/stakeholders/{link_id}:
    patch:
      tags:
        - Projects
      summary: Update a stakeholder link
      description: |
        Edits the role or the description of one link. Requires editor access
        to the deal's root.

        The target is not writable: a link is one entity in one role, so
        pointing it at somebody else is a removal and an addition rather than
        an edit. `expected_version` is required and is the link's own version.

        A link belonging to another deal answers 404: the route named a deal,
        and that is not one of its stakeholders.
      operationId: updateStakeholder
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/StakeholderLinkId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateStakeholderRequest'
      responses:
        '200':
          description: The updated link.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StakeholderEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/StakeholderConflict'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Projects
      summary: Remove a stakeholder from a deal
      description: |
        Ends one relationship. Requires editor access to the deal's root.

        The removal is **soft**: the row stays, so who was involved and when
        remains readable, and who removed it is in the deal's timeline. The
        response is the link with `removed_at` set, not an empty body.

        Removing a link that was already removed is 409 `invalid_state`, so a
        stale view is told rather than quietly agreed with.
      operationId: removeStakeholder
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/StakeholderLinkId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoveStakeholderRequest'
      responses:
        '200':
          description: The removed link, with `removed_at` set.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StakeholderEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/StakeholderConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}:
    get:
      tags:
        - Projects
      summary: Get one pipeline
      description: |
        Returns the pipeline if the caller may read its root. `rights` is the
        caller's role in that root, which is what tells a client whether to
        offer the configuration controls at all.

        Returns 404 when the pipeline is unknown, its root is not visible, or
        the capability is off, so existence is never leaked.
      operationId: getPipeline
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
      responses:
        '200':
          description: The pipeline.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - Projects
      summary: Update a pipeline
      description: |
        Edits a pipeline's descriptive configuration. Requires **admin**
        access to its root.

        Patch semantics: an omitted key preserves the stored value, a key
        present with `null` clears it. Unknown fields are rejected with 400
        rather than ignored, and the body must name at least one patchable
        field. `expected_version` is required and must equal the pipeline's
        current `version`; a mismatch is 409 `version_conflict` and writes
        nothing. A patch whose values all equal the stored ones is a 200 that
        writes nothing and leaves `version` where it was.

        Stage order, stage rules, copying and archiving are not writable
        here: each is its own command, because changing one has consequences
        a generic edit cannot carry. `position` is not writable either.

        A write bumps `configuration_version` as well as `version`, because a
        pipeline row is configuration: a consumer holding cached rules should
        notice, and refusing a stale move too often is safer than refusing
        one too rarely.

        `Idempotency-Key` is optional: `expected_version` already stops a
        repeat from applying twice.
      operationId: updatePipeline
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePipelineRequest'
      responses:
        '200':
          description: The updated pipeline.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PipelineConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/copy:
    post:
      tags:
        - Projects
      summary: Copy a pipeline's configuration
      description: |
        Reuses one pipeline's configuration, in its own root or another one.
        Needs **read** access to the source and **admin** access to the
        destination root: a configuration discloses no work, but a pipeline's
        stages are the rules its deals are judged by.

        A copy takes the stages, their criteria and their guidance, and
        nothing else. No deals, no people, no history and no permissions come
        with it, and the copy does not receive later edits to the source.

        Every stage, criterion and guidance rule gets a fresh id, so an
        attestation against one pipeline never counts for the other. Semantic
        work keys are preserved, because `task_done` names a requirement
        rather than a task. A guidance rule that points at a stage or a
        criterion by id is rewritten to the copy's own row; one whose
        reference did not come along is dropped rather than left dangling.

        `destination_root_id` defaults to the source's own root, and `title`
        to the source's title. `Idempotency-Key` is required.
      operationId: copyPipeline
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CopyPipelineRequest'
      responses:
        '200':
          description: The result of the first execution under this key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineEnvelope'
        '201':
          description: |
            The new pipeline. A replay of an earlier idempotency key returns
            200 with that execution's result, not 201.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PipelineConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/stages:
    post:
      tags:
        - Projects
      summary: Add a stage to a pipeline
      description: |
        Adds one board column. Requires **admin** access to the pipeline's
        root: a stage's criteria are the rules every deal in the root is
        judged by.

        The stage is appended to the end of the column order; `position` is
        not a field a client may set, because reordering is its own command.

        `category` is required and has no default. It is semantic, not
        cosmetic: `done` derives a won deal, `canceled` a lost one, and the
        rest derive open. A stage flagged `is_default` must be nonterminal,
        because the column a deal lands in by default cannot be one that
        closes it.

        `criteria` and `action_guidance` are bounded versioned documents. A
        criterion names one of six checks (`has_target`, `has_stakeholder`,
        `field_present`, `task_done`, `action_succeeded`, `user_confirmed`)
        and the one argument that check takes; there is no expression
        language and no free-form predicate. Guidance rules name an action
        type from the server's registry and may carry default parameters for
        it; the registry is reviewed Go, so a document cannot introduce a
        tool, a URL, SQL or a shell command.

        Adding a stage bumps the pipeline's `configuration_version`, so a
        client holding cached rules learns that they moved.

        `Idempotency-Key` is required.
      operationId: createStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateStageRequest'
      responses:
        '200':
          description: The result of the first execution under this key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StageEnvelope'
        '201':
          description: |
            The new stage. A replay of an earlier idempotency key returns 200
            with that execution's result, not 201.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StageEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/StageConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/stages/{stage_id}:
    patch:
      tags:
        - Projects
      summary: Update a stage
      description: |
        Edits one board column. Requires **admin** access to the pipeline's
        root.

        Patch semantics: an omitted key preserves the stored value, a key
        present with `null` clears it, and the body must name at least one
        patchable field. `expected_version` is required and must equal the
        stage's current `version`.

        `criteria` and `action_guidance` are replaced whole or left alone.
        There is no merge: half of one gate and half of another is not a gate
        anyone could read. Sending `{}` clears a document.

        `category` cannot change once a deal sits in the stage. The category
        derives that deal's outcome, so changing it would rewrite what the
        deal's position means; add a replacement stage and move the deals
        explicitly instead. That refusal is 409 `stage_in_use`.

        Setting `is_default` moves the flag: whichever live stage holds it
        loses it in the same transaction. A default stage must be
        nonterminal. Clearing the flag leaves the pipeline with no default,
        which is allowed while it is being configured.

        Archiving a stage and reordering the columns are not writable here:
        an archive names a replacement for the deals it holds, and a reorder
        writes a rank. Every edit bumps the pipeline's
        `configuration_version`, whether or not it changes a gate.

        `Idempotency-Key` is optional: `expected_version` already stops a
        repeat from applying twice.
      operationId: updateStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/StageId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateStageRequest'
      responses:
        '200':
          description: The updated stage.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StageEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/StageConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/stages/{stage_id}/reorder:
    post:
      tags:
        - Projects
      summary: Reorder a stage on its board
      description: |
        Moves one column along the board. Requires **admin** access to the
        pipeline's root: the columns are the process, and their order is part
        of it.

        `after_stage_id` names the column this one goes after. `null` or an
        omitted key puts it first. Ranks are re-spaced automatically when the
        board runs out of room between two columns; the rebalance writes only
        the positions, so the columns it moves aside keep their `version`.

        A reorder bumps the pipeline's `configuration_version`, not its
        `board_version`: the cards did not move, the process did, and a client
        judging a move against the old column order is judging it against an
        order that no longer exists.
      operationId: reorderStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/StageId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderStageRequest'
      responses:
        '200':
          description: The stage, in its new place.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StageEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/StageConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/stages/{stage_id}/archive:
    post:
      tags:
        - Projects
      summary: Retire a stage
      description: |
        Retires one column and sends its live cards to another. Requires
        **admin** access to the pipeline's root.

        `replacement_stage_id` is required and must name a different live stage
        of the same pipeline. The move and the retirement happen in one
        transaction, so no open deal is ever left in a column that is no longer
        part of the process. Cards keep their rank, and the moved cards each
        get a new `version`; a client holding one of them must refetch.

        The retired stage loses `is_default` in the same write: a retired
        column is nowhere for a new card to land. If it was the pipeline's only
        default, the pipeline can no longer admit deals until another stage is
        made default.

        It bumps `configuration_version`, and `board_version` too when it
        actually moved cards. The history row carries `replacement_stage_id`
        and `deals_moved`.
      operationId: archiveStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/StageId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchiveStageRequest'
      responses:
        '200':
          description: The retired stage.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StageEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/StageConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/archive:
    post:
      tags:
        - Projects
      summary: Archive a pipeline
      description: |
        Puts a whole pipeline away. Requires **admin** access to its root.

        A pipeline that still holds live deals is refused with 409
        `pipeline_in_use`, carrying a `live_deals` count in `error.details`.
        Archiving it would take the board away from work that is still going
        on, and there would be nowhere left to see that work. Close or archive
        the deals first.

        The pipeline stays readable at its own id, and its stages and closed
        deals stay where they are. There is no unarchive in this release.
      operationId: archivePipeline
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchivePipelineRequest'
      responses:
        '200':
          description: The archived pipeline.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PipelineEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PipelineArchiveConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/board:
    get:
      tags:
        - Projects
      summary: Read a pipeline's board
      description: |
        Returns one pipeline's whole board: its live columns in order, how many
        live cards each holds, and the first cards of each. Requires viewer
        access to the pipeline's root.

        The board is drawn inside one read-only snapshot, so the counts, the
        columns and the cards are one picture rather than several: a card
        caught mid-move can never appear in two columns at once.

        Each column carries at most 25 cards. A column with more carries a
        `next_cursor`, which
        `GET /pipelines/{id}/stages/{stage_id}/deals` resumes from. That route
        draws its pages with the same statement this one uses, so the cards on
        the board and the cards paged in after them cannot differ.

        Every card carries `node`: the person or company the deal is about,
        rendered for the calling user, with their own contact record and their
        stored paths to it. A card whose target no longer resolves carries
        `node: null` and stays on the board. A transient failure to render the
        entities costs the cards their `node`, never the board.

        `pipeline.board_version` is what a column cursor is bound to, and
        `pipeline.configuration_version` is what a move asserts.
      operationId: getPipelineBoard
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
      responses:
        '200':
          description: The board.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BoardEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /pipelines/{id}/stages/{stage_id}/deals:
    get:
      tags:
        - Projects
      summary: Page one column of a board
      description: |
        Returns one column's live cards in board order, keyset paged. Requires
        viewer access to the pipeline's root. It is what a board's column
        scrolls with; the first page is the one the board already carries.

        The cursor is bound to two things. It names the column it was drawn
        from, and it names the `board_version` it was drawn under. A cursor
        replayed after any card in the pipeline moved, arrived or left is
        refused with 400 `cursor: board_moved`: a cursor is a position in an
        order, and resuming one in an order that has changed underneath would
        show a card twice or skip one. Reopen the column; the first page needs
        no cursor.

        Cards carry `node` exactly as the board's do.
      operationId: listStageDeals
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PipelineId'
        - $ref: '#/components/parameters/StageId'
        - $ref: '#/components/parameters/ColumnLimit'
        - $ref: '#/components/parameters/ColumnCursor'
      responses:
        '200':
          description: One page of the column.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ColumnPageEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
  /me/todos:
    get:
      tags:
        - Projects
      summary: List the signed-in user's ToDo
      description: |
        Returns the caller's project-free tasks: the rows with no project
        whose `personal_user_id` is the caller. No parameter names an owner,
        and organization, team or root-admin standing confers nothing here.

        `filter` selects one section, and `total` counts that section alone.
        `suggested` is the unaccepted proposals: a section of its own, never
        part of the accepted workload `active` counts. Archived rows are left
        out unless `include_archived` asks for them.

        Paging is keyset rather than offset, so a row written while a client
        pages does not shift the pages it has still to read. `next_cursor` is
        empty on the last page. A cursor is bound to the caller, the filters
        and the sort that produced it; replayed under any other combination it
        is 400 with `details: [{field: "cursor", issue: "filter_mismatch"}]`
        rather than a page that silently skips or repeats rows.
      operationId: listTodos
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskFilter'
        - $ref: '#/components/parameters/TaskSort'
        - $ref: '#/components/parameters/TaskSearch'
        - $ref: '#/components/parameters/TaskIncludeArchived'
        - $ref: '#/components/parameters/TaskLimit'
        - $ref: '#/components/parameters/TaskCursor'
      responses:
        '200':
          description: One page of the caller's ToDo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Projects
      summary: Capture a personal ToDo
      description: |
        Creates one project-free task in the signed-in user's own ToDo and
        assigns it to them, in one transaction.

        The owner is taken from the session. The body cannot name an owner, a
        project, a deal or a work key: those keys are not part of the request
        and an unknown key is rejected with 400, so a client that sends
        `project_id` is refused rather than quietly given a private ToDo.
        Moving a ToDo into a project is `PUT /tasks/{id}/project`.

        The result has `project_id` and `root_project_id` null,
        `personal_user_id` equal to the caller, `status: "todo"` and a set
        `accepted_at`: a direct human create is committed work, not an AI
        proposal. A `target_date` later than `due_date` is accepted and
        reported in `warnings`.

        `Idempotency-Key` is required. A retry of the same key with the same
        body returns the first execution's task with 200 instead of 201, so a
        client that lost the first response cannot capture the task twice; the
        same key with a different body is 409 `idempotency_conflict`.
      operationId: createTodo
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskDraftRequest'
      responses:
        '200':
          description: |
            The ToDo an earlier request with this `Idempotency-Key` already
            captured. Nothing was written.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '201':
          description: The captured ToDo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: |
            The command did not run. `error.details` carries one entry with
            `field: "reason"` and one of these `issue` values:

            - `idempotency_conflict`: this `Idempotency-Key` was already used
              by this caller for this operation with a different body.
            - `scope_busy`: the caller's ToDo stayed locked by other commands
              past the lock timeout. Safe to retry with the same key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks:
    get:
      tags:
        - Projects
      summary: List My Tasks
      description: |
        Returns the caller's workload across both scopes in one page: their
        own project-free tasks, and the tasks they are assigned to in the
        projects they can reach. Nothing else is in it, so another user's
        private ToDo never appears however the caller is related to them, and
        an assignment inside a project they cannot read is left out.

        `assigned_to` has one value, `me`, and defaults to it. The caller
        comes from the session, so no parameter can ask for another user's
        work; any other value is 400 `unknown_value`.

        `filter` selects one section, and `total` counts that section alone.
        `suggested` is the unaccepted proposals: it is returned only when it
        is asked for by name, so a proposal is never counted as committed
        workload. Archived rows are left out unless `include_archived` asks
        for them.

        Paging is keyset rather than offset, so a task written while a client
        pages does not shift the pages it has still to read. `next_cursor` is
        empty on the last page. A cursor is bound to the caller, the filters
        and the sort that produced it, and to this list rather than to
        `GET /me/todos`; replayed under any other combination it is 400 with
        `details: [{field: "cursor", issue: "filter_mismatch"}]`.
      operationId: listMyTasks
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskAssignedTo'
        - $ref: '#/components/parameters/TaskFilter'
        - $ref: '#/components/parameters/TaskSort'
        - $ref: '#/components/parameters/TaskSearch'
        - $ref: '#/components/parameters/TaskIncludeArchived'
        - $ref: '#/components/parameters/TaskLimit'
        - $ref: '#/components/parameters/TaskCursor'
      responses:
        '200':
          description: One page of the caller's own and assigned work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    post:
      tags:
        - Projects
      summary: Create a task
      description: |
        The unified create. `project_id` decides which command runs: absent or
        `null` captures a personal ToDo in the caller's own list, exactly as
        `POST /me/todos` does, and a value creates a task in that project,
        exactly as `POST /projects/{id}/tasks` does, under editor access to
        its root.

        An invalid `project_id` is an error, never a fall back to a private
        ToDo. A malformed id is 400 `invalid_uuid`, a project that does not
        exist or that the caller cannot read is 404, and one they may only
        read is 403.

        `deal_id` and `work_key` are not accepted on this route yet; an
        unknown key is rejected with 400 rather than ignored.

        `Idempotency-Key` is required. The two destinations are separate
        operations with separate receipts, so a key replays only against the
        one its first run wrote in; the same key with a different body is 409
        `idempotency_conflict`.
      operationId: createTask
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '200':
          description: |
            The task an earlier request with this `Idempotency-Key` already
            created. Nothing was written.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '201':
          description: The created task.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/TaskCreateConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}:
    get:
      tags:
        - Projects
      summary: Get one task
      description: |
        Returns the task if the caller may read it. A project task is
        readable by an admin, editor or viewer of its root; a personal task
        only by its owner.

        Returns 404 (not 403) when the task is unknown, not visible, or the
        capability is off, so existence is never leaked. 403 `account is not
        active` is reached only by a caller who does have a route to the task:
        an inactive account with no route gets the same 404 as a uuid that
        names nothing.
      operationId: getTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The task.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/Internal'
    patch:
      tags:
        - Projects
      summary: Update a task
      description: |
        Edits the caller-editable content of one task. Requires editor
        access to the task's root, or ownership of a personal task.

        Patch semantics: an omitted key preserves the stored value, a key
        present with `null` clears it. Unknown fields are rejected with 400
        rather than ignored, so a typo cannot silently drop an edit; the
        body must name at least one patchable field, and one carrying only
        `expected_version` is 400 `VALIDATION_FAILED`. `expected_version` is
        required and must equal the task's current `version`; a mismatch is
        409 `version_conflict` and writes nothing. A patch whose values all
        equal the stored ones is a 200 that writes nothing and leaves
        `version` where it was.

        `status` is not writable here. Acceptance, completion, archiving and
        placement are their own commands, so a PATCH cannot bypass their
        rules.

        An unaccepted task (`accepted_at: null`, the Suggested state) accepts
        only `title`, `target_date`, `due_date` and `estimate`. Patching its
        `description`, `acceptance_criteria`, `priority` or `next_action` is
        409 `invalid_state`, as is any patch of an archived task or of a task
        in an archived root.

        Changing an estimate never recalculates dates, completes the task or
        creates a calendar event. A `target_date` later than `due_date` is
        accepted and reported in the response `warnings`.

        `Idempotency-Key` is optional. When sent, a retry of the same key
        with the same body returns the first execution's result without
        re-applying it; the same key with a different body is 409
        `idempotency_conflict`.
      operationId: updateTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          description: |
            Client-generated retry key, 1 to 128 characters of
            `A-Z a-z 0-9 . _ ~ -`. Scoped to the caller and this operation.
          schema:
            type: string
            minLength: 1
            maxLength: 128
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaskRequest'
      responses:
        '200':
          description: |
            The updated task. A replay of an earlier idempotency key returns
            200 with that execution's result, not 201.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: |
            The command did not run. `error.details` carries one entry with
            `field: "reason"` and one of these `issue` values:

            - `version_conflict`: `expected_version` is not the task's
              current version. Refetch and reapply; nothing was written.
            - `idempotency_conflict`: this `Idempotency-Key` was already used
              by this caller for this operation with a different body.
            - `invalid_state`: the task is archived, or it is an unaccepted
              proposal and the body touches a field only accepted work has.
              Accept or restore it first.
            - `root_archived`: the task's project root is archived. Reopen
              the root before editing work inside it.
            - `scope_busy`: the task's root or the owner's ToDo stayed locked
              by other commands past the lock timeout. Safe to retry.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Projects
      summary: Delete a Suggested task
      description: |
        Deletes one **personal** Suggested task the caller owns. It is not a
        hard delete and it is not the archive command: it does three things in
        one transaction, and a client cannot do them separately without
        leaving one of them undone.

        The task is archived, so the row, its history and its provenance all
        stay. The proposal behind it is withdrawn with the reason
        `user_deleted`, so nobody else's screen keeps offering it. And the
        idea is recorded as dismissed **by its stable intent**, which is what
        stops the same idea coming back tomorrow worded differently.

        The expected version travels in `If-Match`, holding the task's
        `version` (quoted or not). It is required: `*` and a missing header
        are both refused, because a delete that raced an edit would otherwise
        throw away work the caller never saw.

        Only a personal proposal is deletable here. A shared project proposal
        is withdrawn for everybody through
        `POST /project-suggestions/{id}/withdraw`, or dismissed for the caller
        alone through its `my-state` route; representing a personal dismissal
        as a shared deletion is exactly what this route does not do. Accepted
        work uses `POST /tasks/{id}/archive`.

        `POST /tasks/{id}/restore-suggestion` undoes it.
      operationId: deleteSuggestedTask
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/IfMatchVersion'
        - $ref: '#/components/parameters/DeleteReason'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      responses:
        '200':
          description: The deleted task, archived and no longer offered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SuggestedTaskConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/accept:
    post:
      tags:
        - Projects
      summary: Accept a Suggested task
      description: |
        Turns an agent's proposal into committed work. The owner of a personal
        proposal, or an editor of its project, can accept.

        It works on **the same row**. The id does not change, so the task a
        person has been reading and editing is the task they accept, and what
        is accepted is its current fields rather than the payload the model
        first wrote. Editing a proposal is not accepting it, and there is no
        PATCH of `status` that does this instead.

        The task's status becomes `todo` and its acceptance actor and time are
        stamped; the proposal is marked adopted with
        `adoption_type: accepted_task` and the same task id, in one
        transaction. No action run and no external side effect is implied.

        Accepting twice is 409 `invalid_state`, and so is accepting through a
        stale row whose proposal has since been withdrawn, superseded or
        deleted. Normal assignment, placement and reminder policy apply
        afterwards; a proposed due date already in the past is disclosed on
        the accepted task rather than silently dropped.
      operationId: acceptTask
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExpectedVersionRequest'
      responses:
        '200':
          description: The accepted task, now ordinary work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SuggestedTaskConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/restore-suggestion:
    post:
      tags:
        - Projects
      summary: Restore a deleted Suggested task
      description: |
        Undoes a delete. The task comes back as Suggested, the proposal comes
        back to open, and the caller's recorded dismissal of the idea is
        cleared, all in one transaction.

        It restores the **proposal**, never an acceptance: a restored idea is
        one to consider again, not work anybody agreed to. Accepted work is
        reopened with `POST /tasks/{id}/reopen` instead.

        A proposal somebody adopted cannot come back, and neither can one
        whose `expires_at` has passed: 409 `invalid_state` and 409
        `suggestion_expired` say which. Those are actionable answers rather
        than a silent no-op, because the client's Undo button has to say
        something.
      operationId: restoreSuggestion
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExpectedVersionRequest'
      responses:
        '200':
          description: The task, Suggested again.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SuggestedTaskConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/complete:
    post:
      tags:
        - Projects
      summary: Complete a task
      description: |
        Marks one task delivered. Requires editor access to the task's root,
        or ownership of a personal task.

        Sets `status: "done"`, stamps `completed_at` and stores the optional
        `completion_summary`. It also moves `last_progress_at`: delivery is
        the advancement that column records. Acceptance is left alone, so a
        completed task is still accepted work.

        Completing cancels the task's undispatched action runs, agent runs
        and follow-ups, so nothing still fires for finished work. Runs
        already dispatched and notifications already being delivered are left
        as they are: their outcome has to be reconciled, not overwritten.

        Refused with 409 `prerequisites_open` while any prerequisite of the
        task is neither complete nor archived.
      operationId: completeTask
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteTaskRequest'
      responses:
        '200':
          description: The completed task.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WorkConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/reopen:
    post:
      tags:
        - Projects
      summary: Reopen a completed task
      description: |
        Puts a completed task back into the work. Requires editor access to
        the task's root, or ownership of a personal task.

        Clears `completed_at` and `completion_summary` and sets
        `status: "todo"`, whatever the status was before completion: where
        the work restarts is the next progress update's decision. Acceptance,
        `started_at`, the history and `last_progress_at` are all preserved,
        because reopening is a decision rather than evidence of advancement.

        A task that is not complete is 409 `invalid_state`.
      operationId: reopenTask
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskNoteRequest'
      responses:
        '200':
          description: The reopened task.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WorkConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/archive:
    post:
      tags:
        - Projects
      summary: Archive a task
      description: |
        Puts one task aside. Requires editor access to the task's root, or
        ownership of a personal task.

        Recoverable, not a delete: the row, its status, its completion and
        its history all stay, and only `archived_at` is stamped. Default
        listings leave archived work out. `last_progress_at` does not move,
        since putting work aside is not progress.

        Archiving cancels the task's undispatched action runs, agent runs and
        follow-ups, on the same terms as completion. An already archived task
        is 409 `invalid_state`, and so is an unaccepted proposal: a
        suggestion is deleted rather than archived, which also withdraws it.
      operationId: archiveTask
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskNoteRequest'
      responses:
        '200':
          description: The archived task.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WorkConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/progress:
    post:
      tags:
        - Projects
      summary: Record progress on a task
      description: |
        The explicit progress update, and the only route whose purpose is to
        move `last_progress_at`. Requires editor access to the task's root,
        or ownership of a personal task.

        A task still in `todo` becomes `in_progress` and gets `started_at`
        stamped, so the first report of work is also its start. A task
        already `in_progress`, `blocked` or `in_review` keeps the status its
        own command put it in; this route never sets `done`.

        `next_action` is the one field where an omitted key and `null`
        differ: omitting it keeps the stored next step, `null` clears it.
        `note` goes into the history row as written.

        A completed task is 409 `invalid_state`: reopen it first.
      operationId: recordTaskProgress
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordProgressRequest'
      responses:
        '200':
          description: The task, with its progress time moved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WorkConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/project:
    put:
      tags:
        - Projects
      summary: Move a task into a project, or into someone's ToDo
      description: |
        Moves one task between a person's ToDo and a project, keeping its id,
        content, dates and estimate. It is the only supported change of a
        task's audience: clearing the assignees does not make a project task
        private, and neither does a patch.

        `project_id` has to be present, `null` included. A project id moves the
        task there and needs the personal owner of a ToDo, or editor access to
        the task as it stands, plus editor access to the destination. `null`
        makes the task private: that needs admin on the task's root and a
        `recipient_user_id` who is currently an editor of it, and it leaves
        that recipient holding the only route to the work.

        `assignees` is the destination set, each an eligible editor of the
        destination root; one that is not is 400 with `field:
        "assignees.<user_id>"` and `issue: "not_editor"`. Omitting it keeps
        whoever is on the task and is still eligible, which for a ToDo is its
        owner, so the current user is kept by default and naming anyone else is
        explicit. It is rejected together with `project_id: null`, whose one
        assignee is the recipient.

        The move is refused while the task is linked to a deal or holds a work
        key, while any prerequisite link joins it to other work, and while an
        action on it is running or its result is unknown. Resolve those first.
        Queued actions, queued background reviews and pending follow-ups are
        cancelled with the move.

        History is never rewritten. Rows already written keep the audience they
        were written for, so private history is not republished into the
        project, and the placement is recorded once, in the destination,
        carrying only which audience the task came from and went to. The
        response says where the task now stands; its contents are fetched
        separately, under whatever access it now sits behind.

        Moving a task into the project it is already in writes nothing and
        leaves `version` where it was.
      operationId: placeTask
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaceTaskRequest'
      responses:
        '200':
          description: Where the task stands after the move.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskPlacementEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PlacementConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/assignees:
    put:
      tags:
        - Projects
      summary: Replace a task's assignees
      description: |
        Stores `assignees` as the task's whole assignee set. It is a
        replacement, not a patch: a user left out is unassigned, and `[]`
        clears the set. Omitting the key is 400, so a body that forgot it
        cannot quietly unassign everyone. Requires editor access to the
        task's root, or ownership of a personal task.

        Every named user must already be an eligible editor of that root: an
        active account with an editor or admin route to it. One that is not
        is 400, with one `details` entry per rejected user, `field:
        "assignees.<user_id>"` and `issue: "not_editor"`. A user id that
        names no account is reported the same way.

        Assignment grants nothing. It does not make the task readable to the
        people named, whose access still comes from the root ACL alone, and
        it is not attribution: who did what is in the history rows. A
        personal ToDo keeps exactly one assignee, its owner; any other set is
        400 with `issue: "personal_owner_only"`.

        The list is sorted and deduplicated before it is stored, so a request
        naming the same people in another order is the same request and
        replays under one `Idempotency-Key`. Asking for the set the task
        already has writes nothing and leaves `version` where it was.
        `last_progress_at` never moves: assignment is not advancement.
      operationId: setTaskAssignees
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetTaskAssigneesRequest'
      responses:
        '200':
          description: The task's resulting assignee set.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskAssigneesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/TaskLinkConflict'
        '500':
          $ref: '#/components/responses/Internal'
  /tasks/{id}/dependencies/{prerequisite_id}:
    put:
      tags:
        - Projects
      summary: Make a task wait on another
      description: |
        Records a finish-to-start prerequisite: the task in `id` cannot be
        completed until the task in `prerequisite_id` is complete or
        archived. Requires editor access to both, which for tasks in one root
        is the same check.

        Both ends must be accepted, non-archived work, and must share one
        project root or one personal owner. A link between a project task and
        a ToDo, or between two people's ToDos, is 400 with `field:
        "prerequisite_task_id"`, `issue: "different_scope"`. A task named as
        its own prerequisite is 400 with `issue: "same_task"`.

        A link that would let the two tasks wait on each other, directly or
        through any number of tasks in between, is 409 `dependency_cycle`.
        Recording a prerequisite that is already there writes nothing and
        leaves `version` where it was. `last_progress_at` never moves.
      operationId: addTaskDependency
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/PrerequisiteTaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskDependencyRequest'
      responses:
        '200':
          description: The task's resulting prerequisites.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskDependenciesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/TaskLinkConflict'
        '500':
          $ref: '#/components/responses/Internal'
    delete:
      tags:
        - Projects
      summary: Stop a task waiting on another
      description: |
        Removes the finish-to-start prerequisite. Requires editor access to
        the waiting task's root, or ownership of a personal task; both ends
        of a stored link share one scope, so that is the whole check.

        A prerequisite that is not recorded, or that belongs to another task,
        removes nothing: the response is the task's current prerequisites,
        `version` is unchanged, and no history row is written. A retry after
        a removal that did land therefore answers the same whether or not an
        `Idempotency-Key` was sent.
      operationId: removeTaskDependency
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/PrerequisiteTaskId'
        - $ref: '#/components/parameters/OptionalIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskDependencyRequest'
      responses:
        '200':
          description: The task's remaining prerequisites.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskDependenciesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/TaskLinkConflict'
        '500':
          $ref: '#/components/responses/Internal'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: WorkOS access token returned by /sign-in or /verify-email.
    adminToken:
      type: apiKey
      in: header
      name: X-Admin-Token
      description: |
        Operator shared secret (ADMIN_API_TOKEN) for the admin routes. These
        rewrite identity plumbing for an arbitrary user id, so they are not
        reachable with an end-user bearer token. Unset in an environment →
        the routes return 401.
  schemas:
    Meta:
      type: object
      properties:
        requestId:
          type: string
        page:
          type: integer
          format: int32
        pageSize:
          type: integer
          format: int32
        total:
          type: integer
          format: int64
    ErrorDetail:
      type: object
      required:
        - field
        - issue
      properties:
        field:
          type: string
        issue:
          type: string
    ApiError:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          description: Stable machine code — NOT_FOUND / VALIDATION_FAILED / CONFLICT / UNAUTHORIZED / FORBIDDEN / INTERNAL.
        message:
          type: string
        details:
          type: array
          items:
            $ref: '#/components/schemas/ErrorDetail'
        debug:
          type:
            - string
            - 'null'
          description: Wrapped technical cause; present only in debug mode.
    ErrorEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      description: Error response — data is always null.
      properties:
        data:
          type: 'null'
        error:
          $ref: '#/components/schemas/ApiError'
        meta:
          $ref: '#/components/schemas/Meta'
    EmptyEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      description: Success with no payload (e.g. deletes) — data and error are null.
      properties:
        data:
          type: 'null'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CompanyRef:
      type: object
      properties:
        id:
          type: string
          format: uuid
        company_name:
          type: string
        logo:
          type:
            - string
            - 'null'
    LocationRef:
      type: object
      description: |
        A person's place as plain text on their row: the display string plus
        coordinates. The object is null when the row holds no place.
      properties:
        formatted_location:
          type:
            - string
            - 'null'
        latitude:
          type:
            - number
            - 'null'
        longitude:
          type:
            - number
            - 'null'
    ConnectionRequest:
      type: object
      description: |
        A connection request. Where this is embedded on a person, only live and
        blocked requests are returned: a plain decline (or a clear) drops the
        object to null so the client can offer connect again. `from_blocked`
        true means the request is dead for good — hide the connect action.
      properties:
        id:
          type: string
          format: uuid
        user_from_id:
          type:
            - string
            - 'null'
          format: uuid
        user_to_id:
          type:
            - string
            - 'null'
          format: uuid
        request_message:
          type:
            - string
            - 'null'
        declined:
          type: boolean
        from_blocked:
          type: boolean
        connected_at:
          type:
            - string
            - 'null'
          format: date-time
        accept_message:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    Connection:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        user_connected_to_id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
    EmailActivity:
      type: object
      properties:
        id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        user_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        subject:
          type:
            - string
            - 'null'
        grant_id:
          type: string
        thread_id:
          type:
            - string
            - 'null'
        message_id:
          type:
            - string
            - 'null'
        snippet:
          type:
            - string
            - 'null'
        unread:
          type: boolean
        clean_message:
          type:
            - string
            - 'null'
    CalendarEventActivity:
      type: object
      properties:
        id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        user_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        subject:
          type:
            - string
            - 'null'
        activity_timestamp:
          type:
            - string
            - 'null'
          format: date-time
        grant_id:
          type: string
        event_id:
          type: string
          format: uuid
        status:
          type: string
    DisplayProperties:
      type: object
      additionalProperties:
        type:
          - string
          - number
          - boolean
          - 'null'
    GraphNode:
      type: object
      required:
        - id
        - labels
        - properties
      properties:
        id:
          type: string
          format: uuid
        labels:
          type: array
          items:
            type: string
        properties:
          $ref: '#/components/schemas/DisplayProperties'
    GraphEdge:
      type: object
      required:
        - id
        - type
        - sourceId
        - targetId
        - properties
      properties:
        id:
          type: string
        type:
          type: string
        sourceId:
          type: string
          format: uuid
        targetId:
          type: string
          format: uuid
        properties:
          $ref: '#/components/schemas/DisplayProperties'
    Graph:
      type: object
      required:
        - rootUuid
        - nodes
        - edges
        - truncated
      properties:
        rootUuid:
          type: string
          format: uuid
        truncated:
          type: boolean
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/GraphNode'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/GraphEdge'
    GraphEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Graph'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SignUpRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          maxLength: 320
        password:
          type: string
          minLength: 8
          maxLength: 128
        first_name:
          type:
            - string
            - 'null'
          maxLength: 128
        last_name:
          type:
            - string
            - 'null'
          maxLength: 128
        timezone:
          type:
            - string
            - 'null'
          maxLength: 64
          description: IANA timezone, e.g. "America/New_York".
        invitation_token:
          type:
            - string
            - 'null'
          maxLength: 512
          description: |
            Token from an org-invitation email's accept link. Sending an
            invitation makes WorkOS pre-create the invitee's user (so a
            plain sign-up would fail on the duplicate email); with this
            token, sign-up claims that pre-created user instead — setting
            its password and marking the email verified — and the
            invitation is accepted during authentication, activating the
            pending org membership. 403 when the token was issued to a
            different email; 409 when the invitation is no longer pending
            or the account has already signed in (sign in instead).
    OnboardingStatus:
      type: string
      description: Server-owned account-verification gate state. "ready" for every grandfathered or fully verified user; any other value routes the frontend to the blocking verification surface. New users always start at "account_verification_required".
      enum:
        - ready
        - account_verification_required
        - oauth_in_progress
        - support_review_required
        - verified_pending_person
        - person_setup_failed
    UserResponse:
      type: object
      required:
        - id
        - active_account
        - copilot_enabled
        - onboarding_status
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        username:
          type:
            - string
            - 'null'
        workos_id:
          type:
            - string
            - 'null'
        graph_id:
          type:
            - string
            - 'null'
          description: FalkorDB per-user graph name, "user:<local user id>".
        primary_timezone:
          type:
            - string
            - 'null'
        active_account:
          type: boolean
        copilot_enabled:
          type: boolean
        onboarding_status:
          $ref: '#/components/schemas/OnboardingStatus'
        last_activity:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AuthTokensResponse:
      type: object
      required:
        - user
        - access_token
        - refresh_token
      properties:
        user:
          $ref: '#/components/schemas/UserResponse'
        access_token:
          type: string
          description: 'Send as Authorization: Bearer on subsequent requests.'
        refresh_token:
          type: string
    OrganizationOptionResponse:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string
    OrganizationSelectionRequiredResponse:
      type: object
      required:
        - error
        - message
        - pending_authentication_token
        - organizations
      properties:
        error:
          type: string
          example: organization_selection_required
        message:
          type: string
        pending_authentication_token:
          type: string
        organizations:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationOptionResponse'
    SignUpResponse:
      type: object
      required:
        - user
        - email_verification_required
        - pending_authentication_token
        - email_verification_id
        - tokens
        - organization_selection
      properties:
        user:
          $ref: '#/components/schemas/UserResponse'
        email_verification_required:
          type: boolean
          description: When true, post pending_authentication_token + the emailed code to /verify-email.
        pending_authentication_token:
          type: string
          description: Token for /verify-email. Empty when verification is not required.
        email_verification_id:
          type: string
        tokens:
          oneOf:
            - $ref: '#/components/schemas/AuthTokensResponse'
            - type: 'null'
          description: Set (and the user logged in) only when verification was not required; null otherwise.
        organization_selection:
          oneOf:
            - $ref: '#/components/schemas/OrganizationSelectionRequiredResponse'
            - type: 'null'
          description: |
            Set instead of tokens when the account belongs to several
            organizations — an invited sign-up always does (personal +
            invited). Finish by posting its pending_authentication_token
            and the chosen organization id to /sign-in/select-organization.
    SignUpResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SignUpResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SignInRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          maxLength: 320
        password:
          type: string
          maxLength: 128
        timezone:
          type:
            - string
            - 'null'
          maxLength: 64
        invitation_token:
          type:
            - string
            - 'null'
          maxLength: 512
          description: |
            Token from an org-invitation email's accept link. WorkOS
            accepts the invitation during authentication, activating the
            pending org membership — the path for an invitee who already
            has an account.
    AuthTokensResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/AuthTokensResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    EmailVerificationRequiredResponse:
      type: object
      required:
        - error
        - message
        - pending_authentication_token
      properties:
        error:
          type: string
          example: email_verification_required
        message:
          type: string
        pending_authentication_token:
          type: string
        email_verification_id:
          type: string
    EmailVerificationRequiredResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EmailVerificationRequiredResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    OrganizationSelectionRequiredResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/OrganizationSelectionRequiredResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RefreshRequest:
      type: object
      required:
        - refresh_token
      properties:
        refresh_token:
          type: string
        timezone:
          type:
            - string
            - 'null'
          maxLength: 64
    SelectOrganizationRequest:
      type: object
      required:
        - pending_authentication_token
        - organization_id
      properties:
        pending_authentication_token:
          type: string
        organization_id:
          type: string
        timezone:
          type:
            - string
            - 'null'
          maxLength: 64
    VerifyEmailRequest:
      type: object
      required:
        - code
        - pending_authentication_token
      properties:
        code:
          type: string
          minLength: 4
          maxLength: 12
        pending_authentication_token:
          type: string
    SendVerificationEmailRequest:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email
          maxLength: 320
    ForgotPasswordRequest:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email
          maxLength: 320
    ResetPasswordRequest:
      type: object
      required:
        - token
        - new_password
        - confirm_password
      properties:
        token:
          type: string
          description: The ?token= value from the reset email link.
        new_password:
          type: string
          minLength: 8
          maxLength: 128
        confirm_password:
          type: string
          description: Must match new_password.
          minLength: 8
          maxLength: 128
    OAuthStartResponse:
      type: object
      required:
        - url
      properties:
        url:
          type: string
          format: uri
    OAuthStartResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/OAuthStartResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    StartVerificationRequest:
      type: object
      required:
        - return_to
      properties:
        return_to:
          type: string
          maxLength: 256
          description: Allowlisted same-origin frontend path to land on after the provider round-trip.
    StartVerificationResponse:
      type: object
      required:
        - authorization_url
        - expires_at
      properties:
        authorization_url:
          type: string
        expires_at:
          type: string
          format: date-time
    StartVerificationEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/StartVerificationResponse'
        error:
          type: 'null'
        meta:
          type: object
    StatusResponse:
      type: object
      required:
        - status
        - provider
        - safe_reason
        - retryable
      properties:
        status:
          $ref: '#/components/schemas/OnboardingStatus'
        provider:
          type:
            - string
            - 'null'
          enum:
            - github
            - linkedin
            - x
            - null
          description: Provider of the latest verification attempt, if any.
        safe_reason:
          type:
            - string
            - 'null'
          description: Safe failure code of the latest attempt (name_mismatch, insufficient_provider_data, subject_conflict). Never a raw provider error.
        retryable:
          type: boolean
    StatusEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/StatusResponse'
        error:
          type: 'null'
        meta:
          type: object
    FinalizeResponse:
      type: object
      required:
        - status
      properties:
        status:
          $ref: '#/components/schemas/OnboardingStatus'
    FinalizeEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/FinalizeResponse'
        error:
          type: 'null'
        meta:
          type: object
    ReviewDecisionRequest:
      type: object
      required:
        - decision
        - reason
      properties:
        decision:
          type: string
          enum:
            - approved
            - rejected
        reason:
          type: string
          maxLength: 2000
    ReviewDecisionResponse:
      type: object
      required:
        - review_id
        - status
      properties:
        review_id:
          type: string
          format: uuid
        status:
          type: string
          enum:
            - approved
            - rejected
    ReviewDecisionEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/ReviewDecisionResponse'
        error:
          type: 'null'
        meta:
          type: object
    ListUsersResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/UserResponse'
    ListUsersResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListUsersResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UserSearchPerson:
      type: object
      required:
        - id
        - name
        - avatar
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        company_name:
          type:
            - string
            - 'null'
    UserSearchResponse:
      type: object
      required:
        - id
        - person_id
        - created_at
        - updated_at
        - person
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        username:
          type:
            - string
            - 'null'
        workos_id:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        person:
          $ref: '#/components/schemas/UserSearchPerson'
    SearchUsersResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/UserSearchResponse'
    SearchUsersResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SearchUsersResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MePersonResponse:
      type: object
      required:
        - id
        - full_name
        - first_name
        - last_name
        - avatar
        - current_title
        - bio
      properties:
        id:
          type: string
          format: uuid
        full_name:
          type: string
        first_name:
          type:
            - string
            - 'null'
        last_name:
          type:
            - string
            - 'null'
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
    MeResponse:
      type: object
      required:
        - id
        - person_id
        - workos_id
        - email
        - tiptap_collab_jwt
        - tiptap_ai_jwt
        - knock_jwt
        - primary_timezone
        - copilot_enabled
        - onboarding_status
        - last_signed_in_at
        - person
        - persons_seeded
        - companies_seeded
        - created_at
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        workos_id:
          type:
            - string
            - 'null'
        email:
          type:
            - string
            - 'null'
          description: WorkOS account email from the verified access token's JWT-template claim. Null when the token predates the claim.
        tiptap_collab_jwt:
          type:
            - string
            - 'null'
        tiptap_ai_jwt:
          type:
            - string
            - 'null'
        knock_jwt:
          type:
            - string
            - 'null'
        primary_timezone:
          type:
            - string
            - 'null'
        copilot_enabled:
          type: boolean
        onboarding_status:
          $ref: '#/components/schemas/OnboardingStatus'
        last_signed_in_at:
          type:
            - string
            - 'null'
          format: date-time
          description: When the user last completed a sign-in (password, org selection, or email verification). Null until their first sign-in; not bumped by token refresh.
        person:
          oneOf:
            - $ref: '#/components/schemas/MePersonResponse'
            - type: 'null'
          description: The user's own person; null when unset or unwired.
        persons_seeded:
          type: integer
          format: int64
          description: How many people exist in Orbiter because this user caused them to be enriched. Counts records they created, not records they hold as contacts; bulk-imported records have no creator and never count.
        companies_seeded:
          type: integer
          format: int64
          description: How many companies exist in Orbiter because this user caused them to be enriched. Same rule as persons_seeded.
        created_at:
          type: string
          format: date-time
    MeResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/MeResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AuthCapabilitiesResponse:
      type: object
      required:
        - has_password
        - oauth_providers
      properties:
        has_password:
          type: boolean
          description: |
            Whether the account has a WorkOS password. Tracked locally;
            false can mean "not observed yet" for an account that predates
            the field (self-corrects on the next password sign-in).
        oauth_providers:
          type: array
          description: |
            Linked social providers, as the short tokens accepted by
            /v1/oauth/{provider}/start (e.g. "google"). Empty array when
            none are linked.
          items:
            type: string
    AuthCapabilitiesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/AuthCapabilitiesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SessionResponse:
      type: object
      required:
        - id
        - status
        - auth_method
        - ip_address
        - user_agent
        - is_current
        - created_at
        - expires_at
      properties:
        id:
          type: string
          description: WorkOS session id ("session_...").
        status:
          type: string
          description: |
            WorkOS session status. Listing returns only "active" sessions.
        auth_method:
          type: string
          description: |
            How the session was created, as WorkOS reports it (e.g. "password", "oauth", "magic_auth").
        ip_address:
          type:
            - string
            - 'null'
          description: IP address the session was created from.
        user_agent:
          type:
            - string
            - 'null'
          description: User agent of the device that created the session.
        is_current:
          type: boolean
          description: True for the session backing this request ("this device").
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
    SessionListResponse:
      type: object
      required:
        - sessions
      properties:
        sessions:
          type: array
          description: Active sessions, newest first. Empty array when none.
          items:
            $ref: '#/components/schemas/SessionResponse'
    SessionListResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SessionListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ChangePasswordRequest:
      type: object
      required:
        - current_password
        - new_password
        - confirm_password
      properties:
        current_password:
          type: string
          description: The caller's existing password. Proves it's really them.
          maxLength: 128
        new_password:
          type: string
          minLength: 8
          maxLength: 128
        confirm_password:
          type: string
          description: Must match new_password.
          minLength: 8
          maxLength: 128
    UserResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/UserResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    JWTsResponse:
      type: object
      required:
        - user_id
      properties:
        user_id:
          type: string
          format: uuid
        workos_id:
          type:
            - string
            - 'null'
        tiptap_collab_jwt:
          type:
            - string
            - 'null'
        tiptap_ai_jwt:
          type:
            - string
            - 'null'
        knock_jwt:
          type:
            - string
            - 'null'
    JWTsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/JWTsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ProvisioningEventResponse:
      type: object
      required:
        - id
        - user_id
        - correlation_id
        - trigger
        - task
        - status
        - attempts
        - payload
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        correlation_id:
          type: string
          format: uuid
        trigger:
          type: string
          description: What caused the attempt (e.g. sign_up).
        task:
          type: string
          description: Provisioning task name (knock_identify, personal_org, user_graph).
        status:
          type: string
          enum:
            - pending
            - in_progress
            - succeeded
            - failed
            - dead_lettered
        attempts:
          type: integer
          format: int32
        last_error:
          type:
            - string
            - 'null'
        payload:
          type: string
          description: Raw JSON message body, as-stored.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ProvisioningEventResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ProvisioningEventResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SetExternalIDRequest:
      type: object
      required:
        - workos_user_id
        - user_id
      properties:
        workos_user_id:
          type: string
        user_id:
          type: string
          format: uuid
    AdminProjectionSyncRequest:
      type: object
      required:
        - person_ids
      properties:
        person_ids:
          type: array
          minItems: 1
          maxItems: 500
          items:
            type: string
            format: uuid
    AdminProjectionSync:
      type: object
      required:
        - requested
        - unknown
      properties:
        requested:
          type: integer
          description: Syncs queued.
        unknown:
          type: array
          description: Ids this backend does not hold; nothing was queued for them.
          items:
            type: string
            format: uuid
    AdminProjectionSyncEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/AdminProjectionSync'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UserWebhookResponse:
      type: object
      required:
        - event
        - action
        - user_id
      properties:
        event:
          type: string
        action:
          type: string
          enum:
            - provisioned
            - existing
            - ignored
        user_id:
          type:
            - string
            - 'null'
          format: uuid
    UserWebhookResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/UserWebhookResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ConnectionRequest-2:
      type: object
      required:
        - id
        - user_from_id
        - user_to_id
        - request_message
        - declined
        - from_blocked
        - connected_at
        - accept_message
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_from_id:
          type:
            - string
            - 'null'
          format: uuid
        user_to_id:
          type:
            - string
            - 'null'
          format: uuid
        request_message:
          type:
            - string
            - 'null'
        declined:
          type: boolean
        from_blocked:
          type: boolean
        connected_at:
          type:
            - string
            - 'null'
          format: date-time
        accept_message:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ConnectionRequestsResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ConnectionRequest-2'
    ConnectionRequestsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ConnectionRequestsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateConnectionRequestRequest:
      type: object
      required:
        - user_to_id
      properties:
        user_to_id:
          type: string
          format: uuid
        request_message:
          type:
            - string
            - 'null'
          maxLength: 2000
    ConnectionRequestResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ConnectionRequest-2'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateConnectionRequestRequest:
      type: object
      required:
        - action
      properties:
        action:
          type: string
          enum:
            - accept
            - decline
        accept_message:
          type:
            - string
            - 'null'
          maxLength: 2000
    OrganizationListItem:
      type: object
      required:
        - id
        - name
        - is_personal
        - workos_organization_id
        - company_id
        - activated
        - activated_at
        - created_at
        - updated_at
        - role
        - membership_status
        - workos_membership_id
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        is_personal:
          type: boolean
        workos_organization_id:
          type:
            - string
            - 'null'
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        activated:
          type: boolean
          description: |
            Whether this org may be switched into. False until a team
            org verifies its first domain; always true for personal orgs.
        activated_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        role:
          type: string
          enum:
            - owner
            - admin
            - member
        membership_status:
          type: string
          enum:
            - active
            - inactive
            - pending
        workos_membership_id:
          type:
            - string
            - 'null'
    ListOrganizationsResponse:
      type: object
      required:
        - items
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationListItem'
        total:
          type: integer
    ListOrganizationsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListOrganizationsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateOrganizationRequest:
      type: object
      required:
        - name
        - domains
      properties:
        name:
          type: string
          minLength: 1
          description: Display name of the organization.
        domains:
          type: array
          minItems: 1
          items:
            type: string
          description: |
            Domains to claim for the organization (FQDNs, e.g.
            example.com). At least one is required; ownership must be
            proven via DNS TXT verification before the domain counts as
            verified. Consumer email domains (gmail.com, ...) are rejected.
        external_id:
          type:
            - string
            - 'null'
          maxLength: 255
          description: |
            Caller-chosen id mirrored onto the WorkOS organization;
            defaults to the local organization id.
    OrganizationDomain:
      type: object
      required:
        - id
        - domain
        - status
        - verification_token
        - verified_at
        - created_at
      properties:
        id:
          type: string
          format: uuid
        domain:
          type: string
        status:
          type: string
          description: |
            Ownership verification state: pending, verified, or failed.
        verification_token:
          type:
            - string
            - 'null'
          description: |
            Value the domain owner publishes as a DNS TXT record; null
            until WorkOS has issued one.
        verified_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
    OrganizationResponse:
      type: object
      required:
        - id
        - name
        - is_personal
        - workos_organization_id
        - company_id
        - domains
        - organization_domains
        - activated
        - activated_at
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        is_personal:
          type: boolean
          description: Always false for organizations created via this endpoint.
        workos_organization_id:
          type:
            - string
            - 'null'
          description: Null when the WorkOS mirror has not completed yet.
        company_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Company auto-linked by domain match at creation;
            null when no company claims the domain.
        domains:
          type: array
          items:
            type: string
          description: Flat claimed-domain names; see organization_domains.
        organization_domains:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationDomain'
          description: Claimed domains with ownership-verification state.
        activated:
          type: boolean
          description: |
            Whether the org may be selected and used. Personal orgs are
            always activated; a team org activates when its first claimed
            domain verifies. Until then it is absent from the sign-in org
            picker and cannot mint an org-scoped session.
        activated_at:
          type:
            - string
            - 'null'
          format: date-time
          description: Null for personal orgs and unactivated team orgs.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    OrganizationResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/OrganizationResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MembershipResponse:
      type: object
      required:
        - id
        - user_id
        - organization_id
        - role
        - status
        - workos_membership_id
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        organization_id:
          type: string
          format: uuid
        role:
          type: string
          enum:
            - owner
            - admin
            - member
        status:
          type: string
          enum:
            - active
            - inactive
            - pending
        workos_membership_id:
          type:
            - string
            - 'null'
          description: Null when the WorkOS mirror has not completed yet.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    OrganizationDetailResponse:
      allOf:
        - $ref: '#/components/schemas/OrganizationResponse'
        - type: object
          required:
            - organization_memberships
          properties:
            organization_memberships:
              type: array
              items:
                $ref: '#/components/schemas/MembershipResponse'
    OrganizationDetailResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/OrganizationDetailResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateOrganizationRequest:
      type: object
      properties:
        name:
          type:
            - string
            - 'null'
          minLength: 1
          description: New display name; omit to keep.
        domains:
          type:
            - array
            - 'null'
          minItems: 1
          items:
            type: string
          description: |
            Replacement set of claimed domains (FQDNs); omit to keep.
            Must contain at least one domain; replaced domains restart
            DNS ownership verification.
        external_id:
          type:
            - string
            - 'null'
          maxLength: 255
          description: New WorkOS external id; omit to keep.
    ListOrganizationDomainsResponse:
      type: object
      required:
        - items
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationDomain'
        total:
          type: integer
    ListOrganizationDomainsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListOrganizationDomainsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    OrgGraphStats:
      type: object
      properties:
        members:
          type: integer
        contributors:
          type: integer
          description: Members who have contributed at least one relationship.
        edges:
          type: integer
          description: Shared relationships, counted once per contributor.
        people:
          type: integer
          description: Distinct people known, counted once however many members know them.
    InvitationResponse:
      type: object
      required:
        - id
        - organization_id
        - email
        - role
        - status
        - workos_invitation_id
        - invited_by_user_id
        - accepted_user_id
        - expires_at
        - accepted_at
        - revoked_at
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        organization_id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        role:
          type: string
          description: Role granted on acceptance (owner, admin, member).
        status:
          type: string
          description: |
            Lifecycle state (pending, accepted, expired). Revoked
            invitations are deleted outright, so the state never appears
            here.
        workos_invitation_id:
          type:
            - string
            - 'null'
          description: |
            Null only when the invitation was recorded without a WorkOS
            client configured, in which case no email was delivered.
        invited_by_user_id:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
          description: |
            Who invited them; null once that account is deleted, or for an
            invitation created in the WorkOS dashboard.
        accepted_user_id:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
          description: |
            The joined user; null until the invitation is accepted and
            their local user row exists.
        expires_at:
          type:
            - string
            - 'null'
          format: date-time
        accepted_at:
          type:
            - string
            - 'null'
          format: date-time
        revoked_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            Always null: revoked invitations are deleted rather than
            stored. Kept for wire-shape stability until the column drops.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListInvitationsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: object
          required:
            - items
            - total
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/InvitationResponse'
            total:
              type: integer
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    InviteMemberRequest:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email
          description: |
            Address to invite. Normalised to lower case before it is
            stored or sent; need not belong to an Orbiter user yet.
        role:
          type:
            - string
            - 'null'
          description: |
            Role granted on acceptance (owner, admin, member). Defaults to
            member. Validated server-side.
        expires_in_days:
          type:
            - integer
            - 'null'
          description: |
            How long the invitation stays valid, 1 to 30 days. Defaults to
            the WorkOS default of 7.
    InvitationResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/InvitationResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    InvitationPreviewResponse:
      type: object
      required:
        - organization
        - email
        - state
        - expires_at
      properties:
        organization:
          oneOf:
            - type: object
              required:
                - id
                - name
              properties:
                id:
                  oneOf:
                    - type: string
                      format: uuid
                    - type: 'null'
                  description: |
                    Local organization id; null when the organization is
                    not mirrored locally yet.
                name:
                  type: string
            - type: 'null'
          description: |
            The organization the invitee would join; null when the
            invitation carries none.
        email:
          type: string
          format: email
          description: The invited address.
        state:
          type: string
          description: |
            WorkOS invitation state (pending, accepted, expired,
            revoked).
        expires_at:
          type:
            - string
            - 'null'
          format: date-time
    InvitationPreviewEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/InvitationPreviewResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    PersonSummary:
      type: object
      required:
        - id
        - full_name
        - avatar
        - current_title
        - bio
      properties:
        id:
          type: string
          format: uuid
        full_name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
    MembershipDetailResponse:
      allOf:
        - $ref: '#/components/schemas/MembershipResponse'
        - type: object
          required:
            - person
          properties:
            person:
              oneOf:
                - $ref: '#/components/schemas/PersonSummary'
                - type: 'null'
              description: The member's person summary; null when unlinked.
    ListMembershipsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: object
          required:
            - items
            - total
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/MembershipDetailResponse'
            total:
              type: integer
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddMembershipRequest:
      type: object
      required:
        - user_id
        - role
      properties:
        user_id:
          type: string
          format: uuid
          description: Local id of the user to add.
        role:
          type: string
          enum:
            - owner
            - admin
            - member
        status:
          type:
            - string
            - 'null'
          enum:
            - active
            - inactive
            - pending
            - null
          description: Defaults to "active" when omitted.
    MembershipResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/MembershipResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateMembershipRequest:
      type: object
      description: Omitted fields are left unchanged.
      properties:
        role:
          type: string
          enum:
            - owner
            - admin
            - member
        status:
          type: string
          enum:
            - active
            - inactive
            - pending
    UpdateMembershipRoleRequest:
      type: object
      required:
        - role
      properties:
        role:
          type: string
          enum:
            - owner
            - admin
            - member
    SetMembershipStatusRequest:
      type: object
      required:
        - action
      properties:
        action:
          type: string
          enum:
            - deactivate
            - reactivate
    BackfillResponse:
      type: object
      required:
        - scanned
        - synced
        - failed
      properties:
        scanned:
          type: integer
        synced:
          type: integer
        failed:
          type: integer
    BackfillResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/BackfillResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MembershipWebhookResponse:
      type: object
      required:
        - event
        - action
        - membership
        - stub_org_created
        - deleted_count
      properties:
        event:
          type: string
        action:
          type: string
          enum:
            - upserted
            - deleted
            - ignored
        membership:
          oneOf:
            - $ref: '#/components/schemas/MembershipResponse'
            - type: 'null'
        stub_org_created:
          type: boolean
        deleted_count:
          type: integer
    MembershipWebhookResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/MembershipWebhookResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    OrganizationWebhookResponse:
      type: object
      required:
        - event
        - action
        - workos_organization_id
        - organization_id
        - deleted_count
      properties:
        event:
          type: string
        action:
          type: string
          enum:
            - upserted
            - deleted
            - not_found
            - skipped_not_found
            - ignored
        workos_organization_id:
          type: string
        organization_id:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        deleted_count:
          type: integer
    OrganizationWebhookResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/OrganizationWebhookResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    InvitationWebhookResponse:
      type: object
      required:
        - event
        - action
        - workos_invitation_id
        - invitation_id
      properties:
        event:
          type: string
        action:
          type: string
          description: What the handler did (updated, adopted, deleted, ignored).
        workos_invitation_id:
          type: string
        invitation_id:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
          description: |
            Null when the event was ignored without a matching local row;
            an ignored event on a known row still carries its id.
    InvitationWebhookResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/InvitationWebhookResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    DomainWebhookResponse:
      type: object
      required:
        - event
        - action
        - organization_domain
        - organization_activated
      properties:
        event:
          type: string
        action:
          type: string
          description: One of updated, skipped_not_found, ignored.
        organization_activated:
          type: boolean
          description: |
            True when this event verified the org's first domain, flipping
            the org activated and mirroring its memberships to WorkOS.
        organization_domain:
          oneOf:
            - $ref: '#/components/schemas/OrganizationDomain'
            - type: 'null'
    DomainWebhookResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/DomainWebhookResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    NoteResponse:
      type: object
      required:
        - id
        - is_document
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type:
            - string
            - 'null'
        note:
          type:
            - string
            - 'null'
        is_document:
          type: boolean
        tiptap_reference:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListNotesResponse:
      type: object
      required:
        - items
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/NoteResponse'
        total:
          type: integer
          format: int64
    ListNotesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListNotesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateNoteRequest:
      type: object
      description: |
        my_person_mentions / my_company_mentions are saved with mention=true;
        my_person_id / my_company_id (when set) with mention=false.
      properties:
        title:
          type:
            - string
            - 'null'
          maxLength: 512
        note:
          type:
            - string
            - 'null'
        is_document:
          type: boolean
        tiptap_reference:
          type:
            - string
            - 'null'
          maxLength: 512
        my_person_mentions:
          type: array
          items:
            type: string
            format: uuid
        my_company_mentions:
          type: array
          items:
            type: string
            format: uuid
        my_person_id:
          type:
            - string
            - 'null'
          format: uuid
        my_company_id:
          type:
            - string
            - 'null'
          format: uuid
    NoteResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/NoteResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MentionPersonResponse:
      type: object
      required:
        - person_id
        - contact_id
        - full_name
        - title
        - person
      properties:
        person_id:
          type: string
          format: uuid
          description: The id to insert into the note body.
        contact_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The caller's own contact for them; null when they hold none.
        full_name:
          type: string
        title:
          type:
            - string
            - 'null'
        person:
          type: object
          required:
            - id
            - avatar
          properties:
            id:
              type: string
              format: uuid
            avatar:
              type:
                - string
                - 'null'
    MentionCompanyResponse:
      type: object
      required:
        - company_id
        - company_contact_id
        - company_name
        - company
      properties:
        company_id:
          type: string
          format: uuid
          description: The id to insert into the note body.
        company_contact_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The caller's own company contact; null when they hold none.
        company_name:
          type: string
        company:
          type: object
          required:
            - id
            - logo
          properties:
            id:
              type: string
              format: uuid
            logo:
              type:
                - string
                - 'null'
    MentionsResponse:
      type: object
      required:
        - persons
        - companies
      properties:
        persons:
          type: array
          items:
            $ref: '#/components/schemas/MentionPersonResponse'
        companies:
          type: array
          items:
            $ref: '#/components/schemas/MentionCompanyResponse'
    MentionsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/MentionsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MentionedPersonResponse:
      type: object
      required:
        - id
        - person_id
        - full_name
        - person
      properties:
        id:
          type: string
          format: uuid
          description: Contact-row id.
        person_id:
          type: string
          format: uuid
        full_name:
          type: string
        person:
          type: object
          required:
            - id
            - name
            - avatar
          properties:
            id:
              type: string
              format: uuid
            name:
              type: string
            avatar:
              type:
                - string
                - 'null'
    ListMentionedPersonsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/MentionedPersonResponse'
        error:
          type: 'null'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                total:
                  type: integer
                nextCursor:
                  type: string
                  description: Empty on the last page.
    MentionedCompanyResponse:
      type: object
      required:
        - id
        - company_id
        - company_name
        - company
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        company_name:
          type: string
        company:
          type: object
          required:
            - id
            - name
            - logo
          properties:
            id:
              type: string
              format: uuid
            name:
              type: string
            logo:
              type:
                - string
                - 'null'
    ListMentionedCompaniesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/MentionedCompanyResponse'
        error:
          type: 'null'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                total:
                  type: integer
                nextCursor:
                  type: string
                  description: Empty on the last page.
    UpdateNoteRequest:
      type: object
      properties:
        title:
          type:
            - string
            - 'null'
          maxLength: 512
        note:
          type:
            - string
            - 'null'
        is_document:
          type: boolean
        tiptap_reference:
          type:
            - string
            - 'null'
          maxLength: 512
    CollaboratorUser:
      type: object
      required:
        - person_id
        - person
      properties:
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        person:
          type: object
          required:
            - name
            - avatar
          properties:
            name:
              type: string
            avatar:
              type:
                - string
                - 'null'
    CollaboratorResponse:
      type: object
      required:
        - id
        - note_id
        - user_id
        - mode
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        note_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        mode:
          type: string
          enum:
            - viewer
            - editor
            - commentor
        user:
          description: Enriched person card for the collaborator. Present only when the collaborator's account has a person; omitted otherwise so the client can fall back to a placeholder.
          $ref: '#/components/schemas/CollaboratorUser'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListCollaboratorsResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CollaboratorResponse'
    ListCollaboratorsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListCollaboratorsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddCollaboratorRequest:
      type: object
      required:
        - user_id
        - mode
      properties:
        user_id:
          type: string
          format: uuid
        mode:
          type: string
          enum:
            - viewer
            - editor
            - commentor
    CollaboratorResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollaboratorResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateCollaboratorModeRequest:
      type: object
      required:
        - mode
      properties:
        mode:
          type: string
          enum:
            - viewer
            - editor
            - commentor
    EmailTemplateResponse:
      type: object
      required:
        - id
        - user_id
        - title
        - subject
        - template
        - cc
        - bcc
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        title:
          type: string
        subject:
          type: string
        template:
          type: string
        tiptap_reference:
          type:
            - string
            - 'null'
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListEmailTemplatesResponse:
      type: object
      required:
        - items
      description: |
        One keyset page of templates. next_cursor is the opaque cursor for the
        next page; absent/empty when there are no more results.
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EmailTemplateResponse'
        next_cursor:
          type: string
    ListEmailTemplatesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListEmailTemplatesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateEmailTemplateRequest:
      type: object
      required:
        - title
      description: |
        The owner is taken from the session, never the body. cc / bcc are
        lists of email addresses (default empty).
      properties:
        title:
          type: string
          maxLength: 512
        subject:
          type: string
          maxLength: 998
        template:
          type: string
        tiptap_reference:
          type:
            - string
            - 'null'
          maxLength: 512
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
    EmailTemplateEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EmailTemplateResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateEmailTemplateRequest:
      type: object
      required:
        - title
      properties:
        title:
          type: string
          maxLength: 512
        subject:
          type: string
          maxLength: 998
        template:
          type: string
        tiptap_reference:
          type:
            - string
            - 'null'
          maxLength: 512
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
    Recipient:
      type: object
      required:
        - name
        - email
      properties:
        name:
          type: string
        email:
          type: string
          format: email
    EmailDraftResponse:
      type: object
      required:
        - id
        - user_id
        - to
        - cc
        - bcc
        - status
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        grant_id:
          type:
            - string
            - 'null'
        nylas_draft_id:
          type:
            - string
            - 'null'
        thread_id:
          type:
            - string
            - 'null'
        reply_to_message_id:
          type:
            - string
            - 'null'
        subject:
          type:
            - string
            - 'null'
        body:
          type:
            - string
            - 'null'
        to:
          type: array
          items:
            $ref: '#/components/schemas/Recipient'
        cc:
          type: array
          items:
            $ref: '#/components/schemas/Recipient'
        bcc:
          type: array
          items:
            $ref: '#/components/schemas/Recipient'
        status:
          type: string
          enum:
            - draft
            - synced
            - sending
            - sent
            - failed
            - discarded
        last_synced_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListEmailDraftsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      description: List of the caller's drafts. meta includes a total count.
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/EmailDraftResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateEmailDraftRequest:
      type: object
      required:
        - grant_id
      description: |
        multipart/form-data. grant_id identifies the Nylas grant (mailbox).
        to/cc/bcc are repeated address fields; attachments are repeated file
        parts. message_id, when set, makes the draft a reply.
      properties:
        grant_id:
          type: string
        subject:
          type: string
        body:
          type: string
        thread_id:
          type: string
        message_id:
          type: string
        to:
          type: array
          items:
            type: string
            format: email
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
        attachments:
          type: array
          items:
            type: string
            format: binary
    EmailDraftEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EmailDraftResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    EmailCollaboratorResponse:
      type: object
      required:
        - id
        - user_id
        - collaborator_user_id
        - mode
        - created_at
      properties:
        id:
          type: string
          format: uuid
        email_template_id:
          type:
            - string
            - 'null'
          format: uuid
        email_thread_id:
          type:
            - string
            - 'null'
        user_id:
          type: string
          format: uuid
        collaborator_user_id:
          type: string
          format: uuid
        mode:
          type: string
          description: 'Collaborator mode (validated server-side). Current values: editor, commentor, mention, owner, viewer — not a frozen set.'
        user_name:
          type:
            - string
            - 'null'
        user_avatar:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
    ListEmailCollaboratorsResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EmailCollaboratorResponse'
    ListEmailCollaboratorsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListEmailCollaboratorsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateEmailCollaboratorRequest:
      type: object
      required:
        - collaborator_user_id
        - mode
      description: |
        The sharer is taken from the session, never the body. At least one of
        email_template_id / email_thread_id must be set.
      properties:
        email_template_id:
          type:
            - string
            - 'null'
          format: uuid
        email_thread_id:
          type:
            - string
            - 'null'
          maxLength: 512
        collaborator_user_id:
          type: string
          format: uuid
        mode:
          type: string
          description: 'Collaborator mode (validated server-side). Current values: editor, commentor, mention, owner, viewer — not a frozen set.'
    EmailCollaboratorEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EmailCollaboratorResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateEmailCollaboratorRequest:
      type: object
      required:
        - mode
      properties:
        mode:
          type: string
          description: 'Collaborator mode (validated server-side). Current values: editor, commentor, mention, owner, viewer — not a frozen set.'
    UpdateEmailDraftRequest:
      type: object
      description: Fields to update on a draft. to/cc/bcc replace the existing recipient lists.
      properties:
        subject:
          type: string
        body:
          type: string
        to:
          type: array
          items:
            type: string
            format: email
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
    DraftAttachmentResponse:
      type: object
      required:
        - id
        - draft_id
        - created_at
      properties:
        id:
          type: string
          format: uuid
        draft_id:
          type: string
          format: uuid
        file_id:
          type:
            - string
            - 'null'
          format: uuid
        file_name:
          type:
            - string
            - 'null'
        mime_type:
          type:
            - string
            - 'null'
        size_bytes:
          type:
            - integer
            - 'null'
          format: int64
        url:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
    DraftAttachmentListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DraftAttachmentResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddDraftAttachmentsRequest:
      type: object
      required:
        - attachments
      description: |
        multipart/form-data. attachments are repeated file parts; the total
        request body is capped at 25 MB.
      properties:
        attachments:
          type: array
          items:
            type: string
            format: binary
    NylasFolder:
      type: object
      required:
        - id
        - grant_id
        - name
        - system_folder
        - child_count
        - unread_count
        - total_count
      properties:
        id:
          type: string
        grant_id:
          type: string
        name:
          type: string
        system_folder:
          type: boolean
        child_count:
          type: integer
        unread_count:
          type: integer
        total_count:
          type: integer
        parent_id:
          type: string
        background_color:
          type: string
        text_color:
          type: string
    GrantFolderGroup:
      type: object
      required:
        - nylas_grant_id
        - email
        - folders
      description: One Nylas grant's folder list.
      properties:
        nylas_grant_id:
          type: string
        email:
          type: string
          format: email
        folders:
          type: array
          items:
            $ref: '#/components/schemas/NylasFolder'
    ListFoldersEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/GrantFolderGroup'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateFolderRequest:
      type: object
      required:
        - grant_id
        - name
      properties:
        grant_id:
          type: string
        name:
          type: string
        parent_id:
          type: string
        text_color:
          type: string
        background_color:
          type: string
    NylasFolderEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/NylasFolder'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateFolderRequest:
      type: object
      required:
        - grant_id
      properties:
        grant_id:
          type: string
        name:
          type: string
        parent_id:
          type: string
        text_color:
          type: string
        background_color:
          type: string
    DeleteFolderRequest:
      type: object
      required:
        - grant_id
      properties:
        grant_id:
          type: string
    PersonRef:
      type: object
      required:
        - person_id
        - full_name
      description: Orbiter person identity matched to a Nylas email address.
      properties:
        person_id:
          type: string
        full_name:
          type: string
        avatar_url:
          type:
            - string
            - 'null'
    EnrichedParticipant:
      type: object
      required:
        - name
        - email
      description: A Nylas participant enriched with an Orbiter person reference.
      properties:
        name:
          type: string
        email:
          type: string
          format: email
        email_address:
          $ref: '#/components/schemas/PersonRef'
    EnrichedThread:
      type: object
      required:
        - id
        - grant_id
        - subject
        - participants
        - message_ids
        - folders
        - unread
        - starred
        - has_attachments
        - has_drafts
        - latest_message_received_date
        - latest_message_sent_date
        - earliest_message_date
        - snippet
      description: Nylas thread enriched with person data and an optional AI summary.
      properties:
        id:
          type: string
        grant_id:
          type: string
        subject:
          type: string
        participants:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedParticipant'
        message_ids:
          type: array
          items:
            type: string
        folders:
          type: array
          items:
            type: string
        unread:
          type: boolean
        starred:
          type: boolean
        has_attachments:
          type: boolean
        has_drafts:
          type: boolean
        latest_draft_or_message: {}
        latest_message_received_date:
          type: integer
          format: int64
        latest_message_sent_date:
          type: integer
          format: int64
        earliest_message_date:
          type: integer
          format: int64
        snippet:
          type: string
        ai_summary: {}
    GrantThreadGroup:
      type: object
      required:
        - nylas_grant_id
        - email
        - enabled
        - error
        - threads
      description: |
        One Nylas grant's thread list, optionally with a next page cursor. A grant whose upstream fetch failed (e.g. provider rate limit) is returned with enabled=false, error set, and empty threads — one throttled account never fails the whole response.
      properties:
        nylas_grant_id:
          type: string
        email:
          type: string
          format: email
        enabled:
          type: boolean
        error:
          type:
            - string
            - 'null'
        next_cursor:
          type: string
        threads:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedThread'
    ListThreadsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/GrantThreadGroup'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    EmailAISummaryResponse:
      type: object
      required:
        - id
        - grant_id
        - thread_id
        - summary
        - bullet_points
        - action_items
        - links
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        grant_id:
          type: string
        thread_id:
          type: string
        summary:
          type: string
        bullet_points:
          type: array
          items:
            type: string
        sentiment:
          type:
            - string
            - 'null'
        action_items:
          type: array
          items:
            type: string
        links:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    NylasParticipant:
      type: object
      required:
        - name
        - email
      properties:
        name:
          type: string
        email:
          type: string
          format: email
    NylasAttachment:
      type: object
      required:
        - id
        - filename
        - content_type
        - size
      properties:
        id:
          type: string
        filename:
          type: string
        content_type:
          type: string
        size:
          type: integer
          format: int64
        is_inline:
          type: boolean
        content_id:
          type: string
    EnrichedMessage:
      type: object
      required:
        - id
        - grant_id
        - thread_id
        - subject
        - from
        - to
        - cc
        - bcc
        - reply_to
        - body
        - snippet
        - date
        - unread
        - starred
        - folders
        - attachments
        - created_at
      properties:
        id:
          type: string
        grant_id:
          type: string
        thread_id:
          type: string
        subject:
          type: string
        from:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedParticipant'
        to:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedParticipant'
        cc:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedParticipant'
        bcc:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedParticipant'
        reply_to:
          type: array
          items:
            $ref: '#/components/schemas/NylasParticipant'
        body:
          type: string
        snippet:
          type: string
        date:
          type: integer
          format: int64
        unread:
          type: boolean
        starred:
          type: boolean
        folders:
          type: array
          items:
            type: string
        attachments:
          type: array
          items:
            $ref: '#/components/schemas/NylasAttachment'
        created_at:
          type: integer
          format: int64
    ThreadDetailResponse:
      type: object
      required:
        - nylas_grant_id
        - email
        - enabled
        - messages
      description: Full thread with enriched messages and optional AI summary.
      properties:
        nylas_grant_id:
          type: string
        email:
          type: string
          format: email
        enabled:
          type: boolean
        ai_summary:
          $ref: '#/components/schemas/EmailAISummaryResponse'
        messages:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedMessage'
    ThreadDetailEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ThreadDetailResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateThreadRequest:
      type: object
      required:
        - grant_id
      description: Partial thread update — omit fields to leave them unchanged.
      properties:
        grant_id:
          type: string
        folders:
          type: array
          items:
            type: string
        starred:
          type:
            - boolean
            - 'null'
        unread:
          type:
            - boolean
            - 'null'
    NylasThread:
      type: object
      required:
        - id
        - grant_id
        - subject
        - participants
        - message_ids
        - folders
        - unread
        - starred
        - has_attachments
        - has_drafts
        - latest_message_received_date
        - latest_message_sent_date
        - earliest_message_date
        - snippet
      description: Raw Nylas thread (no enrichment). Returned by update-thread.
      properties:
        id:
          type: string
        grant_id:
          type: string
        subject:
          type: string
        participants:
          type: array
          items:
            $ref: '#/components/schemas/NylasParticipant'
        message_ids:
          type: array
          items:
            type: string
        folders:
          type: array
          items:
            type: string
        unread:
          type: boolean
        starred:
          type: boolean
        has_attachments:
          type: boolean
        has_drafts:
          type: boolean
        latest_draft_or_message: {}
        latest_message_received_date:
          type: integer
          format: int64
        latest_message_sent_date:
          type: integer
          format: int64
        earliest_message_date:
          type: integer
          format: int64
        snippet:
          type: string
    NylasThreadEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/NylasThread'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SnoozeThreadRequest:
      type: object
      required:
        - grant_id
        - snoozed_folder_id
        - snooze_until
      properties:
        grant_id:
          type: string
        snoozed_folder_id:
          type: string
        snooze_until:
          type: string
          format: date-time
    SnoozedThreadResponse:
      type: object
      required:
        - id
        - user_id
        - grant_id
        - thread_id
        - snooze_until
        - snoozed_folder_id
        - original_folders
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        grant_id:
          type: string
        thread_id:
          type: string
        snooze_until:
          type: string
          format: date-time
        snoozed_folder_id:
          type: string
        original_folders:
          type: array
          items:
            type: string
        error:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    SnoozedThreadEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SnoozedThreadResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UnsnoozeThreadRequest:
      type: object
      required:
        - grant_id
      properties:
        grant_id:
          type: string
    GenerateSummaryRequest:
      type: object
      required:
        - grant_id
        - thread_id
      properties:
        grant_id:
          type: string
        thread_id:
          type: string
    EmailAISummaryEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EmailAISummaryResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SendEmailRequest:
      type: object
      required:
        - grant_id
        - to
      description: |
        multipart/form-data. grant_id identifies the Nylas mailbox. to/cc/bcc
        are repeated address fields. reply_to_message_id, when set, threads the
        send as a reply. attachments are repeated file parts (total ≤ 25 MB).
      properties:
        grant_id:
          type: string
        subject:
          type: string
        body:
          type: string
        reply_to_message_id:
          type: string
        to:
          type: array
          items:
            type: string
            format: email
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
        attachments:
          type: array
          items:
            type: string
            format: binary
    NylasGrantResponse:
      type: object
      required:
        - id
        - user_id
        - grant_id
        - email_account
        - provider
        - email_type
        - primary_for_sending
        - ai_enabled
        - parsed
        - oldest_sync
        - grant_expired
        - last_contact_sync
        - show_in_email_widget
        - email_signature_html
        - signature_enabled
        - signature_on_replies
        - orbiter_signature_enabled
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        grant_id:
          type: string
        email_account:
          type: string
        provider:
          type:
            - string
            - 'null'
        email_type:
          type:
            - string
            - 'null'
        primary_for_sending:
          type: boolean
        ai_enabled:
          type: boolean
        parsed:
          type: boolean
        seven_day_parsed:
          type: boolean
        history_complete:
          type: boolean
        parse_status:
          type: string
        earliest_interaction_at:
          type:
            - string
            - 'null'
          format: date-time
        last_parsed_at:
          type:
            - string
            - 'null'
          format: date-time
        oldest_sync:
          type:
            - string
            - 'null'
          format: date-time
        grant_expired:
          type: boolean
        last_contact_sync:
          type:
            - string
            - 'null'
          format: date-time
        show_in_email_widget:
          type: boolean
        email_signature_html:
          type:
            - string
            - 'null'
        signature_enabled:
          type: boolean
        signature_on_replies:
          type: boolean
        orbiter_signature_enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListNylasGrantsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/NylasGrantResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    XSyncRun:
      type: object
      required:
        - id
        - status
        - include_dms
        - followers_synced
        - following_synced
        - dm_events_synced
        - estimated_cost_cents
        - followers_stop_reason
        - following_stop_reason
        - dm_stop_reason
        - error
        - started_at
        - completed_at
        - created_at
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          description: pending, running, completed, or failed.
        include_dms:
          type: boolean
        followers_synced:
          type: integer
        following_synced:
          type: integer
        dm_events_synced:
          type: integer
        estimated_cost_cents:
          type: integer
          description: Estimated X charge for the run in cents (one cent per follower, following, or DM event returned).
        followers_stop_reason:
          type:
            - string
            - 'null'
          description: Why the followers section ended early (page_cap, spend_cap, rate_limited); null when it ran to the end.
        following_stop_reason:
          type:
            - string
            - 'null'
          description: Why the following section ended early (page_cap, spend_cap, rate_limited); null when it ran to the end.
        dm_stop_reason:
          type:
            - string
            - 'null'
          description: Why the DM section ended early (page_cap, spend_cap, rate_limited); null when it ran to the end or was skipped.
        error:
          type:
            - string
            - 'null'
          description: Failure reason; `reauth_required` when the user must reconnect.
        started_at:
          type:
            - string
            - 'null'
          format: date-time
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
    XConnection:
      type: object
      required:
        - connected
        - reauth_required
        - x_username
        - dms_enabled
        - last_synced_at
        - current_run
      properties:
        connected:
          type: boolean
        reauth_required:
          type: boolean
          description: X rejected the stored grant; the user must reconnect.
        x_username:
          type:
            - string
            - 'null'
        dms_enabled:
          type: boolean
        last_synced_at:
          type:
            - string
            - 'null'
          format: date-time
        current_run:
          oneOf:
            - $ref: '#/components/schemas/XSyncRun'
            - type: 'null'
    XConnectionEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/XConnection'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateXConnectionRequest:
      type: object
      required:
        - dms_enabled
      properties:
        dms_enabled:
          type: boolean
    XSyncRunEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/XSyncRun'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    StartXConnectRequest:
      type: object
      required:
        - return_to
      properties:
        return_to:
          type: string
          description: Allowlisted frontend path the popup lands on after X.
    StartXConnectResponse:
      type: object
      required:
        - authorization_url
        - expires_at
      properties:
        authorization_url:
          type: string
          format: uri
        expires_at:
          type: string
          format: date-time
    StartXConnectEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/StartXConnectResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ActivityWebhook:
      type: object
      required:
        - url
        - webhook_id
        - valid
      properties:
        url:
          type: string
          format: uri
        webhook_id:
          type: string
        valid:
          type: boolean
          description: False once X's periodic CRC failed; re-register to revalidate.
    ActivityWebhookEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/ActivityWebhook'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ActivitySubscription:
      type: object
      required:
        - id
        - event_type
        - x_user_id
        - direction
        - webhook_id
        - tag
        - expires_at
      properties:
        id:
          type: string
        event_type:
          type: string
          description: follow.follow or follow.unfollow.
        x_user_id:
          type: string
        direction:
          type: string
          description: inbound (done to the account) or outbound (done by it).
        webhook_id:
          type: string
        tag:
          type: string
          description: The owning Orbiter user id.
        expires_at:
          type:
            - string
            - 'null'
          format: date-time
    ActivitySubscriptionsEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ActivitySubscription'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ActivityBackfill:
      type: object
      required:
        - subscribed
        - failed
      properties:
        subscribed:
          type: integer
        failed:
          type: integer
    ActivityBackfillEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/ActivityBackfill'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ActivityTeardown:
      type: object
      required:
        - deleted
      properties:
        deleted:
          type: integer
    ActivityTeardownEnvelope:
      type: object
      required:
        - data
        - error
      properties:
        data:
          $ref: '#/components/schemas/ActivityTeardown'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    TokenExchangeRequest:
      type: object
      required:
        - code
        - redirect_url
      properties:
        code:
          type: string
          description: OAuth authorization code returned by the Nylas hosted-auth consent redirect.
        redirect_url:
          type: string
          format: uri
          description: |
            The redirect URI used to start the Nylas auth flow. Must exactly match
            the redirect_uri sent in the authorize request — Nylas re-validates it
            at token exchange (OAuth 2.0 authorization_code grant).
    NylasGrantEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/NylasGrantResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateNylasGrantRequest:
      type: object
      description: |
        Full replace — send the complete object. Omitting a boolean writes
        false; omitting email_signature_html writes null.
      required:
        - primary_for_sending
        - ai_enabled
        - show_in_email_widget
        - signature_enabled
        - signature_on_replies
        - orbiter_signature_enabled
      properties:
        primary_for_sending:
          type: boolean
        ai_enabled:
          type: boolean
        show_in_email_widget:
          type: boolean
        email_signature_html:
          type:
            - string
            - 'null'
        signature_enabled:
          type: boolean
        signature_on_replies:
          type: boolean
        orbiter_signature_enabled:
          type: boolean
    EmailWorkspaceResponse:
      type: object
      required:
        - id
        - user_id
        - name
        - sort_order
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        name:
          type: string
        sort_order:
          type: integer
          format: int32
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListEmailWorkspacesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/EmailWorkspaceResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateEmailWorkspaceRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          maxLength: 255
    EmailWorkspaceEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EmailWorkspaceResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ReorderEmailWorkspacesRequest:
      type: object
      required:
        - workspace_ids
      properties:
        workspace_ids:
          type: array
          items:
            type: string
            format: uuid
    UpdateEmailWorkspaceRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          maxLength: 255
    AddGrantRequest:
      type: object
      required:
        - grant_id
      properties:
        grant_id:
          type: string
          format: uuid
    CalendarResponse:
      type: object
      required:
        - id
        - external_id
        - grant_id
        - name
        - timezone
        - is_primary
        - read_only
        - event_widget_preference
        - agenda_panel
        - created_at
        - updated_at
      properties:
        id:
          type: string
        external_id:
          type: string
        grant_id:
          type: string
        name:
          type: string
        timezone:
          type:
            - string
            - 'null'
        is_primary:
          type: boolean
        read_only:
          type: boolean
        event_widget_preference:
          type: boolean
        agenda_panel:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListCalendarsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CalendarResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateCalendarRequest:
      type: object
      required:
        - event_widget_preference
      properties:
        event_widget_preference:
          type: boolean
    CalendarEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CalendarResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    EventWhen:
      type: object
      required:
        - object
      description: |
        Event timing. Timed events carry start_time/end_time (unix epoch
        seconds); all-day events carry start_date/end_date (YYYY-MM-DD). The
        `object` field marks which variant ("timespan", "date", ...).
      properties:
        object:
          type: string
        start_time:
          type: integer
          format: int64
        end_time:
          type: integer
          format: int64
        date:
          type: string
          description: Single all-day calendar date.
        start_date:
          type: string
        end_date:
          type: string
    PersonRef-2:
      type: object
      required:
        - person_id
        - full_name
        - avatar_url
      properties:
        person_id:
          type: string
          format: uuid
        full_name:
          type: string
        avatar_url:
          type:
            - string
            - 'null'
    EnrichedEventParticipant:
      type: object
      required:
        - name
        - email
        - status
        - email_address
      properties:
        name:
          type: string
        email:
          type: string
        status:
          type: string
        email_address:
          oneOf:
            - $ref: '#/components/schemas/PersonRef-2'
            - type: 'null'
    EnrichedParticipant-2:
      type: object
      required:
        - name
        - email
        - email_address
      properties:
        name:
          type: string
        email:
          type: string
        email_address:
          oneOf:
            - $ref: '#/components/schemas/PersonRef-2'
            - type: 'null'
    EnrichedEvent:
      type: object
      required:
        - id
        - grant_id
        - calendar_id
        - title
        - when
        - participants
        - organizer
        - status
      properties:
        id:
          type: string
        grant_id:
          type: string
        calendar_id:
          type: string
        title:
          type: string
        description:
          type: string
        location:
          type: string
        when:
          $ref: '#/components/schemas/EventWhen'
        participants:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedEventParticipant'
        organizer:
          $ref: '#/components/schemas/EnrichedParticipant-2'
        status:
          type: string
        busy:
          type: boolean
        read_only:
          type: boolean
        visibility:
          type: string
        recurrence:
          type: array
          items:
            type: string
        html_link:
          type: string
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    CalendarEventGroup:
      type: object
      required:
        - id
        - external_id
        - grant_id
        - name
        - is_primary
        - event_widget_preference
        - enabled
        - error
        - events
      description: |
        A calendar paired with its enriched events for the requested range. A calendar whose upstream fetch failed (e.g. provider rate limit) is returned with enabled=false, error set, and empty events instead of being silently dropped.
      properties:
        id:
          type: string
        external_id:
          type: string
        grant_id:
          type: string
        name:
          type: string
        is_primary:
          type: boolean
        event_widget_preference:
          type: boolean
        enabled:
          type: boolean
        error:
          type:
            - string
            - 'null'
        events:
          type: array
          items:
            $ref: '#/components/schemas/EnrichedEvent'
    ListCalendarEventsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CalendarEventGroup'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SendRsvpRequest:
      type: object
      required:
        - event_id
        - calendar_id
        - grant_id
        - status
      properties:
        event_id:
          type: string
        calendar_id:
          type: string
        grant_id:
          type: string
        status:
          type: string
          enum:
            - 'yes'
            - 'no'
            - maybe
    MirrorCalendarView:
      allOf:
        - $ref: '#/components/schemas/CalendarResponse'
        - type: object
          required:
            - email_account
            - provider
            - grant_expired
          properties:
            email_account:
              type: string
            provider:
              type: string
            grant_expired:
              type: boolean
    ListMirrorCalendarsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/MirrorCalendarView'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    GrantView:
      type: object
      required:
        - id
        - email_account
        - provider
        - expired
      properties:
        id:
          type: string
        email_account:
          type: string
        provider:
          type: string
        expired:
          type: boolean
    ListGrantsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/GrantView'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MirrorEventOrganizer:
      type: object
      required:
        - email
        - name
      properties:
        email:
          type: string
        name:
          type: string
    MirrorEventParticipant:
      type: object
      required:
        - email
        - name
        - status
      properties:
        email:
          type: string
        name:
          type: string
        status:
          type: string
    MirrorEvent:
      type: object
      required:
        - id
        - calendar_id
        - title
        - start
        - end
        - all_day
        - status
        - busy
        - read_only
        - recurring
        - participants
        - location
        - html_link
        - updated_at
      description: Public event DTO; `id` is the internal mirror UUID.
      properties:
        id:
          type: string
          format: uuid
        calendar_id:
          type: string
          format: uuid
        master_event_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type: string
        start:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
        all_day:
          type: boolean
        start_date:
          type:
            - string
            - 'null'
        end_date:
          type:
            - string
            - 'null'
        timezone:
          type:
            - string
            - 'null'
        status:
          type: string
        busy:
          type: boolean
        read_only:
          type: boolean
        recurring:
          type: boolean
        organizer:
          $ref: '#/components/schemas/MirrorEventOrganizer'
        participants:
          type: array
          items:
            $ref: '#/components/schemas/MirrorEventParticipant'
        location:
          type: string
        conference_url:
          type:
            - string
            - 'null'
        html_link:
          type: string
        description:
          type:
            - string
            - 'null'
        updated_at:
          type: string
          format: date-time
    ListMirrorEventsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      description: meta carries `cursor` (int64) + `coverage` (CoverageWindow[]).
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/MirrorEvent'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    EventWhenRequest:
      type: object
      required:
        - object
      properties:
        object:
          type: string
          description: timespan | date | datespan
        start_time:
          type: integer
          format: int64
        end_time:
          type: integer
          format: int64
        start_timezone:
          type: string
        end_timezone:
          type: string
        start_date:
          type: string
        end_date:
          type: string
        date:
          type: string
    CreateMirrorEventRequest:
      type: object
      required:
        - calendar_id
        - when
      properties:
        calendar_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        location:
          type: string
        when:
          $ref: '#/components/schemas/EventWhenRequest'
        participants:
          type: array
          items:
            $ref: '#/components/schemas/MirrorEventParticipant'
        busy:
          type: boolean
        visibility:
          type: string
        recurrence:
          type: array
          items:
            type: string
        notify:
          type: boolean
          description: Send provider invitations.
    MirrorEventEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/MirrorEvent'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CommandPending:
      type: object
      required:
        - command_id
        - status
      properties:
        command_id:
          type: string
          format: uuid
        status:
          type: string
    CommandPendingEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CommandPending'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateMirrorEventRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        location:
          type: string
        when:
          $ref: '#/components/schemas/EventWhenRequest'
        participants:
          type: array
          items:
            $ref: '#/components/schemas/MirrorEventParticipant'
        busy:
          type: boolean
        notify:
          type: boolean
    RsvpMirrorRequest:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          enum:
            - 'yes'
            - 'no'
            - maybe
    AvailabilityRequest:
      type: object
      required:
        - from
        - to
      properties:
        calendar_ids:
          type: array
          items:
            type: string
            format: uuid
        from:
          type: string
          format: date-time
        to:
          type: string
          format: date-time
    BusyBlock:
      type: object
      required:
        - start
        - end
        - busy
      properties:
        start:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
        busy:
          type: boolean
    AvailabilityEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/BusyBlock'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    WindowRequest:
      type: object
      required:
        - from
        - to
      properties:
        from:
          type: string
          format: date-time
        to:
          type: string
          format: date-time
    WindowQueuedEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: object
          required:
            - queued
          properties:
            queued:
              type: boolean
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CommandStatus:
      type: object
      required:
        - command_id
        - status
      properties:
        command_id:
          type: string
          format: uuid
        status:
          type: string
        error:
          type: string
    CommandStatusEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CommandStatus'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ContactImportResponse:
      type: object
      required:
        - id
        - input_source
        - status
      properties:
        id:
          type: string
          format: uuid
        input_source:
          type: string
        source_filename:
          type: string
        nylas_grant_id:
          type:
            - string
            - 'null'
          description: Nylas grant id this import syncs; null for file sources.
        status:
          type: string
          description: 'Job lifecycle: pending, parsing, importing, completed, failed.'
        import_initiated_at:
          type:
            - string
            - 'null'
          format: date-time
        import_completed_at:
          type:
            - string
            - 'null'
          format: date-time
        total_records:
          type: integer
          description: Records found in the source file.
        imported_records:
          type: integer
          description: Records staged successfully — the headline number to show users.
        warning_records:
          type: integer
          description: Records imported with parse warnings.
        skipped_records:
          type: integer
          description: In-file duplicates that were not imported twice.
        failed_records:
          type: integer
          description: Records that could not be imported.
        processed_records:
          type: integer
          description: Records the pipeline has handled so far (progress).
        expected_new_persons:
          type: integer
          description: Report field — brand-new people (no existing match) this import added.
        total_persons_added:
          type: integer
          description: All contacts added — new people plus rows matched to existing persons.
        unresolved_contacts:
          type: integer
          description: Rows that could neither match nor create (needs review).
        error:
          type:
            - string
            - 'null'
          description: Terminal error when the whole job failed.
        error_summary:
          type: array
          description: Rollup of row-failure reasons.
          items:
            type: object
            properties:
              reason:
                type: string
              count:
                type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListImportsResponse:
      type: object
      required:
        - items
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ContactImportResponse'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: Empty/absent on the last page.
    ListImportsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListImportsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RegisterImportRequest:
      type: object
      required:
        - input_source
      properties:
        input_source:
          type: string
          description: Source format. JSON registration is nylas-only; file sources use multipart.
        nylas_grant_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Connected email account id (from GET /v1/nylas-grants). Required for the nylas source.
    ContactImportEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ContactImportResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ImportRowResponse:
      type: object
      required:
        - record_index
        - status
      properties:
        record_index:
          type: integer
        display_name:
          type: string
        email:
          type: string
        url:
          type: string
        parse_status:
          type: string
          description: ok or warning.
        parse_warnings:
          type: array
          items:
            type: string
        status:
          type: string
          description: 'Row outcome: pending, imported, merged, skipped, failed.'
        error:
          type:
            - string
            - 'null'
        contact_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The contact this row became.
        processed_at:
          type:
            - string
            - 'null'
          format: date-time
    ListImportRowsResponse:
      type: object
      required:
        - items
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ImportRowResponse'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: Empty/absent on the last page.
    ListImportRowsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListImportRowsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    LogParseRunResponse:
      type: object
      required:
        - id
        - grant_id
        - depth
        - status
        - messages_seen
        - events_seen
        - nylas_requests
        - addresses_written
        - gmail_quota_units
        - provider_requests_email
        - provider_requests_event
        - throttle_wait_ms
        - created_at
      properties:
        id:
          type: string
          format: uuid
        grant_id:
          type: string
        depth:
          type: string
        status:
          type: string
          description: pending | sweeping | completed | failed | grant_error | rate_limited
        messages_seen:
          type: integer
        events_seen:
          type: integer
        nylas_requests:
          type: integer
        addresses_written:
          type: integer
        gmail_quota_units:
          type: integer
          description: Gmail quota units consumed (Nylas-Gmail-Quota-Usage header, email endpoints).
        provider_requests_email:
          type: integer
        provider_requests_event:
          type: integer
        throttle_wait_ms:
          type: integer
          description: Total ms self-throttled under the Gmail unit budget.
        window_start:
          type:
            - string
            - 'null'
          format: date-time
        window_end:
          type:
            - string
            - 'null'
          format: date-time
        started_at:
          type:
            - string
            - 'null'
          format: date-time
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
        error:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
        candidates_processed:
          type: integer
          description: Discovered counterparts evaluated in the processing pass.
        contacts_added:
          type: integer
          description: People promoted to contacts.
        company_contacts_added:
          type: integer
          description: Company contacts promoted.
        threads_summarized:
          type: integer
          description: Email threads AI-summarized (0 when AI is off).
        processed_at:
          type:
            - string
            - 'null'
          format: date-time
          description: When the processing pass finished (distinct from completed_at, the sweep).
    ListLogParseRunsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/LogParseRunResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    StartRunRequest:
      type: object
      required:
        - depth
        - grant_id
      properties:
        depth:
          type: string
          description: Processing scope — "7d" or "all". Not enforced as an enum server-side.
          example: 7d
        grant_id:
          type: string
          description: Required Nylas grant id — a run parses exactly one connected account.
    LogParseRunEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/LogParseRunResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ActivityRefreshRun:
      type: object
      required:
        - id
        - status
        - window_start
        - window_end
        - created_at
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          description: pending | running | completed | failed | rate_limited.
        window_start:
          type: string
          format: date-time
          description: Start of the swept window (7 days before the run was queued).
        window_end:
          type: string
          format: date-time
        grants_swept:
          type: integer
          format: int32
        messages_seen:
          type: integer
          format: int32
        events_seen:
          type: integer
          format: int32
        contacts_touched:
          type: integer
          format: int32
          description: Contacts that gained a new activity in this run, summed across its attempts.
        contacts_created:
          type: integer
          format: int32
          description: Known persons promoted to contacts by this refresh.
        email_activities_written:
          type: integer
          format: int32
          description: Email activities this run created, summed across its attempts and recorded when it parks or completes; ones already on record are not counted.
        calendar_activities_written:
          type: integer
          format: int32
          description: Calendar activities this run created, summed across its attempts and recorded when it parks or completes; ones already on record are not counted.
        nylas_requests:
          type: integer
          format: int32
        gmail_quota_units:
          type: integer
          format: int32
        throttle_wait_ms:
          type: integer
          format: int64
          description: Time spent self-throttling against the Gmail unit budget.
        retry_after:
          type:
            - string
            - 'null'
          format: date-time
          description: Set when the provider throttled us; the run auto-resumes after this.
        error:
          type:
            - string
            - 'null'
        started_at:
          type:
            - string
            - 'null'
          format: date-time
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
    ListActivityRefreshRunsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ActivityRefreshRun'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ActivityRefreshRunEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ActivityRefreshRun'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    Memory:
      type: object
      required:
        - id
        - text
        - metadata
        - score
        - created_at
      properties:
        id:
          type: string
        text:
          type: string
        metadata:
          type: object
          additionalProperties: true
        score:
          type:
            - number
            - 'null'
        created_at:
          type: string
    ListMemoriesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Memory'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SearchMemoriesRequest:
      type: object
      required:
        - query
      properties:
        query:
          type: string
          description: The search query.
    TravelUser:
      type: object
      required:
        - id
        - person_id
        - name
      description: The trip owner's person card, embedded on list items.
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
    PersonCard:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
    EmbeddedPerson:
      type: object
      required:
        - id
        - person_id
        - person
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        person:
          $ref: '#/components/schemas/PersonCard'
    TravelCompanion:
      type: object
      required:
        - id
        - travel_id
        - my_person_id
        - my_person
      properties:
        id:
          type: string
          format: uuid
        travel_id:
          type: string
          format: uuid
        my_person_id:
          type: string
          format: uuid
        my_person:
          $ref: '#/components/schemas/EmbeddedPerson'
    TravelSharedUser:
      type: object
      required:
        - id
        - shared_to_user
        - team_id
        - user
      properties:
        id:
          type: string
          format: uuid
        shared_to_user:
          type: string
          format: uuid
        team_id:
          type:
            - string
            - 'null'
          format: uuid
        user:
          $ref: '#/components/schemas/EmbeddedPerson'
    TravelResponse:
      type: object
      required:
        - id
        - user_id
        - departing_from
        - destination
        - destination_longitude
        - destination_latitude
        - share_to_all
        - share_to_connections
        - private
        - team_ids
        - contact_ids
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        departing_from:
          type: string
        destination:
          type: string
        start_date:
          type:
            - string
            - 'null'
          format: date-time
        end_date:
          type:
            - string
            - 'null'
          format: date-time
        destination_longitude:
          type: number
          format: double
        destination_latitude:
          type: number
          format: double
        note:
          type:
            - string
            - 'null'
        link:
          type:
            - string
            - 'null'
        link_company:
          type:
            - string
            - 'null'
        share_to_all:
          type: boolean
        share_to_connections:
          type: boolean
        private:
          type: boolean
        shared_type:
          type:
            - string
            - 'null'
          enum:
            - all
            - connections
            - teams
            - users
            - null
        team_ids:
          type: array
          items:
            type: string
            format: uuid
        contact_ids:
          type: array
          items:
            type: string
            format: uuid
        travel_user:
          $ref: '#/components/schemas/TravelUser'
        travel_companions:
          type: array
          items:
            $ref: '#/components/schemas/TravelCompanion'
          description: |
            Resolved passengers for the trip — contact_ids mapped to person
            cards. Emitted on both list and detail responses; omitted when the
            trip has no companions.
        shared_users:
          type: array
          items:
            $ref: '#/components/schemas/TravelSharedUser'
          description: Direct per-user shares, detail response only.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListTravelsResponse:
      type: object
      required:
        - active_travels
        - archived_travels
      description: The combined count is in the envelope meta (meta.total), not here.
      properties:
        active_travels:
          type: array
          items:
            $ref: '#/components/schemas/TravelResponse'
        archived_travels:
          type: array
          items:
            $ref: '#/components/schemas/TravelResponse'
    ListTravelsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListTravelsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateTravelRequest:
      type: object
      required:
        - departing_from
        - destination
        - start_date
        - end_date
        - destination_longitude
        - destination_latitude
      description: |
        The owner comes from the session, not the body. Coordinates are
        range-validated (not required), so 0,0 is technically accepted.
      properties:
        departing_from:
          type: string
          maxLength: 512
        destination:
          type: string
          maxLength: 512
        start_date:
          type: string
          format: date-time
        end_date:
          type: string
          format: date-time
        destination_longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
        destination_latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
        note:
          type:
            - string
            - 'null'
        link:
          type:
            - string
            - 'null'
        link_company:
          type:
            - string
            - 'null'
        share_to_all:
          type: boolean
        share_to_connections:
          type: boolean
        contact_ids:
          type: array
          items:
            type: string
            format: uuid
        team_ids:
          type: array
          items:
            type: string
            format: uuid
        private:
          type: boolean
        shared_type:
          type:
            - string
            - 'null'
          enum:
            - all
            - connections
            - teams
            - users
            - null
    TravelEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TravelResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateTravelRequest:
      type: object
      required:
        - departing_from
        - destination
        - start_date
        - end_date
        - destination_longitude
        - destination_latitude
      description: |
        Full replace of the user-editable fields. contact_ids and team_ids
        replace the travel's companions and team shares; an omitted/empty list
        clears that relation.
      properties:
        departing_from:
          type: string
          maxLength: 512
        destination:
          type: string
          maxLength: 512
        start_date:
          type: string
          format: date-time
        end_date:
          type: string
          format: date-time
        destination_longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
        destination_latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
        note:
          type:
            - string
            - 'null'
        link:
          type:
            - string
            - 'null'
        link_company:
          type:
            - string
            - 'null'
        share_to_all:
          type: boolean
        share_to_connections:
          type: boolean
        contact_ids:
          type: array
          items:
            type: string
            format: uuid
        team_ids:
          type: array
          items:
            type: string
            format: uuid
        private:
          type: boolean
        shared_type:
          type:
            - string
            - 'null'
          enum:
            - all
            - connections
            - teams
            - users
            - null
    EmailAddress:
      type: object
      required:
        - id
        - email
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
    Person:
      type: object
      description: |
        current_title, company and primary_location use the same names and
        shapes as the person on GET /v1/contacts, and are null when unknown.
      required:
        - id
        - name
        - current_title
        - company
        - primary_location
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        company:
          oneOf:
            - $ref: '#/components/schemas/CompanyRef'
            - type: 'null'
        primary_location:
          oneOf:
            - $ref: '#/components/schemas/LocationRef'
            - type: 'null'
        email_address:
          oneOf:
            - $ref: '#/components/schemas/EmailAddress'
            - type: 'null'
    Participant:
      type: object
      required:
        - id
        - person
      properties:
        id:
          type: string
          format: uuid
        person:
          $ref: '#/components/schemas/Person'
    FeedItemBase:
      type: object
      required:
        - id
        - is_group
        - activity_type
        - activity_timestamp
        - participants
        - details
      properties:
        id:
          type: string
        is_group:
          type: boolean
        activity_type:
          type: string
          enum:
            - email
            - event
        activity_timestamp:
          type: string
          format: date-time
        participants:
          type: array
          items:
            $ref: '#/components/schemas/Participant'
    EmailDetails:
      type: object
      required:
        - grant_id
        - unread
        - messages
      properties:
        thread_id:
          type:
            - string
            - 'null'
        subject:
          type:
            - string
            - 'null'
        grant_id:
          type: string
        message_id:
          type:
            - string
            - 'null'
        unread:
          type: boolean
        messages:
          type: array
          items:
            type: string
          description: |
            Message bodies, oldest-first. Each is the stored clean_message, else
            the stored Nylas snippet; a message with neither is left out. Group
            threads carry one entry per message_id.
    EmailFeedItem:
      allOf:
        - $ref: '#/components/schemas/FeedItemBase'
        - type: object
          required:
            - activity_type
            - details
          properties:
            activity_type:
              type: string
              const: email
            details:
              $ref: '#/components/schemas/EmailDetails'
    EventDetails:
      type: object
      required:
        - event_id
        - status
        - start_at
        - end_at
      properties:
        event_id:
          type: string
          format: uuid
        subject:
          type:
            - string
            - 'null'
        status:
          type: string
        start_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            activities.event.start_at in UTC; null when the event record or the
            value is missing. All-day events are stored at 00:00:00Z.
        end_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            activities.event.end_at in UTC; null when the event record or the
            value is missing. A single-day all-day event has end_at == start_at.
        event:
          description: Raw event record payload.
    EventFeedItem:
      allOf:
        - $ref: '#/components/schemas/FeedItemBase'
        - type: object
          required:
            - activity_type
            - details
          properties:
            activity_type:
              type: string
              const: event
            details:
              $ref: '#/components/schemas/EventDetails'
    FeedItem:
      oneOf:
        - $ref: '#/components/schemas/EmailFeedItem'
        - $ref: '#/components/schemas/EventFeedItem'
      discriminator:
        propertyName: activity_type
        mapping:
          email: '#/components/schemas/EmailFeedItem'
          event: '#/components/schemas/EventFeedItem'
    ActivityFeedResponse:
      type: object
      required:
        - show_empty_orbits
        - active_day_count
        - activities
      properties:
        show_empty_orbits:
          type: boolean
        active_day_count:
          type: integer
          description: |
            Number of days in the window that had at least one activity. Empty
            days emitted under show_empty_orbits are NOT counted, so this can be
            less than the number of keys in `activities`.
        activities:
          type: object
          description: Days keyed by YYYY-MM-DD, each an array of cards.
          additionalProperties:
            type: array
            items:
              $ref: '#/components/schemas/FeedItem'
    ActivityFeedEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ActivityFeedResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    TeamMembership:
      type: object
      required:
        - id
        - is_admin
        - status
      description: The authenticated user's membership subset for a listed team.
      properties:
        id:
          type: string
          format: uuid
        is_admin:
          type: boolean
        status:
          type: string
          enum:
            - pending
            - accepted
            - rejected
        position:
          type:
            - integer
            - 'null'
          format: int32
    TeamListItem:
      type: object
      required:
        - id
        - icon_name
        - team_color
        - member_count
        - team_member
        - created_at
        - updated_at
      description: |
        A team the authenticated user belongs to, plus the team's member count and the
        authenticated user's own membership. user_id (the creator) is omitted once the
        creator's account is deleted.
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        icon_name:
          type: string
        custom_icon_url:
          type:
            - string
            - 'null'
        team_color:
          type: string
        member_count:
          type: integer
          format: int64
        team_member:
          $ref: '#/components/schemas/TeamMembership'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListTeamsResponse:
      type: object
      required:
        - items
        - total
        - page
        - per_page
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TeamListItem'
        total:
          type: integer
          format: int64
        page:
          type: integer
          format: int32
        per_page:
          type: integer
          format: int32
    ListTeamsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListTeamsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateTeamRequest:
      type: object
      required:
        - title
      description: |
        The owner comes from the session, not the body. user_ids are additional
        members to invite as pending; the creator is added as an accepted admin.
      properties:
        title:
          type: string
          maxLength: 255
        description:
          type:
            - string
            - 'null'
          maxLength: 2000
        icon_name:
          type: string
          maxLength: 255
        custom_icon_url:
          type:
            - string
            - 'null'
          maxLength: 2048
        team_color:
          type: string
          maxLength: 64
        user_ids:
          type: array
          items:
            type: string
            format: uuid
    TeamResponse:
      type: object
      required:
        - id
        - icon_name
        - team_color
        - created_at
        - updated_at
      description: |
        A single team. user_id (the creator) is omitted once the creator's
        account is deleted (the team survives, owner-less).
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        icon_name:
          type: string
        custom_icon_url:
          type:
            - string
            - 'null'
        team_color:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TeamResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TeamResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateTeamRequest:
      type: object
      required:
        - title
      description: |
        Full replace of a team's editable fields. The authenticated user must be an accepted
        admin member of the team.
      properties:
        title:
          type: string
          maxLength: 255
        description:
          type:
            - string
            - 'null'
          maxLength: 2000
        icon_name:
          type: string
          maxLength: 255
        custom_icon_url:
          type:
            - string
            - 'null'
          maxLength: 2048
        team_color:
          type: string
          maxLength: 64
    MemberProfile:
      type: object
      required:
        - person_id
        - name
      description: |
        Read-only enrichment of a listed member — their person profile plus
        the authenticated user's own saved-contact nickname (the "my_person" link). Present only
        on the members-list endpoint, and omitted entirely when the member's user
        has no linked person. `name` is the three name parts concatenated.
        Nullable fields are always present (null when unknown).
      properties:
        person_id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
          format: uri
        bio:
          type:
            - string
            - 'null'
        title:
          type:
            - string
            - 'null'
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        company_name:
          type:
            - string
            - 'null'
        company_logo:
          type:
            - string
            - 'null'
        location:
          type:
            - string
            - 'null'
        nickname:
          type:
            - string
            - 'null'
    TeamMember:
      type: object
      required:
        - id
        - team_id
        - user_id
        - is_admin
        - status
        - is_favourite
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        team_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        is_admin:
          type: boolean
        status:
          type: string
          enum:
            - pending
            - accepted
            - rejected
        invited_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
        invited_at:
          type:
            - string
            - 'null'
          format: date-time
        responded_at:
          type:
            - string
            - 'null'
          format: date-time
        position:
          type:
            - integer
            - 'null'
          format: int32
        is_favourite:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        profile:
          $ref: '#/components/schemas/MemberProfile'
    ListTeamMembersResponse:
      type: object
      required:
        - items
        - total
        - page
        - per_page
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TeamMember'
        total:
          type: integer
          format: int64
        page:
          type: integer
          format: int32
        per_page:
          type: integer
          format: int32
    ListTeamMembersResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListTeamMembersResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddTeamMembersRequest:
      type: object
      required:
        - user_ids
      properties:
        user_ids:
          type: array
          minItems: 1
          items:
            type: string
            format: uuid
    AddTeamMembersResponse:
      type: object
      required:
        - added
      properties:
        added:
          type: array
          items:
            $ref: '#/components/schemas/TeamMember'
    AddTeamMembersResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/AddTeamMembersResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    PatchTeamMemberRequest:
      type: object
      description: Partial — only the provided fields change. At least one required.
      properties:
        is_admin:
          type: boolean
        is_favourite:
          type: boolean
    TeamMemberEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TeamMember'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RespondInvitationRequest:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          enum:
            - accepted
            - rejected
    SharedTeamsResponse:
      type: object
      required:
        - items
      description: The teams the authenticated user and the queried user are both accepted members of. Unpaginated.
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TeamListItem'
    SharedTeamsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SharedTeamsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    FavouriteTeamsResponse:
      type: object
      required:
        - items
      description: The authenticated user's favourited teams, in their custom order. Unpaginated.
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TeamListItem'
    FavouriteTeamsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/FavouriteTeamsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    PatchTeamFavouriteRequest:
      type: object
      required:
        - team_id
        - is_favourite
      description: |
        Toggle the authenticated user's favourite flag on a team. team_id identifies the team
        (the membership acted on is always the authenticated user's own).
      properties:
        team_id:
          type: string
          format: uuid
        is_favourite:
          type: boolean
    ReorderFavouritesRequest:
      type: object
      required:
        - team_ids
      description: The authenticated user's favourited team_ids in desired order (first → position 0). Unique, non-empty.
      properties:
        team_ids:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            type: string
            format: uuid
    ReorderFavouritesResponse:
      type: object
      required:
        - success
      properties:
        success:
          type: boolean
    ReorderFavouritesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ReorderFavouritesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateTeamQuestionRequest:
      type: object
      required:
        - team_id
        - question
      description: |
        The asker comes from the session, not the body. team_question_id threads
        the new question under a parent question in the same team.
      properties:
        team_id:
          type: string
          format: uuid
        question:
          type: string
          maxLength: 10000
        team_question_id:
          type:
            - string
            - 'null'
          format: uuid
    TeamQuestionResponse:
      type: object
      required:
        - id
        - team_id
        - question
        - llm_response
        - created_at
        - updated_at
      description: |
        A single team question. user_id (the asker) is omitted once their account
        is deleted; team_question_id is the parent when this is a threaded
        follow-up. llm_response is empty until the semantic-query pipeline fills it.
      properties:
        id:
          type: string
          format: uuid
        team_id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        question:
          type: string
        llm_response:
          type: string
        team_question_id:
          type:
            - string
            - 'null'
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TeamQuestionResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TeamQuestionResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateTeamQuestionRequest:
      type: object
      required:
        - question
      description: Edits only the question text. Only the asker may edit.
      properties:
        question:
          type: string
          maxLength: 10000
    PersonRef-3:
      type: object
      description: |
        Lean person card. `id` is the persons uuid, so a client can open the
        person sheet straight from the card.
      required:
        - id
        - name
        - avatar
        - current_title
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
    ConnectRequest:
      type: object
      description: |
        A connection request. State is derived: pending is `connected_at` null
        with `declined` and `cleared` both false; accepted is `connected_at`
        set. A `person_*` field is null when that user has no linked person.
      required:
        - id
        - user_from_id
        - user_to_id
        - person_from_id
        - person_to_id
        - request_message
        - declined
        - from_blocked
        - cleared
        - connected_at
        - accept_message
        - person_from
        - person_to
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_from_id:
          type:
            - string
            - 'null'
          format: uuid
        user_to_id:
          type:
            - string
            - 'null'
          format: uuid
        person_from_id:
          type:
            - string
            - 'null'
          format: uuid
        person_to_id:
          type:
            - string
            - 'null'
          format: uuid
        request_message:
          type:
            - string
            - 'null'
        declined:
          type: boolean
        from_blocked:
          type: boolean
        cleared:
          type: boolean
        connected_at:
          type:
            - string
            - 'null'
          format: date-time
        accept_message:
          type:
            - string
            - 'null'
        person_from:
          oneOf:
            - $ref: '#/components/schemas/PersonRef-3'
            - type: 'null'
        person_to:
          oneOf:
            - $ref: '#/components/schemas/PersonRef-3'
            - type: 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListConnectRequestsResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ConnectRequest'
    ListConnectRequestsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListConnectRequestsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SendConnectRequest:
      type: object
      required:
        - user_id
      properties:
        user_id:
          type: string
          format: uuid
          description: The user to connect with.
        request_message:
          type:
            - string
            - 'null'
          maxLength: 1000
    SendConnectRequestResponse:
      type: object
      required:
        - status
        - request
      properties:
        status:
          type: string
          description: |
            `pending` — a request was created and awaits a response.
            `connected` — the target had already asked, so the two were
            connected immediately.
        request:
          $ref: '#/components/schemas/ConnectRequest'
    SendConnectRequestEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SendConnectRequestResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateConnectRequest:
      type: object
      description: Exactly one of the four booleans must be true.
      properties:
        accepted:
          type: boolean
          default: false
        declined:
          type: boolean
          default: false
        blocked:
          type: boolean
          default: false
        cleared:
          type: boolean
          default: false
        accept_message:
          type:
            - string
            - 'null'
          maxLength: 1000
    ConnectRequestEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ConnectRequest'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    Connection-2:
      type: object
      description: |
        One direction of an established connection — the authenticated user's
        own row. `person_id` / `person` describe the connected-to user.
      required:
        - id
        - user_id
        - user_connected_to_id
        - person_id
        - person
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        user_connected_to_id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        person:
          oneOf:
            - $ref: '#/components/schemas/PersonRef-3'
            - type: 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListConnectionsResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Connection-2'
    ListConnectionsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListConnectionsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CollectionQuestion:
      type: object
      required:
        - id
        - collection_id
        - question
        - llm_response
        - sourcing
        - processing
      description: |
        One of the authenticated user's questions on a collection. `llm_response`
        is null until the answer has been written; `processing` is true while the
        answer or its nodes' paths are still being produced.
      properties:
        id:
          type: string
          format: uuid
        collection_id:
          type: string
          format: uuid
        question:
          type: string
        llm_response:
          type:
            - string
            - 'null'
        sourcing:
          type:
            - string
            - 'null'
          description: |
            Where the model said the answer's facts came from: `context` (all from
            the collection's graph), `blended` (graph plus public knowledge) or
            `world_knowledge` (mostly public knowledge). Null until answered.
        processing:
          type: boolean
    ListCollectionQuestionsResponse:
      type: object
      required:
        - questions
      properties:
        questions:
          type: array
          items:
            $ref: '#/components/schemas/CollectionQuestion'
    ListCollectionQuestionsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListCollectionQuestionsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AskCollectionQuestionRequest:
      type: object
      required:
        - collection_id
        - query
      description: |
        A question to answer over a collection's members. The asker comes from the
        session.
      properties:
        collection_id:
          type: string
          format: uuid
        query:
          type: string
          maxLength: 5000
    CollectionQuestionEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionQuestion'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateCollectionQuestionRequest:
      type: object
      required:
        - question
      properties:
        question:
          type: string
          maxLength: 5000
    CollectionQuestionNode:
      type: object
      required:
        - id
        - collection_question_id
        - kind
        - paths_processing
        - entity_type
        - node_uuid
        - node_targeted
        - person
        - company
        - live_event
        - film_tv
        - film_tv_award
        - funding_round
        - music_group
        - music_recording
        - video_game
        - game_series
        - game_award
        - stage_production
        - stage_award
        - paths_to_person
        - paths_to_company
        - created_at
      description: |
        One answer node: a member the model named, in the shared node shape every
        feature serves (the same objects a discover node carries). `node_uuid` is
        the entity's row id in the table `entity_type` names, so exactly one entity
        object is non-null and the rest are null. Paths are the asker's stored
        relationship paths to the node.
      properties:
        id:
          type: string
          format: uuid
        collection_question_id:
          type: string
          format: uuid
        kind:
          type: string
          description: '`response` for a member the model named as the answer.'
        paths_processing:
          type: boolean
          description: True until the asker's paths to this node are stored.
        entity_type:
          type: string
          description: '`persons` or `companies` today.'
        node_uuid:
          type: string
          format: uuid
        node_targeted:
          type: boolean
        person:
          type:
            - object
            - 'null'
          additionalProperties: true
        company:
          type:
            - object
            - 'null'
          additionalProperties: true
        live_event:
          type:
            - object
            - 'null'
          additionalProperties: true
        film_tv:
          type:
            - object
            - 'null'
          additionalProperties: true
        film_tv_award:
          type:
            - object
            - 'null'
          additionalProperties: true
        funding_round:
          type:
            - object
            - 'null'
          additionalProperties: true
        music_group:
          type:
            - object
            - 'null'
          additionalProperties: true
        music_recording:
          type:
            - object
            - 'null'
          additionalProperties: true
        video_game:
          type:
            - object
            - 'null'
          additionalProperties: true
        game_series:
          type:
            - object
            - 'null'
          additionalProperties: true
        game_award:
          type:
            - object
            - 'null'
          additionalProperties: true
        stage_production:
          type:
            - object
            - 'null'
          additionalProperties: true
        stage_award:
          type:
            - object
            - 'null'
          additionalProperties: true
        paths_to_person:
          type: array
          items:
            type: object
            additionalProperties: true
        paths_to_company:
          type: array
          items:
            type: object
            additionalProperties: true
        created_at:
          type: string
          format: date-time
    ListCollectionQuestionNodesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CollectionQuestionNode'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateCommentReactionRequest:
      type: object
      required:
        - collection_comment_id
        - emoji
      properties:
        collection_comment_id:
          type: string
          format: uuid
        emoji:
          type: string
          maxLength: 64
    CollectionReaction:
      type: object
      required:
        - id
        - collection_comment_id
        - user_id
        - emoji
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        collection_comment_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        emoji:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CollectionReactionEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionReaction'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CollectionContains:
      type: object
      required:
        - id
        - icon_name
        - collection_color
        - collection_node_count
        - collection_node_id
        - created_at
        - updated_at
      description: A collection that pins the entity, with its node count and the linking node id.
      properties:
        id:
          type: string
          format: uuid
        created_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        icon_name:
          type: string
        custom_icon_url:
          type:
            - string
            - 'null'
        collection_color:
          type: string
        collection_node_count:
          type: integer
          format: int64
        collection_node_id:
          type: string
          format: uuid
          description: The node that links the entity to this collection.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListCollectionsContainingResponse:
      type: object
      required:
        - collections
      properties:
        collections:
          type: array
          items:
            $ref: '#/components/schemas/CollectionContains'
    ListCollectionsContainingResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListCollectionsContainingResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CommentAuthor:
      type: object
      required:
        - user_id
        - name
      description: A comment author's display fields (their person).
      properties:
        user_id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
    CommentReaction:
      type: object
      required:
        - id
        - user_id
        - emoji
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        emoji:
          type: string
    CollectionCommentItem:
      type: object
      required:
        - id
        - collection_id
        - comment
        - reactions
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        collection_id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        comment:
          type: string
        author:
          $ref: '#/components/schemas/CommentAuthor'
        reactions:
          type: array
          items:
            $ref: '#/components/schemas/CommentReaction'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListCollectionCommentsResponse:
      type: object
      required:
        - comments
      properties:
        comments:
          type: array
          items:
            $ref: '#/components/schemas/CollectionCommentItem'
    ListCollectionCommentsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListCollectionCommentsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateCollectionCommentRequest:
      type: object
      required:
        - collection_id
        - comment
      properties:
        collection_id:
          type: string
          format: uuid
        comment:
          type: string
          maxLength: 5000
    CollectionComment:
      type: object
      required:
        - id
        - collection_id
        - comment
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        collection_id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        comment:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CollectionCommentEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionComment'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateCollectionCommentRequest:
      type: object
      required:
        - comment
      properties:
        comment:
          type: string
          maxLength: 5000
    LinkedTeam:
      type: object
      required:
        - id
        - icon_name
        - team_color
      description: A team a collection is shared through (display fields only).
      properties:
        id:
          type: string
          format: uuid
        title:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        icon_name:
          type: string
        custom_icon_url:
          type:
            - string
            - 'null'
        team_color:
          type: string
    CollectionListItem:
      type: object
      required:
        - id
        - icon_name
        - collection_color
        - collection_node_count
        - teams
        - collection_questions
        - created_at
        - updated_at
      description: |
        A collection the authenticated user can see, plus its node count, the teams it's
        shared through, and the authenticated user's own questions on it.
      properties:
        id:
          type: string
          format: uuid
        created_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        icon_name:
          type: string
        custom_icon_url:
          type:
            - string
            - 'null'
        collection_color:
          type: string
        collection_node_count:
          type: integer
          format: int64
        teams:
          type: array
          items:
            $ref: '#/components/schemas/LinkedTeam'
        collection_questions:
          type: array
          items:
            $ref: '#/components/schemas/CollectionQuestion'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListCollectionsResponse:
      type: object
      required:
        - items
        - total
        - page
        - per_page
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CollectionListItem'
        total:
          type: integer
          format: int64
        page:
          type: integer
          format: int32
        per_page:
          type: integer
          format: int32
    ListCollectionsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListCollectionsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddCollectionFavouriteRequest:
      type: object
      required:
        - collection_id
      properties:
        collection_id:
          type: string
          format: uuid
    CollectionFavourite:
      type: object
      required:
        - id
        - collection_id
        - position
        - created_at
      properties:
        id:
          type: string
          format: uuid
        collection_id:
          type: string
          format: uuid
        position:
          type: integer
          format: int32
          description: 0-based order within the authenticated user's favourites.
        created_at:
          type: string
          format: date-time
    CollectionFavouriteEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionFavourite'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ReorderCollectionFavouritesRequest:
      type: object
      required:
        - collection_ids
      description: The authenticated user's favourites in their new order; each id must already be a favourite.
      properties:
        collection_ids:
          type: array
          minItems: 1
          items:
            type: string
            format: uuid
    NodeRef:
      type: object
      required:
        - entity_type
        - node_uuid
      description: |
        One graph entity to pin: the table it lives in (persons, companies,
        film_tv, video_game, stage_production, ... — plain string, no enum) and
        its row id, which is also the master graph's node id.
      properties:
        entity_type:
          type: string
          maxLength: 64
        node_uuid:
          type: string
          format: uuid
    CreateCollectionRequest:
      type: object
      required:
        - title
        - collection_color
      description: |
        The owner comes from the session, not the body. team_id is optional:
        when present, the team's other members are added to the collection. The
        node lists are optional: any provided entities are pinned as nodes in
        the same transaction (a bad ref fails the whole create). `nodes` is the
        shape every kind uses; `person_ids` and `company_ids` remain as aliases.
      properties:
        title:
          type: string
          maxLength: 255
        description:
          type:
            - string
            - 'null'
          maxLength: 2000
        icon_name:
          type: string
          maxLength: 255
        custom_icon_url:
          type:
            - string
            - 'null'
          maxLength: 2048
        collection_color:
          type: string
          maxLength: 64
        team_id:
          type:
            - string
            - 'null'
          format: uuid
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/NodeRef'
        person_ids:
          type: array
          items:
            type: string
            format: uuid
        company_ids:
          type: array
          items:
            type: string
            format: uuid
    CollectionResponse:
      type: object
      required:
        - id
        - icon_name
        - collection_color
        - created_at
        - updated_at
      description: |
        A single collection. created_by_user_id (the creator) is omitted once
        the creator's account is deleted (the collection survives, owner-less).
      properties:
        id:
          type: string
          format: uuid
        created_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        icon_name:
          type: string
        custom_icon_url:
          type:
            - string
            - 'null'
        collection_color:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CollectionResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ReorderCollectionsRequest:
      type: object
      required:
        - collection_ids
      description: |
        The authenticated user's sidebar order for the submitted collections;
        ids may be a subset — omitted collections keep their existing slots.
      properties:
        collection_ids:
          type: array
          minItems: 1
          maxItems: 1000
          uniqueItems: true
          items:
            type: string
            format: uuid
    CollectionListItemEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionListItem'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateCollectionRequest:
      type: object
      required:
        - title
      description: |
        Full replace of a collection's editable fields. The authenticated user must be the
        owner. collection_color is optional here (required only on create).
      properties:
        title:
          type: string
          maxLength: 255
        description:
          type:
            - string
            - 'null'
          maxLength: 2000
        icon_name:
          type: string
          maxLength: 255
        custom_icon_url:
          type:
            - string
            - 'null'
          maxLength: 2048
        collection_color:
          type: string
          maxLength: 64
    ListCollectionTeamsResponse:
      type: object
      required:
        - teams
      properties:
        teams:
          type: array
          items:
            $ref: '#/components/schemas/LinkedTeam'
    ListCollectionTeamsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListCollectionTeamsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ShareCollectionWithTeamRequest:
      type: object
      required:
        - collection_id
        - team_id
      description: Share an existing collection with a team (its other members are invited).
      properties:
        collection_id:
          type: string
          format: uuid
        team_id:
          type: string
          format: uuid
    ShareCollectionWithTeamResponse:
      type: object
      required:
        - message
        - collection_id
        - invited
      properties:
        message:
          type: string
        collection_id:
          type: string
          format: uuid
        invited:
          type: integer
          description: Number of members newly invited (already-members are skipped).
    ShareCollectionWithTeamResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ShareCollectionWithTeamResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    NodeUser:
      type: object
      required:
        - id
        - name
      description: The user who added a node, plus their display name.
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        name:
          type: string
    CollectionNode:
      type: object
      required:
        - id
        - collection_id
        - rating
        - added_by_user
        - assigned_to_me
        - entity_type
        - node_uuid
        - node_targeted
        - person
        - company
        - live_event
        - film_tv
        - film_tv_award
        - funding_round
        - music_group
        - music_recording
        - video_game
        - game_series
        - game_award
        - stage_production
        - stage_award
        - paths_to_person
        - paths_to_company
        - created_at
        - updated_at
      description: |
        One node pinned to a collection, in the shared node shape every feature
        serves (the same objects a discover node carries) plus the row's own
        fields. `node_uuid` is the entity's row id in the table `entity_type`
        names, so exactly one entity object is non-null and the rest are null;
        all of them are null for a pin whose entity no longer resolves. Paths
        are the caller's stored relationship paths to the node.
      properties:
        id:
          type: string
          format: uuid
        collection_id:
          type: string
          format: uuid
        rating:
          type: number
          format: double
        added_by_user:
          oneOf:
            - $ref: '#/components/schemas/NodeUser'
            - type: 'null'
        assigned_to_me:
          type: boolean
          description: Whether this node is assigned to the authenticated user.
        entity_type:
          type: string
        node_uuid:
          type: string
          format: uuid
        node_targeted:
          type: boolean
        person:
          type:
            - object
            - 'null'
          additionalProperties: true
        company:
          type:
            - object
            - 'null'
          additionalProperties: true
        live_event:
          type:
            - object
            - 'null'
          additionalProperties: true
        film_tv:
          type:
            - object
            - 'null'
          additionalProperties: true
        film_tv_award:
          type:
            - object
            - 'null'
          additionalProperties: true
        funding_round:
          type:
            - object
            - 'null'
          additionalProperties: true
        music_group:
          type:
            - object
            - 'null'
          additionalProperties: true
        music_recording:
          type:
            - object
            - 'null'
          additionalProperties: true
        video_game:
          type:
            - object
            - 'null'
          additionalProperties: true
        game_series:
          type:
            - object
            - 'null'
          additionalProperties: true
        game_award:
          type:
            - object
            - 'null'
          additionalProperties: true
        stage_production:
          type:
            - object
            - 'null'
          additionalProperties: true
        stage_award:
          type:
            - object
            - 'null'
          additionalProperties: true
        paths_to_person:
          type: array
          items:
            type: object
            additionalProperties: true
        paths_to_company:
          type: array
          items:
            type: object
            additionalProperties: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListCollectionNodesResponse:
      type: object
      required:
        - items
        - total
        - page
        - per_page
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CollectionNode'
        total:
          type: integer
          format: int64
        page:
          type: integer
          format: int32
        per_page:
          type: integer
          format: int32
    ListCollectionNodesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListCollectionNodesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddCollectionNodesRequest:
      type: object
      required:
        - collection_id
      description: |
        Bulk-pin entities to a collection. `nodes` is the shape every kind uses;
        `person_ids` and `company_ids` remain as aliases for the two kinds older
        clients send. Every list is optional; an empty request is a no-op.
      properties:
        collection_id:
          type: string
          format: uuid
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/NodeRef'
        person_ids:
          type: array
          items:
            type: string
            format: uuid
        company_ids:
          type: array
          items:
            type: string
            format: uuid
    AddCollectionNodesResponse:
      type: object
      required:
        - added
      properties:
        added:
          type: integer
          description: Number of nodes newly added (already-pinned entities are skipped).
    AddCollectionNodesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/AddCollectionNodesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MoveCollectionNodesRequest:
      type: object
      required:
        - collection_node_ids
        - collection_id
      description: |
        Move the listed nodes into collection_id. A node already present in the
        target (same entity) is merged: its source row is removed rather than
        duplicated.
      properties:
        collection_node_ids:
          type: array
          minItems: 1
          items:
            type: string
            format: uuid
        collection_id:
          type: string
          format: uuid
          description: Target collection.
    MoveCollectionNodesResponse:
      type: object
      required:
        - moved
        - merged
      properties:
        moved:
          type: integer
          description: Nodes re-pointed to the target collection.
        merged:
          type: integer
          description: Nodes already present in the target; the source row was removed.
    MoveCollectionNodesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/MoveCollectionNodesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AssignCollectionNodeRequest:
      type: object
      required:
        - user_id
        - label
      description: Assign a node to a user, in a labelled context.
      properties:
        user_id:
          type: string
          format: uuid
          description: The user the node is assigned to.
        label:
          type: string
          description: Assignment context. The value space may grow over time.
          enum:
            - collection
            - team
            - person
            - company
    CollectionNodeAssignment:
      type: object
      required:
        - id
        - node_id
        - assigned_to_user_id
        - label
        - created_at
      properties:
        id:
          type: string
          format: uuid
        node_id:
          type: string
          format: uuid
        assigned_to_user_id:
          type: string
          format: uuid
        assigned_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
        label:
          type: string
          enum:
            - collection
            - team
            - person
            - company
        created_at:
          type: string
          format: date-time
    CollectionNodeAssignmentEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionNodeAssignment'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RateCollectionNodeRequest:
      type: object
      required:
        - user_id
        - rating
      description: Rate a node 1-5 on behalf of user_id.
      properties:
        user_id:
          type: string
          format: uuid
          description: The user the rating is recorded for.
        rating:
          type: integer
          format: int32
          minimum: 1
          maximum: 5
    CollectionNodeRatingSummary:
      type: object
      required:
        - average
        - rater_count
      description: A node's aggregate score after a rating.
      properties:
        average:
          type: number
          format: double
          description: Mean score (full precision; the node stores it rounded to 1dp).
        rater_count:
          type: integer
          format: int64
          description: Number of distinct raters.
    CollectionNodeRatingSummaryEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CollectionNodeRatingSummary'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    Phone:
      type: object
      required:
        - id
        - is_primary
        - person_id
        - company_id
        - phone_number
        - country_calling_code
        - phone_extension
        - phone_type
        - connected_only
        - verified
        - created_by
        - source
        - phone_note
        - created_at
        - updated_at
      properties:
        is_primary:
          type: boolean
          description: Primary for the caller's contact; false for company or unlinked records.
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        phone_number:
          type: string
        country_calling_code:
          type:
            - string
            - 'null'
        phone_extension:
          type:
            - integer
            - 'null'
        phone_type:
          type: string
          description: |
            Phone kind. Canonical values today: mobile, office, home, work, other. Validated server-side; new values may appear without a spec change.
        connected_only:
          type: boolean
        verified:
          type: boolean
        created_by:
          type:
            - string
            - 'null'
          format: uuid
          description: Creating user; null marks a public/enrichment record.
        source:
          type:
            - string
            - 'null'
        phone_note:
          type:
            - string
            - 'null'
          description: The caller's private note on this phone.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    PhoneEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Phone'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ListPhonesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Phone'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreatePhoneRequest:
      type: object
      required:
        - phone_number
      description: |
        Exactly one of person_id / company_id must be set. phone_type
        defaults to mobile.
      properties:
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        phone_number:
          type: string
          minLength: 1
        phone_extension:
          type:
            - integer
            - 'null'
        phone_type:
          type: string
          description: Phone kind (mobile, office, home, work, other).
        phone_note:
          type:
            - string
            - 'null'
    UpdatePhoneRequest:
      type: object
      required:
        - phone_number
      description: |
        phone_note null deletes the caller's note; a string upserts it.
      properties:
        phone_number:
          type: string
          minLength: 1
        phone_extension:
          type:
            - integer
            - 'null'
        phone_type:
          type: string
          description: Phone kind (mobile, office, home, work, other).
        phone_note:
          type:
            - string
            - 'null'
    PhoneOrNullEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          oneOf:
            - $ref: '#/components/schemas/Phone'
            - type: 'null'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    Link:
      type: object
      required:
        - id
        - person_id
        - company_id
        - domain
        - domain_label
        - link_url
        - social_profile
        - source
        - created_by
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        domain:
          type:
            - string
            - 'null'
          description: Host of the link URL without www (derived server-side).
        domain_label:
          type:
            - string
            - 'null'
          description: Display label for known domains (e.g. LinkedIn, X).
        link_url:
          type: string
        social_profile:
          type: boolean
        source:
          type:
            - string
            - 'null'
        created_by:
          type:
            - string
            - 'null'
          format: uuid
          description: Creating user; null marks a public/enrichment record.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListLinksEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Link'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateLinkRequest:
      type: object
      required:
        - link_url
      description: Exactly one of person_id / company_id must be set.
      properties:
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        link_url:
          type: string
          minLength: 1
    LinkEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Link'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateLinkRequest:
      type: object
      required:
        - link_url
      properties:
        link_url:
          type: string
          minLength: 1
    SpeechRequest:
      type: object
      required:
        - text
        - voice_id
      properties:
        text:
          type: string
          maxLength: 5000
        voice_id:
          type: string
          pattern: ^[A-Za-z0-9]{1,64}$
    Transcription:
      type: object
      required:
        - text
      properties:
        text:
          type: string
    TranscriptionEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Transcription'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    EmailAddress-2:
      type: object
      required:
        - id
        - person_id
        - company_id
        - address
        - email_type
        - is_active
        - shared_publicly
        - connected_only
        - verified
        - is_primary
        - source
        - created_by
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        address:
          type: string
          format: email
        email_type:
          type: string
          description: |
            Email kind. Canonical values today: work, personal, other. Validated server-side; new values may appear without a spec change.
        is_active:
          type: boolean
        shared_publicly:
          type: boolean
        connected_only:
          type: boolean
        verified:
          type: boolean
        is_primary:
          type: boolean
          description: The caller's contact-email join flag; false outside contact-scoped reads.
        source:
          type:
            - string
            - 'null'
        created_by:
          type:
            - string
            - 'null'
          format: uuid
          description: Creating user; null marks a public/enrichment record.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    EmailAddressEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EmailAddress-2'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ListEmailAddressesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/EmailAddress-2'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateEmailAddressRequest:
      type: object
      required:
        - address
      description: |
        Exactly one of person_id / company_id must be set. email_type
        defaults to work.
      properties:
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        address:
          type: string
          format: email
        email_type:
          type: string
          description: Email kind (work, personal, other).
    UpdateEmailAddressRequest:
      type: object
      required:
        - address
      properties:
        address:
          type: string
          format: email
        email_type:
          type: string
          description: Email kind (work, personal, other).
    Address:
      type: object
      required:
        - id
        - is_primary
        - person_id
        - company_id
        - address_label
        - formatted_address
        - unit
        - number
        - address_line_1
        - address_line_2
        - postal_code
        - longitude
        - latitude
        - city
        - state_region
        - country
        - connected_only
        - is_mailing_address
        - source
        - created_by
        - address_note
        - created_at
        - updated_at
      properties:
        is_primary:
          type: boolean
          description: Primary for the caller's contact; false for company or unlinked records.
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        address_label:
          type:
            - string
            - 'null'
        formatted_address:
          type:
            - string
            - 'null'
        unit:
          type:
            - string
            - 'null'
        number:
          type:
            - string
            - 'null'
        address_line_1:
          type:
            - string
            - 'null'
        address_line_2:
          type:
            - string
            - 'null'
        postal_code:
          type:
            - string
            - 'null'
        longitude:
          type:
            - number
            - 'null'
        latitude:
          type:
            - number
            - 'null'
        city:
          type:
            - string
            - 'null'
        state_region:
          type:
            - string
            - 'null'
        country:
          type:
            - string
            - 'null'
        connected_only:
          type: boolean
        is_mailing_address:
          type: boolean
        source:
          type:
            - string
            - 'null'
        created_by:
          type:
            - string
            - 'null'
          format: uuid
          description: Creating user; null marks a public/enrichment record.
        address_note:
          type:
            - string
            - 'null'
          description: The caller's private note on this address.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AddressEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Address'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ListAddressesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Address'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddressComponents:
      type: object
      description: |
        The place as the client sends it, typically the picked Radar hit.
        Every field is optional and stored as given; nothing is resolved or
        geocoded server-side. latitude outside [-90, 90] or longitude
        outside [-180, 180] is rejected.
      properties:
        unit:
          type:
            - string
            - 'null'
        number:
          type:
            - string
            - 'null'
        address_line_1:
          type:
            - string
            - 'null'
        address_line_2:
          type:
            - string
            - 'null'
        postal_code:
          type:
            - string
            - 'null'
        city:
          type:
            - string
            - 'null'
        state_region:
          type:
            - string
            - 'null'
        country:
          type:
            - string
            - 'null'
        latitude:
          type:
            - number
            - 'null'
          minimum: -90
          maximum: 90
        longitude:
          type:
            - number
            - 'null'
          minimum: -180
          maximum: 180
    CreateAddressRequest:
      allOf:
        - $ref: '#/components/schemas/AddressComponents'
        - type: object
          required:
            - formatted_address
          description: Exactly one of person_id / company_id must be set.
          properties:
            person_id:
              type:
                - string
                - 'null'
              format: uuid
            company_id:
              type:
                - string
                - 'null'
              format: uuid
            address_label:
              type:
                - string
                - 'null'
            formatted_address:
              type: string
              minLength: 1
            address_note:
              type:
                - string
                - 'null'
    UpdateAddressRequest:
      allOf:
        - $ref: '#/components/schemas/AddressComponents'
        - type: object
          description: |
            A full replace of the place: components and coordinates follow the
            body (absent means null). formatted_address is stored as given when
            present and rebuilt from the non-empty components when absent.
            address_note null deletes the caller's note.
          properties:
            address_label:
              type:
                - string
                - 'null'
            formatted_address:
              type:
                - string
                - 'null'
            address_note:
              type:
                - string
                - 'null'
    AddressOrNullEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          oneOf:
            - $ref: '#/components/schemas/Address'
            - type: 'null'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    Date:
      type: object
      required:
        - id
        - contact_id
        - date_descriptor_id
        - date_descriptor
        - date
        - annual_recurring
        - note
        - reminder
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
          description: The caller's contact this date belongs to.
        date_descriptor_id:
          type: string
          format: uuid
        date_descriptor:
          type: string
          description: Resolved label, so a row renders without a second lookup.
        date:
          type: string
          format: date
          description: The date itself, YYYY-MM-DD (no time, no zone).
        annual_recurring:
          type: boolean
        note:
          type:
            - string
            - 'null'
        reminder:
          type: integer
          description: Days before the date to remind; 0 means no reminder.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListDatesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Date'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateDateRequest:
      type: object
      required:
        - person_id
        - date
      description: |
        One of date_descriptor_id / date_descriptor must identify the label.
      properties:
        person_id:
          type: string
          format: uuid
        date_descriptor_id:
          type:
            - string
            - 'null'
          format: uuid
        date_descriptor:
          type: string
          description: Label name; found-or-created when no id is given.
        date:
          type: string
          format: date
        annual_recurring:
          type: boolean
        note:
          type:
            - string
            - 'null'
        reminder:
          type: integer
    DateEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Date'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateDateRequest:
      type: object
      required:
        - date
      properties:
        date_descriptor_id:
          type:
            - string
            - 'null'
          format: uuid
        date_descriptor:
          type: string
        date:
          type: string
          format: date
        annual_recurring:
          type: boolean
        note:
          type:
            - string
            - 'null'
        reminder:
          type: integer
    DateDescriptor:
      type: object
      required:
        - id
        - descriptor
        - created_by
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        descriptor:
          type: string
        created_by:
          type:
            - string
            - 'null'
          format: uuid
          description: Creating user; null marks a seeded record.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListDateDescriptorsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DateDescriptor'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateDateDescriptorRequest:
      type: object
      required:
        - descriptor
      properties:
        descriptor:
          type: string
          minLength: 1
    DateDescriptorEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/DateDescriptor'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RelationshipNuggets:
      type: object
      description: |
        The distilled memory of the relationship: short phrases per key,
        null where unknown, and up to three concrete milestones.
      required:
        - how_met
        - superpower
        - kryptonite
        - cadence
        - trust
        - milestones
      properties:
        how_met:
          type:
            - string
            - 'null'
        superpower:
          type:
            - string
            - 'null'
        kryptonite:
          type:
            - string
            - 'null'
        cadence:
          type:
            - string
            - 'null'
        trust:
          type:
            - string
            - 'null'
        milestones:
          type: array
          items:
            type: string
    Relationship:
      type: object
      required:
        - id
        - user_id
        - person_id
        - contact_id
        - transcript
        - relationship_summary
        - interview_complete
        - relationship_nuggets
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        contact_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The caller's contact for the person, when they hold one.
        transcript:
          type: string
          description: 'Line-formatted `User: …` / `Assistant: …` turns.'
        relationship_summary:
          type: string
          description: The running summary, or the closing reflection once complete. Empty when none yet.
        interview_complete:
          type: boolean
        relationship_nuggets:
          oneOf:
            - $ref: '#/components/schemas/RelationshipNuggets'
            - type: 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    RelationshipOrNullEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          oneOf:
            - $ref: '#/components/schemas/Relationship'
            - type: 'null'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpsertRelationshipRequest:
      type: object
      required:
        - transcript
      properties:
        transcript:
          type: string
          maxLength: 50000
          description: The running transcript. Always replaces the stored value.
        relationship_summary:
          type: string
          maxLength: 4000
          description: Replaces the stored summary when present.
        interview_complete:
          type: boolean
          description: Replaces the stored flag when present.
        relationship_nuggets:
          oneOf:
            - $ref: '#/components/schemas/RelationshipNuggets'
            - type: 'null'
          description: Replaces the stored nuggets when present; null clears them.
    RelationshipEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Relationship'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    InterviewRequest:
      type: object
      required:
        - transcript
      properties:
        transcript:
          type: string
          maxLength: 50000
          description: The running transcript, or the literal `(no prior turns yet)` on the opening turn.
        current_summary:
          type: string
          maxLength: 4000
          description: The summary the previous turn returned, when any.
    InterviewTurn:
      type: object
      required:
        - ready
        - summary
        - next_question
        - exchange_count
        - nuggets
      properties:
        ready:
          type: boolean
          description: True when the interview closed this turn.
        summary:
          type: string
          description: Best-guess summary so far; the closing reflection when ready.
        next_question:
          type: string
          description: The single next question, plain text. Empty when ready.
        exchange_count:
          type: integer
          description: User answers in the transcript so far.
        nuggets:
          oneOf:
            - $ref: '#/components/schemas/RelationshipNuggets'
            - type: 'null'
    InterviewTurnEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/InterviewTurn'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CompanyRef-2:
      type: object
      required:
        - id
        - name
        - logo
        - domain
      description: The employer or school linked via company_id.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        logo:
          type:
            - string
            - 'null'
        domain:
          type:
            - string
            - 'null'
    WorkExperience:
      type: object
      required:
        - id
        - person_id
        - company_id
        - title
        - location
        - description
        - start_year
        - start_month
        - end_year
        - end_month
        - current
        - source
        - company
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
          description: Always set; the row is dropped if its employer isn't a known company.
        title:
          type:
            - string
            - 'null'
        location:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        start_year:
          type:
            - integer
            - 'null'
        start_month:
          type:
            - integer
            - 'null'
        end_year:
          type:
            - integer
            - 'null'
          description: Null means the position is ongoing.
        end_month:
          type:
            - integer
            - 'null'
        current:
          type: boolean
        source:
          type:
            - string
            - 'null'
        company:
          $ref: '#/components/schemas/CompanyRef-2'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListWorkExperiencesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/WorkExperience'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    EducationExperience:
      type: object
      required:
        - id
        - person_id
        - company_id
        - degree_name
        - field_of_study
        - activities_societies
        - start_year
        - start_month
        - end_year
        - end_month
        - source
        - company
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
          description: Always set; the row is dropped if its school isn't a known company.
        degree_name:
          type: array
          items:
            type: string
        field_of_study:
          type: array
          items:
            type: string
        activities_societies:
          type: array
          items:
            type: string
        start_year:
          type:
            - integer
            - 'null'
        start_month:
          type:
            - integer
            - 'null'
        end_year:
          type:
            - integer
            - 'null'
        end_month:
          type:
            - integer
            - 'null'
        source:
          type:
            - string
            - 'null'
        company:
          $ref: '#/components/schemas/CompanyRef-2'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListEducationExperiencesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/EducationExperience'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    DiscoverQuestion:
      type: object
      required:
        - id
        - question
        - processing
      description: A follow-up question on a discover.
      properties:
        id:
          type: string
          format: uuid
        question:
          type: string
        answer:
          type:
            - string
            - 'null'
        processing:
          type: boolean
          description: True while research or web enrichment is active.
    DiscoverListItem:
      type: object
      required:
        - id
        - query
        - answer
        - archived
        - processing
        - discover_questions
        - created_at
        - updated_at
      description: |
        A discover record as shown in the list — lean scalars plus its
        follow-up questions.
      properties:
        id:
          type: string
          format: uuid
        query:
          type: string
        answer:
          type:
            - string
            - 'null'
          description: The quick answer when available while enrichment continues.
        archived:
          type: boolean
        processing:
          type: boolean
          description: True until research and web enrichment finish and response cards are available.
        discover_questions:
          type: array
          items:
            $ref: '#/components/schemas/DiscoverQuestion'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListDiscoverEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      description: |
        A page of discover records. Pagination (page / pageSize / total) lives in
        meta; data is the bare item array.
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DiscoverListItem'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateDiscoverRequest:
      type: object
      description: |
        A new research query. At least one of `query` or `selected_nodes` must
        be present. The owner is taken from the session, never the body.
      properties:
        query:
          type: string
          description: |
            The natural-language research question. Required unless
            `selected_nodes` is non-empty.
        selected_nodes:
          type: array
          description: |
            Entity ids (the `node_uuid` values the node endpoints return) the
            caller already picked. When non-empty these replace the vector
            search as the answer's context. With `discover_id` and no explicit
            selection, all saved response and context nodes from that parent
            become the graph scope. Card filters and pagination do not narrow it.
          items:
            type: string
            format: uuid
        discover_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Files the new record as a follow-up question under this discover.
            The caller must own it.
        team_ids:
          type: array
          description: |
            Teams the research is scoped to. Their accepted members' person
            nodes join the caller's own as connection-path anchors. Omitted on a
            follow-up question means "inherit the parent's teams".
          items:
            type: string
            format: uuid
        deep_research:
          type: boolean
          description: |
            Widens the context set, asks the model for a more thorough
            synthesis, and also runs the Deep Discover web lane: entity search
            beyond the Universe, reported through the record's `deep` block and
            the candidates endpoint. Costs more tokens and
            latency.
          default: false
    DeepProgress:
      type: object
      required:
        - found
        - enriching
        - ready
        - leads
        - timed_out
        - response
      description: |
        Where the lane's candidates are. `found` excludes what the relevance
        gate rejected; `enriching` are seeded and awaiting Universe;
        `ready` have local rows; `leads` were kept but could never be added.
        `response` counts candidates the placement step judged to be the
        answer itself — the response cards to expect — while they are still
        enriching, so the client can skeleton that many cards.
      properties:
        found:
          type: integer
        enriching:
          type: integer
        ready:
          type: integer
        leads:
          type: integer
        timed_out:
          type: integer
        response:
          type: integer
    DeepLane:
      type: object
      required:
        - status
        - trigger
        - message
        - progress
        - answer_entities
      description: |
        The Deep Discover web lane as the client should show it. Poll the
        record while `status` is `queued`, `running`, `enriching` or
        `completing`; `complete` and `failed` are terminal. `trigger` is
        `request` when the caller set `deep_research`, `miss` when the graph
        answer cited nothing and the lane opened on its own. `message` is the
        one line to show for the current state. What the lane finds is not
        here: on completion the record's `answer` is rewritten as the sum of
        the graph research and the web findings, and the entities it added
        appear among the response nodes.
      properties:
        status:
          type: string
        trigger:
          type: string
        message:
          type: string
        progress:
          oneOf:
            - $ref: '#/components/schemas/DeepProgress'
            - type: 'null'
          description: |
            Candidate counts, loaded by the detail endpoint only; null on
            create and update responses.
        answer_entities:
          type: array
          items:
            type: string
          description: |
            On a `miss` lane, the names the early answer text is about: the
            response cards the lane owes, known the moment the answer is
            written and before any web search. Empty when the answer named
            none or the lane was requested.
    DiscoverRecord:
      type: object
      required:
        - id
        - parent_id
        - query
        - team_ids
        - archived
        - processing
        - deep
        - created_at
        - updated_at
      description: |
        A bare discover record (no follow-up questions). A question is itself
        a child discover record: parent_id points at the discover (or
        question) it hangs off, null for top-level research.
      properties:
        id:
          type: string
          format: uuid
        parent_id:
          type:
            - string
            - 'null'
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        query:
          type: string
        answer:
          type:
            - string
            - 'null'
          description: |
            The final answer, null until one exists. A graph miss that opens
            the web lane writes nothing here; show the search from `deep`
            until the lane completes and writes the aggregated answer.
        team_ids:
          type: array
          items:
            type: string
            format: uuid
        archived:
          type: boolean
        processing:
          type: boolean
          description: |
            True while the research pipeline is in flight, including a web
            lane that opened on a graph miss.
        deep:
          oneOf:
            - $ref: '#/components/schemas/DeepLane'
            - type: 'null'
          description: |
            The Deep Discover web lane, null when it never opened. It opens
            when the request sets `deep_research`, or automatically when the
            graph answer cited nothing and the Universe had nothing to say.
            While it runs `answer` may be null; when it completes the answer
            and the response nodes carry what it found.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    DiscoverDetail:
      description: A discover record plus its follow-up questions.
      allOf:
        - $ref: '#/components/schemas/DiscoverRecord'
        - type: object
          required:
            - discover_questions
          properties:
            discover_questions:
              type: array
              items:
                $ref: '#/components/schemas/DiscoverQuestion'
    DiscoverDetailEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/DiscoverDetail'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    DiscoverUpdateRequest:
      type: object
      required:
        - query
        - archived
      description: |
        Editable fields of a discover record. The owner is taken from the
        session, never the body.
      properties:
        query:
          type: string
        archived:
          type: boolean
    DiscoverRecordEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/DiscoverRecord'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    DeepCandidate:
      type: object
      required:
        - id
        - entity_type
        - name
        - anchor_kind
        - anchor
        - source_url
        - snippet
        - source_engines
        - score
        - reason
        - kept
        - status
        - bucket
        - entity_id
        - created
        - created_at
        - updated_at
      description: |
        One entity the Deep Discover web lane found. `anchor_kind` is
        `domain`, `linkedin_company`, `linkedin_person` or `none`; `status`
        is `rejected`, `lead`, `shadow`, `pending`, `polling`, `ready`,
        `timed_out` or `failed`; `bucket`, set by the fan-in, is `in_answer`,
        `in_graph`, `net_new` or `lead`. `entity_id` is the Universe id once
        the entity was seeded.
      properties:
        id:
          type: string
          format: uuid
        entity_type:
          type: string
          description: persons or companies.
        name:
          type: string
        anchor_kind:
          type: string
        anchor:
          type: string
        source_url:
          type: string
        snippet:
          type: string
        source_engines:
          type: array
          items:
            type: string
        score:
          type:
            - number
            - 'null'
        reason:
          type:
            - string
            - 'null'
        kept:
          type: boolean
          description: True when the relevance gate scored it at or above the keep threshold.
        status:
          type: string
        bucket:
          type:
            - string
            - 'null'
        entity_id:
          type:
            - string
            - 'null'
          format: uuid
        created:
          type: boolean
          description: True when seeding created the entity in Universe rather than matching one.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    DeepCandidateListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DeepCandidate'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    PathPerson:
      type: object
      required:
        - id
        - name
        - avatar
      description: |
        A resolved person endpoint. avatar is the same picture the node read
        shows on the card, empty when unknown.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type: string
    PathCompany:
      type: object
      required:
        - id
        - name
        - logo
      description: A resolved company endpoint. logo is null when unknown.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        logo:
          type:
            - string
            - 'null'
    PathEndpoint:
      type: object
      required:
        - entity_type
        - node_uuid
      description: |
        One end of a hop: its entity type and row id, plus the resolved person /
        company. Both are absent for a kind we hold no table for — a path can
        pass through one, and dropping the hop would break the chain.
      properties:
        entity_type:
          type: string
        node_uuid:
          type: string
        name:
          type: string
          description: Display name for other entity kinds.
        image_url:
          type:
            - string
            - 'null'
        person:
          $ref: '#/components/schemas/PathPerson'
        company:
          $ref: '#/components/schemas/PathCompany'
    NodePathHop:
      type: object
      required:
        - hop_index
        - from_entity_type
        - from_node_uuid
        - to_entity_type
        - to_node_uuid
        - edge_type
        - edge_weight
      description: |
        One hop along a path. Each endpoint carries its own entity type, so an
        endpoint of any kind resolves to the right table.
      properties:
        hop_index:
          type: integer
          format: int32
        from_entity_type:
          type: string
        from_node_uuid:
          type: string
        to_entity_type:
          type: string
        to_node_uuid:
          type: string
        edge_type:
          type: string
        edge_weight:
          type: integer
          format: int32
        edge_description:
          type:
            - string
            - 'null'
        from:
          $ref: '#/components/schemas/PathEndpoint'
        to:
          $ref: '#/components/schemas/PathEndpoint'
    NodePath:
      type: object
      required:
        - id
        - source_entity_type
        - source_node_uuid
        - target_entity_type
        - target_node_uuid
        - hops_count
        - total_weight
        - hops
      description: |
        One of the authenticated user's computed graph paths to a node's entity
        (graph.node_path), with its ordered hops. Each end is an entity type plus
        that entity's row id — the same id the master graph uses for the node.
      properties:
        id:
          type: string
          format: uuid
        source_entity_type:
          type: string
        source_node_uuid:
          type: string
        target_entity_type:
          type: string
        target_node_uuid:
          type: string
        hops_count:
          type: integer
          format: int32
        total_weight:
          type: integer
          format: int32
        hops:
          type: array
          items:
            $ref: '#/components/schemas/NodePathHop'
    PersonCompany:
      type: object
      required:
        - id
        - name
      description: A person's current company (lean).
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        logo:
          type:
            - string
            - 'null'
    ContextLocation:
      type: object
      description: 'A person''s place as text: the display string plus coordinates.'
      properties:
        formatted_location:
          type:
            - string
            - 'null'
        latitude:
          type:
            - number
            - 'null'
        longitude:
          type:
            - number
            - 'null'
    ContactEmail:
      type: object
      required:
        - id
        - email_address
        - email_type
      description: One email on a my_person contact.
      properties:
        id:
          type: string
          format: uuid
        email_address:
          type: string
        email_type:
          type: string
    MyPerson:
      type: object
      required:
        - id
        - email_addresses
      description: The authenticated user's own contact record (contacts) for a person.
      properties:
        id:
          type: string
          format: uuid
        nickname:
          type:
            - string
            - 'null'
        email_addresses:
          type: array
          items:
            $ref: '#/components/schemas/ContactEmail'
    ContextPerson:
      type: object
      required:
        - id
        - name
        - avatar
        - orbiter_connect_request_sent
        - orbiter_connect_request_received
        - orbiter_connect_blocked
        - orbiter_connection
      description: The person a node points at, with the authenticated user's contact (my_person) and connection state.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type: string
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
        company:
          $ref: '#/components/schemas/PersonCompany'
        primary_location:
          $ref: '#/components/schemas/ContextLocation'
        my_person:
          $ref: '#/components/schemas/MyPerson'
        orbiter_connect_request_sent:
          type: boolean
          description: Caller has a live outgoing connection request to this person. False once the person declines it or either side clears it.
        orbiter_connect_request_received:
          type: boolean
          description: This person has a live connection request out to the authenticated user.
        orbiter_connect_blocked:
          type: boolean
          description: This person blocked the caller. The caller cannot send a connection request; clients should hide the connect action entirely.
        orbiter_connection:
          type: boolean
          description: Caller is connected to this person.
    MyCompany:
      type: object
      required:
        - id
      description: The authenticated user's own company-contact record (company_contacts) for a company.
      properties:
        id:
          type: string
          format: uuid
    NodeCompany:
      type: object
      required:
        - id
        - name
        - logo
        - domain
        - tagline
        - short_description
        - my_company
      description: |
        The company a node points at, with the authenticated user's company
        contact (my_company). Carries everything the company card renders:
        tagline under the name, short_description as the about text. Nullable
        fields are always present, null when unknown.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        logo:
          type:
            - string
            - 'null'
        domain:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        short_description:
          type:
            - string
            - 'null'
        my_company:
          oneOf:
            - $ref: '#/components/schemas/MyCompany'
            - type: 'null'
    LiveEvent:
      type: object
      required:
        - id
        - name
        - visibility
      description: |
        The live-event a node points at (core fields only), resolved via the
        node's permanent live_event_id UUID. node_uuid is the event's single
        graph-node id and may later change type; the UUID id is the stable ref.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        display_label:
          type:
            - string
            - 'null'
        visibility:
          type: boolean
        node_uuid:
          type:
            - string
            - 'null'
        start_date:
          type:
            - string
            - 'null'
          format: date-time
        end_date:
          type:
            - string
            - 'null'
          format: date-time
        city_name:
          type:
            - string
            - 'null'
        region_name:
          type:
            - string
            - 'null'
        country_name:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        short_description:
          type:
            - string
            - 'null'
        logo_url:
          type:
            - string
            - 'null'
        website:
          type:
            - string
            - 'null'
    FilmTv:
      type: object
      required:
        - id
        - genres
      description: |
        The film/TV title a node points at (core display fields), resolved via
        the node's permanent film_tv_id UUID. year / end_year / imdb_rating /
        running_time / award_wins / award_nominations are numeric columns
        rendered as strings on the wire, so the client's parsing is unchanged.
      properties:
        id:
          type: string
          format: uuid
        node_uuid:
          type:
            - string
            - 'null'
        title:
          type:
            - string
            - 'null'
        type:
          type:
            - string
            - 'null'
        image_url:
          type:
            - string
            - 'null'
        imdb_url:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        year:
          type:
            - string
            - 'null'
        end_year:
          type:
            - string
            - 'null'
        imdb_rating:
          type:
            - string
            - 'null'
        running_time:
          type:
            - string
            - 'null'
        mpaa_rating:
          type:
            - string
            - 'null'
        genres:
          type: array
          items:
            type: string
        award_wins:
          type:
            - string
            - 'null'
        award_nominations:
          type:
            - string
            - 'null'
    FilmTvAward:
      type: object
      required:
        - id
        - shared_with
      description: |
        The film/TV award a node points at, resolved via the node's permanent
        film_tv_award_id UUID. film_tv_id is the awarded title. imdb_person_id is
        the Xano-era id kept for seeded rows; a graph-materialised award resolves
        the awarded person through persons instead.
      properties:
        id:
          type: string
          format: uuid
        node_uuid:
          type:
            - string
            - 'null'
        year:
          type:
            - integer
            - 'null'
          format: int32
        award_type:
          type:
            - string
            - 'null'
        award_description:
          type:
            - string
            - 'null'
        nominee_or_winner:
          type:
            - string
            - 'null'
        shared_with:
          type: array
          items:
            type: string
        project_title:
          type:
            - string
            - 'null'
        project_imdb_url:
          type:
            - string
            - 'null'
        project_image_url:
          type:
            - string
            - 'null'
          description: The awarded title's poster, read live from film_tv. Null when the award names no title, or that title has no image.
        film_tv_id:
          type:
            - string
            - 'null'
          format: uuid
        imdb_person_id:
          type:
            - string
            - 'null'
          format: uuid
        category:
          type:
            - string
            - 'null'
          description: The award category, e.g. Best Comedic Performance.
        source:
          type:
            - string
            - 'null'
    FundingRound:
      type: object
      required:
        - id
        - company_id
        - company_name
        - name
        - funding_type
        - money_raised
      description: |
        The funding round a node points at, resolved via the node's permanent
        funding_rounds id UUID. money_raised mirrors the company sheet
        ({value, currency, value_usd}, amounts in MILLIONS) and is the only money
        field here. investors_text is the free-text investor list the graph
        carries; structured investors live on the company sheet.
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        company_name:
          type: string
        company_logo:
          type:
            - string
            - 'null'
          description: The raising company's logo URL, null when it has none.
        name:
          type: string
        funding_type:
          type: string
          description: Round stage, e.g. SEED, SERIES_A, DEBT_FINANCING.
        announced_date:
          type:
            - string
            - 'null'
          format: date-time
        money_raised:
          description: '{value, currency, value_usd} in millions, or null.'
        investors_text:
          type:
            - string
            - 'null'
        investor_count:
          type:
            - integer
            - 'null'
          format: int32
        description:
          type:
            - string
            - 'null'
        short_description:
          type:
            - string
            - 'null'
        long_description:
          type:
            - string
            - 'null'
        source:
          type:
            - string
            - 'null'
    MusicGroup:
      type: object
      required:
        - id
        - name
        - ended
        - genres
        - tags
      description: |
        The music group a node points at, resolved via the node's permanent
        music_group id UUID. A solo artist is a person and a record label is a
        company, so neither appears here; this is bands, orchestras and choirs.
        begin_year / end_year are numeric columns rendered as strings.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        sort_name:
          type:
            - string
            - 'null'
        disambiguation:
          type:
            - string
            - 'null'
        artist_type:
          type:
            - string
            - 'null'
          description: MusicBrainz artist type, e.g. Group, Orchestra, Choir.
        country:
          type:
            - string
            - 'null'
        area:
          type:
            - string
            - 'null'
        begin_year:
          type:
            - string
            - 'null'
        end_year:
          type:
            - string
            - 'null'
        ended:
          type: boolean
        genres:
          type: array
          items:
            type: string
        tags:
          type: array
          items:
            type: string
        description:
          type:
            - string
            - 'null'
        image_url:
          type:
            - string
            - 'null'
        image_source_url:
          type:
            - string
            - 'null'
        mbid:
          type:
            - string
            - 'null'
          description: The MusicBrainz id.
    MusicRecording:
      type: object
      required:
        - id
        - title
        - video
        - genres
        - tags
      description: |
        The recording a node points at, resolved via the node's permanent
        music_recording id UUID. primary_artist_name is denormalised from the
        performing artist so the card needs no second lookup. MusicBrainz dates
        are often partial, so the year / month / day parts stand alongside
        first_release_date, which is set only when all three are known.
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        disambiguation:
          type:
            - string
            - 'null'
        primary_artist_name:
          type:
            - string
            - 'null'
        length_ms:
          type:
            - integer
            - 'null'
          format: int32
        first_release_date:
          type:
            - string
            - 'null'
          format: date-time
        first_release_year:
          type:
            - string
            - 'null'
        first_release_month:
          type:
            - string
            - 'null'
        first_release_day:
          type:
            - string
            - 'null'
        video:
          type: boolean
          description: True for a music video rather than an audio recording.
        genres:
          type: array
          items:
            type: string
        tags:
          type: array
          items:
            type: string
        cover_art_url:
          type:
            - string
            - 'null'
        cover_art_source_url:
          type:
            - string
            - 'null'
        mbid:
          type:
            - string
            - 'null'
          description: The MusicBrainz id.
    VideoGame:
      type: object
      required:
        - id
        - name
        - tags
        - genres
        - steam_app_ids
      description: |
        The video game a node points at, resolved via the node's permanent
        games.video_game id UUID (the Universe Video_Game node). The card shows
        image_url, name, first_release_year, description, the tags chips and
        the developer. first_release_date is text with date_precision (day |
        month | year) beside it, as the graph carries it.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        tags:
          type: array
          items:
            type: string
        genres:
          type: array
          items:
            type: string
        game_kind:
          type:
            - string
            - 'null'
          description: base_game, expansion, edition ... as the graph sends it.
        first_release_date:
          type:
            - string
            - 'null'
        date_precision:
          type:
            - string
            - 'null'
        first_release_year:
          type:
            - integer
            - 'null'
          format: int32
        developer:
          type:
            - string
            - 'null'
        image_url:
          type:
            - string
            - 'null'
          description: The game's artwork on the Universe media host; null when the deployment has no media host configured.
        wikidata_qid:
          type:
            - string
            - 'null'
        imdb_id:
          type:
            - string
            - 'null'
        mobygames_id:
          type:
            - string
            - 'null'
        igdb_slug:
          type:
            - string
            - 'null'
        steam_app_ids:
          type: array
          items:
            type: string
        source:
          type:
            - string
            - 'null'
    GameSeries:
      type: object
      required:
        - id
        - name
        - developers
      description: |
        The game series a node points at, resolved via the node's permanent
        games.game_series id UUID (the Universe Game_Series node). developers
        is the graph's sorted developer-name projection, so the card needs no
        join.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        series_kind:
          type:
            - string
            - 'null'
        wikidata_qid:
          type:
            - string
            - 'null'
        description:
          type:
            - string
            - 'null'
        developers:
          type: array
          items:
            type: string
        official_website:
          type:
            - string
            - 'null'
        image_url:
          type:
            - string
            - 'null'
        source:
          type:
            - string
            - 'null'
    GameAward:
      type: object
      required:
        - id
        - name
      description: |
        The game award entry a node points at (one nominee-entry result),
        resolved via the node's permanent games.game_award id UUID. The card
        shows game_image_url, category_label, award_body_label · award_year,
        description, game_name and the result badge. game_name and
        game_image_url are the awarded game's, read live from video_game.
        award_body_label and category_label are the keys rendered as titles.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type:
            - string
            - 'null'
        award_body_key:
          type:
            - string
            - 'null'
        award_body_label:
          type:
            - string
            - 'null'
        ceremony_key:
          type:
            - string
            - 'null'
        category_key:
          type:
            - string
            - 'null'
        category_label:
          type:
            - string
            - 'null'
        award_entry_key:
          type:
            - string
            - 'null'
        award_year:
          type:
            - integer
            - 'null'
          format: int32
        subject_kind:
          type:
            - string
            - 'null'
        result:
          type:
            - string
            - 'null'
          description: winner or nominee; null while unresolved.
        video_game_id:
          type:
            - string
            - 'null'
          format: uuid
        game_name:
          type:
            - string
            - 'null'
        game_image_url:
          type:
            - string
            - 'null'
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        source:
          type:
            - string
            - 'null'
    StageProduction:
      type: object
      required:
        - id
        - title
        - source_names
        - source_urls
      description: |
        The stage production a node points at, resolved via the node's
        permanent stage.stage_production id UUID (the Universe
        Stage_Production node). The card shows poster_url, title, market,
        description, the form and production_kind chips and opening_date.
        Dates are ISO days (YYYY-MM-DD).
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        form:
          type:
            - string
            - 'null'
          description: play, musical ...
        production_kind:
          type:
            - string
            - 'null'
          description: original, revival ...
        market:
          type:
            - string
            - 'null'
          description: broadway, west_end ...
        season_label:
          type:
            - string
            - 'null'
        run_status:
          type:
            - string
            - 'null'
        official_website_url:
          type:
            - string
            - 'null'
        poster_url:
          type:
            - string
            - 'null'
        first_preview_date:
          type:
            - string
            - 'null'
          format: date
        opening_date:
          type:
            - string
            - 'null'
          format: date
        closing_date:
          type:
            - string
            - 'null'
          format: date
        accepted_credit_count:
          type:
            - integer
            - 'null'
          format: int32
        credit_coverage:
          type:
            - string
            - 'null'
        source_names:
          type: array
          items:
            type: string
        source_urls:
          type: array
          items:
            type: string
        source:
          type:
            - string
            - 'null'
    StageAward:
      type: object
      required:
        - id
        - name
        - recipient_display_names
        - production_titles
      description: |
        The stage award entry a node points at (one scoped award result on a
        production), resolved via the node's permanent stage.stage_award id
        UUID. The card shows poster_url, category_name, awarding_body ·
        award_year, description, the recipient names and the outcome badge.
        poster_url is the award's own copy, else the production's.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type:
            - string
            - 'null'
        awarding_body:
          type:
            - string
            - 'null'
        awarding_body_key:
          type:
            - string
            - 'null'
        award_year:
          type:
            - integer
            - 'null'
          format: int32
        category_name:
          type:
            - string
            - 'null'
        category_key:
          type:
            - string
            - 'null'
        outcome:
          type:
            - string
            - 'null'
          description: winner, nominee or honoree.
        result_scope:
          type:
            - string
            - 'null'
        poster_url:
          type:
            - string
            - 'null'
        production_id:
          type:
            - string
            - 'null'
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        recipient_display_names:
          type: array
          items:
            type: string
        production_titles:
          type: array
          items:
            type: string
        source_url:
          type:
            - string
            - 'null'
        source:
          type:
            - string
            - 'null'
    Node:
      type: object
      required:
        - id
        - entity_type
        - node_uuid
        - paths_processing
        - created_at
        - updated_at
      description: |
        One discover_node enriched with the entity it points at. entity_type
        names the joined table (persons | companies | film_tv | film_tv_award |
        funding_rounds | music_group | music_recording | live_events |
        video_game | game_series | game_award | stage_production |
        stage_award — plain string, no enum) and node_uuid is that table's row
        id; exactly one nested entity is present.

        node_uuid is also the node's id in the master graph — one value, so a
        graph citation needs no translation. All display comes from the nested
        entity; the node carries none of its own.
      properties:
        id:
          type: string
          format: uuid
        entity_type:
          type: string
        node_uuid:
          type: string
          format: uuid
        paths_processing:
          type: boolean
          description: True while this node's connection paths are still being computed. Poll until it clears.
        node_targeted:
          type: boolean
          description: True when the authenticated user has targeted this person / company node.
        person:
          $ref: '#/components/schemas/ContextPerson'
        company:
          $ref: '#/components/schemas/NodeCompany'
        live_event:
          $ref: '#/components/schemas/LiveEvent'
        film_tv:
          $ref: '#/components/schemas/FilmTv'
        film_tv_award:
          $ref: '#/components/schemas/FilmTvAward'
        funding_round:
          $ref: '#/components/schemas/FundingRound'
        music_group:
          $ref: '#/components/schemas/MusicGroup'
        music_recording:
          $ref: '#/components/schemas/MusicRecording'
        video_game:
          $ref: '#/components/schemas/VideoGame'
        game_series:
          $ref: '#/components/schemas/GameSeries'
        game_award:
          $ref: '#/components/schemas/GameAward'
        stage_production:
          $ref: '#/components/schemas/StageProduction'
        stage_award:
          $ref: '#/components/schemas/StageAward'
        paths_to_person:
          type: array
          items:
            $ref: '#/components/schemas/NodePath'
          description: Caller's computed graph paths to the node's person.
        paths_to_company:
          type: array
          items:
            $ref: '#/components/schemas/NodePath'
          description: Caller's computed graph paths to the node's company.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    NodesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      description: |
        A page of discover graph nodes (one kind per route: context or
        response; entities the web lane adds are response nodes). Pagination
        (page / pageSize / total) lives in meta; data is the bare item array.
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Node'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    LeverageLoopSuggestion:
      type: object
      required:
        - id
        - suggestion_request_id
        - header_text
        - node_targeted
        - archived
        - paths_to_person
        - paths_to_company
        - created_at
        - updated_at
      description: 'One leverage_loop_suggestion with its primary node flattened onto it: entity_type + node_uuid name the entity, person / company carry it.'
      properties:
        id:
          type: string
          format: uuid
        suggestion_request_id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        header_text:
          type: string
        suggestion_context:
          type:
            - string
            - 'null'
        entity_type:
          type:
            - string
            - 'null'
        node_uuid:
          type:
            - string
            - 'null'
          format: uuid
        node_targeted:
          type: boolean
        archived:
          type: boolean
        person:
          oneOf:
            - $ref: '#/components/schemas/ContextPerson'
            - type: 'null'
        company:
          oneOf:
            - $ref: '#/components/schemas/NodeCompany'
            - type: 'null'
        paths_to_person:
          type: array
          items:
            $ref: '#/components/schemas/NodePath'
        paths_to_company:
          type: array
          items:
            $ref: '#/components/schemas/NodePath'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    LeverageLoop:
      type: object
      required:
        - id
        - copilot_mode
        - status
        - request_panel_title
        - request_header_title
        - modified
        - batch_id
        - failure_reason
        - leverage_loop_suggestions
        - leverage_loop_suggestion_count
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        copilot_mode:
          type: string
          description: Always `loop` on this surface.
        status:
          type: string
          description: 'One of draft, submitted, processing, suggestion, failed, archived. `processing` means the worker is filing or polling the Universe loop; `suggestion` means the batch is stored; `failed` means the pull ended without suggestions: Universe failed it, it timed out here, or it could not be stored (see `failure_reason`). A failed loop can be dispatched again.'
        request_panel_title:
          type: string
        request_header_title:
          type: string
        request_context:
          type:
            - string
            - 'null'
        class_slug:
          type:
            - string
            - 'null'
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        number_of_results:
          type:
            - integer
            - 'null'
        deliver_format:
          type:
            - string
            - 'null'
        llm_summary_context:
          description: Raw JSON (array of strings) or null.
        modified:
          type: boolean
        batch_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The id Universe's package is stored under, reserved just before the store. Read the batch via `GET /suggestion-batches/{batch_id}` once `status` is `suggestion`; a `processing` or `failed` loop may carry an id whose batch was never stored. Null before delivery.
        failure_reason:
          type:
            - string
            - 'null'
          description: Why the loop is `failed`, or what a `processing` loop is waiting on while it retries. Plain language, safe to show; technical causes stay in the server logs. Null otherwise.
        person:
          oneOf:
            - $ref: '#/components/schemas/ContextPerson'
            - type: 'null'
        leverage_loop_suggestions:
          type: array
          items:
            $ref: '#/components/schemas/LeverageLoopSuggestion'
        leverage_loop_suggestion_count:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    PageMeta:
      type: object
      description: Envelope meta for keyset lists. nextCursor is empty on the last page.
      properties:
        requestId:
          type: string
        total:
          type: integer
          format: int64
        nextCursor:
          type: string
    ListLeverageLoopsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/LeverageLoop'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/PageMeta'
    CreateLeverageLoopRequest:
      type: object
      properties:
        request_panel_title:
          type: string
          maxLength: 500
        request_header_title:
          type: string
          maxLength: 500
        request_context:
          type:
            - string
            - 'null'
        class_slug:
          type:
            - string
            - 'null'
          maxLength: 100
        person_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The person the loop is about. Omit for a generic loop.
        number_of_results:
          type:
            - integer
            - 'null'
          minimum: 1
          maximum: 200
        deliver_format:
          type:
            - string
            - 'null'
          description: One of suggestions, csv, pdf, create collection.
    LeverageLoopEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/LeverageLoop'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateLeverageLoopRequest:
      type: object
      description: Partial update; absent fields are unchanged.
      properties:
        request_panel_title:
          type: string
          maxLength: 500
        request_header_title:
          type: string
          maxLength: 500
        request_context:
          type:
            - string
            - 'null'
        status:
          type: string
          description: '`archived` from any status, or `draft` / `submitted` while the loop is still a draft. Other values are rejected.'
        number_of_results:
          type: integer
          minimum: 1
          maximum: 200
        deliver_format:
          type: string
    ListLeverageLoopSuggestionsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/LeverageLoopSuggestion'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/PageMeta'
    LeverageLoopSuggestionEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/LeverageLoopSuggestion'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    LeverageLoopAction:
      type: object
      required:
        - id
        - leverage_loop_suggestion_id
        - action_context
        - draft_email
        - warm_intro
        - message
        - archived
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        leverage_loop_suggestion_id:
          type: string
          format: uuid
        action_context:
          type: string
        draft_email:
          type: boolean
        warm_intro:
          type: boolean
        message:
          type: boolean
        archived:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListLeverageLoopActionsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/LeverageLoopAction'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/PageMeta'
    SuggestionNode:
      type: object
      required:
        - id
        - entity_type
        - node_uuid
        - label
        - node_targeted
        - paths_to_person
        - paths_to_company
        - created_at
        - updated_at
      description: A stored node plus the entity it points at, in the Discover node shape. Exactly one entity block is non-null; all are null when the entity no longer resolves.
      properties:
        id:
          type: string
          format: uuid
        entity_type:
          type: string
        node_uuid:
          type: string
          format: uuid
        label:
          type: array
          items:
            type: string
        node_targeted:
          type: boolean
        person:
          oneOf:
            - $ref: '#/components/schemas/ContextPerson'
            - type: 'null'
        company:
          oneOf:
            - $ref: '#/components/schemas/NodeCompany'
            - type: 'null'
        live_event:
          oneOf:
            - $ref: '#/components/schemas/LiveEvent'
            - type: 'null'
        film_tv:
          oneOf:
            - $ref: '#/components/schemas/FilmTv'
            - type: 'null'
        film_tv_award:
          oneOf:
            - $ref: '#/components/schemas/FilmTvAward'
            - type: 'null'
        funding_round:
          oneOf:
            - $ref: '#/components/schemas/FundingRound'
            - type: 'null'
        music_group:
          oneOf:
            - $ref: '#/components/schemas/MusicGroup'
            - type: 'null'
        music_recording:
          oneOf:
            - $ref: '#/components/schemas/MusicRecording'
            - type: 'null'
        video_game:
          oneOf:
            - $ref: '#/components/schemas/VideoGame'
            - type: 'null'
        game_series:
          oneOf:
            - $ref: '#/components/schemas/GameSeries'
            - type: 'null'
        game_award:
          oneOf:
            - $ref: '#/components/schemas/GameAward'
            - type: 'null'
        stage_production:
          oneOf:
            - $ref: '#/components/schemas/StageProduction'
            - type: 'null'
        stage_award:
          oneOf:
            - $ref: '#/components/schemas/StageAward'
            - type: 'null'
        paths_to_person:
          type: array
          items:
            $ref: '#/components/schemas/NodePath'
        paths_to_company:
          type: array
          items:
            $ref: '#/components/schemas/NodePath'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    LeverageLoopTrajectory:
      type: object
      required:
        - id
        - leverage_loop_suggestion_id
        - trajectory_context
        - leverage_loop_trajectory_nodes
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        leverage_loop_suggestion_id:
          type: string
          format: uuid
        trajectory_context:
          type: string
        leverage_loop_trajectory_nodes:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionNode'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListLeverageLoopTrajectoriesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/LeverageLoopTrajectory'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/PageMeta'
    LeverageLoopQuestion:
      type: object
      required:
        - id
        - leverage_loop_suggestion_id
        - question
        - processing
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        leverage_loop_suggestion_id:
          type: string
          format: uuid
        parent_id:
          type:
            - string
            - 'null'
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        question:
          type: string
        answer:
          type:
            - string
            - 'null'
        processing:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    LeverageLoopQuestionListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/LeverageLoopQuestion'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateLeverageLoopQuestionRequest:
      type: object
      required:
        - query
      properties:
        query:
          type: string
          maxLength: 5000
        parent_id:
          type:
            - string
            - 'null'
          format: uuid
          description: An earlier question on the same suggestion to thread under.
        selected_nodes:
          type: array
          items:
            type: string
            format: uuid
          description: Entity ids to pin as context for the question.
    LeverageLoopQuestionEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/LeverageLoopQuestion'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ListSuggestionNodesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionNode'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/PageMeta'
    SuggestionWhy:
      type: object
      required:
        - headline
        - body
      properties:
        headline:
          type: string
        body:
          type: array
          items:
            type: string
    SuggestionHook:
      type: object
      required:
        - entity_type
        - node_uuid
        - angle
        - text
      properties:
        entity_type:
          type: string
        node_uuid:
          type: string
          format: uuid
        angle:
          type: string
        text:
          type: string
    SuggestionTrajectory:
      type: object
      required:
        - ref
        - rank
        - connector_node_uuids
        - body
      properties:
        ref:
          type: string
        rank:
          type: integer
        connector_node_uuids:
          type: array
          items:
            type: string
            format: uuid
        body:
          type: array
          items:
            type: string
    SuggestionSequenceStep:
      type: object
      required:
        - rank
        - headline
        - body
        - trajectory_refs
      properties:
        rank:
          type: integer
        headline:
          type: string
        body:
          type: array
          items:
            type: string
        trajectory_refs:
          type: array
          items:
            type: string
    SuggestionEntity:
      allOf:
        - $ref: '#/components/schemas/Node'
        - type: object
          required:
            - role
          properties:
            role:
              type: string
              description: The entity's role on this suggestion, e.g. `suggested`, `beneficiary`, `connector`, `target_org`.
    SuggestionActionGroup:
      type: object
      required:
        - ref
        - rank
        - trajectory_ref
      properties:
        ref:
          type: string
        rank:
          type: integer
        trajectory_ref:
          type:
            - string
            - 'null'
    SuggestionAction:
      type: object
      required:
        - ref
        - group_ref
        - action_type
        - rank
        - label
        - target_node_uuid
        - params
      properties:
        ref:
          type: string
        group_ref:
          type: string
        action_type:
          type: string
          description: One of `make_intro`, `draft_email`, `draft_sms`, `create_task`, `leverage_loop`.
        rank:
          type: integer
        label:
          type: string
        target_node_uuid:
          type:
            - string
            - 'null'
          format: uuid
        params:
          type: object
          additionalProperties: true
          description: 'Typed by `action_type`, as delivered: `draft_email` has ordered `emails[]` and `draft.subject`/`draft.message`; `draft_sms` has ordered `mobile_phones[]` and `draft.message`; `make_intro` has `workflow`, `recipients[]` (each with `emails[]`) and `steps[]`; `create_task` has `due_in_days`; `leverage_loop` is `{}`.'
    FeedSuggestion:
      type: object
      description: One stored suggestion with its batch context and hydrated nodes. `node` is the suggested entity's card in the shared node shape; `entities` are the registry entries in the same shape plus `role`. Nullable fields are present as null.
      required:
        - id
        - batch_id
        - batch_seq
        - batch_status
        - suggestion_request_id
        - request_panel_title
        - generated_at
        - source_ref
        - rank
        - entity_type
        - node_uuid
        - content_status
        - suggestion_mode
        - badge
        - why
        - score
        - key_people
        - hooks
        - trajectories
        - suggested_sequencing
        - entities
        - action_groups
        - actions
        - paths_processing
        - dismissed_at
        - created_at
        - node
      properties:
        id:
          type: string
          format: uuid
        batch_id:
          type: string
          format: uuid
        batch_seq:
          type: integer
        batch_status:
          type: string
          description: 'The owning batch''s status: `complete`, `partial` or `failed`. The feed only lists `complete`.'
        suggestion_request_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The request the batch answers; null for a batch delivered without one.
        request_panel_title:
          type: string
          description: The request's user-editable title, shown as the row title in the outcomes panel. Empty when never set; edit it with `PATCH /v1/leverage-loops/{loop_id}` (`request_panel_title`).
        generated_at:
          type: string
          format: date-time
        source_ref:
          type: string
        rank:
          type: integer
        entity_type:
          type: string
          description: 'Table the suggested entity lives in: `persons`, `companies`, ...'
        node_uuid:
          type: string
          format: uuid
        content_status:
          type: string
          description: '`ready` or `placeholder`.'
        suggestion_mode:
          type:
            - string
            - 'null'
        badge:
          type:
            - string
            - 'null'
        why:
          oneOf:
            - $ref: '#/components/schemas/SuggestionWhy'
            - type: 'null'
        score:
          type: object
          additionalProperties: true
        key_people:
          type:
            - array
            - 'null'
          items:
            type: string
            format: uuid
        hooks:
          type:
            - array
            - 'null'
          items:
            $ref: '#/components/schemas/SuggestionHook'
        trajectories:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionTrajectory'
        suggested_sequencing:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionSequenceStep'
        entities:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionEntity'
        action_groups:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionActionGroup'
        actions:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionAction'
        paths_processing:
          type: boolean
          description: True while the caller's connection paths to the suggested entity are being built.
        dismissed_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        node:
          $ref: '#/components/schemas/Node'
    ListSuggestionFeedEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/FeedSuggestion'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/PageMeta'
    SuggestionBatch:
      type: object
      required:
        - id
        - request_suggestion_id
        - request_panel_title
        - organization_id
        - requested_by
        - suggestion_type
        - beneficiary_contact_id
        - origin
        - trigger_source
        - batch_seq
        - status
        - schema_version
        - suggestion_count
        - generated_at
        - ingested_at
        - suggestions
      properties:
        id:
          type: string
          format: uuid
        request_suggestion_id:
          type:
            - string
            - 'null'
          format: uuid
        request_panel_title:
          type: string
          description: The request's user-editable title; empty for a batch with no request or an unset title.
        organization_id:
          type: string
          format: uuid
        requested_by:
          type:
            - string
            - 'null'
          format: uuid
        suggestion_type:
          type: string
        beneficiary_contact_id:
          type:
            - string
            - 'null'
          format: uuid
        origin:
          type: string
        trigger_source:
          type:
            - string
            - 'null'
        batch_seq:
          type: integer
        status:
          type: string
          description: '`complete`, `partial` or `failed`, as stored.'
        schema_version:
          type: integer
        suggestion_count:
          type: integer
        generated_at:
          type: string
          format: date-time
        ingested_at:
          type: string
          format: date-time
        suggestions:
          type: array
          items:
            $ref: '#/components/schemas/FeedSuggestion'
    SuggestionBatchEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SuggestionBatch'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateFeedSuggestionRequest:
      type: object
      required:
        - dismissed
      properties:
        dismissed:
          type: boolean
          description: true dismisses (soft-deletes) the suggestion; false restores it.
    EnsureThreadRequest:
      type: object
      required:
        - surface
        - surface_key
      additionalProperties: false
      properties:
        surface:
          type: string
          enum:
            - outcome
        surface_key:
          type: string
          minLength: 1
          maxLength: 128
        title:
          type:
            - string
            - 'null'
          minLength: 1
          maxLength: 500
    RunView:
      type: object
      description: Public run projection; lease credentials never appear here.
      properties:
        run_id:
          type: string
        input_message_id:
          type: string
        status:
          type: string
          enum:
            - active
            - complete
            - failed
            - cancelled
        failure_reason:
          type:
            - string
            - 'null'
        retryable:
          type: boolean
        retry_block_reason:
          type:
            - string
            - 'null'
        lease_expires_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    Thread:
      type: object
      properties:
        id:
          type: string
          format: uuid
        surface:
          type: string
        surface_key:
          type: string
        surface_ref_id:
          type:
            - string
            - 'null'
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        title:
          type:
            - string
            - 'null'
        transcript_version:
          type: integer
        latest_run:
          $ref: '#/components/schemas/RunView'
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    EnsureThread:
      type: object
      properties:
        conversation:
          $ref: '#/components/schemas/Thread'
        created:
          type: boolean
    EnsureThreadEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/EnsureThread'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ThreadListEnvelope:
      type: object
      properties:
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/Thread'
            next_cursor:
              type: string
            total:
              type: integer
              format: int64
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateThreadRequest-2:
      type: object
      additionalProperties: false
      description: Nonempty subset of {title, archived}; null title clears it.
      properties:
        title:
          type:
            - string
            - 'null'
          maxLength: 500
        archived:
          type: boolean
    ThreadEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/Thread'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    MessagePart:
      type: object
      description: One TanStack message part (text, tool-call, tool-result, lang).
    Message:
      type: object
      properties:
        id:
          type: string
          format: uuid
        message_id:
          type: string
          maxLength: 128
        sequence:
          type: integer
          format: int64
        revision:
          type: integer
        status:
          type: string
          enum:
            - streaming
            - complete
            - interrupted
            - error
        role:
          type: string
          enum:
            - user
            - assistant
        parts:
          type: array
          items:
            $ref: '#/components/schemas/MessagePart'
        question_context:
          type:
            - object
            - 'null'
        ui_library_version:
          type:
            - string
            - 'null'
          maxLength: 128
        run_id:
          type: string
        turn_id:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    MessageListEnvelope:
      type: object
      properties:
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/Message'
            next_cursor:
              type: string
            total:
              type: integer
              format: int64
            transcript_version:
              type: integer
            high_water_sequence:
              type: integer
              format: int64
            latest_run:
              $ref: '#/components/schemas/RunView'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CheckpointMessage:
      type: object
      required:
        - message_id
        - expected_revision
        - revision
        - status
        - role
        - parts
        - ui_library_version
        - turn_id
      additionalProperties: false
      properties:
        message_id:
          type: string
          minLength: 1
          maxLength: 128
        expected_revision:
          type: integer
          minimum: 0
        revision:
          type: integer
          minimum: 1
        status:
          type: string
          enum:
            - streaming
            - complete
            - interrupted
            - error
        role:
          type: string
          enum:
            - assistant
        parts:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/MessagePart'
        question_context:
          type:
            - object
            - 'null'
        ui_library_version:
          type:
            - string
            - 'null'
          maxLength: 128
        turn_id:
          type: string
          minLength: 1
          maxLength: 128
    CheckpointRequest:
      type: object
      required:
        - run_id
        - lease_token
        - expected_transcript_version
        - messages
      additionalProperties: false
      properties:
        run_id:
          type: string
          minLength: 1
          maxLength: 128
        lease_token:
          type: string
          format: uuid
        expected_transcript_version:
          type: integer
          minimum: 0
        messages:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: '#/components/schemas/CheckpointMessage'
    CheckpointResponse:
      type: object
      properties:
        stored_count:
          type: integer
        updated_count:
          type: integer
        replayed_count:
          type: integer
        transcript_version:
          type: integer
        last_message_id:
          type: string
    CheckpointResponseEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/CheckpointResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    InputMessage:
      type: object
      required:
        - id
        - role
        - parts
      additionalProperties: false
      properties:
        id:
          type: string
          minLength: 1
          maxLength: 128
        role:
          type: string
          enum:
            - user
        parts:
          type: array
          minItems: 1
          maxItems: 1
          items:
            $ref: '#/components/schemas/MessagePart'
    AnswerRef:
      type: object
      required:
        - message_id
        - question_id
        - context_version
      additionalProperties: false
      properties:
        message_id:
          type: string
          maxLength: 128
        question_id:
          type: string
          maxLength: 128
        context_version:
          type: integer
          minimum: 0
    Answer:
      type: object
      required:
        - kind
      additionalProperties: false
      properties:
        kind:
          type: string
          enum:
            - option
            - multi_select
            - free_text
            - skip
        values:
          type: array
          items:
            type: string
            maxLength: 200
        text:
          type:
            - string
            - 'null'
          maxLength: 8000
    StartRunRequest-2:
      type: object
      required:
        - run_id
      additionalProperties: false
      description: Exactly one of message or retry_of_run_id; answer_to and answer pair or are both null.
      properties:
        run_id:
          type: string
          minLength: 1
          maxLength: 128
        message:
          $ref: '#/components/schemas/InputMessage'
        retry_of_run_id:
          type:
            - string
            - 'null'
          maxLength: 128
        answer_to:
          $ref: '#/components/schemas/AnswerRef'
        answer:
          $ref: '#/components/schemas/Answer'
    AcceptedInput:
      type: object
      properties:
        message:
          $ref: '#/components/schemas/Message'
        answer_to:
          $ref: '#/components/schemas/AnswerRef'
        answer:
          $ref: '#/components/schemas/Answer'
        accepted_options:
          type: array
          items:
            type: object
            properties:
              label:
                type: string
              value:
                type: string
    CommittedEffect:
      type: object
      properties:
        kind:
          type: string
          enum:
            - route
            - reroute
            - context
            - handoff
        operation_id:
          type: string
        context_version:
          type: integer
    ExecutionMetadata:
      type: object
      required:
        - schema_version
        - routing_policy_version
        - accepted_turn_index
        - opening_turn_count
        - stage
        - gateway
        - intake_model_id
        - provider_policy_version
        - prompt_version
        - registry_version
        - ui_library_version
      additionalProperties: false
      properties:
        schema_version:
          type: integer
          enum:
            - 1
        routing_policy_version:
          type: string
          maxLength: 128
        accepted_turn_index:
          type: integer
          minimum: 1
        opening_turn_count:
          type: integer
          minimum: 1
        stage:
          type: string
          enum:
            - opening
            - continuation
        gateway:
          type: string
          enum:
            - openrouter
        intake_model_id:
          type: string
          maxLength: 256
        classifier_model_id:
          type:
            - string
            - 'null'
          maxLength: 256
        provider_policy_version:
          type: string
          maxLength: 128
        prompt_version:
          type: string
          maxLength: 128
        registry_version:
          type: string
          maxLength: 128
        ui_library_version:
          type: string
          maxLength: 128
        evaluation_id:
          type:
            - string
            - 'null'
          maxLength: 128
        case_id:
          type:
            - string
            - 'null'
          maxLength: 128
        variant_id:
          type:
            - string
            - 'null'
          maxLength: 128
    RunAcquisition:
      type: object
      description: Signed-harness payload; never reaches a browser bundle.
      properties:
        run_id:
          type: string
        status:
          type: string
          enum:
            - active
            - complete
            - failed
            - cancelled
        replayed:
          type: boolean
        failure_reason:
          type:
            - string
            - 'null'
        lease_token:
          type: string
          format: uuid
        lease_expires_at:
          type: string
          format: date-time
        transcript_version:
          type: integer
        accepted_message_id:
          type: string
        accepted_turn_index:
          type: integer
        accepted_input:
          $ref: '#/components/schemas/AcceptedInput'
        committed_effects:
          type: array
          items:
            $ref: '#/components/schemas/CommittedEffect'
        execution_metadata:
          $ref: '#/components/schemas/ExecutionMetadata'
        conversation_execution_policy:
          $ref: '#/components/schemas/ExecutionMetadata'
    RunAcquisitionEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/RunAcquisition'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateRunRequest:
      type: object
      required:
        - lease_token
        - action
      additionalProperties: false
      properties:
        lease_token:
          type: string
          format: uuid
        action:
          type: string
          enum:
            - renew
            - complete
            - fail
            - cancel
            - configure_execution
        reason:
          type:
            - string
            - 'null'
          enum:
            - provider_error
            - checkpoint_error
            - context_limit_reached
            - cancelled
            - internal_error
            - 'null'
        execution_metadata:
          $ref: '#/components/schemas/ExecutionMetadata'
    RunPatch:
      type: object
      properties:
        run_id:
          type: string
        status:
          type: string
          enum:
            - active
            - complete
            - failed
            - cancelled
        lease_expires_at:
          type: string
          format: date-time
        execution_metadata:
          $ref: '#/components/schemas/ExecutionMetadata'
    RunPatchEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/RunPatch'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SlotSpec:
      type: object
      properties:
        key:
          type: string
        label:
          type: string
        question:
          type: string
        required:
          type: boolean
        can_skip:
          type: boolean
        allows_multiple:
          type: boolean
        required_if:
          type:
            - object
            - 'null'
          properties:
            key:
              type: string
            equals:
              type: string
    ClassEntry:
      type: object
      properties:
        slug:
          type: string
        label:
          type: string
        prompt_asset:
          type: string
        live:
          type: boolean
        slots:
          type: array
          items:
            $ref: '#/components/schemas/SlotSpec'
    ClassesEnvelope:
      type: object
      properties:
        data:
          type: object
          properties:
            registry_version:
              type: string
            classes:
              type: array
              items:
                $ref: '#/components/schemas/ClassEntry'
            clarification_slug:
              type: string
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateDraftRequest:
      type: object
      required:
        - conversation_id
        - run_id
        - lease_token
        - operation_id
      additionalProperties: false
      properties:
        conversation_id:
          type: string
          format: uuid
        run_id:
          type: string
          minLength: 1
          maxLength: 128
        lease_token:
          type: string
          format: uuid
        operation_id:
          type: string
          enum:
            - draft-create
        class_slug:
          type:
            - string
            - 'null'
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        request_context:
          type:
            - string
            - 'null'
          maxLength: 8000
        request_panel_title:
          type: string
          maxLength: 500
    IntakeContext:
      type: object
      description: The versioned intake document. An uncertain outcome classification immediately resolves to get_advice with routing_source advice_fallback and confidence 0, indicating an application default rather than a model match. Existing saved clarifications remain answerable.
    SubmissionReceipt:
      type: object
      description: The public receipt; the stored document also carries a private approved_snapshot.
      properties:
        request_id:
          type: string
          format: uuid
        approval_id:
          type: string
          format: uuid
        approved_context_version:
          type: integer
        submitted_at:
          type: string
          format: date-time
        status:
          type: string
          enum:
            - submitted
        dispatch_started:
          type: boolean
    OutcomeRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
        conversation_id:
          type:
            - string
            - 'null'
          format: uuid
        status:
          type: string
        copilot_mode:
          type: string
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        phase:
          type: string
          enum:
            - unrouted
            - clarifying
            - collecting
            - awaiting_result_count
            - ready
            - submitted
            - handed_off
            - unavailable
        ready:
          type: boolean
        missing_fields:
          type: array
          items:
            type: string
        skipped_fields:
          type: array
          items:
            type: string
        unavailable_reason:
          type:
            - string
            - 'null'
        class_slug:
          type:
            - string
            - 'null'
        request_panel_title:
          type: string
        request_header_title:
          type: string
        request_context:
          type:
            - string
            - 'null'
        number_of_results:
          type:
            - integer
            - 'null'
          minimum: 1
          maximum: 200
        context_version:
          type: integer
        intake_context:
          $ref: '#/components/schemas/IntakeContext'
        submission_receipt:
          $ref: '#/components/schemas/SubmissionReceipt'
        latest_run:
          $ref: '#/components/schemas/RunView'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateDraftResult:
      type: object
      properties:
        request:
          $ref: '#/components/schemas/OutcomeRequest'
        created:
          type: boolean
    CreateDraftEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/CreateDraftResult'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ContextChange:
      type: object
      required:
        - kind
      additionalProperties: false
      properties:
        kind:
          type: string
          enum:
            - route
            - slots
            - reroute
            - handoff
        route:
          type:
            - object
            - 'null'
        slots:
          type:
            - object
            - 'null'
        reroute:
          type:
            - object
            - 'null'
        handoff:
          type:
            - object
            - 'null'
    ToolCheckpoint:
      type: object
      required:
        - expected_transcript_version
        - message_id
        - expected_revision
        - parts
        - tool_call_id
        - ui_library_version
      additionalProperties: false
      properties:
        expected_transcript_version:
          type: integer
          minimum: 0
        message_id:
          type: string
          minLength: 1
          maxLength: 128
        expected_revision:
          type: integer
          minimum: 0
        parts:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/MessagePart'
        tool_call_id:
          type: string
          minLength: 1
          maxLength: 128
        ui_library_version:
          type: string
          maxLength: 128
    PatchContextRequest:
      type: object
      required:
        - conversation_id
        - run_id
        - lease_token
        - operation_id
        - context_version
        - change
        - checkpoint
      additionalProperties: false
      properties:
        conversation_id:
          type: string
          format: uuid
        run_id:
          type: string
          minLength: 1
          maxLength: 128
        lease_token:
          type: string
          format: uuid
        operation_id:
          type: string
          minLength: 1
          maxLength: 128
        context_version:
          type: integer
          minimum: 0
        change:
          $ref: '#/components/schemas/ContextChange'
        checkpoint:
          $ref: '#/components/schemas/ToolCheckpoint'
    PatchContextResult:
      type: object
      properties:
        request:
          $ref: '#/components/schemas/OutcomeRequest'
        transcript_version:
          type: integer
        message_revision:
          type: integer
        message:
          $ref: '#/components/schemas/Message'
        terminal_message:
          $ref: '#/components/schemas/Message'
        run_status:
          type: string
          enum:
            - active
            - complete
    PatchContextEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/PatchContextResult'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    DispatchRequest:
      type: object
      required:
        - conversation_id
        - request_id
        - approval_id
        - expected_context_version
      additionalProperties: false
      properties:
        conversation_id:
          type: string
          format: uuid
        request_id:
          type: string
          format: uuid
        approval_id:
          type: string
          format: uuid
        expected_context_version:
          type: integer
          minimum: 0
    SubmissionReceiptEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/SubmissionReceipt'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    OutcomeRequestListEnvelope:
      type: object
      properties:
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/OutcomeRequest'
            next_cursor:
              type: string
            total:
              type: integer
              format: int64
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    OutcomeRequestEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/OutcomeRequest'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    Rebuild:
      type: object
      required:
        - scheduled
        - reason
      properties:
        scheduled:
          type: boolean
          description: Always true; the work runs on the relay's next pass.
        reason:
          type: string
          description: Why the rebuild was queued, recorded on the outbox event.
      additionalProperties: false
    RebuildEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Rebuild'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
      additionalProperties: false
    Backfill:
      type: object
      required:
        - queued
        - reason
      properties:
        queued:
          type: integer
          description: How many rebuilds were scheduled.
        reason:
          type: string
          description: Why the sweep was queued, recorded on each outbox event.
      additionalProperties: false
    BackfillEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Backfill'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
      additionalProperties: false
    Status:
      type: object
      required:
        - pending_events
      properties:
        pending_events:
          type: integer
          format: int64
          description: Outbox events not yet applied.
      additionalProperties: false
    StatusEnvelope-2:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/Status'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
      additionalProperties: false
    FileResponse:
      type: object
      required:
        - id
        - gcs_uri
        - file_source
        - status
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        gcs_uri:
          type: string
        file_source:
          type: string
        data_source:
          type: string
        file_name:
          type: string
          description: Populated by the worker after GCS stat.
        mime_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        preview_image_url:
          type: string
          format: uri
        classification:
          type: string
          description: LLM-assigned file type (e.g. pitch_deck, term_sheet).
        display_name:
          type:
            - string
            - 'null'
          description: |
            The user's rename, null until renamed. file_name keeps the
            upload-time name; clients render display_name ?? file_name.
        status:
          type: string
          description: |
            Pipeline status. Common values: pending, extracting, classifying,
            classified, completed, failed.
        xano_user_id:
          type: integer
          format: int64
        xano_file_id:
          type: integer
          format: int64
        suggestion_request_id:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListFilesResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/FileResponse'
        next_cursor:
          type: string
          description: Opaque cursor; omitted/empty when no further pages exist.
    ListFilesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListFilesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    FileMatch:
      type: object
      required:
        - id
        - file_name
        - gcs_uri
        - status
        - match_type
      description: |
        One search hit. `snippet` and `distance` are non-null only for a semantic
        match, where the ranking came from a chunk rather than the name.
      properties:
        id:
          type: string
          format: uuid
        file_name:
          type: string
        display_name:
          type:
            - string
            - 'null'
        gcs_uri:
          type: string
        classification:
          type:
            - string
            - 'null'
        status:
          type: string
        match_type:
          type: string
          description: '`name` or `semantic`.'
        snippet:
          type:
            - string
            - 'null'
          description: The nearest matching chunk's text.
        distance:
          type:
            - number
            - 'null'
          description: Cosine distance of the nearest chunk; smaller is closer.
    SearchFilesResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/FileMatch'
    SearchFilesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SearchFilesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UploadFilesRequest:
      type: object
      required:
        - file
      description: |
        Multi-file upload body. Send all text fields FIRST, then one `file` part
        per file. No markdown field: the worker extracts every file.
      properties:
        file_source:
          type: string
          maxLength: 255
          description: Free-form source label. Defaults to `user-upload`.
        contact_id:
          type: string
          format: uuid
          description: |
            Attach every file in this request to this contact. Must be one of the
            caller's own contacts, else 404. May be combined with
            `company_contact_id`: a deck can belong to a person and their company.
        company_contact_id:
          type: string
          format: uuid
          description: Attach every file to this company contact. Must be the caller's.
        file:
          type: array
          items:
            type: string
            format: binary
          description: |
            One or more file parts, each named `file`. Send them LAST, after
            every text field.
    UploadedFile:
      type: object
      required:
        - name
      description: |
        One element of an upload result. Either the success fields or `error` is
        populated, never both.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: The file's name; the rename if one exists, else the uploaded name.
        path:
          type: string
          description: Public GCS URL of the stored object.
        status:
          type: string
          description: Always `pending` here; the pipeline runs asynchronously.
        error:
          type:
            - object
            - 'null'
          description: Present only for a file that failed.
          properties:
            code:
              type: string
            message:
              type: string
    UploadFilesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/UploadedFile'
        error:
          type: 'null'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                uploaded:
                  type: integer
                failed:
                  type: integer
    FileResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/FileResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UploadFileResponse:
      description: |
        FileResponse plus the document markdown — extracted synchronously by
        the raw endpoint, or echoed from the supplied value by the markdown
        endpoint. List/get responses omit markdown to stay lean.
      allOf:
        - $ref: '#/components/schemas/FileResponse'
        - type: object
          required:
            - markdown
          properties:
            markdown:
              type: string
    UploadFileResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/UploadFileResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    FilePreview:
      type: object
      required:
        - url
        - expires_at
      properties:
        url:
          type: string
          format: uri
          description: Signed, short-lived link to the preview image.
        expires_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When the link stops working. Null when the preview is hosted
            elsewhere and carries its own lifetime.
    FilePreviewEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/FilePreview'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RenameFileRequest:
      type: object
      required:
        - file_name
      properties:
        file_name:
          type: string
          maxLength: 255
          description: The new display name. Stored in `display_name`.
    CreateFileRequest:
      type: object
      required:
        - gcs_uri
        - file_source
        - data_source
      properties:
        gcs_uri:
          type: string
          description: |
            Full GCS URI of the uploaded object (e.g. `gs://bucket/path/to.pdf`).
            The worker reads metadata (name, mime, size) from the object.
        file_source:
          type: string
          maxLength: 255
          description: Free-form source label, e.g. "linkedin-import", "manual-upload".
        data_source:
          type: string
          maxLength: 64
          description: |
            Xano datasource scope — live | staging | sandbox | etc.
            Required because every file currently originates from a Xano-managed source.
        xano_user_id:
          type:
            - integer
            - 'null'
          format: int64
          description: Xano user id pass-through. Body wins over JWT when both are present.
        file_id:
          type:
            - integer
            - 'null'
          format: int64
          description: Xano file id pass-through (round-tripped to webhooks).
        suggestion_request_id:
          type:
            - integer
            - 'null'
          format: int64
          description: Xano suggestion-request id (only set for suggestion-flow ingestions).
    UploadFileRawRequest:
      type: object
      required:
        - file_source
        - data_source
        - file
      description: |
        Raw direct-upload body. No markdown — the worker extracts. The `file`
        part must be sent LAST (streamed to GCS in one pass).
      properties:
        file_source:
          type: string
          maxLength: 255
          description: Free-form source label, e.g. "manual-upload".
        data_source:
          type: string
          maxLength: 64
          description: Xano datasource scope — live | staging | sandbox | etc.
        xano_user_id:
          type: integer
          format: int64
          description: Optional Xano user id pass-through.
        file_id:
          type: integer
          format: int64
          description: Optional Xano file id pass-through.
        suggestion_request_id:
          type: integer
          format: int64
          description: Optional Xano suggestion-request id pass-through.
        file:
          type: string
          format: binary
          description: The file bytes to upload. Send this part LAST.
    UploadFileMarkdownRequest:
      description: |
        Raw upload body plus pre-extracted `markdown`; when present the worker
        stores it and skips re-extraction.
      allOf:
        - $ref: '#/components/schemas/UploadFileRawRequest'
        - type: object
          properties:
            markdown:
              type: string
              description: Pre-extracted document markdown.
    EventTypesResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            type: string
    EventTypesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/EventTypesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SubscriptionResponse:
      type: object
      required:
        - id
        - environment
        - event_type
        - url
        - enabled
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - 'null'
          format: uuid
        organization_id:
          type:
            - string
            - 'null'
          format: uuid
        environment:
          type: string
        event_type:
          type: string
        url:
          type: string
          format: uri
        enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListSubscriptionsResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SubscriptionResponse'
    ListSubscriptionsResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListSubscriptionsResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateSubscriptionRequest:
      type: object
      required:
        - environment
        - event_type
        - url
      properties:
        environment:
          type: string
          maxLength: 64
          description: Logical environment (e.g. live, staging, sandbox).
        event_type:
          type: string
          maxLength: 128
          description: One of the values returned by GET /webhooks/event-types.
        url:
          type: string
          format: uri
          description: Destination HTTPS endpoint that will receive deliveries.
        enabled:
          type:
            - boolean
            - 'null'
          description: Defaults to true when omitted.
    CreateSubscriptionWithSecretResponse:
      allOf:
        - $ref: '#/components/schemas/SubscriptionResponse'
        - type: object
          required:
            - secret
          properties:
            secret:
              type: string
              description: |
                HMAC signing secret. Returned **only on create** — store
                this immediately; later GETs will not include it. Used
                to verify the X-Webhook-Signature header on deliveries.
    CreateSubscriptionWithSecretResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CreateSubscriptionWithSecretResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SubscriptionResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SubscriptionResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateSubscriptionRequest:
      type: object
      required:
        - url
        - enabled
      properties:
        url:
          type: string
          format: uri
        enabled:
          type: boolean
    DeliveryResponse:
      type: object
      required:
        - id
        - subscription_id
        - event_id
        - status
        - attempts
        - max_attempts
        - created_at
      properties:
        id:
          type: string
          format: uuid
        subscription_id:
          type: string
          format: uuid
        event_id:
          type: string
        status:
          type: string
          description: |
            Delivery state. Common values: pending, in_progress, succeeded,
            failed, dead.
        attempts:
          type: integer
          format: int32
        max_attempts:
          type: integer
          format: int32
        last_error:
          type: string
        last_attempt_at:
          type: string
          format: date-time
        next_retry_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    ListDeliveriesResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/DeliveryResponse'
    ListDeliveriesResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ListDeliveriesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    DeliveryResponseEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/DeliveryResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ContactNested:
      type: object
      description: The authenticated user's contact nested in a master-person item.
      properties:
        id:
          type: string
          format: uuid
        full_name:
          type: string
        note:
          type:
            - string
            - 'null'
          description: The holder's private note on this contact, from the New Contact form.
        last_email_activity_id:
          type:
            - string
            - 'null'
          format: uuid
        last_calendar_event_activity_id:
          type:
            - string
            - 'null'
          format: uuid
        last_activity_at:
          type:
            - string
            - 'null'
          format: date-time
        last_email_activity:
          oneOf:
            - $ref: '#/components/schemas/EmailActivity'
            - type: 'null'
        last_calendar_event_activity:
          oneOf:
            - $ref: '#/components/schemas/CalendarEventActivity'
            - type: 'null'
    Person-2:
      type: object
      description: One person for GET /persons.
      properties:
        id:
          type: string
          format: uuid
        orbiter_user:
          type: boolean
        name:
          type: string
          description: Chosen public or professional name from the projection, including middle names when used publicly. Falls back to stored name parts when unavailable.
        avatar:
          type:
            - string
            - 'null'
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
        contact:
          oneOf:
            - $ref: '#/components/schemas/ContactNested'
            - type: 'null'
        company:
          oneOf:
            - $ref: '#/components/schemas/CompanyRef'
            - type: 'null'
        primary_location:
          oneOf:
            - $ref: '#/components/schemas/LocationRef'
            - type: 'null'
        orbiter_connect_request_sent:
          oneOf:
            - $ref: '#/components/schemas/ConnectionRequest'
            - type: 'null'
        orbiter_connect_request_received:
          oneOf:
            - $ref: '#/components/schemas/ConnectionRequest'
            - type: 'null'
        orbiter_connection:
          oneOf:
            - $ref: '#/components/schemas/Connection'
            - type: 'null'
    CreatePersonPhone:
      type: object
      required:
        - phone_number
      properties:
        phone_number:
          type: string
          description: Normalized to E.164 before it is stored.
        phone_extension:
          type:
            - integer
            - 'null'
    CreatePersonRequest:
      type: object
      description: The New Contact form. Strings are trimmed server-side.
      properties:
        person_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Set when url-lookup prefilled the form; the person is then reused, never re-resolved.
        full_name:
          type: string
          maxLength: 200
          description: Required unless person_id identifies a person with a stored name. Seeds the provider and fills a blank local name, split on the last whitespace. Existing names are preserved.
        title:
          type: string
        company_id:
          type:
            - string
            - 'null'
          format: uuid
          description: 'From the url-lookup company or the company picker. An unknown id fails with the field detail `company_id: unknown`.'
        company_name:
          type: string
          description: A typed company; matched case-insensitively, else created as a minimal row.
        company_domain:
          type: string
          description: Only arrives with a url-lookup company; matched against held domains.
        emails:
          type: array
          maxItems: 10
          items:
            type: string
            format: email
        phones:
          type: array
          maxItems: 10
          items:
            $ref: '#/components/schemas/CreatePersonPhone'
        profile_url:
          type: string
          description: An http(s) social-profile URL, stored as a public link on the person.
        note:
          type: string
          maxLength: 10000
          description: Stored on the caller's contact when non-blank. A blank value leaves an existing note untouched.
    PersonLookupCompany:
      type: object
      description: The person's current employer, keyed by the shared company id.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        logo:
          type:
            - string
            - 'null'
        domain:
          type:
            - string
            - 'null'
    PersonLookupEmail:
      type: object
      description: One stored address on the person.
      properties:
        address:
          type: string
        verified:
          type: boolean
        source:
          type:
            - string
            - 'null'
    PersonLookupPhone:
      type: object
      description: One stored phone on the person.
      properties:
        number:
          type: string
        type:
          type: string
        verified:
          type: boolean
        source:
          type:
            - string
            - 'null'
    PersonLookupResult:
      type: object
      description: |
        Lean person returned when a lookup matches, plus the current employer when held locally and the person's stored contact details.
      properties:
        id:
          type: string
          format: uuid
        full_name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
        company:
          oneOf:
            - $ref: '#/components/schemas/PersonLookupCompany'
            - type: 'null'
        emails:
          type: array
          items:
            $ref: '#/components/schemas/PersonLookupEmail'
        phones:
          type: array
          items:
            $ref: '#/components/schemas/PersonLookupPhone'
    CreatePersonResult:
      type: object
      required:
        - person
        - person_created
        - contact_id
        - contact_added
      properties:
        person:
          $ref: '#/components/schemas/PersonLookupResult'
        person_created:
          type: boolean
          description: True only when this call persisted the person row (201).
        contact_id:
          type: string
          format: uuid
        contact_added:
          type: boolean
          description: True only when this call created the caller's contact.
    CreatePersonEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CreatePersonResult'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    PersonLookupRequest:
      type: object
      description: The signal for POST /persons/url-lookup.
      required:
        - profile_url
      properties:
        profile_url:
          type: string
          description: |
            Any social-profile URL (LinkedIn, IMDB, X, Instagram, ...); matched against the person's stored links.
    PersonLookup:
      type: object
      description: Result of POST /persons/url-lookup.
      properties:
        found:
          type: boolean
        status:
          type: string
          description: |
            found (matched an existing person) | created (person was created via the enrichment provider on this call; enrichment continues in the background) | pending_enrichment (no match and the signal cannot seed a create yet, or the person's name has not resolved).
        person:
          oneOf:
            - $ref: '#/components/schemas/PersonLookupResult'
            - type: 'null'
    PersonInvestment:
      type: object
      description: |
        One round the person participated in. `firm_*` is populated only for
        institutional investments. `money_raised` amounts are in MILLIONS.
        Rounds held locally carry `funding_round_id` and `company_id`; deals
        that come only from the person's enrichment package carry
        `external_deal_id`, and their ids are null when the company is not
        held. Newest first.
      required:
        - funding_round_id
        - external_deal_id
        - funding_type
        - company_id
        - company_name
        - is_lead_investor
        - acquired
      properties:
        funding_round_id:
          type:
            - string
            - 'null'
          format: uuid
        external_deal_id:
          type:
            - string
            - 'null'
        funding_type:
          type: string
        money_raised:
          description: '{value, currency, value_usd} in millions, or null.'
        announced_date:
          type:
            - string
            - 'null'
          format: date
        description:
          type:
            - string
            - 'null'
        is_lead_investor:
          type: boolean
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        company_name:
          type: string
        company_logo:
          type:
            - string
            - 'null'
        firm_company_id:
          type:
            - string
            - 'null'
          format: uuid
        firm_name:
          type:
            - string
            - 'null'
        firm_logo:
          type:
            - string
            - 'null'
        acquired:
          type: boolean
        ipo_status:
          type:
            - string
            - 'null'
    PersonFinancials:
      type: object
      description: |
        The investor profile fields are flattened onto this object. They are
        zero/null when the person has no investor profile yet; the arrays still
        render.
      required:
        - investor_type
        - angel_investments
        - institutional_investments
        - exits
      properties:
        investor_type:
          type: string
        llm_investor_summary:
          type:
            - string
            - 'null'
        active_in_sector:
          type:
            - string
            - 'null'
        location:
          type:
            - string
            - 'null'
        investment_range:
          type:
            - string
            - 'null'
        investment_sweet_spot:
          type:
            - string
            - 'null'
        sectors_stages:
          type:
            - string
            - 'null'
        investments:
          type:
            - string
            - 'null'
          description: Unnormalised pipe-delimited upstream blob, e.g. "Acme | EQUITY | 2026".
        exits_summary:
          type:
            - string
            - 'null'
        derived_thesis_summary:
          type:
            - string
            - 'null'
        board_advisory_summary:
          type:
            - string
            - 'null'
        angel_investments:
          type: array
          description: Rounds the person invested in directly, excluding exits.
          items:
            $ref: '#/components/schemas/PersonInvestment'
        institutional_investments:
          type: array
          description: Rounds where the person was the named partner at an investing firm.
          items:
            $ref: '#/components/schemas/PersonInvestment'
        exits:
          type: array
          description: Investments whose company was acquired or went public.
          items:
            $ref: '#/components/schemas/PersonInvestment'
    PersonFinancialsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/PersonFinancials'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UserRef:
      type: object
      properties:
        id:
          type: string
          format: uuid
    PersonDetail:
      type: object
      description: |
        Full person detail (GET /persons/{id}). Expertise, skills, and roles are the arrays stored from the Universe projection; each is empty when no values are available. Unimplemented sections remain empty/null: work_history, education_experiences, scheduling_links, mutual_connections (→ []), birthday, financial_data (→ null).
      properties:
        id:
          type: string
          format: uuid
        node_uuid:
          type:
            - string
            - 'null'
        orbiter_user:
          type: boolean
        name:
          type: string
          description: Chosen public or professional name from the projection, including middle names when used publicly. Falls back to stored name parts when unavailable.
        avatar:
          type:
            - string
            - 'null'
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
        bio_500:
          type:
            - string
            - 'null'
        deep_bio:
          type: array
          items:
            type: string
        work_history:
          type: array
          items: {}
        education_experiences:
          type: array
          items: {}
        expertise:
          type: array
          items:
            type: string
        skills:
          type: array
          items:
            type: string
        roles:
          type: array
          items:
            type: string
        person_contact:
          oneOf:
            - $ref: '#/components/schemas/ContactNested'
            - type: 'null'
        current_company:
          oneOf:
            - $ref: '#/components/schemas/CompanyRef'
            - type: 'null'
        primary_location:
          oneOf:
            - $ref: '#/components/schemas/LocationRef'
            - type: 'null'
        orbiter_connect_request_sent:
          oneOf:
            - $ref: '#/components/schemas/ConnectionRequest'
            - type: 'null'
        orbiter_connect_request_received:
          oneOf:
            - $ref: '#/components/schemas/ConnectionRequest'
            - type: 'null'
        orbiter_connection:
          oneOf:
            - $ref: '#/components/schemas/Connection'
            - type: 'null'
        user:
          oneOf:
            - $ref: '#/components/schemas/UserRef'
            - type: 'null'
        birthday:
          type: 'null'
        scheduling_links:
          type: array
          items: {}
        node_targeted:
          type: boolean
        mutual_connections:
          type: array
          items: {}
        financial_data:
          type: 'null'
    EmailAddressRef:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email_address:
          type: string
        email_type:
          type:
            - string
            - 'null'
    ContactEmail-2:
      type: object
      properties:
        id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        email_address_id:
          type: string
          format: uuid
        is_primary:
          type: boolean
          description: Primary email for this contact.
        email_address:
          $ref: '#/components/schemas/EmailAddressRef'
    PersonNested:
      type: object
      description: The person object nested in a contact item (connections hang off it).
      properties:
        id:
          type: string
          format: uuid
        orbiter_user:
          type: boolean
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        company:
          oneOf:
            - $ref: '#/components/schemas/CompanyRef'
            - type: 'null'
        primary_location:
          oneOf:
            - $ref: '#/components/schemas/LocationRef'
            - type: 'null'
        orbiter_connect_request_sent:
          oneOf:
            - $ref: '#/components/schemas/ConnectionRequest'
            - type: 'null'
        orbiter_connect_request_received:
          oneOf:
            - $ref: '#/components/schemas/ConnectionRequest'
            - type: 'null'
        orbiter_connection:
          oneOf:
            - $ref: '#/components/schemas/Connection'
            - type: 'null'
    ContactListItem:
      type: object
      description: One enriched contact for GET /contacts.
      properties:
        id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        person_id:
          type: string
          format: uuid
        full_name:
          type: string
        nickname:
          type:
            - string
            - 'null'
        note:
          type:
            - string
            - 'null'
        status_connected:
          type:
            - boolean
            - 'null'
        last_activity_at:
          type:
            - string
            - 'null'
          format: date-time
        last_email_activity_id:
          type:
            - string
            - 'null'
          format: uuid
        last_calendar_event_activity_id:
          type:
            - string
            - 'null'
          format: uuid
        contact_emails:
          type: array
          items:
            $ref: '#/components/schemas/ContactEmail-2'
        person:
          $ref: '#/components/schemas/PersonNested'
        last_email_activity:
          oneOf:
            - $ref: '#/components/schemas/EmailActivity'
            - type: 'null'
        last_calendar_event_activity:
          oneOf:
            - $ref: '#/components/schemas/CalendarEventActivity'
            - type: 'null'
    AddContactResult:
      type: object
      properties:
        contact_id:
          type: string
          format: uuid
        full_name:
          type: string
        person_id:
          type: string
          format: uuid
        contact_added:
          type: boolean
    CloneContactsResult:
      type: object
      properties:
        donor_user_id:
          type: string
          format: uuid
        donor_contacts:
          type: integer
        cloned:
          type: integer
    ContactTableEmail:
      type: object
      required:
        - id
        - address
        - email_type
        - is_active
        - verified
        - is_primary
      properties:
        id:
          type: string
          format: uuid
        address:
          type: string
        email_type:
          type: string
        is_active:
          type: boolean
        verified:
          type: boolean
        is_primary:
          type: boolean
          description: Primary email for this contact.
    ContactTablePhone:
      type: object
      required:
        - id
        - phone_number
        - phone_extension
        - phone_type
        - is_primary
      properties:
        is_primary:
          type: boolean
        id:
          type: string
          format: uuid
        phone_number:
          type: string
        phone_extension:
          type:
            - integer
            - 'null'
        phone_type:
          type: string
    ContactTableAddress:
      type: object
      required:
        - id
        - formatted_address
        - label
        - is_primary
      properties:
        id:
          type: string
          format: uuid
        formatted_address:
          type:
            - string
            - 'null'
        label:
          type:
            - string
            - 'null'
        is_primary:
          type: boolean
    ContactPlannedActivity:
      type: object
      required:
        - id
        - kind
        - title
        - starts_at
        - ends_at
        - all_day
        - start_date
        - location
        - url
        - calendar_name
      properties:
        id:
          type: string
          format: uuid
        kind:
          type: string
          description: Currently meeting; reserved for future task integration.
        title:
          type: string
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: string
          format: date-time
        all_day:
          type: boolean
        start_date:
          type:
            - string
            - 'null'
          format: date
        location:
          type: string
        url:
          type: string
        calendar_name:
          type: string
    ContactTableRow:
      type: object
      description: One flat contact row for GET /contacts/table.
      properties:
        can_edit:
          type: boolean
          description: True only for a contact owned by the caller; shared contacts are read-only.
        contact_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Null for an unsaved collection person.
        person_id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        name:
          type: string
        first_name:
          type:
            - string
            - 'null'
        last_name:
          type:
            - string
            - 'null'
        nickname:
          type:
            - string
            - 'null'
        avatar:
          type:
            - string
            - 'null'
        orbiter_user:
          type: boolean
        current_title:
          type:
            - string
            - 'null'
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        company_name:
          type:
            - string
            - 'null'
        company_logo:
          type:
            - string
            - 'null'
        location_formatted:
          type:
            - string
            - 'null'
          description: The person's place as text.
        primary_email:
          type:
            - string
            - 'null'
          description: Contact email marked is_primary, falling back to the first link by (created_at, id) when none is marked.
        email_count:
          type: integer
        emails:
          type: array
          description: All contact-linked addresses, with the primary display address first.
          items:
            $ref: '#/components/schemas/ContactTableEmail'
        primary_phone:
          type:
            - string
            - 'null'
          description: Primary contact phone, falling back to the earliest link when unset.
        phones:
          type: array
          description: Contact-linked phones using the same access rules as the person detail panel.
          items:
            $ref: '#/components/schemas/ContactTablePhone'
        primary_address:
          type:
            - string
            - 'null'
          description: Primary linked postal address.
        addresses:
          type: array
          description: Caller-visible contact addresses, primary first.
          items:
            $ref: '#/components/schemas/ContactTableAddress'
        connected_at:
          type:
            - string
            - 'null'
          format: date-time
        next_activity_at:
          type:
            - string
            - 'null'
          format: date-time
          description: Earliest upcoming occurrence in the caller's cached calendars; null when none is available. Planned window is 18 months; calendar refresh failure is reported separately by sync-upcoming.
        next_activity:
          oneOf:
            - $ref: '#/components/schemas/ContactPlannedActivity'
            - type: 'null'
        last_activity_at:
          type:
            - string
            - 'null'
          format: date-time
          description: Latest logged interaction, or the contact's added date when none is logged. Redacted for contacts owned by other users.
        last_activity_kind:
          type:
            - string
            - 'null'
          description: Email, event, or added when the date falls back to contact creation. Redacted for contacts owned by other users.
        last_email_activity_id:
          type:
            - string
            - 'null'
          format: uuid
        last_calendar_event_id:
          type:
            - string
            - 'null'
          format: uuid
        total_email_activity:
          type: integer
        total_remote_events:
          type: integer
        total_emails:
          type:
            - integer
            - 'null'
          description: Interaction-log rollup; null when no rollup row exists.
        total_meetings:
          type:
            - integer
            - 'null'
          description: Interaction-log rollup; null when no rollup row exists.
        momentum:
          type:
            - string
            - 'null'
          description: Interaction-log rollup label; null when no rollup row exists.
        note_count:
          type: integer
          format: int64
          minimum: 0
          description: Distinct caller-owned notes naming this person, matching GET /notes?person_id. Company-only notes are excluded.
        note:
          type:
            - string
            - 'null'
          description: Single relationship-context note (How I know them), separate from the notes collection.
        visibility:
          type: string
          description: Go-validated value set (visible or private)
        organization_id:
          type:
            - string
            - 'null'
          format: uuid
        organization_name:
          type:
            - string
            - 'null'
        custom_fields:
          type: object
          description: Raw JSONB values keyed by field-definition id.
          additionalProperties: true
    PersonSummary-2:
      type: object
      properties:
        id:
          type: string
          format: uuid
        full_name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
    ContactWithSummary:
      type: object
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        nickname:
          type:
            - string
            - 'null'
        note:
          type:
            - string
            - 'null'
          description: The holder's private note on this contact, from the New Contact form.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        person:
          $ref: '#/components/schemas/PersonSummary-2'
    ContactVisibility:
      type: object
      properties:
        contact_id:
          type: string
          format: uuid
        visibility:
          type: string
          description: visible or private
    CompanyContactNested:
      type: object
      description: The authenticated user's company contact nested in a master-company item.
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        node_uuid:
          type:
            - string
            - 'null'
        contact_count:
          type: integer
    Company:
      type: object
      description: One company for GET /companies.
      properties:
        id:
          type: string
          format: uuid
        company_name:
          type: string
        logo:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        long_description:
          type:
            - string
            - 'null'
        short_description:
          type:
            - string
            - 'null'
        is_school:
          type: boolean
        company_contact:
          oneOf:
            - $ref: '#/components/schemas/CompanyContactNested'
            - type: 'null'
    CompanyCreatePhone:
      type: object
      required:
        - phone_number
      properties:
        phone_number:
          type: string
          description: Normalized to E.164 before it is stored.
        phone_extension:
          type:
            - integer
            - 'null'
    CompanyCreateRequest:
      type: object
      description: Body for POST /companies. Strings are trimmed server-side.
      required:
        - company_name
        - domain
      properties:
        company_name:
          type: string
        brand_selected:
          type: boolean
          default: false
          description: Revalidate a selected Logo.dev brand and reject conflicting company identities before adding it.
        profile_urls:
          type: array
          maxItems: 10
          items:
            type: string
            format: uri
          description: |
            Company profile URLs discovered on the original website. LinkedIn is preferred, then Crunchbase, as Universe's primary profile signal. Accepted profiles are also merged into the saved company's links.
        domain:
          type: string
          description: |
            The company's website, with or without a scheme (acme.com, https://acme.com), or its LinkedIn/Crunchbase company page (https://www.linkedin.com/company/acme).
        phone:
          oneOf:
            - $ref: '#/components/schemas/CompanyCreatePhone'
            - type: 'null'
          description: Stored as a public number on the company and linked to the caller's contact.
        note:
          type: string
          maxLength: 10000
          description: Stored on the caller's company contact when non-blank. A blank value leaves an existing note untouched.
    CompanyCreate:
      type: object
      description: Result of POST /companies.
      properties:
        company_id:
          type: string
          format: uuid
        name:
          type: string
        domain:
          type:
            - string
            - 'null'
        linkedin_url:
          type:
            - string
            - 'null'
        logo:
          type:
            - string
            - 'null'
        company_created:
          type: boolean
          description: True only when this call created the company.
        company_contact_id:
          type: string
          format: uuid
        contact_added:
          type: boolean
          description: True only when this call added the company to the caller's contacts.
    CompanyCreateEnvelope:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/CompanyCreate'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CompanyBrand:
      type: object
      required:
        - name
        - domain
        - logo_url
      properties:
        name:
          type: string
        domain:
          type: string
        logo_url:
          type: string
          format: uri
    CompanyLookupRequest:
      type: object
      description: The signal for POST /companies/url-lookup.
      required:
        - domain
      properties:
        domain:
          type: string
          description: A website URL or host, e.g. https://www.acme.com/about or acme.com.
    CompanyLookupResult:
      type: object
      description: Company metadata; logo.dev and website previews have no canonical id until saved.
      properties:
        brand_selected:
          type: boolean
          description: Logo.dev preview requiring brand validation on save.
        id:
          type:
            - string
            - 'null'
          format: uuid
        profile_urls:
          type: array
          items:
            type: string
            format: uri
          description: Company profiles published on the matching website.
        name:
          type: string
        logo:
          type:
            - string
            - 'null'
        domain:
          type:
            - string
            - 'null'
    CompanyLookup:
      type: object
      description: Result of POST /companies/url-lookup.
      properties:
        found:
          type: boolean
        status:
          type: string
          description: |
            found (preview or existing company) | created (company resolved and persisted through Universe) | manual_entry (automatic lookup exhausted; preserve the requested domain and enter a name).
        company:
          oneOf:
            - $ref: '#/components/schemas/CompanyLookupResult'
            - type: 'null'
    Employees:
      type: object
      properties:
        employee_range:
          type:
            - string
            - 'null'
        employees_citation:
          type:
            - string
            - 'null'
        employees_citation_url:
          type:
            - string
            - 'null'
    CompanyAddress:
      type: object
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        address_label:
          type:
            - string
            - 'null'
        formatted_address:
          type:
            - string
            - 'null'
        address_line_1:
          type:
            - string
            - 'null'
        address_line_2:
          type:
            - string
            - 'null'
        city:
          type:
            - string
            - 'null'
        state_region:
          type:
            - string
            - 'null'
        postal_code:
          type:
            - string
            - 'null'
        country:
          type:
            - string
            - 'null'
        created_by_id:
          type:
            - string
            - 'null'
          format: uuid
    CompanyEmail:
      type: object
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        email_address:
          type: string
        email_type:
          type: string
        active_status:
          type: boolean
        created_by_id:
          type:
            - string
            - 'null'
          format: uuid
    CompanyPhone:
      type: object
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        first_name:
          type:
            - string
            - 'null'
        phone_number:
          type: string
        phone_extension:
          type:
            - integer
            - 'null'
        phone_type:
          type: string
        created_by_id:
          type:
            - string
            - 'null'
          format: uuid
    CompanyLink:
      type: object
      properties:
        id:
          type: string
          format: uuid
        service:
          type:
            - string
            - 'null'
        service_label:
          type:
            - string
            - 'null'
        icon_url:
          type:
            - string
            - 'null'
        link_url:
          type: string
        username:
          type:
            - string
            - 'null'
        display_username:
          type:
            - string
            - 'null'
        followers:
          type:
            - integer
            - 'null'
        created_by_id:
          type:
            - string
            - 'null'
          format: uuid
    CompanyBanner:
      type: object
      properties:
        id:
          type: string
          format: uuid
        url:
          type: string
    CompanyCollection:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type:
            - string
            - 'null'
        collection_nodes_count:
          type: integer
    CompanyEmployee:
      type: object
      description: One live contact belonging to the caller with a role at this company.
      required:
        - id
        - person_id
        - name
        - avatar
        - current_title
      properties:
        id:
          type: string
          format: uuid
          description: The caller's person-contact ID.
        person_id:
          type: string
          format: uuid
        name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
          description: The role at this company, including the former role for past employees.
    CompanyDetail:
      type: object
      description: |
        Full company detail (GET /companies/{id}). Employee lists contain the caller's live contacts, even when the company is not saved. A person's current company or a current work-history role places them in current_employees; other matching work history places them in past_employees. Each person appears once, with current membership taking precedence. Financials use a separate endpoint.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        logo:
          type:
            - string
            - 'null'
        domain:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        long_description:
          type:
            - string
            - 'null'
        short_description:
          type:
            - string
            - 'null'
        founded:
          type:
            - integer
            - 'null'
        employees:
          oneOf:
            - $ref: '#/components/schemas/Employees'
            - type: 'null'
        addresses:
          type: array
          items:
            $ref: '#/components/schemas/CompanyAddress'
        emails:
          type: array
          items:
            $ref: '#/components/schemas/CompanyEmail'
        phones:
          type: array
          items:
            $ref: '#/components/schemas/CompanyPhone'
        links:
          type: array
          items:
            $ref: '#/components/schemas/CompanyLink'
        _company_banner:
          type: array
          items:
            $ref: '#/components/schemas/CompanyBanner'
        collections:
          type: array
          items:
            $ref: '#/components/schemas/CompanyCollection'
        company_contact:
          oneOf:
            - type: object
              properties:
                id:
                  type: string
                  format: uuid
            - type: 'null'
        node_targeted:
          type: boolean
        current_employees:
          type: array
          items:
            $ref: '#/components/schemas/CompanyEmployee'
        past_employees:
          type: array
          items:
            $ref: '#/components/schemas/CompanyEmployee'
        total_contacts_linked:
          type: integer
          description: Distinct contacts across both employee lists.
        show_add_button:
          type: boolean
        visible_button:
          type: string
        financial_data:
          type: 'null'
        funding:
          type: array
          items: {}
    CompanyFounderPerson:
      type: object
      description: The lean person card nested under a founder row.
      required:
        - id
        - full_name
        - avatar
        - current_title
        - bio
      properties:
        id:
          type: string
          format: uuid
        full_name:
          type: string
        avatar:
          type:
            - string
            - 'null'
        current_title:
          type:
            - string
            - 'null'
        bio:
          type:
            - string
            - 'null'
    CompanyFounder:
      type: object
      description: One founder link with the person it names, keyed by its table name.
      required:
        - id
        - company_id
        - person_id
        - person
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        person:
          $ref: '#/components/schemas/CompanyFounderPerson'
    ListCompanyFoundersEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CompanyFounder'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CompanyPeoplePage:
      type: object
      required:
        - items
        - next_cursor
      properties:
        items:
          type: array
          items:
            type: object
            required:
              - person_id
              - name
              - avatar
              - roles
            properties:
              person_id:
                type: string
                format: uuid
              name:
                type: string
              avatar:
                type:
                  - string
                  - 'null'
              roles:
                type: array
                items:
                  type: string
                description: Qualifying roles at this company.
        next_cursor:
          type: string
          description: Empty when all company neighbours have been read.
    FinancialData:
      type: object
      description: |
        A company's financial record. `acquired` and `ipo_status` are not display
        fields — the person sheet derives its exits list from them.
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        company_type:
          type:
            - string
            - 'null'
        yc_batch:
          type:
            - string
            - 'null'
        is_public:
          type: boolean
        went_public_on:
          type:
            - string
            - 'null'
          format: date
        stock_label:
          type:
            - string
            - 'null'
        ticker:
          type:
            - string
            - 'null'
        primary_exchange:
          type:
            - string
            - 'null'
        stock_link:
          type:
            - string
            - 'null'
        funding_summary:
          type:
            - string
            - 'null'
        funding_total_value:
          type:
            - number
            - 'null'
          description: Whole units, not millions.
        funding_total_currency:
          type:
            - string
            - 'null'
        funding_total_value_usd:
          type:
            - number
            - 'null'
          description: Whole units, not millions.
        total_rounds_raised:
          type: integer
        revenue:
          description: Upstream revenue object, or null.
        acquired:
          type: boolean
        acquired_by:
          type:
            - string
            - 'null'
          description: The acquirer as the enrichment provider names it.
        acquired_ticker:
          type:
            - string
            - 'null'
          description: The acquirer stock symbol, including exchange when known (e.g. NASDAQ:ADBE).
        acquired_date:
          type:
            - string
            - 'null'
          description: Acquisition date at source precision (YYYY, YYYY-MM, or YYYY-MM-DD).
        acquired_by_permalink:
          type:
            - string
            - 'null'
        acquirer_company_id:
          type:
            - string
            - 'null'
          format: uuid
        acquirer_name:
          type:
            - string
            - 'null'
        acquirer_logo:
          type:
            - string
            - 'null'
        acquisition_price:
          description: Upstream price object, or null.
        operating_status:
          type:
            - string
            - 'null'
        ipo_status:
          type:
            - string
            - 'null'
    RoundPartner:
      type: object
      description: The named partner at an investing firm, credited on its investment.
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        name:
          type:
            - string
            - 'null'
        avatar_url:
          type:
            - string
            - 'null'
    RoundInvestor:
      type: object
      description: |
        One participant in a round. `company_id` / `person_id` are null when the
        investor has no local master row; `name` is then the upstream label and
        `image_url` is null.
      required:
        - id
        - kind
        - is_lead_investor
        - partners
      properties:
        id:
          type: string
          format: uuid
        kind:
          type: string
          description: institutional (an investing firm) or angel (an individual).
        is_lead_investor:
          type: boolean
        name:
          type:
            - string
            - 'null'
        image_url:
          type:
            - string
            - 'null'
          description: The linked company's logo or person's avatar.
        company_id:
          type:
            - string
            - 'null'
          format: uuid
        person_id:
          type:
            - string
            - 'null'
          format: uuid
        partners:
          type: array
          items:
            $ref: '#/components/schemas/RoundPartner'
    FundingRound-2:
      type: object
      description: |
        One funding round. `money_raised` mirrors the upstream object
        `{value, currency, value_usd}`; amounts are in MILLIONS.
      required:
        - id
        - funding_type
        - investors
      properties:
        id:
          type: string
          format: uuid
        round_permalink:
          type:
            - string
            - 'null'
        funding_type:
          type: string
          description: Round stage, e.g. SEED, SERIES_A, DEBT_FINANCING, GRANT.
        money_raised:
          description: '{value, currency, value_usd} in millions, or null.'
        announced_date:
          type:
            - string
            - 'null'
          format: date
        description:
          type:
            - string
            - 'null'
        long_description:
          type:
            - string
            - 'null'
        extension:
          type: boolean
          description: An extension of a prior round.
        pre:
          type: boolean
          description: A pre-<stage> round.
        investors:
          type: array
          items:
            $ref: '#/components/schemas/RoundInvestor'
    CompanyFinancials:
      type: object
      required:
        - financial_data
        - funding
      properties:
        financial_data:
          oneOf:
            - $ref: '#/components/schemas/FinancialData'
            - type: 'null'
        funding:
          type: array
          items:
            $ref: '#/components/schemas/FundingRound-2'
    CompanyFinancialsEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/CompanyFinancials'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CompanyNested:
      type: object
      description: The company object nested in a company-contact item.
      properties:
        id:
          type: string
          format: uuid
        company_name:
          type: string
        logo:
          type:
            - string
            - 'null'
        tagline:
          type:
            - string
            - 'null'
        about:
          type:
            - string
            - 'null'
        about_500:
          type:
            - string
            - 'null'
        is_school:
          type: boolean
        is_vc:
          type: boolean
    CompanyContactListItem:
      type: object
      description: One enriched company contact for GET /company-contacts.
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        note:
          type:
            - string
            - 'null'
          description: The caller's private note on the contact; null until one is saved.
        company:
          $ref: '#/components/schemas/CompanyNested'
        contact_count:
          type: integer
    CompanyAddContactRequest:
      type: object
      required:
        - company_id
      properties:
        company_id:
          type: string
          format: uuid
    CompanyAddContact:
      type: object
      description: Result of POST /company-contacts.
      properties:
        company_contact_id:
          type: string
          format: uuid
        name:
          type: string
        company_id:
          type: string
          format: uuid
        contact_added:
          type: boolean
    CloneCompanyContactsResult:
      type: object
      properties:
        donor_user_id:
          type: string
          format: uuid
        donor_contacts:
          type: integer
        cloned:
          type: integer
    MergeSuspect:
      type: object
      properties:
        id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        merge_with_contact_id:
          type: string
          format: uuid
        kind:
          type: string
        match_kind:
          type: string
        confidence:
          type:
            - number
            - 'null'
        do_not_merge:
          type: boolean
        needs_rescore:
          type: boolean
        merged:
          type: boolean
        merged_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    BuildThesisRequest:
      type: object
      required:
        - context
      properties:
        master_person_id:
          type:
            - integer
            - 'null'
          format: int64
          description: Owner person id. Exactly one of the two owner ids is required.
        master_company_id:
          type:
            - integer
            - 'null'
          format: int64
          description: Owner company id. Exactly one of the two owner ids is required.
        context:
          description: Free-form investor context JSON the extraction runs over.
        force:
          type: boolean
          description: Rebuild even when a fresh thesis exists.
        data_source:
          type: string
          description: Datasource tag written on the thesis row (defaults server-side).
    BuildThesisResponse:
      type: object
      required:
        - success
        - thesis_id
        - cached
      properties:
        success:
          type: boolean
        thesis_id:
          type: string
        firm_name:
          type: string
        investor_type:
          type: string
        declared_summary:
          type: string
        derived_summary:
          type: string
        cached:
          type: boolean
          description: True when the existing thesis was returned without re-running the LLM.
        last_validated_date:
          type: string
        days_old:
          type:
            - integer
            - 'null'
        environment:
          type: string
          description: The data_source the row was tagged with.
    BuildThesisEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/BuildThesisResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ThesisFreshnessResponse:
      type: object
      required:
        - fresh
      properties:
        fresh:
          type: boolean
        reason:
          type: string
          description: 'On fresh: false — stale, no_thesis_yet, or not_ready.'
        thesis_id:
          type: string
        firm_name:
          type: string
        last_validated_date:
          type: string
        days_old:
          type:
            - integer
            - 'null'
    ThesisFreshnessEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ThesisFreshnessResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ThesisMatchRequest:
      type: object
      required:
        - pitch_profile_id
      properties:
        pitch_profile_id:
          type: string
          format: uuid
        top_n:
          type: integer
          description: Clamped to [1, 50]; missing or zero defaults to 10.
        filters:
          type: object
          description: Optional candidate-pool restrictions (case-insensitive any-of).
          properties:
            stage_focus:
              type: array
              items:
                type: string
            geography:
              type: array
              items:
                type: string
    ThesisDimensionScores:
      type: object
      description: Per-dimension cosine similarities in [0, 1].
      required:
        - founder_fit
        - problem_market
        - competitive_moat
        - traction_momentum
        - business_model
        - expansion_roadmap
      properties:
        founder_fit:
          type: number
        problem_market:
          type: number
        competitive_moat:
          type: number
        traction_momentum:
          type: number
        business_model:
          type: number
        expansion_roadmap:
          type: number
    ThesisMatch:
      type: object
      required:
        - thesis_id
        - master_company_id
        - master_person_id
        - scores
        - composite_score
      properties:
        thesis_id:
          type: string
        master_company_id:
          type:
            - integer
            - 'null'
          format: int64
          description: Exactly one of the two owner ids is non-null.
        master_person_id:
          type:
            - integer
            - 'null'
          format: int64
        firm_name:
          type: string
        investor_type:
          type: string
        scores:
          $ref: '#/components/schemas/ThesisDimensionScores'
        composite_score:
          type: number
        environment:
          type: string
    ThesisMatchResponse:
      type: object
      required:
        - matches
        - pitch_profile_id
        - total_evaluated
      properties:
        matches:
          type: array
          items:
            $ref: '#/components/schemas/ThesisMatch'
        pitch_profile_id:
          type: string
          format: uuid
        total_evaluated:
          type: integer
          format: int64
          description: Completed theses considered before user filters.
    ThesisMatchEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ThesisMatchResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    FundThesisMatchRequest:
      type: object
      required:
        - fund_profile_id
      properties:
        fund_profile_id:
          type: string
          format: uuid
        top_n:
          type: integer
          description: Clamped to [1, 200]; missing or zero defaults to 50.
        filters:
          type: object
          description: Optional candidate-pool restrictions (case-insensitive any-of).
          properties:
            stage_focus:
              type: array
              items:
                type: string
            geography:
              type: array
              items:
                type: string
            investor_type:
              type: array
              items:
                type: string
              description: Thesis investor_type values to keep, e.g. family_office, fund_of_funds, corporate_vc.
    FundDimensionScores:
      type: object
      description: |
        Per-dimension cosine similarities in [0, 1], keyed by the fund-native
        dimension. Each is the similarity of that fund narrative to the thesis
        dimension it maps onto (gp_track_record→founder_fit,
        fund_thesis→problem_market, sourcing_edge→competitive_moat,
        performance_momentum→traction_momentum, fund_economics→business_model,
        portfolio_strategy→expansion_roadmap).
      required:
        - gp_track_record
        - fund_thesis
        - sourcing_edge
        - performance_momentum
        - fund_economics
        - portfolio_strategy
      properties:
        gp_track_record:
          type: number
        fund_thesis:
          type: number
        sourcing_edge:
          type: number
        performance_momentum:
          type: number
        fund_economics:
          type: number
        portfolio_strategy:
          type: number
    FundThesisMatch:
      type: object
      required:
        - thesis_id
        - master_company_id
        - master_person_id
        - scores
        - composite_score
      properties:
        thesis_id:
          type: string
        master_company_id:
          type:
            - integer
            - 'null'
          format: int64
          description: Exactly one of the two owner ids is non-null.
        master_person_id:
          type:
            - integer
            - 'null'
          format: int64
        firm_name:
          type: string
        investor_type:
          type: string
        scores:
          $ref: '#/components/schemas/FundDimensionScores'
        composite_score:
          type: number
        environment:
          type: string
    FundThesisMatchResponse:
      type: object
      required:
        - matches
        - fund_profile_id
        - total_evaluated
      properties:
        matches:
          type: array
          items:
            $ref: '#/components/schemas/FundThesisMatch'
        fund_profile_id:
          type: string
          format: uuid
        total_evaluated:
          type: integer
          format: int64
          description: Completed theses considered before user filters.
    FundThesisMatchEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/FundThesisMatchResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ProjectRootSettings:
      type: object
      required:
        - owning_organization_id
        - team_id
        - timezone
        - agent_assistance_enabled
      description: |
        The root-only settings, already resolved: a root's own, and on a
        subproject the ones it inherits from its root. They appear here and
        nowhere else, so a client never has to tell an own setting from an
        inherited one by reading two fields.
      properties:
        owning_organization_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The organization the root belongs to. Its active owners and
            admins reach the work, and so does the root owner while they are
            still an active member. Null only if that organization was
            deleted, which removes the route without deleting the work.
        team_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The linked team, set by `PUT /projects/{root_id}/team`. Accepted
            members are editors and accepted team admins are admins,
            independently of organization membership.
        timezone:
          type: string
          description: |
            Region-qualified IANA name, such as `Africa/Nairobi`, or `UTC`. A
            root always has one, so this is a string rather than a nullable one.
          examples:
            - Africa/Nairobi
        agent_assistance_enabled:
          type: boolean
          description: |
            Whether background agent assistance runs on this tree. False
            until `PUT /projects/{root_id}/agent-settings` turns it on.
    TaskWarning:
      type: object
      required:
        - code
        - field
      description: |
        A non-blocking observation about the returned task. Warnings never
        prevent the write; they tell the UI what to surface.
      properties:
        code:
          type: string
          description: |
            Stable machine code. `target_after_due` means `target_date` is
            later than `due_date`.
          examples:
            - target_after_due
        field:
          type: string
          description: |
            The response field the warning is about. A warning always names
            one, so there is no null case for a client to branch on.
          examples:
            - target_date
    ProjectResponse:
      type: object
      required:
        - id
        - parent_project_id
        - root_project_id
        - title
        - description
        - success_criteria
        - status
        - priority
        - owner_user_id
        - target_date
        - due_date
        - next_action
        - last_progress_at
        - completed_at
        - archived_at
        - version
        - created_at
        - updated_at
        - rights
        - root_settings
        - warnings
      description: |
        The project as these routes return it. Every field listed is always
        present: a nullable one is returned as `null` rather than omitted,
        and `warnings` is `[]` rather than null.

        The stored project carries more than this: the access version, the
        creation provenance and the agent review schedule. Each is added here
        in the pull request that puts it on the wire, so a generated client
        is never given a required field the handler does not send. A guard
        test compares this property list with the Go DTO.
      properties:
        id:
          type: string
          format: uuid
        parent_project_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Null exactly on a root project, which is also the row that owns
            `root_settings` rather than inheriting them.
        root_project_id:
          type: string
          format: uuid
          description: |
            The root of this project's tree, and the scope every access check
            and every lock uses. A root holds its own id here.
        title:
          type: string
        description:
          type:
            - string
            - 'null'
        success_criteria:
          type:
            - string
            - 'null'
        status:
          type: string
          description: |
            One of `planned`, `active`, `on_hold`, `completed`, `canceled`.
            Validated in Go, not an OpenAPI enum.
          examples:
            - planned
        priority:
          type: string
          description: |
            One of `low`, `normal`, `high`, `urgent`. Validated in Go, not an
            OpenAPI enum.
          examples:
            - normal
        owner_user_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The accountable lead. It is the creator until `PUT
            /projects/{root_id}/owner` hands it over, and null once that
            user's account is removed, which keeps the work rather than the
            attribution.
        target_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`. The intended completion date.
        due_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`. The committed deadline.
        next_action:
          type:
            - string
            - 'null'
        last_progress_at:
          type: string
          format: date-time
          description: |
            When the project last really advanced. An edit through PATCH
            leaves it where it was, so a stale-work scan is not reset by
            retitling or re-prioritising.
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
          description: Null exactly while the project is not complete.
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
        version:
          type: integer
          format: int64
          description: |
            Optimistic-lock counter. Echo it as `expected_version` on the
            next write; every applied write increments it by one.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        rights:
          type: string
          description: |
            The caller's effective role on this project's root: `admin`,
            `editor` or `viewer`, the strongest of the routes that reach it.
            A caller with none of them is answered 404 instead.
          examples:
            - editor
        root_settings:
          $ref: '#/components/schemas/ProjectRootSettings'
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/TaskWarning'
    ProjectListResponse:
      type: object
      required:
        - items
        - total
        - next_cursor
      description: |
        One page of reachable root projects. `total` counts every root the
        same filters match that the caller can reach, not the page.
      properties:
        items:
          type: array
          description: The page, an empty array when nothing matches.
          items:
            $ref: '#/components/schemas/ProjectResponse'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: Where to resume, empty on the last page.
    ProjectListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ProjectListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateProjectRequest:
      type: object
      required:
        - title
      additionalProperties: false
      description: |
        The body of a project create. Unknown fields are rejected with 400,
        so an owner key, a team or an agent setting is refused rather than
        ignored. Unlike a patch, an omitted key and an explicit `null` mean
        the same thing here: there is no stored value to preserve.

        `parent_project_id` decides which of the two shapes applies.
        `owning_organization_id` and `timezone` are required when it is
        absent or null and rejected with `issue: "root_only"` when it is set.
      properties:
        parent_project_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The project this one hangs off. Absent or null creates a root.
            A value creates a subproject in that project's root, which needs
            editor access to it.
        title:
          type: string
          minLength: 1
          description: |
            Required, and not blank once trimmed: whitespace alone is 400
            with `details: [{field: "title", issue: "blank"}]`.
        description:
          type:
            - string
            - 'null'
        success_criteria:
          type:
            - string
            - 'null'
        priority:
          type: string
          description: |
            One of `low`, `normal`, `high`, `urgent`, defaulting to `normal`
            when omitted. Not nullable. Validated in Go, not an OpenAPI enum.
        owning_organization_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Required on a root: an organization the caller is an active
            member of, which is also what makes them an admin of the root
            they are creating. Another organization is 400 with `issue:
            "not_a_member"`, the same answer one that does not exist gets.
        timezone:
          type:
            - string
            - 'null'
          description: |
            Required on a root: a region-qualified IANA name the server can
            load, such as `Africa/Nairobi`, or `UTC`. A bare abbreviation
            (`EST`, `CET`, `GMT`), the `Etc/` zones, `Local` and the empty
            string are all rejected: they load as frozen offsets, so dates
            under that root would be an hour out for half the year. The status
            defaults to `planned`; there is no status field here.
          examples:
            - Africa/Nairobi
        target_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            Date only, `YYYY-MM-DD`, validated strictly: `2026-02-30` is
            rejected rather than rolled forward.
        due_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`, validated strictly.
        next_action:
          type:
            - string
            - 'null'
    ProjectEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ProjectResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateProjectRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        An omitted key preserves the stored value; a key present with `null`
        clears it. Unknown fields are rejected with 400. At least one
        patchable field must be named: a body carrying only
        `expected_version` is 400 `VALIDATION_FAILED` with the message `no
        fields to update`. A patch whose values equal the stored ones is a
        200 that writes nothing and does not bump `version`.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The project's current `version`. A mismatch is 409.
        title:
          type: string
          minLength: 1
          description: |
            Not nullable: `null` is a validation error, since a project with
            no title cannot exist. Leading and trailing whitespace does not
            count towards the minimum.
        description:
          type:
            - string
            - 'null'
        success_criteria:
          type:
            - string
            - 'null'
        status:
          type: string
          description: |
            One of `planned`, `active`, `on_hold`, `canceled`. Not nullable.
            `completed` is 409 `invalid_state` here: completing a project is
            `POST /projects/{id}/complete`. Validated in Go, not an OpenAPI
            enum.
        priority:
          type: string
          description: |
            One of `low`, `normal`, `high`, `urgent`. Not nullable: a project
            always has a priority, so send `normal` to reset it.
        target_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            Date only, `YYYY-MM-DD`, validated strictly: `2026-02-30` is
            rejected rather than rolled forward.
        due_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`, validated strictly.
        next_action:
          type:
            - string
            - 'null'
        timezone:
          type: string
          description: |
            Root only, and not nullable: a root always has a timezone, and a
            subproject has none of its own, so setting it there is 409
            `invalid_state`. A region-qualified IANA name the server can load,
            or `UTC`; a bare abbreviation or an `Etc/` zone is refused.
    MoveProjectRequest:
      type: object
      required:
        - expected_version
        - parent_project_id
      additionalProperties: false
      description: |
        Where the project goes, and the version it is being moved from.
        Unknown fields are rejected with 400.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The project's current `version`. A mismatch is 409.
        parent_project_id:
          type: string
          format: uuid
          description: |
            The destination, in the project's own root. Not nullable: a
            cleared parent would make the project a root of its own, which is
            a cross-root change V1 does not support.
    ProjectNoteRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        The body of an archive, a reopen or a completion: the version being
        acted on and, optionally, why. Which transition runs is the route,
        never a field. Unknown fields are rejected with 400.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The project's current `version`. A mismatch is 409.
        note:
          type:
            - string
            - 'null'
          description: |
            A short remark for the history row, stored as written. Whitespace
            alone is 400 with `issue: "blank"`: omit the key instead.
    GrantOrganizationAccessRequest:
      type: object
      required:
        - expected_version
        - role
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The root's current `version`. A mismatch is 409 `version_conflict`.
        role:
          type: string
          description: |
            `viewer` or `editor`. `admin` is not grantable: it comes from
            root ownership, the owning organization or a linked team.
    RootAccessResponse:
      type: object
      description: |
        What all four sharing commands answer with. It is not the project
        row: a sharing change can remove the caller's own last route, so the
        answer stays readable to the client that succeeded in making it.
      required:
        - root_project_id
        - version
        - access_version
      additionalProperties: false
      properties:
        root_project_id:
          type: string
          format: uuid
        version:
          type: integer
          format: int64
          description: The root's optimistic-lock counter, to echo on the next command.
        access_version:
          type: integer
          format: int64
          description: |
            Bumped by every permission change, in the same statement that
            writes it. A client caching effective rights refetches when it moves.
    RootAccessEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/RootAccessResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RevokeOrganizationAccessRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The root's current `version`. A mismatch is 409 `version_conflict`.
    SetProjectTeamRequest:
      type: object
      required:
        - expected_version
        - team_id
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The root's current `version`. A mismatch is 409 `version_conflict`.
        team_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The team to link, or null to clear the link. The key is required.
    SetProjectOwnerRequest:
      type: object
      required:
        - expected_version
        - owner_user_id
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The root's current `version`. A mismatch is 409 `version_conflict`.
        owner_user_id:
          type: string
          format: uuid
          description: |
            The new owner. Must be an active account and an active member of
            the root's owning organization.
    PipelineResponse:
      type: object
      required:
        - id
        - root_project_id
        - title
        - description
        - kind
        - icon_name
        - color
        - default_currency
        - position
        - configuration_version
        - board_version
        - source_template_key
        - source_template_version
        - copied_from_pipeline_id
        - archived_at
        - version
        - created_at
        - updated_at
        - rights
      properties:
        id:
          type: string
          format: uuid
        root_project_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type:
            - string
            - 'null'
        kind:
          type: string
        icon_name:
          type:
            - string
            - 'null'
        color:
          type:
            - string
            - 'null'
        default_currency:
          type:
            - string
            - 'null'
        position:
          type: string
          description: |
            The pipeline's place in the root's board order: twelve base-36
            digits that sort as bytes. Opaque to clients; moving a pipeline is
            its own command.
        configuration_version:
          type: integer
          format: int64
          description: |
            Counts edits to the rules a deal is judged by. A client holding
            cached configuration compares this to know it is stale.
        board_version:
          type: integer
          format: int64
          description: |
            Counts card membership and order changes. A board page loaded
            under an older value is refused rather than shown with a card
            twice or missing.
        source_template_key:
          type:
            - string
            - 'null'
        source_template_version:
          type:
            - string
            - 'null'
        copied_from_pipeline_id:
          type:
            - string
            - 'null'
          format: uuid
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        rights:
          type: string
          description: |
            The caller's role in the pipeline's root: `admin`, `editor` or
            `viewer`. Configuration needs `admin`.
    PipelineListResponse:
      type: object
      required:
        - items
        - total
      description: |
        A root's whole set of pipelines. There is no `next_cursor`: the list
        is not paged, so `total` is always the length of `items`.
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PipelineResponse'
        total:
          type: integer
    PipelineListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/PipelineListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreatePipelineRequest:
      type: object
      additionalProperties: false
      description: |
        `title` is required unless `starter` names a template, which carries
        one of its own.
      properties:
        title:
          type: string
          minLength: 1
          description: |
            Refused when blank once trimmed. Optional with a `starter`, whose
            title it then overrides.
        description:
          type:
            - string
            - 'null'
        kind:
          type:
            - string
            - 'null'
          description: |
            What the pipeline is for. Descriptive only: a kind never changes
            how a pipeline behaves. One of `fundraising`, `sales`,
            `partnerships`, `hiring`, `custom`; omitted or null is `custom`.
            Any other value is 400 with `issue: "unknown_value"`.
        icon_name:
          type:
            - string
            - 'null'
        color:
          type:
            - string
            - 'null'
        starter:
          type:
            - string
            - 'null'
          description: |
            A shipped template to configure the pipeline from, such as
            `investors` or `sales`. The pipeline then arrives with its stages,
            their criteria and their guidance already set, and records which
            template and revision it came from.

            With a starter the body may carry nothing else but `title`, which
            overrides the template's: a template half overridden is a
            configuration nobody reviewed. `description`, `kind`, `icon_name`,
            `color` or `default_currency` alongside one is 400
            `not_with_starter`, and an unknown key is 400 `unknown_value`.
        default_currency:
          type:
            - string
            - 'null'
          description: |
            The currency new deals in this pipeline default to, as an ISO 4217
            alpha-3 code such as `USD`. The codes are not enumerated: the
            standard adds and retires them without us, so only the shape is
            checked. A copied default currency does not redenominate deals
            that already carry one.
    PipelineEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/PipelineResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ActivityEntry:
      type: object
      required:
        - id
        - project_id
        - root_project_id
        - task_id
        - deal_id
        - event_type
        - actor_type
        - actor_user_id
        - agent_key
        - agent_run_id
        - command_id
        - action_run_id
        - entity_version
        - body
        - payload
        - visibility
        - created_at
      description: |
        One history row. History is append-only, so a row has no version of its
        own and is never edited. Every key is always present.
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type:
            - string
            - 'null'
          format: uuid
        root_project_id:
          type:
            - string
            - 'null'
          format: uuid
        task_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The task the event is about, null for a project event.
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
        event_type:
          type: string
          description: |
            What happened, such as `task.created`, `task.placed` or
            `project.archived`. The set is open and grows without a version
            change, so treat an unknown value as an event you cannot render
            rather than as an error.
        actor_type:
          type: string
          description: |
            Who acted, one of `user`, `agent` or `system`.
        actor_user_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The human behind the event, null once that account is deleted. An
            agent carries the authority of a user too.
        agent_key:
          type:
            - string
            - 'null'
        agent_run_id:
          type:
            - string
            - 'null'
          format: uuid
        command_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The command receipt this row committed with.
        action_run_id:
          type:
            - string
            - 'null'
          format: uuid
        entity_version:
          type:
            - integer
            - 'null'
          format: int64
          description: The version the change left the entity at.
        body:
          type:
            - string
            - 'null'
          description: The note the actor wrote, stored as written and never parsed.
        payload:
          type: object
          description: |
            The structured detail the event carries beyond these columns, such
            as `changed` on an edit or the scopes a placement moved between.
            Never null; an event with nothing to add carries `{}`.
        visibility:
          type: string
          description: |
            Who the row was written for: `root` for everyone who can read the
            project, `admin` for its admins alone.
        created_at:
          type: string
          format: date-time
    ActivityListResponse:
      type: object
      required:
        - items
        - total
        - next_cursor
      description: |
        One page of history, newest first. `total` counts every row the same
        filters and the same audience match, not the page.
      properties:
        items:
          type: array
          description: The page, an empty array when nothing matches.
          items:
            $ref: '#/components/schemas/ActivityEntry'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: |
            Where to resume, empty on the last page. It is the one field the
            API answers with `""` rather than `null`.
    ActivityListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ActivityListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SuggestionMyState:
      type: object
      required:
        - disposition
        - snoozed_until
        - first_viewed_at
      properties:
        disposition:
          type: string
          description: '`saved`, `snoozed` or `dismissed`. `undecided` is sent as a null `my_state`.'
        snoozed_until:
          type:
            - string
            - 'null'
          format: date-time
        first_viewed_at:
          type:
            - string
            - 'null'
          format: date-time
    SuggestionResponse:
      type: object
      required:
        - id
        - project_id
        - root_project_id
        - personal_user_id
        - task_id
        - deal_id
        - proposed_task_id
        - kind
        - suggestion_type
        - title
        - description
        - rationale
        - expected_benefit
        - suggested_for_user_id
        - action_type
        - action_payload
        - intent_key
        - agent_key
        - agent_run_id
        - model_key
        - status
        - expires_at
        - adopted_at
        - adoption_type
        - result_task_id
        - action_run_id
        - resolved_at
        - resolution_reason
        - supersedes_suggestion_id
        - my_state
        - version
        - created_at
        - updated_at
        - rights
      description: |
        One proposal. `intent_key` is its stable identity: what it is about
        rather than how it is worded, so two rewordings of one idea share it
        and a dismissal of either covers both.

        The provenance here is what a reader needs to judge it: which agent,
        model and run produced it. The context fingerprint, the resource
        versions and the evidence manifest stay server-side, because they
        describe what the agent looked at and some of that is private to one
        person.

        `my_state` is the calling user's own response and nobody else's. It is
        null when they have not answered, and null for `undecided` too: no row
        and a cleared row mean the same thing to a reader.
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type:
            - string
            - 'null'
          format: uuid
        root_project_id:
          type:
            - string
            - 'null'
          format: uuid
        personal_user_id:
          type:
            - string
            - 'null'
          format: uuid
        task_id:
          type:
            - string
            - 'null'
          format: uuid
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
        proposed_task_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The Suggested task this proposal came with, accepted through
            `POST /tasks/{id}/accept`. Null on advice with nothing to accept
            and on a change request.
        kind:
          type: string
          description: '`next_step` or `change_request`.'
        suggestion_type:
          type: string
        title:
          type: string
        description:
          type:
            - string
            - 'null'
        rationale:
          type: string
        expected_benefit:
          type:
            - string
            - 'null'
        suggested_for_user_id:
          type:
            - string
            - 'null'
          format: uuid
        action_type:
          type:
            - string
            - 'null'
        action_payload:
          description: |
            The change a change request asks for. Proposing it executes
            nothing; a person adopting it is what queues anything.
          oneOf:
            - type: object
              additionalProperties: true
            - type: 'null'
        intent_key:
          type: string
        agent_key:
          type: string
        agent_run_id:
          type:
            - string
            - 'null'
          format: uuid
        model_key:
          type:
            - string
            - 'null'
        status:
          type: string
        expires_at:
          type:
            - string
            - 'null'
          format: date-time
        adopted_at:
          type:
            - string
            - 'null'
          format: date-time
        adoption_type:
          type:
            - string
            - 'null'
        result_task_id:
          type:
            - string
            - 'null'
          format: uuid
        action_run_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The authorization a `queued_action` adoption produced. Queueing is
            adoption, not completion: read the action at
            `GET /project-actions/{id}` to find out whether it happened.
        resolved_at:
          type:
            - string
            - 'null'
          format: date-time
        resolution_reason:
          type:
            - string
            - 'null'
        supersedes_suggestion_id:
          type:
            - string
            - 'null'
          format: uuid
        my_state:
          oneOf:
            - $ref: '#/components/schemas/SuggestionMyState'
            - type: 'null'
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        rights:
          type: string
    SuggestionListResponse:
      type: object
      required:
        - items
        - total
        - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SuggestionResponse'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: Empty on the last page.
    SuggestionListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SuggestionListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SuggestionEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/SuggestionResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SuggestionMyStateRequest:
      type: object
      required:
        - disposition
      additionalProperties: false
      properties:
        disposition:
          type: string
          description: |
            `saved`, `snoozed`, `dismissed`, or `undecided` to clear the
            answer. Plain string rather than an enum: the set is closed in Go.
        snoozed_until:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            Required with `snoozed` and refused with everything else. A snooze
            with no wake time is a dismissal wearing a softer word.
        reason:
          type:
            - string
            - 'null'
        note:
          type:
            - string
            - 'null'
          description: The caller's own private note. Nobody else ever reads it.
    AgentSettingsRequest:
      type: object
      required:
        - expected_version
        - enabled
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        enabled:
          type: boolean
        review_interval_hours:
          type:
            - integer
            - 'null'
          format: int32
          minimum: 1
          description: |
            How often a review runs. Optional even when enabling: a root that
            says nothing gets twenty-four hours. Refused when `enabled` is
            false, because settings on a root nobody is reviewing are a lie a
            later sweep would act on.
        daily_budget_usd:
          type:
            - string
            - 'null'
          description: |
            What agent work may spend on this root in a day, as an exact
            decimal string. Optional even when enabling: a root that says
            nothing gets the deployment's default.
    RequestAgentRunRequest:
      type: object
      additionalProperties: false
      description: |
        What to review. Nothing here says what the review may cost: the limits
        and the budget are the deployment's and the root's decision.
      properties:
        task_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Narrow the review to one task of the same project.
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Narrow the review to one deal of the same project. Refused
            together with `task_id`, and refused on a ToDo review.
        note:
          type:
            - string
            - 'null'
    AgentRunResponse:
      type: object
      required:
        - id
        - project_id
        - root_project_id
        - personal_user_id
        - task_id
        - deal_id
        - requested_by_user_id
        - trigger_type
        - status
        - attempts
        - model_key
        - limits
        - usage
        - result_summary
        - error_code
        - started_at
        - finished_at
        - created_at
        - updated_at
        - rights
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type:
            - string
            - 'null'
          format: uuid
        root_project_id:
          type:
            - string
            - 'null'
          format: uuid
        personal_user_id:
          type:
            - string
            - 'null'
          format: uuid
        task_id:
          type:
            - string
            - 'null'
          format: uuid
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
        requested_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
        trigger_type:
          type: string
        status:
          type: string
          description: |
            `queued`, `running`, `succeeded`, `partial`, `failed`, `canceled`
            or `budget_exhausted`.
        attempts:
          type: integer
          format: int32
          description: |
            How many times this job has been claimed. A crashed worker's job
            keeps its count: a crash does not buy a job a fresh budget.
        model_key:
          type: string
        limits:
          type: object
          additionalProperties: true
          description: What this run was allowed to do, frozen when it was queued.
        usage:
          type: object
          additionalProperties: true
          description: What it has spent so far.
        result_summary:
          type:
            - string
            - 'null'
        error_code:
          type:
            - string
            - 'null'
        started_at:
          type:
            - string
            - 'null'
          format: date-time
        finished_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        rights:
          type: string
    AgentRunEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/AgentRunResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AuthorizeActionRequest:
      type: object
      required:
        - action_type
      additionalProperties: false
      properties:
        action_type:
          type: string
          description: |
            The registered action to carry out. Only an action this release can
            execute is accepted; the registry lives in reviewed code, so this
            can never introduce a tool, a URL, SQL or a shell command.

            **Introductions are three actions, not one.**
            `introduction.request_consent` asks one person, and you authorize
            it once per person. `introduction.send` makes the introduction and
            is refused until both asks actually went out and you have recorded,
            in its own `input.consents`, that each person agreed. Every message
            carries its own approval and its own window, so approving the asks
            is never approval for the introduction itself.
        project_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The project the action runs in. Required unless `personal`.
        personal:
          type: boolean
          default: false
          description: Set for an action on the caller's own ToDo.
        task_id:
          type:
            - string
            - 'null'
          format: uuid
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
        suggestion_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The proposal this carries out, recorded as provenance. A suggestion
            never authorizes anything on its own; the authorization does.
        input:
          type: object
          additionalProperties: true
          description: |
            The exact content being authorized, validated against the action's
            declared parameters. It is hashed onto the row, so a later edit
            needs a fresh authorization.
        expected_versions:
          type: object
          additionalProperties:
            type: integer
            format: int64
          description: |
            The versions the caller judged this action against, keyed by name
            (`deal`, `task`, `stakeholder`, `pipeline_configuration`). The
            worker re-reads them before acting and refuses on any that moved.
        shared:
          type: boolean
          default: false
          description: |
            Ask for an artifact the project can read. Only an artifact action
            may set it: what one person authorized is theirs unless they say
            otherwise, and a brief written for one reader is not automatically
            everybody's. Refused on a personal action, which has one possible
            audience already.
        note:
          type:
            - string
            - 'null'
          maxLength: 2000
    ActionResultRef:
      type: object
      required:
        - kind
        - evidence
        - id
        - external_id
        - media_type
        - content
      properties:
        kind:
          type: string
        evidence:
          type: string
          description: |
            How much this is worth: `provider_acknowledged` is the provider
            saying it happened, `domain_committed` is our own transaction
            saying it, and `user_reported` is one person's account. A user
            report alone never satisfies a provider criterion.
        id:
          type:
            - string
            - 'null'
          format: uuid
        external_id:
          type:
            - string
            - 'null'
        media_type:
          type:
            - string
            - 'null'
        content:
          type:
            - string
            - 'null'
          description: A prepared artifact, such as a draft or a brief.
    ActionRunResponse:
      type: object
      required:
        - id
        - action_type
        - status
        - project_id
        - root_project_id
        - task_id
        - deal_id
        - suggestion_id
        - personal
        - visibility
        - attempts
        - authorized_at
        - authorization_expires_at
        - started_at
        - finished_at
        - result_summary
        - error_code
        - results
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        action_type:
          type: string
        status:
          type: string
          description: |
            `queued`, `running`, `succeeded`, `failed`, `unknown` or
            `canceled`. `unknown` is neither done nor failed: nobody can say
            whether the side effect happened, so the action stopped and waits
            for a person. Never render `queued` as finished.
        project_id:
          type:
            - string
            - 'null'
          format: uuid
        root_project_id:
          type:
            - string
            - 'null'
          format: uuid
        task_id:
          type:
            - string
            - 'null'
          format: uuid
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
        suggestion_id:
          type:
            - string
            - 'null'
          format: uuid
        personal:
          type: boolean
        visibility:
          type: string
          description: |
            `actor` keeps the input and results with the person who authorized
            them even inside a shared project; `personal` is a ToDo action's;
            `root` is the project's.
        attempts:
          type: integer
          format: int32
        authorized_at:
          type: string
          format: date-time
        authorization_expires_at:
          type: string
          format: date-time
          description: When the authorization lapses if nothing has started it.
        started_at:
          type:
            - string
            - 'null'
          format: date-time
        finished_at:
          type:
            - string
            - 'null'
          format: date-time
        result_summary:
          type:
            - string
            - 'null'
        error_code:
          type:
            - string
            - 'null'
        input:
          type:
            - object
            - 'null'
          additionalProperties: true
          description: |
            Returned to the person who authorized the action, and to the
            project's readers when `visibility` is `root`.
        expected_versions:
          type:
            - object
            - 'null'
          additionalProperties:
            type: integer
            format: int64
          description: Returned under the same rule as `input`.
        results:
          type: array
          items:
            $ref: '#/components/schemas/ActionResultRef'
          description: |
            What the attempt produced, including a drafted artifact's text in
            `content`. Empty unless you authorized the action or it was shared
            with the project.
        last_error:
          type:
            - string
            - 'null'
          description: Returned under the same rule as `input`.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ActionRunEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ActionRunResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CancelActionRequest:
      type: object
      additionalProperties: false
      properties:
        note:
          type:
            - string
            - 'null'
          maxLength: 2000
    RetryActionRequest:
      type: object
      additionalProperties: false
      properties:
        note:
          type:
            - string
            - 'null'
          maxLength: 2000
    ResolveUnknownRequest:
      type: object
      additionalProperties: false
      description: |
        Exactly one answer: `provider_result_id` (with `grant_id`), or
        `happened`. Sending both leaves the server deciding which one counted.
      properties:
        provider_result_id:
          type:
            - string
            - 'null'
          description: |
            The provider's own id for the thing that may have happened, such
            as a message id in your sent folder. Checked with the provider
            before it is recorded.
        grant_id:
          type:
            - string
            - 'null'
          description: The mailbox the evidence is in. Required with `provider_result_id`.
        happened:
          type:
            - boolean
            - 'null'
          description: |
            Your own account of whether it happened, recorded as
            `user_reported` evidence and never as a confirmation.
        note:
          type:
            - string
            - 'null'
          maxLength: 2000
    PreferencesResponse:
      type: object
      required:
        - root_project_id
        - personal
        - reminders_enabled
        - suggestion_digest_enabled
        - muted_until
        - quiet_hours_start
        - quiet_hours_end
        - minimum_reminder_interval_hours
        - stored
      properties:
        root_project_id:
          type:
            - string
            - 'null'
          format: uuid
        personal:
          type: boolean
        reminders_enabled:
          type: boolean
        suggestion_digest_enabled:
          type: boolean
        muted_until:
          type:
            - string
            - 'null'
          format: date-time
        quiet_hours_start:
          type:
            - string
            - 'null'
        quiet_hours_end:
          type:
            - string
            - 'null'
        minimum_reminder_interval_hours:
          type: integer
        stored:
          type: boolean
          description: |
            False when you have never changed anything and these are the
            defaults. Both behave the same; this only tells them apart.
    PreferencesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/PreferencesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SetPreferencesRequest:
      type: object
      additionalProperties: false
      description: |
        A patch. An absent field is unchanged; an explicit `null` clears the
        setting.
      properties:
        reminders_enabled:
          type: boolean
        suggestion_digest_enabled:
          type: boolean
          description: |
            Off by default. Suggestions are pull-based cards unless you ask for
            a digest, so saving or snoozing one never schedules a message.
        muted_until:
          type:
            - string
            - 'null'
          format: date-time
          description: Nothing is delivered until this instant. `null` unmutes.
        quiet_hours_start:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
          description: |
            Local clock time, `HH:MM`. Paired with `quiet_hours_end`: send both
            or neither. A window that wraps past midnight is ordinary, and
            because these are clock times rather than instants the window means
            the same thing across a daylight-saving change.
        quiet_hours_end:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
        minimum_reminder_interval_hours:
          type: integer
          minimum: 1
          maximum: 720
          description: |
            The shortest gap between two reminders about the same thing.
            Defaults to 24.
    TaskEstimate:
      type: object
      required:
        - amount
        - unit
      additionalProperties: false
      description: |
        Effort as entered. The unit is stored as given and never normalised
        to minutes or converted, so two tasks with different units are not
        summed by the server. On a request an unknown key here is rejected
        with 400 and `details: [{field: "estimate", issue: "unknown_field"}]`,
        the same way an unknown key at the top level is.
      properties:
        amount:
          type: string
          pattern: ^(0|[1-9][0-9]{0,9})(\.[0-9]{1,2})?$
          description: |
            Exact decimal as a string, never a JSON number, so no client or
            server float rounds it. At most 10 integer digits and 2 decimal
            places; a third decimal is rejected rather than rounded, as are
            a sign, an exponent, `NaN` and `Infinity`.
          examples:
            - '1.5'
            - '0.25'
            - '3'
        unit:
          type: string
          description: |
            One of `minutes`, `hours`, `days`, `weeks`, `months`. Validated
            in Go, not an OpenAPI enum, so adding a unit is not a spec-
            breaking change for generated clients.
          examples:
            - hours
    TaskDraftRequest:
      type: object
      required:
        - title
      additionalProperties: false
      description: |
        The content of a new task, and the whole body of the two creates that
        already know where the task goes: `POST /me/todos`, whose owner is the
        session, and `POST /projects/{id}/tasks`, whose project is the path.

        Unknown fields are rejected with 400, at the top level and inside
        `estimate` alike, so `project_id`, `deal_id`, `work_key` and any owner
        key are refused rather than ignored. Unlike a patch, an omitted key
        and an explicit `null` mean the same thing here: there is no stored
        value to preserve.
      properties:
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            An opportunity in the same project this task belongs to. A personal
            ToDo may not carry one, and a deal in another project is 400
            `not_in_project`.
        work_key:
          type:
            - string
            - 'null'
          description: |
            The requirement this task fills for that deal, such as
            `diligence_review`. It is a semantic name rather than an id, so a
            copied pipeline keeps meaning its own work, and only one live task
            may hold a given key per deal: a second is 409 `work_key_taken`.
            A work key without a `deal_id` is 400 `needs_deal`.
        title:
          type: string
          minLength: 1
          description: |
            Required, and not blank once trimmed: whitespace alone is 400
            with `details: [{field: "title", issue: "blank"}]`.
        description:
          type:
            - string
            - 'null'
        acceptance_criteria:
          type:
            - string
            - 'null'
        priority:
          type: string
          description: |
            One of `low`, `normal`, `high`, `urgent`, defaulting to `normal`
            when omitted. Not nullable: a task always has a priority.
            Validated in Go, not an OpenAPI enum.
        target_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            Date only, `YYYY-MM-DD`, validated strictly: `2026-02-30` is
            rejected rather than rolled forward.
        due_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`, validated strictly.
        estimate:
          oneOf:
            - $ref: '#/components/schemas/TaskEstimate'
            - type: 'null'
          description: |
            A supplied estimate must carry a valid amount and a valid unit;
            a partial object is rejected.
        next_action:
          type:
            - string
            - 'null'
    AdoptSuggestionRequest:
      type: object
      required:
        - expected_version
        - adoption_type
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        adoption_type:
          type: string
          description: |
            `accepted_task`, `created_task`, `linked_task`, `applied_change` or
            `queued_action`. Plain string rather than an enum: the set is
            closed in Go.
        task_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Required by `linked_task`, refused by every other type.
        task:
          description: Required by `created_task`, refused by every other type.
          oneOf:
            - $ref: '#/components/schemas/TaskDraftRequest'
            - type: 'null'
        note:
          type:
            - string
            - 'null'
    CloseSuggestionRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        note:
          type:
            - string
            - 'null'
    DealTarget:
      type: object
      required:
        - entity_type
        - node_uuid
      additionalProperties: false
      description: |
        The canonical person or company a deal is about. It is a logical graph
        reference: no foreign key, no uniqueness, and it confers no access of
        its own.
      properties:
        entity_type:
          type: string
          description: '`persons` or `companies`. Anything else is 400 `unknown_value`.'
        node_uuid:
          type: string
          format: uuid
    DealMoney:
      type: object
      required:
        - value
        - currency
      additionalProperties: false
      description: |
        A deal's value and the currency it is counted in, set together or
        cleared together.
      properties:
        value:
          type: string
          description: |
            An exact decimal as a string, never a number: money that passed
            through a float is money nobody can reconcile. At most 16 integer
            digits and 4 fraction digits, which is what the column holds; more
            fraction digits are 400 `too_many_fraction_digits` rather than
            silently rounded, and more integer digits 400 `too_large`.
        currency:
          type: string
          description: An ISO 4217 alpha-3 code such as `USD`.
    DealResponse:
      type: object
      required:
        - id
        - project_id
        - root_project_id
        - pipeline_id
        - stage_id
        - title
        - description
        - owner_user_id
        - target
        - money
        - expected_close_date
        - next_action
        - blocked_reason
        - last_progress_at
        - stage_entered_at
        - position
        - won_at
        - lost_at
        - lost_reason
        - archived_at
        - version
        - created_at
        - updated_at
        - rights
        - warnings
      description: |
        One opportunity. There is no outcome field: a deal is won, lost or open
        because of the category of the stage it sits in, so nothing here can
        disagree with the board. `won_at` and `lost_at` are the current
        closure, written by the move commands.
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        root_project_id:
          type: string
          format: uuid
        pipeline_id:
          type: string
          format: uuid
        stage_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type:
            - string
            - 'null'
        owner_user_id:
          type:
            - string
            - 'null'
          format: uuid
        target:
          $ref: '#/components/schemas/DealTarget'
        money:
          oneOf:
            - $ref: '#/components/schemas/DealMoney'
            - type: 'null'
        expected_close_date:
          type:
            - string
            - 'null'
          format: date
        next_action:
          type:
            - string
            - 'null'
        blocked_reason:
          type:
            - string
            - 'null'
        last_progress_at:
          type: string
          format: date-time
        stage_entered_at:
          type: string
          format: date-time
        position:
          type: string
          description: |
            The card's place in its column: twelve base-36 digits that sort as
            bytes. Opaque to clients; reordering is its own command.
        won_at:
          type:
            - string
            - 'null'
          format: date-time
        lost_at:
          type:
            - string
            - 'null'
          format: date-time
        lost_reason:
          type:
            - string
            - 'null'
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        rights:
          type: string
          description: The caller's role in the deal's root.
        warnings:
          type: array
          description: |
            Advice on an accepted write. `duplicate_target` reports another
            live deal in the same pipeline pointing at the same entity, which
            is said rather than enforced.
          items:
            $ref: '#/components/schemas/TaskWarning'
    DealListResponse:
      type: object
      required:
        - items
        - total
        - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/DealResponse'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: Empty on the last page.
    DealListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/DealListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateDealRequest:
      type: object
      required:
        - pipeline_id
        - title
        - target
      additionalProperties: false
      properties:
        pipeline_id:
          type: string
          format: uuid
          description: |
            The pipeline the deal is opened in. It must share the project's
            root, and must already be ready to hold deals.
        title:
          type: string
          minLength: 1
        description:
          type:
            - string
            - 'null'
        owner_user_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Who is accountable for the deal; the caller when absent. Must be an
            eligible editor of the root.
        target:
          $ref: '#/components/schemas/DealTarget'
        money:
          $ref: '#/components/schemas/DealMoney'
        expected_close_date:
          type:
            - string
            - 'null'
          format: date
        next_action:
          type:
            - string
            - 'null'
    DealEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/DealResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    TaskResponse:
      type: object
      required:
        - id
        - project_id
        - root_project_id
        - personal_user_id
        - deal_id
        - work_key
        - title
        - description
        - acceptance_criteria
        - status
        - priority
        - target_date
        - due_date
        - estimate
        - next_action
        - accepted_at
        - started_at
        - completed_at
        - completion_summary
        - last_progress_at
        - archived_at
        - created_at
        - updated_at
        - version
        - warnings
      description: |
        The task as these two routes return it. Every field listed is always
        present: a nullable one is returned as `null` rather than omitted,
        and `warnings` is `[]` rather than null.

        The stored task carries more than this: assignees, the acceptance and
        creation provenance, and the blocked, started, completed and progress
        timestamps. Each is added here in the pull request that puts it on the
        wire, so a generated client is never given a required field the
        handler does not send. A guard test compares this property list with
        the Go DTO.
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Null on a personal ToDo.
        root_project_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The root of `project_id`'s tree, and the scope every access check
            and every lock uses. Null on a personal ToDo.
        personal_user_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The sole owner of a personal ToDo, and the only user who may read
            it. Null on a project task.
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
        work_key:
          type:
            - string
            - 'null'
          description: |
            Stable requirement key inside a deal, unique per deal. Only an
            accepted task tied to a deal can hold one.
        title:
          type: string
        description:
          type:
            - string
            - 'null'
        acceptance_criteria:
          type:
            - string
            - 'null'
        status:
          type: string
          description: |
            One of `suggested`, `todo`, `in_progress`, `blocked`,
            `in_review`, `done`, `canceled`. `suggested` is an unaccepted AI
            proposal and has a null `accepted_at`. Validated in Go, not an
            OpenAPI enum.
          examples:
            - todo
        priority:
          type: string
          description: |
            One of `low`, `normal`, `high`, `urgent`. Validated in Go, not an
            OpenAPI enum.
          examples:
            - normal
        target_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`. The intended working date.
        due_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`. The committed deadline.
        estimate:
          oneOf:
            - $ref: '#/components/schemas/TaskEstimate'
            - type: 'null'
          description: |
            Null when no estimate is set. Amount and unit are stored and
            cleared together, so there is no half-set estimate.
        accepted_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            Null exactly while the task is an unaccepted proposal. Such a
            task cannot be started, completed or given a work key.
        next_action:
          type:
            - string
            - 'null'
        started_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When work first began. Stamped by the first progress update and
            never moved again, so it survives a completion and a reopen.
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            Null exactly while the task is not complete. Reopening clears it.
        completion_summary:
          type:
            - string
            - 'null'
          description: |
            What was delivered, as the completing caller wrote it. Cleared by
            a reopen along with `completed_at`.
        last_progress_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When the task last really advanced. It moves on an explicit
            progress update and on completion, and on nothing else: a
            priority or deadline edit, a reopen, an archive, sharing, an
            assignment and a rank change all leave it where it was, so a
            stale-work scan is not reset by activity that is not progress.
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
          description: |
            Optimistic-lock counter. Echo it as `expected_version` on the
            next write; every applied write increments it by one.
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/TaskWarning'
    TaskListResponse:
      type: object
      required:
        - items
        - total
        - next_cursor
      description: |
        One page of a ToDo. `total` counts every row the same filters match,
        not the page, so a Suggested total counts proposals and is never the
        caller's committed workload.
      properties:
        items:
          type: array
          description: The page, an empty array when the filters match nothing.
          items:
            $ref: '#/components/schemas/TaskResponse'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: |
            Where to resume, empty on the last page. It is the one field the
            API answers with `""` rather than `null`.
    TaskListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TaskListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    TaskEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TaskResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateDealRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        Every key is optional but `expected_version`. Stage, outcome and board
        position are absent on purpose: each has its own command.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        title:
          type: string
          minLength: 1
        description:
          type:
            - string
            - 'null'
        owner_user_id:
          type: string
          format: uuid
          description: |
            Not nullable: a deal always has someone accountable for it, so
            handing it over is a value rather than a clear.
        target:
          $ref: '#/components/schemas/DealTarget'
        money:
          oneOf:
            - $ref: '#/components/schemas/DealMoney'
            - type: 'null'
          description: Set as a pair, or `null` to clear both columns.
        expected_close_date:
          type:
            - string
            - 'null'
          format: date
        next_action:
          type:
            - string
            - 'null'
        blocked_reason:
          type:
            - string
            - 'null'
    MoveDealRequest:
      type: object
      required:
        - expected_version
        - expected_configuration_version
        - stage_id
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        expected_configuration_version:
          type: integer
          format: int64
          minimum: 1
          description: |
            The pipeline's `configuration_version` as the client judged the
            move. A mismatch is 409 `configuration_conflict`.
        stage_id:
          type: string
          format: uuid
          description: A live stage of this deal's own pipeline.
        lost_reason:
          type:
            - string
            - 'null'
          description: |
            Required when the destination's category is `canceled`, and
            refused anywhere else: a closure nobody explained is one nobody can
            learn from.
        note:
          type:
            - string
            - 'null'
    ReopenDealRequest:
      type: object
      required:
        - expected_version
        - expected_configuration_version
        - stage_id
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        expected_configuration_version:
          type: integer
          format: int64
          minimum: 1
        stage_id:
          type: string
          format: uuid
          description: |
            The stage the deal comes back to. It cannot be one that would close
            it again.
        note:
          type:
            - string
            - 'null'
    RecordDealProgressRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        note:
          type:
            - string
            - 'null'
          description: A human remark, stored as written and never parsed.
        next_action:
          type:
            - string
            - 'null'
        blocked_reason:
          type:
            - string
            - 'null'
    ReorderDealRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        after_deal_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The live card this one goes below, in the same column. `null` or
            omitted puts it at the top. Naming the moved card itself, or a card
            in another column, is 400.
    ArchiveDealRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        note:
          type:
            - string
            - 'null'
    AttestCriterionRequest:
      type: object
      required:
        - expected_version
        - criterion_id
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        criterion_id:
          type: string
          format: uuid
          description: |
            The stable id of a `user_confirmed` criterion on a live stage of
            this deal's pipeline.
        note:
          type:
            - string
            - 'null'
    StakeholderResponse:
      type: object
      required:
        - id
        - deal_id
        - project_id
        - root_project_id
        - target
        - role
        - description
        - added_by_user_id
        - removed_at
        - version
        - created_at
        - updated_at
        - rights
      description: |
        One person or company attached to a deal. `removed_at` is what says the
        relationship ended: the row is kept, so it stays readable afterwards.
      properties:
        id:
          type: string
          format: uuid
        deal_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        root_project_id:
          type: string
          format: uuid
        target:
          $ref: '#/components/schemas/DealTarget'
        role:
          type: string
        description:
          type:
            - string
            - 'null'
        added_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
        removed_at:
          type:
            - string
            - 'null'
          format: date-time
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        rights:
          type: string
          description: The caller's role in the deal's root.
    StakeholderListResponse:
      type: object
      required:
        - items
        - total
      description: |
        A deal's whole set of links. There is no `next_cursor`: the list is not
        paged, so `total` is always the length of `items`.
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/StakeholderResponse'
        total:
          type: integer
    StakeholderListEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/StakeholderListResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    AddStakeholderRequest:
      type: object
      required:
        - target
        - role
      additionalProperties: false
      properties:
        target:
          $ref: '#/components/schemas/DealTarget'
        role:
          type: string
          description: |
            One of `decision_maker`, `champion`, `influencer`, `blocker`,
            `other`. Anything else is 400 `unknown_value`.
        description:
          type:
            - string
            - 'null'
    StakeholderEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/StakeholderResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    RemoveStakeholderRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: A removal names nothing beyond the path, so the version is all it carries.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
    UpdateStakeholderRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        The target is absent on purpose: a link is one entity in one role, so
        pointing it elsewhere is a removal and an addition. The body must name
        at least one of `role` and `description`.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        role:
          type: string
        description:
          type:
            - string
            - 'null'
    UpdatePipelineRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        Every key is optional but `expected_version`. An omitted key preserves
        the stored value; a key present with `null` clears it. `title` and
        `kind` are not nullable: a pipeline always has both.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        title:
          type: string
          minLength: 1
        description:
          type:
            - string
            - 'null'
        kind:
          type: string
        icon_name:
          type:
            - string
            - 'null'
        color:
          type:
            - string
            - 'null'
        default_currency:
          type:
            - string
            - 'null'
    CopyPipelineRequest:
      type: object
      additionalProperties: false
      description: |
        Both fields are optional: the copy lands in the source's own root under
        the source's title unless the body says otherwise.
      properties:
        destination_root_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The root the copy lands in. It must be a root, not a subproject,
            and the caller must be an admin of it.
        title:
          type:
            - string
            - 'null'
          minLength: 1
    StageCriteria:
      type: object
      description: |
        A stage's gate: the checks a deal must satisfy to enter it. The empty
        object means the stage gates on nothing, which is what the column
        defaults to. `all` is a conjunction; there is no any-of and no
        nesting, so a gate is readable at a glance.

        Each criterion carries a stable `id` that outlives edits to the
        wording around it, which is what lets an attestation keep naming the
        same requirement. A criterion carries exactly the one argument its
        check takes and no other: a stray argument is refused rather than
        ignored. At most 20 checks.
      additionalProperties: false
      properties:
        version:
          type: integer
          description: Always 1. A later version is refused, never read partially.
        all:
          type: array
          maxItems: 20
          items:
            type: object
            additionalProperties: false
            required:
              - id
              - check
            properties:
              id:
                type: string
                format: uuid
              check:
                type: string
                description: |
                  One of `has_target`, `has_stakeholder`, `field_present`,
                  `task_done`, `action_succeeded`, `user_confirmed`. Anything
                  else is refused.
              role:
                type: string
                description: '`has_stakeholder` only: the stakeholder role required.'
              field:
                type: string
                description: |
                  `field_present` only: one of `owner_user_id`,
                  `expected_close_date`, `value`, `next_action`,
                  `description`.
              work_key:
                type: string
                description: |
                  `task_done` only: the semantic requirement, never one deal's
                  task id, so a copied pipeline keeps meaning its own tasks.
              action_type:
                type: string
                description: '`action_succeeded` only: a registered action type.'
              label:
                type: string
                description: '`user_confirmed` only: what the person is confirming.'
    StageActionGuidance:
      type: object
      description: |
        The next steps a client offers on a card in this stage. It is advice,
        never an authorization: every executable side effect has its own
        authorized action run. The empty object means the stage offers
        nothing, and at most 20 rules are allowed.
      additionalProperties: false
      properties:
        version:
          type: integer
          description: Always 1.
        rules:
          type: array
          maxItems: 20
          items:
            type: object
            additionalProperties: false
            required:
              - id
              - title
              - action_type
            properties:
              id:
                type: string
                format: uuid
              title:
                type: string
                minLength: 1
              rationale_template:
                type: string
              action_type:
                type: string
                description: |
                  A key from the server's action registry, which is reviewed
                  Go. A key the registry does not carry is refused, so a
                  document cannot introduce a tool, a URL, SQL or a shell
                  command.
              default_params:
                type: object
                description: |
                  Defaults for that action type, checked against the
                  parameters the registry declares for it.
    CreateStageRequest:
      type: object
      required:
        - name
        - category
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
        category:
          type: string
          description: |
            One of `backlog`, `unstarted`, `started`, `done`, `canceled`. It
            is semantic: `done` derives a won deal, `canceled` a lost one.
        color:
          type:
            - string
            - 'null'
        is_default:
          type:
            - boolean
            - 'null'
          description: |
            Makes this the column a new deal lands in, taking the flag from
            whichever stage holds it. A terminal category cannot be default.
        criteria:
          $ref: '#/components/schemas/StageCriteria'
        action_guidance:
          $ref: '#/components/schemas/StageActionGuidance'
        stale_after_days:
          type:
            - integer
            - 'null'
          minimum: 1
          description: How long a deal may sit here before a client calls it stale.
    StageResponse:
      type: object
      required:
        - id
        - pipeline_id
        - root_project_id
        - name
        - category
        - color
        - position
        - is_default
        - criteria
        - action_guidance
        - stale_after_days
        - archived_at
        - version
        - created_at
        - updated_at
        - rights
        - configuration_version
      properties:
        id:
          type: string
          format: uuid
        pipeline_id:
          type: string
          format: uuid
        root_project_id:
          type: string
          format: uuid
        name:
          type: string
        category:
          type: string
        color:
          type:
            - string
            - 'null'
        position:
          type: string
          description: |
            The column's place in the board order: twelve base-36 digits that
            sort as bytes. Opaque to clients; reordering is its own command.
        is_default:
          type: boolean
        criteria:
          $ref: '#/components/schemas/StageCriteria'
        action_guidance:
          $ref: '#/components/schemas/StageActionGuidance'
        stale_after_days:
          type:
            - integer
            - 'null'
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        rights:
          type: string
          description: The caller's role in the stage's root.
        configuration_version:
          type: integer
          format: int64
          description: |
            The parent pipeline's counter as this stage was read. Every stage
            edit moves it, so a client comparing it knows its cached rules are
            stale before it tries a move.
    StageEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/StageResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    UpdateStageRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        Every key is optional but `expected_version`. An omitted key preserves
        the stored value; a key present with `null` clears it. `name` and
        `category` are not nullable. A document is replaced whole or left
        alone.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        name:
          type: string
          minLength: 1
        category:
          type: string
        color:
          type:
            - string
            - 'null'
        is_default:
          type: boolean
        criteria:
          $ref: '#/components/schemas/StageCriteria'
        action_guidance:
          $ref: '#/components/schemas/StageActionGuidance'
        stale_after_days:
          type:
            - integer
            - 'null'
          minimum: 1
    ReorderStageRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        after_stage_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The live column this one goes after. `null` or omitted puts it
            first. Naming the moved column itself, or a column of another
            pipeline, is 400.
    ArchiveStageRequest:
      type: object
      required:
        - expected_version
        - replacement_stage_id
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        replacement_stage_id:
          type: string
          format: uuid
          description: |
            Where the retiring column's live cards go. A different live stage
            of the same pipeline; required, because cards left in a retired
            column would be work nobody could reach.
        note:
          type:
            - string
            - 'null'
    ArchivePipelineRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
        note:
          type:
            - string
            - 'null'
    BoardCardResponse:
      allOf:
        - $ref: '#/components/schemas/DealResponse'
        - type: object
          required:
            - node
          properties:
            node:
              description: |
                The person or company the deal is about, in the shared node
                shape every feature serves, rendered for the calling user: the
                entity, the caller's own contact record for it, and their
                stored relationship paths to it. Null when the target no longer
                resolves, or when the entities could not be read; the card is
                on the board either way.
              oneOf:
                - type: object
                  additionalProperties: true
                - type: 'null'
    BoardColumnResponse:
      type: object
      required:
        - stage
        - total
        - deals
        - next_cursor
      properties:
        stage:
          $ref: '#/components/schemas/StageResponse'
        total:
          type: integer
          format: int64
          description: Every live card the column holds, not only the ones carried here.
        deals:
          type: array
          items:
            $ref: '#/components/schemas/BoardCardResponse'
        next_cursor:
          type: string
          description: |
            Empty when the column holds no more cards than the board carries.
            Otherwise the cursor
            `GET /pipelines/{id}/stages/{stage_id}/deals` resumes from.
    BoardResponse:
      type: object
      required:
        - pipeline
        - columns
      properties:
        pipeline:
          $ref: '#/components/schemas/PipelineResponse'
        columns:
          type: array
          items:
            $ref: '#/components/schemas/BoardColumnResponse'
    BoardEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/BoardResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    ColumnPageResponse:
      type: object
      required:
        - items
        - total
        - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BoardCardResponse'
        total:
          type: integer
          format: int64
        next_cursor:
          type: string
          description: Empty on the last page.
    ColumnPageEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ColumnPageResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateTaskRequest:
      type: object
      required:
        - title
      additionalProperties: false
      description: |
        The body of the unified create. `project_id` decides where the task
        goes: absent or `null` is the caller's own ToDo, a value is that
        project. An invalid one is an error, never a private ToDo.

        Every other key is the draft the other two creates take, and unknown
        fields are rejected with 400 at the top level and inside `estimate`
        alike, so `deal_id`, `work_key` and any owner key are refused rather
        than ignored.
      properties:
        project_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The project the task goes in. Omitted and `null` both capture a
            personal ToDo, because a task that does not exist yet has no
            placement to preserve. A value that is not a uuid is 400
            `invalid_uuid`; one that names no readable project is 404 and one
            the caller may only read is 403.
        title:
          type: string
          minLength: 1
          description: |
            Required, and not blank once trimmed: whitespace alone is 400
            with `details: [{field: "title", issue: "blank"}]`.
        description:
          type:
            - string
            - 'null'
        acceptance_criteria:
          type:
            - string
            - 'null'
        priority:
          type: string
          description: |
            One of `low`, `normal`, `high`, `urgent`, defaulting to `normal`
            when omitted. Not nullable: a task always has a priority.
            Validated in Go, not an OpenAPI enum.
        target_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            Date only, `YYYY-MM-DD`, validated strictly: `2026-02-30` is
            rejected rather than rolled forward.
        due_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`, validated strictly.
        estimate:
          oneOf:
            - $ref: '#/components/schemas/TaskEstimate'
            - type: 'null'
          description: |
            A supplied estimate must carry a valid amount and a valid unit;
            a partial object is rejected.
        next_action:
          type:
            - string
            - 'null'
    UpdateTaskRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        An omitted key preserves the stored value; a key present with `null`
        clears it. Unknown fields are rejected with 400, at the top level and
        inside `estimate` alike. At least one patchable field must be named:
        a body carrying only `expected_version` is 400 `VALIDATION_FAILED`
        with the message `no fields to update`. A patch whose values equal
        the stored ones is a 200 that writes nothing and does not bump
        `version`.
      properties:
        deal_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            An opportunity in the same project this task belongs to. A personal
            ToDo may not carry one, and a deal in another project is 400
            `not_in_project`.
        work_key:
          type:
            - string
            - 'null'
          description: |
            The requirement this task fills for that deal, such as
            `diligence_review`. It is a semantic name rather than an id, so a
            copied pipeline keeps meaning its own work, and only one live task
            may hold a given key per deal: a second is 409 `work_key_taken`.
            A work key without a `deal_id` is 400 `needs_deal`.
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The task's current `version`. A mismatch is 409.
        title:
          type: string
          minLength: 1
          description: |
            Not nullable: `null` is a validation error, since a task with no
            title cannot exist. Leading and trailing whitespace does not
            count towards the minimum.
        description:
          type:
            - string
            - 'null'
        acceptance_criteria:
          type:
            - string
            - 'null'
        priority:
          type: string
          description: |
            One of `low`, `normal`, `high`, `urgent`. Not nullable: a task
            always has a priority, so send `normal` to reset it. Validated in
            Go, not an OpenAPI enum.
        target_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            Date only, `YYYY-MM-DD`, validated strictly: `2026-02-30` is
            rejected rather than rolled forward.
        due_date:
          type:
            - string
            - 'null'
          format: date
          description: Date only, `YYYY-MM-DD`, validated strictly.
        estimate:
          oneOf:
            - $ref: '#/components/schemas/TaskEstimate'
            - type: 'null'
          description: |
            `null` clears both the amount and the unit. A supplied estimate
            must carry a valid amount and a valid unit; a partial object is
            rejected.
        next_action:
          type:
            - string
            - 'null'
    ExpectedVersionRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        A command whose whole body is the version it asserts. Accept and
        restore take nothing else: what is accepted is the task's current
        fields, not a payload the client re-sends.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
    CompleteTaskRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        Unknown fields are rejected with 400. There is no status here: what a
        completion sets is the route's business, not the client's.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The task's current `version`. A mismatch is 409.
        completion_summary:
          type:
            - string
            - 'null'
          description: |
            What was delivered. Stored on the task and carried into the
            history row. Whitespace alone is 400 with `details: [{field:
            "completion_summary", issue: "blank"}]`: omit the key instead.
    TaskNoteRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        The body of a reopen or an archive: the version being acted on and,
        optionally, why. Unknown fields are rejected with 400.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The task's current `version`. A mismatch is 409.
        note:
          type:
            - string
            - 'null'
          description: |
            A short remark for the history row, stored as written. Whitespace
            alone is 400 with `issue: "blank"`: omit the key instead.
    RecordProgressRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        Unknown fields are rejected with 400. There is no status and no
        progress percentage: the route decides the status, and what advanced
        belongs in `note`.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The task's current `version`. A mismatch is 409.
        note:
          type:
            - string
            - 'null'
          description: |
            What advanced, stored as written in the history row. Whitespace
            alone is 400 with `issue: "blank"`.
        next_action:
          type:
            - string
            - 'null'
          description: |
            The next step. Unlike the other fields on this route, an omitted
            key preserves the stored value and `null` clears it.
    PlaceTaskRequest:
      type: object
      required:
        - expected_version
        - project_id
      additionalProperties: false
      description: |
        Where the task is going. `project_id` is required and may be null: a
        body that forgot the key cannot make shared work private. Unknown
        fields are rejected with 400.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The task's current `version`. A mismatch is 409 `version_conflict`.
        project_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The destination project, or `null` to make the task private to
            `recipient_user_id`.
        recipient_user_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Who owns the task once it is private, and its sole assignee. It is
            required with `project_id: null` and rejected with a project, where
            no single user owns the work. The recipient must already be an
            editor of the task's root; one who is not is 400 with `issue:
            "not_editor"`.
        assignees:
          type: array
          maxItems: 50
          items:
            type: string
            format: uuid
          description: |
            The destination assignee set, at most 50, each an eligible editor
            of the destination root. Omit the key to keep whoever is on the
            task and is still eligible; send `[]` to assign nobody. Order and
            repeats do not matter: the list is sorted and deduplicated before
            the retry key is hashed. Rejected with `project_id: null`.
    TaskPlacementResponse:
      type: object
      required:
        - task_id
        - version
        - project_id
        - root_project_id
        - personal_user_id
      description: |
        Where a task stands after a placement. Exactly one scope is set: both
        project keys, or the personal owner. It carries no content and no
        assignee set, because the move may have taken the caller's own access
        to the task away; read the task itself to get the rest.
      properties:
        task_id:
          type: string
          format: uuid
        version:
          type: integer
          format: int64
          description: |
            The task's version after the command. Unchanged when the task was
            already in the project the request named.
        project_id:
          type:
            - string
            - 'null'
          format: uuid
        root_project_id:
          type:
            - string
            - 'null'
          format: uuid
        personal_user_id:
          type:
            - string
            - 'null'
          format: uuid
          description: The owner the task is now private to, null in a project.
    TaskPlacementEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TaskPlacementResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    SetTaskAssigneesRequest:
      type: object
      required:
        - expected_version
        - assignees
      additionalProperties: false
      description: |
        The whole resulting assignee set. Unknown fields are rejected with
        400, and so is an omitted `assignees`: a replacement has to say what
        the set is.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The task's current `version`. A mismatch is 409.
        assignees:
          type: array
          maxItems: 50
          items:
            type: string
            format: uuid
          description: |
            The users the task is assigned to, at most 50. `[]` clears the
            set. Order and repeats do not matter: the list is sorted and
            deduplicated before it is stored and before the retry key is
            hashed. `null` is rejected; send `[]` to assign nobody.
    TaskAssignee:
      type: object
      required:
        - user_id
        - assigned_by_user_id
        - assigned_at
      description: One assignment. Every key is always present.
      properties:
        user_id:
          type: string
          format: uuid
          description: The user the task is assigned to.
        assigned_by_user_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Who made the assignment, or `null` once that account is deleted.
            The assignment itself survives; only the attribution goes.
        assigned_at:
          type: string
          format: date-time
          description: When the assignment was stored.
    TaskAssigneesResponse:
      type: object
      required:
        - task_id
        - version
        - assignees
      description: |
        A task's whole assignee set, with the version the command left the
        task at. Every key is always present and `assignees` is always an
        array, empty when the task is assigned to nobody.
      properties:
        task_id:
          type: string
          format: uuid
        version:
          type: integer
          format: int64
          description: |
            The task's version after the command. Unchanged when the request
            asked for the set the task already had.
        assignees:
          type: array
          items:
            $ref: '#/components/schemas/TaskAssignee'
    TaskAssigneesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TaskAssigneesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    TaskDependencyRequest:
      type: object
      required:
        - expected_version
      additionalProperties: false
      description: |
        The body both dependency routes take. The prerequisite is in the
        path, so there is nothing here for it to disagree with. Unknown
        fields are rejected with 400.
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: The waiting task's current `version`. A mismatch is 409.
    TaskDependenciesResponse:
      type: object
      required:
        - task_id
        - version
        - prerequisite_task_ids
      description: |
        Everything a task waits on, with the version the command left the
        task at. Every key is always present and `prerequisite_task_ids` is
        always an array, empty when the task waits on nothing.
      properties:
        task_id:
          type: string
          format: uuid
        version:
          type: integer
          format: int64
          description: |
            The task's version after the command. Unchanged when the link was
            already recorded, or was already gone.
        prerequisite_task_ids:
          type: array
          items:
            type: string
            format: uuid
          description: The tasks that must be complete or archived first, in id order.
    TaskDependenciesEnvelope:
      type: object
      required:
        - data
        - error
        - meta
      properties:
        data:
          $ref: '#/components/schemas/TaskDependenciesResponse'
        error:
          type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
  responses:
    BadRequest:
      description: Validation or malformed request.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    Unauthorized:
      description: Missing or invalid bearer token.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    NotFound:
      description: Resource not found.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    Forbidden:
      description: Authenticated but not permitted to perform this action.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    Conflict:
      description: Resource already exists.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    Internal:
      description: Internal server error.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    ProjectConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the project's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the project is archived or sits under an archived
          project, the patch sets `completed`, it changes the status of an
          already completed project, or it sets `timezone` on a subproject. On
          `POST /projects` it is also a parent that is archived, complete or
          under an archived project.
        - `nesting_too_deep`: on `POST /projects` only. The parent is already
          ten levels from its root.
        - `root_archived`: the project's root is archived. Reopen the root
          before changing anything under it.
        - `scope_busy`: the root stayed locked by other commands past the
          lock timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    ProjectLifecycleConflict:
      description: |
        The command did not run. `error.details` always carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the project's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the transition does not apply. A root being moved,
          an archived project being moved or completed, a destination that is
          archived, complete or under an archived project, a project already
          archived being archived, one already complete being completed, or
          one that is neither archived nor complete being reopened.
        - `cross_root_move`: on `/move` only. The destination belongs to
          another root. V1 has no project-to-project move across roots.
        - `project_cycle`: on `/move` only. The destination is the project
          itself or sits inside its own subtree, which would make it its own
          ancestor.
        - `nesting_too_deep`: on `/move` only. The moved subtree's deepest
          level would land past ten.
        - `blockers_open`: on `/complete` only. Work under the project is not
          finished, and `details` carries three more entries,
          `open_projects`, `open_tasks` and `open_deals`, each an `issue`
          holding a decimal count and present even when zero.
        - `root_archived`: the project's root is archived. Reopen the root
          first. `/reopen` never answers this, because undoing an archive is
          what it is for.
        - `scope_busy`: the root stayed locked by other commands past the
          lock timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    PipelineConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the pipeline's
          current version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the pipeline is archived, or on a create the root
          is archived or complete.
        - `root_archived`: the pipeline's root is archived. Reopen the root
          before changing its configuration.
        - `scope_busy`: the root stayed locked by other commands past the
          lock timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    SuggestionConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the suggestion's
          current version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the suggestion has already left the open set,
          because somebody adopted, withdrew, resolved or superseded it.
        - `root_archived`: the suggestion's project root is archived.
        - `scope_busy`: the scope stayed locked by other commands past the
          lock timeout. Safe to retry.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    AgentRunConflict:
      description: |
        The review was not queued. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `review_in_flight`: a review of this scope is already queued or
          running. One live personal review per owner, and one live background
          review per root; wait for it to finish.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the project is archived or complete.
        - `root_archived`: the project's root is archived.
        - `scope_busy`: the scope stayed locked past the lock timeout.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    AdoptSuggestionConflict:
      description: |
        The adoption did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the suggestion's
          current version. Two people adopting at once end here, and exactly
          one of them wins.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the suggestion has already left the open set; or
          `accepted_task` on a proposal carrying no task; or `created_task` on
          one that already carries one; or `linked_task` naming an archived
          task or one that is itself an unaccepted proposal.
        - `action_not_executable`: `applied_change` or `queued_action` on an
          action this release cannot run. A second `details` entry names the
          `action_type`. Adopt the suggestion as work instead.
        - `root_archived`: the project's root is archived.
        - `scope_busy`: the scope stayed locked past the lock timeout.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    DealConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the deal's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `pipeline_not_ready`: the pipeline has no single live default stage,
          or no live stage for one of the two outcomes. Finish configuring it
          before opening deals in it.
        - `invalid_state`: the deal, its project or its pipeline is archived,
          or the project is complete.
        - `root_archived`: the project's root is archived.
        - `scope_busy`: the root stayed locked by other commands past the lock
          timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    TaskCreateConflict:
      description: |
        The task was not created. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the destination project is archived, complete, or
          sits under an archived project. Finished or suppressed work takes no
          new tasks.
        - `root_archived`: the destination project's root is archived. Reopen
          the root before adding work inside it.
        - `scope_busy`: the destination root, or the caller's ToDo, stayed
          locked by other commands past the lock timeout. Safe to retry with
          the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    DealMoveConflict:
      description: |
        The move did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values, and a
        `criteria_unmet` refusal carries one further entry per gap, keyed
        `criteria.<criterion_id>` with `unmet` or `unknown` as its issue.

        - `version_conflict`: `expected_version` is not the deal's current
          version.
        - `configuration_conflict`: `expected_configuration_version` is not the
          pipeline's. Refetch the configuration; the rules changed.
        - `criteria_unmet`: the destination's criteria are not all met.
          Unknown counts as not met.
        - `invalid_state`: the deal is archived, it is closed and this is a
          move, it is open and this is a reopen, the destination stage is
          archived, or a reopen names a stage that would close it.
        - `idempotency_conflict`: this key was used with a different body.
        - `scope_busy`: the root stayed locked past the lock timeout.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    DealBoardConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the deal's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the deal is already archived.
        - `root_archived`: the deal's root is archived.
        - `scope_busy`: the root stayed locked by other commands past the lock
          timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    StakeholderConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the link's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `stakeholder_exists`: the target is already on this deal in that
          role. A removed link frees the slot.
        - `invalid_state`: the deal is archived, or the link was already
          removed.
        - `root_archived`: the deal's root is archived.
        - `scope_busy`: the root stayed locked by other commands past the lock
          timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    StageConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the stage's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the stage or its pipeline is archived, or on an
          archive the named replacement stage is itself archived.
        - `stage_in_use`: a deal sits in the stage, so its category is fixed.
          Add a replacement stage and move the deals explicitly.
        - `root_archived`: the pipeline's root is archived.
        - `scope_busy`: the root stayed locked by other commands past the
          lock timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    PipelineArchiveConflict:
      description: |
        The pipeline was not archived. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `pipeline_in_use`: the pipeline still holds live deals. A second
          entry, `field: "live_deals"`, carries how many. Close or archive them
          first.
        - `version_conflict`: `expected_version` is not the pipeline's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the pipeline is already archived.
        - `root_archived`: the pipeline's root is archived.
        - `scope_busy`: the root stayed locked by other commands past the lock
          timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    SuggestedTaskConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: the version asserted is not the task's current
          one. Refetch and retry; nothing was written. Two people accepting at
          once end here, and exactly one of them wins.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: on accept, the task is archived, already accepted,
          or not a proposal at all, or its proposal has left the open set
          because somebody withdrew, superseded or deleted it. On delete, the
          task is a shared project proposal rather than a personal one, or is
          accepted work. On restore, the task is not deleted, is accepted
          work, or its proposal was adopted.
        - `suggestion_expired`: on restore only, the proposal's `expires_at`
          has passed. It was drawn on a world that has moved on and cannot
          come back; ask the agent again instead.
        - `root_archived`: the task's project root is archived.
        - `scope_busy`: the task's root or the owner's ToDo stayed locked by
          other commands past the lock timeout. Safe to retry.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    WorkConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the task's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the task is archived, is an unaccepted proposal,
          or is not in the state this command transitions out of.
        - `prerequisites_open`: on `/complete` only, a prerequisite of the
          task is neither complete nor archived. Finish or archive it first.
          The other three routes never count prerequisites.
        - `root_archived`: the task's project root is archived. Reopen the
          root before changing work inside it.
        - `scope_busy`: the task's root or the owner's ToDo stayed locked by
          other commands past the lock timeout. Safe to retry.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    PlacementConflict:
      description: |
        The task did not move, and nothing about it changed. `error.details`
        carries one entry with `field: "reason"` and one of these `issue`
        values:

        - `version_conflict`: `expected_version` is not the task's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: the task is archived or is an unaccepted proposal,
          or the destination project is archived, complete, or sits under an
          archived project.
        - `deal_linked`: the task is linked to a deal or holds a work key.
          Unlink it before moving it.
        - `dependencies_held`: a prerequisite link joins this task to other
          work. Both ends of a link share one scope, so remove the links first.
        - `actions_in_flight`: an action on the task is running or its result
          is unknown. A queued one needs nothing: the move cancels it.
        - `cross_root_move`: the destination belongs to another root. V1 has no
          project-to-project move across roots; move the task to a ToDo and
          place it again.
        - `root_archived`: the task's project root, or the destination's, is
          archived. Reopen the root first.
        - `scope_busy`: a scope the move spans stayed locked by other commands
          past the lock timeout. Safe to retry with the same key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    TaskLinkConflict:
      description: |
        The command did not run. `error.details` carries one entry with
        `field: "reason"` and one of these `issue` values:

        - `version_conflict`: `expected_version` is not the task's current
          version. Refetch and retry; nothing was written.
        - `idempotency_conflict`: this `Idempotency-Key` was already used by
          this caller for this operation with a different body.
        - `invalid_state`: one of the two tasks is archived or is an
          unaccepted proposal. Links belong to committed, live work.
        - `dependency_cycle`: on `PUT /dependencies/{prerequisite_id}` only.
          The link would let the two tasks wait on each other, directly or
          around a longer ring, which is work that could never finish.
        - `root_archived`: the task's project root is archived. Reopen the
          root before changing work inside it.
        - `scope_busy`: the task's root or the owner's ToDo stayed locked by
          other commands past the lock timeout. Safe to retry.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
  parameters:
    UserID:
      in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
      description: User id (UUIDv7).
    PersonID:
      in: path
      name: person_id
      required: true
      schema:
        type: string
        format: uuid
    Limit:
      name: limit
      in: query
      description: Page size (default 50, max 200).
      schema:
        type: integer
        default: 50
        maximum: 200
    Cursor:
      name: cursor
      in: query
      description: Opaque cursor from the previous page's `meta.nextCursor`. Omit for the first page.
      schema:
        type: string
    LoopID:
      name: loop_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SuggestionID:
      name: suggestion_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    BatchID:
      name: batch_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SignatureTimestamp:
      name: X-Copilot-Timestamp
      in: header
      required: true
      description: Unix seconds when the harness signature was computed.
      schema:
        type: string
    Signature:
      name: X-Copilot-Signature
      in: header
      required: true
      description: Hex HMAC-SHA256 over timestamp, method, path+query, body hash, bearer-token hash.
      schema:
        type: string
    SubscriptionID:
      in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
      description: Subscription id.
    ProjectStatus:
      name: status
      in: query
      description: |
        Keep only the roots in this status: `planned`, `active`, `on_hold`,
        `completed` or `canceled`. Omitted returns every status. A value
        outside the set is 400 with `issue: "unknown_value"`.
      schema:
        type: string
    ProjectSearch:
      name: search
      in: query
      description: |
        Case-insensitive substring of the title. Whitespace alone is read as
        no search at all rather than as a match on every row.
      schema:
        type: string
    ProjectIncludeArchived:
      name: include_archived
      in: query
      description: |
        Include archived roots, which are left out by default. A value that
        is not a boolean is 400 rather than read as false.
      schema:
        type: boolean
        default: false
    ProjectLimit:
      name: limit
      in: query
      description: |
        Rows per page, 50 by default. A limit above the cap is clamped to 200
        and `0` reads as the default, so neither bound is declared here: a
        generated client that refused them could not reach the clamp at all.
        A limit that is not a non-negative whole number is 400.
      schema:
        type: integer
        default: 50
    ProjectCursor:
      name: cursor
      in: query
      description: |
        Opaque position taken from the previous page's `next_cursor`. It is
        bound to the caller and the filters it was produced under.
      schema:
        type: string
    RequiredIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: |
        Client-generated retry key, 1 to 128 characters of
        `A-Z a-z 0-9 . _ ~ -`. Scoped to the caller and this operation.
        Required on a create: without one a dropped response would leave the
        client unable to retry without risking a second row.
      schema:
        type: string
        minLength: 1
        maxLength: 128
    OptionalIdempotencyKey:
      name: Idempotency-Key
      in: header
      description: |
        Client-generated retry key, 1 to 128 characters of
        `A-Z a-z 0-9 . _ ~ -`. Scoped to the caller and this operation.
        Optional on a transition: `expected_version` already stops a repeat
        from applying twice.
      schema:
        type: string
        minLength: 1
        maxLength: 128
    RootProjectId:
      name: root_id
      in: path
      required: true
      description: |
        A root project's id. A subproject id reaches no ACL route of its own
        and answers 404: sharing is configured on the root its tree hangs off.
      schema:
        type: string
        format: uuid
    GrantedOrganizationId:
      name: org_id
      in: path
      required: true
      description: The organization the grant is about.
      schema:
        type: string
        format: uuid
    PipelineIncludeArchived:
      name: include_archived
      in: query
      description: |
        Include archived pipelines, which are left out by default. A value
        that is not a boolean is 400 rather than read as false.
      schema:
        type: boolean
        default: false
    ActivityTaskId:
      name: task_id
      in: query
      description: |
        Keep only the rows about this task. A malformed id is 400 rather than
        ignored, so a client filtering on a mistyped id is never handed the
        whole timeline as if it matched.
      schema:
        type: string
        format: uuid
    ActivityDealId:
      name: deal_id
      in: query
      description: |
        Keep only the rows about this deal. A malformed id is 400 rather than
        ignored.
      schema:
        type: string
        format: uuid
    ActivityLimit:
      name: limit
      in: query
      description: |
        Rows per page, 50 by default. A limit above the cap is clamped to 200
        and `0` reads as the default, so neither bound is declared here: a
        generated client that refused them could not reach the clamp at all.
        A limit that is not a non-negative whole number is 400.
      schema:
        type: integer
        default: 50
    ActivityCursor:
      name: cursor
      in: query
      description: |
        Opaque position taken from the previous page's `next_cursor`. It is
        bound to the caller, the project, the filters and the audience it was
        produced under.
      schema:
        type: string
    ProjectId:
      name: id
      in: path
      required: true
      description: The project the deals belong to.
      schema:
        type: string
        format: uuid
    SuggestionStatus:
      name: status
      in: query
      description: |
        Restrict the page to one status. Plain string rather than an enum: the
        set is closed in Go and adding a value should not need a spec release.
      schema:
        type: string
    IncludeHidden:
      name: include_hidden
      in: query
      description: |
        Include the proposals this caller dismissed or snoozed. It widens the
        page to the caller's own responses only; nobody else's are reachable.
      schema:
        type: boolean
        default: false
    SuggestionLimit:
      name: limit
      in: query
      description: |
        Rows per page, 50 by default. A limit above the cap is clamped to 200
        and `0` reads as the default, so neither bound is declared here.
      schema:
        type: integer
        default: 50
    SuggestionCursor:
      name: cursor
      in: query
      description: |
        Opaque position taken from the previous page's `next_cursor`. It is
        bound to the reader as well as to the project and the filters: a page
        is personalised, so one reader's cursor is meaningless in another's
        list.
      schema:
        type: string
    SuggestionId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    AgentRunId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ActionRunId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    DealPipelineFilter:
      name: pipeline_id
      in: query
      description: |
        Only deals in this pipeline. A pipeline outside the project's root is
        400 `not_in_project`.
      schema:
        type: string
        format: uuid
    DealStageFilter:
      name: stage_id
      in: query
      description: |
        Only deals in this stage. A stage outside the project's root, or one
        belonging to a different pipeline than `pipeline_id` names, is 400
        `not_in_project`.
      schema:
        type: string
        format: uuid
    DealIncludeArchived:
      name: include_archived
      in: query
      description: |
        Include archived deals, which are left out by default. A value that is
        not a boolean is 400 rather than read as false.
      schema:
        type: boolean
        default: false
    TaskFilter:
      name: filter
      in: query
      description: |
        Which section to return: `active` (accepted and not complete),
        `completed`, or `suggested` (unaccepted proposals). Defaults to
        `active`. A value outside the set is 400 with
        `issue: "unknown_value"`; there is no fallback to a default.
      schema:
        type: string
        default: active
    TaskSort:
      name: sort
      in: query
      description: |
        Order of the page: `created` (newest first), or `due` or `target`
        (earliest first, rows with no date last). Defaults to `created`. A
        value outside the set is 400.
      schema:
        type: string
        default: created
    TaskSearch:
      name: search
      in: query
      description: |
        Case-insensitive substring of the title. Whitespace alone is read as
        no search at all rather than as a match on every row.
      schema:
        type: string
    TaskIncludeArchived:
      name: include_archived
      in: query
      description: |
        Include archived rows, which are left out by default. A value that is
        not a boolean is 400 rather than read as false.
      schema:
        type: boolean
        default: false
    TaskLimit:
      name: limit
      in: query
      description: |
        Rows per page, 50 by default. A limit above the cap is clamped to 200
        and `0` reads as the default, so neither bound is declared here: a
        generated client that refused them could not reach the clamp at all.
        A limit that is not a non-negative whole number is 400.
      schema:
        type: integer
        default: 50
    TaskCursor:
      name: cursor
      in: query
      description: |
        Opaque position taken from the previous page's `next_cursor`. It is
        bound to the caller, the filters and the sort it was produced under.
      schema:
        type: string
    DealId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    StakeholderIncludeRemoved:
      name: include_removed
      in: query
      description: |
        Include links that were removed, which are left out by default. A value
        that is not a boolean is 400 rather than read as false.
      schema:
        type: boolean
        default: false
    StakeholderLinkId:
      name: link_id
      in: path
      required: true
      description: One of the deal's stakeholder links.
      schema:
        type: string
        format: uuid
    PipelineId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    StageId:
      name: stage_id
      in: path
      required: true
      description: A stage of the pipeline named in the path.
      schema:
        type: string
        format: uuid
    ColumnLimit:
      name: limit
      in: query
      description: |
        Cards per page, 50 by default. A limit above the cap is clamped to 200
        and `0` reads as the default, so neither bound is declared here: a
        generated client that refused them could not reach the clamp at all.
        A limit that is not a non-negative whole number is 400.
      schema:
        type: integer
        default: 50
    ColumnCursor:
      name: cursor
      in: query
      description: |
        Opaque position taken from the previous page's `next_cursor`. It is
        bound to the column it was drawn from and to the `board_version` it was
        drawn under; a cursor from another column, or from before any card
        moved, is 400.
      schema:
        type: string
    TaskAssignedTo:
      name: assigned_to
      in: query
      description: |
        Whose work to return. `me`, the default, is the only value: the caller
        comes from the session, so no parameter can name another user. Any
        other value is 400 with `issue: "unknown_value"`.
      schema:
        type: string
        default: me
    TaskId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    IfMatchVersion:
      name: If-Match
      in: header
      required: true
      description: |
        The task's current `version`, quoted or not. `*` is refused: "whatever
        is there" is the opposite of what an expected version means.
      schema:
        type: string
    DeleteReason:
      name: reason
      in: query
      description: |
        An optional short answer to "why not", kept as the caller's own
        private feedback on the idea. Nobody else ever reads it.
      schema:
        type: string
    PrerequisiteTaskId:
      name: prerequisite_id
      in: path
      required: true
      description: |
        The task that must finish first. It lives in the same project root, or
        belongs to the same person, as the task in `id`.
      schema:
        type: string
        format: uuid
