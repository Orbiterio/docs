Design fixtures only. No production writes.
tagline-qa.json records live OpenRouter tagline QA on 2026-09-09: supplied hand-drafted descriptions, exact requests, accepted outputs and stage telemetry. The production description/tagline stages remain pending. These receipts are not node properties or runtime configuration.
Graph simulation contains proposed system output using real selected source records.
source-evidence.json retains statement IDs, revisions, qualifiers and source references.
Internal UUIDs, fact-election status, graph timestamps and descriptions are simulated.
The three game nodes are internal and hidden until game-source artwork and game-owned publication complete. Counts include internal graph records, not public game cards; IMDb artwork is excluded.
person-role-intake.json preserves all ten supplied role families for a proposed bounded person-credit probe. It is not enabled runtime configuration and adds no graph nodes or edges.
the-game-awards-observations.json contains selected official award-page observations; these are companion evidence, not extra materialized graph nodes or elected source facts. The existing award UUID is preserved with an outcome-independent nominee-entry key.

type-source-contract.json: games-v1 design baseline for 12 source classes, six node families and 15 game endpoint edge variants. No runtime activation.
support-type-examples.json: four companion nodes and three edges with one existing core endpoint; source-backed engine/release illustrations, internal IDs and projection state simulated. Core 29-node/30-edge counts remain unchanged.

Build-agent handoff (2026-09-09): start at /guides/open-work/video-game-waterfall-build-guide. build-evals.json defines 55 required unexecuted eval cases across phases 0-8; it is not an execution report. build-freeze.json pins inspected code and local document hashes. Older graph examples need phase-0 scope-reference/envelope qualification and do not cover all fifteen edge families. Runtime/production validation remains pending.
