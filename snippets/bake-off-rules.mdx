<Warning>
**Bake-off rules — mandatory for every lane, every phase (Mark, 2026-09-06).** Provider and model choices named on this page are *defaults to beat*, not decisions. Before a provider or model is wired into a code path, run the bake-off below on the lane's own Phase 0 fixtures, record the result on this page (a dated table: candidates, fixture set, quality metric, cost per 1K calls, the pick and why), and only then set the config value.

1. **Web search: Exa vs. [Parallel Search](https://parallel.ai/products/search), on real data.** Every Exa/search call site — profile discovery, page discovery, corroboration lookups and fallbacks — must be smoke-tested and run against **both** providers on identical queries drawn from real lane data. Score the actual task (correct identity/page, false candidate rate, downstream gate errors), then latency and cost. A win on one task does not qualify another, and neither provider is the house default. Keep the adapters interchangeable behind the search seam. Retain source inputs, query settings, raw responses and scored results; synthetic negative controls supplement real cases, never replace them. Smoke-test the winner end to end through the production seam before enabling it.
2. **LLM models: the proposed model plus at least FOUR other OpenRouter candidates — FIVE total minimum per call site (Mark's clarification, 2026-09-06).** Every LLM step, including extraction, classification, adjudication, synthesis, descriptions and weak-signal retries/fallbacks, must be smoke-tested and baked off across **≥ 5 distinct models** from the [OpenRouter](https://openrouter.ai/models) catalogue on labelled real lane data. Score **quality first**, **price second** at actual prompt sizes. Set the quality tolerance before running; retain held-out cases. Include frontier, mid-tier and budget candidates, and record model/provider IDs, prompt version, temperature, reasoning setting, `max_tokens`, structured-output validity, latency and usage/cost. Empty/truncated answers count as failures. A different lane's results are candidate evidence, not a waiver. Retain raw outputs and scores, then smoke-test the selected configuration end to end before wiring/enabling it.
3. **Re-run when the world changes.** A new model generation, a provider price change, or a measured quality regression re-opens the bake-off for that call site. The dated table on the page is the audit trail.
</Warning>
