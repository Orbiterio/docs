{
  "schema_check": "passed",
  "environment": "disposable local PostgreSQL cluster on a private Unix socket; no network listener or application database connection",
  "tables": 16,
  "columns": 216,
  "foreign_keys": 63,
  "checks": [
    "Reference DDL applied against minimal external-key fixtures.",
    "Actual Ethan/Katelyn headline and all four summary paragraphs retained in two context versions; changing working headline leaves original object unchanged.",
    "Contact and suggestion deletion preserve identity and source snapshots.",
    "Root deletion is blocked while history exists."
  ],
  "not_tested": [
    "Application transaction guards, authorization, state engine, real models and email delivery are implementation work."
  ],
  "rejected_invalid_references": [
    "Cross-message revision ancestry",
    "Cross-message generation ancestry",
    "Cross-message streaming route",
    "Cross-owner introduction restart",
    "Wrong-slot current message version",
    "Wrong-slot active generation",
    "Duplicate shared final slot"
  ]
}
