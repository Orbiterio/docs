{
  "schema_check": "passed",
  "environment": "disposable local PostgreSQL cluster on a private Unix socket; no network listener or application database connection",
  "tables": 16,
  "columns": 211,
  "foreign_keys": 62,
  "checks": [
    "Reference DDL applied against minimal external-key fixtures.",
    "Two WHY/context versions and original paragraph retained.",
    "Contact and suggestion deletion preserve identity and source snapshots.",
    "Root deletion is blocked while history exists."
  ],
  "not_tested": [
    "Application transaction guards, authorization, state engine, real models and email delivery are implementation work."
  ]
}
