---
title: "Required Tables for Enrichment"
description: "Deployment-phase table inventory and live Xano schemas for Person, Company, IMDB, and Music enrichment."
icon: "table-columns"
---

<Note>
  **Last updated:** 2026-07-29 at 19:06 EAT (Africa/Nairobi, Kenya).
</Note>

<Warning>
  Deploy the **139 tables in Person & Company Enrichment now**. The **17 IMDB & Music Enrichment tables are deferred** to the later media-waterfall phase.
</Warning>

This page separates the enrichment system's **156 unique relational tables** by deployment phase. Classification starts at `mvp/get-add/master-person-new` **#13039**, follows the live Person and Company execution paths, and stops traversal at IMDB- and Music-specific processing handoffs.

Any table read or written by the Person or Company waterfall is listed under **Person & Company Enrichment**. The later section contains only tables not touched until IMDb or Music processing. A table appears exactly once.

<Note>
  Some media-named tables remain in the first phase because Person-side orchestration or terminal readback touches them directly: `film_tv_award`, `imdb_award_join`, `imdb_credit_join`, `imdb_person`, `mb_artist`, `music_reverse_probe`, `queue_imdb_award_graph`, `queue_imdb_title`, and `queue_mb_artist`.
</Note>

The audit was refreshed from **Xano workspace 3 (`OrbiterV2`), branch `v1`, on 2026-07-29**. FalkorDB graph labels/relationships and external-provider payloads are intentionally excluded because they are not relational tables.

<Note>
  The schemas below are exact XanoScript snapshots, including nullability, defaults, list styles, foreign-table references, validators, access visibility, and indexes. They are intended as copy/paste context for the Go/AlloyDB implementation team and AI-assisted schema translation. Xano table IDs are included for source-system traceability.
</Note>

<Note>
  **Required funding tables confirmed:** Xano `company_funding_round` **#201** exists and maps to the Universe table named `funding_round`. Xano `company_investment_partner` **#266** and `company_investor` **#265** also exist and are retained as financial relationship tables.
</Note>

## Person & Company Enrichment

These **139 tables** are the deploy-now set for the Person and Company enrichment phase.

### Unique table inventory

| Table | Xano ID | Waterfall access |
| --- | ---: | --- |
| `about_company` | **#366** | Read + write |
| `about_person` | **#365** | Read + write |
| `award` | **#305** | Read |
| `back_fill_funding_round` | **#625** | Read + write |
| `cb_company` | **#467** | Read + write |
| `certification` | **#283** | Read + write |
| `city` | **#332** | Read + write |
| `city_name_alias` | **#771** | Read |
| `collection_node` | **#356** | Read + write |
| `collection_question_node` | **#568** | Read + write |
| `company_acquirer` | **#239** | Read + write |
| `company_enrich_data` | **#485** | Read + write |
| `company_financial` | **#287** | Read + write |
| `company_funding_round` | **#201** | Read + write — **required**; Universe: `funding_round` |
| `company_investment_partner` | **#266** | Read + write — financial |
| `company_investor` | **#265** | Read + write — financial |
| `company_view` | **#402** | Read + write |
| `country` | **#173** | Read + write |
| `crunchbase_company` | **#238** | Read + write |
| `date_descriptor` | **#243** | Read |
| `discover_context_node` | **#572** | Read + write |
| `discover_question_context_node` | **#592** | Read + write |
| `discover_question_response_node` | **#565** | Read + write |
| `discover_response_node` | **#567** | Read + write |
| `domain_expertise` | **#459** | Read |
| `education_experience` | **#230** | Read + write |
| `email_provider` | **#175** | Read |
| `enrich_history_company` | **#404** | Read + write |
| `enrich_history_person` | **#160** | Read + write |
| `enrichment_date` | **#763** | Read + write |
| `environment_variables` | **#272** | Read |
| `environmental_variables_falkordb` | **#508** | Read |
| `error_current_work` | **#615** | Read + write |
| `error_location_nodes` | **#602** | Read + write |
| `expertise_identification_log` | **#660** | Read + write |
| `expertise_join` | **#469** | Read |
| `file_attached_join` | **#589** | Read + write |
| `film_tv_award` | **#396** | Read + write |
| `founders` | **#320** | Read + write |
| `fundable_angel_investments` | **#679** | Read |
| `fundable_deals` | **#671** | Read + write |
| `fundable_institutional_investments` | **#678** | Read |
| `fundable_institutional_investments_person` | **#681** | Read |
| `fundable_organization_associations` | **#673** | Read |
| `fundable_organizations` | **#666** | Read + write |
| `fundable_people` | **#667** | Read + write |
| `honor` | **#573** | Read + write |
| `human_review_required` | **#757** | Read + write |
| `imdb_award_join` | **#407** | Read + write |
| `imdb_credit_join` | **#397** | Read + write |
| `imdb_person` | **#394** | Read + write |
| `industry` | **#192** | Read + write |
| `industry_join` | **#307** | Read + write |
| `instagram_company` | **#430** | Read + write |
| `instagram_person` | **#369** | Read + write |
| `interest` | **#180** | Read + write |
| `interest_join` | **#327** | Read + write |
| `investment_thesis` | **#709** | Read + write |
| `investor_profile_company` | **#478** | Read + write |
| `investor_profile_person` | **#475** | Read + write |
| `keyword_join` | **#306** | Read + write |
| `language_join` | **#326** | Read + write |
| `languages` | **#178** | Read + write |
| `leverage_loop_question_node` | **#557** | Read + write |
| `leverage_loop_trajectory_node` | **#571** | Read + write |
| `link_type` | **#165** | Read |
| `linkedin_follower` | **#495** | Read + write |
| `linkedin_profile_company` | **#499** | Read + write |
| `live_event_companies` | **#704** | Read + write |
| `live_event_participants` | **#703** | Read + write |
| `log_agent_tools` | **#470** | Read + write |
| `log_crash` | **#542** | Read + write |
| `log_enrichment_company` | **#632** | Read + write |
| `log_enrichment_person` | **#579** | Write |
| `marketing_prefix` | **#174** | Read |
| `master_address` | **#164** | Read + write |
| `master_avatar` | **#227** | Read + write |
| `master_banner` | **#228** | Read + write |
| `master_company` | **#142** | Read + write |
| `master_date` | **#762** | Read + write |
| `master_email` | **#155** | Read + write |
| `master_link` | **#166** | Read + write |
| `master_person` | **#139** | Read + write |
| `master_phone` | **#151** | Read + write |
| `mb_artist` | **#737** | Read + write |
| `merge_audit` | **#721** | Read + write |
| `merge_lock` | **#722** | Read + write |
| `merge_suspect` | **#284** | Read + write |
| `music_reverse_probe` | **#756** | Read + write |
| `my_company` | **#149** | Read + write |
| `my_company_address_join` | **#314** | Read + write |
| `my_company_email_join` | **#315** | Read + write |
| `my_company_link_join` | **#317** | Read + write |
| `my_company_phone_join` | **#316** | Read + write |
| `needs_more_info` | **#249** | Read + write |
| `node_registry` | **#758** | Read + write |
| `organization` | **#695** | Read + write |
| `outcome_suggestion` | **#531** | Read + write |
| `outcome_suggestion_node` | **#534** | Read + write |
| `outcome_trajectory_node` | **#561** | Read + write |
| `permalink_company` | **#431** | Read + write |
| `permalink_person` | **#465** | Read + write |
| `person_alias` | **#759** | Read + write |
| `person_enrich_data` | **#500** | Read + write |
| `person_has_expertise` | **#711** | Read + write |
| `person_terminal_company_dependency` | **#760** | Read + write |
| `person_terminal_stage` | **#761** | Read + write |
| `personal_website` | **#513** | Write |
| `pipeline_lifecycle_epoch` | **#767** | Read |
| `primary_location` | **#385** | Read + write |
| `profile_enrichment_job` | **#686** | Read + write |
| `profile_enrichment_log` | **#770** | Write |
| `project` | **#575** | Read + write |
| `publication` | **#574** | Read + write |
| `queue_enrich_company` | **#583** | Read + write |
| `queue_enrich_funding` | **#584** | Read + write |
| `queue_enrich_person` | **#582** | Read + write |
| `queue_enrich_vc_firm` | **#585** | Read + write |
| `queue_imdb_award_graph` | **#764** | Read + write |
| `queue_imdb_title` | **#707** | Read + write |
| `queue_mb_artist` | **#745** | Read + write |
| `region` | **#331** | Read + write |
| `role` | **#179** | Read + write |
| `role_join` | **#329** | Read + write |
| `runtime_metadata` | **#733** | Read |
| `serendipity_suggestion_node` | **#537** | Read + write |
| `skills` | **#130** | Read + write |
| `skills_join` | **#325** | Read + write |
| `speciality_join` | **#456** | Read + write |
| `specialty` | **#455** | Read + write |
| `sub_domain_expertise` | **#654** | Read + write |
| `team_question_node` | **#663** | Read + write |
| `twitter_company` | **#429** | Read + write |
| `twitter_join` | **#497** | Read + write |
| `twitter_person` | **#368** | Read + write |
| `vc_company` | **#277** | Read + write |
| `volunteering` | **#577** | Read + write |
| `work_experience` | **#147** | Read + write |
| `youtube` | **#441** | Read + write |

### Copy/paste schema reference

<Accordion title="about_company — #366">

```xanoscript
table about_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id?=0 {
      table = "master_company"
    }

    text description? filters=trim
    text hash? filters=trim
    int? data_source_id?=0 {
      table = "data_source"
    }

    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="about_person — #365">

```xanoscript
table about_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    text biography? filters=trim
    text hash? filters=trim
    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="award — #305">

```xanoscript
table award {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id {
      table = "master_person"
    }

    text image_url? filters=trim
    text? award_name? filters=trim
    text award_details? filters=trim
    text project_name? filters=trim
    date? date?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="back_fill_funding_round — #625">

```xanoscript
table back_fill_funding_round {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_company_id? {
      table = "master_company"
    }

    text company_domain? filters=trim
    text[] missing_rounds? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="cb_company — #467">

```xanoscript
table cb_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id?=0 {
      table = "master_company"
    }

    json cb_company_page?
    json response?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["enrichment"]
}
```

</Accordion>

<Accordion title="certification — #283">

```xanoscript
table certification {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text node_uuid? filters=trim
    int? master_person_id? {
      table = "master_person"
    }

    text name? filters=trim
    text description? filters=trim
    text org_name? filters=trim
    text? org_li_url? filters=trim
    text? org_domain? filters=trim
    int? master_company_id?=0 {
      table = "master_company"
    }

    text? issue_date? filters=trim
    text? edge_id? filters=trim
    text? certification_url? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="city — #332">

```xanoscript
table city {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text city_name filters=trim
    text? node_uuid? filters=trim
    int? region_id? {
      table = "region"
    }

    int? country_id? {
      table = "country"
    }

    text? description? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["location tables"]
}
```

</Accordion>

<Accordion title="city_name_alias — #771">

```xanoscript
// Curated city-name alias map consulted by the city natural-key builders (#4528, #4690)
// AFTER the generic " City" suffix strip. One row per human-verified identity claim:
// alias resolves to canonical (both matched lowercase). Never generic rules here
// (Kansas City != Kansas). Populated per-datasource; sandbox-first per Mark 2026-07-28,
// no live backfill.
table city_name_alias {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    // Normalized (lowercase, trimmed) non-canonical city name
    text alias filters=trim|lower

    // Normalized (lowercase, trimmed) canonical city name the alias resolves to
    text canonical filters=trim|lower

    // Why this pair is an identity (provenance for future curators)
    text notes?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "alias", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="collection_node — #356">

```xanoscript
// Has all the data referenced by a collection
table collection_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int collection_id {
      table = "collection"
    }

    decimal rating?
    int added_by_user_id? {
      table = "user"
    }

    text label?
    text name?
    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid?
    text? film_tv_uuid?
    text? film_tv_award_uuid?
    text? live_event_node_uuid?
    text? music_group_uuid? filters=trim
    text? music_artist_uuid? filters=trim
    text? music_recording_uuid? filters=trim
    bool selected?
    json paths?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "id"}, {name: "name"}]
  tags = ["collection", "card node"]
}
```

</Accordion>

<Accordion title="collection_question_node — #568">

```xanoscript
// Has all the data referenced by a collection question
table collection_question_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int collection_question_id {
      table = "collection_question"
    }

    int added_by_user_id? {
      table = "user"
    }

    decimal rating?
    bool selected?
    text label?
    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid_?
    text? film_tv_uuid?
    text film_tv_award_uuid?
    json paths?
    text? live_event_node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["collection", "card node"]
}
```

</Accordion>

<Accordion title="company_acquirer — #239">

```xanoscript
table company_acquirer {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text company_name? filters=trim
    int? master_company_id? {
      table = "master_company"
    }

    text role? filters=trim
    text acquirer_permalink? filters=trim
    int acquired_master_company_id? {
      table = "master_company"
    }

    object acquisition_price? {
      schema {
        int value?
        text currency? filters=trim
        int value_usd?
      }
    }

    object acquisition_news? {
      schema {
        date? announced_on?
        text news_title? filters=trim
        text news_link? filters=trim
      }
    }

    bool triple_added?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["financial"]
}
```

</Accordion>

<Accordion title="company_enrich_data — #485">

```xanoscript
table company_enrich_data {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_company_id?=0 {
      table = "master_company"
    }

    json fundable?
    json company_llm?
    json exa_c_suite?
    json website_scrape?
    json about_page?
    json linkedin_profile?
    json people_data_labs?
    json enrich_layer?
    json enrich_data?
    json yc_data?
    json yc_partner_page?
    json pitchbook_crunchbase_leads?
    json crunchbase_page?
    json contactout?
    json scrapin?
    json signal_nfx_scrape?
    json signal_nfx_json?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["enrichment"]
}
```

</Accordion>

<Accordion title="company_financial — #287">

```xanoscript
// financial data for company side panel > financial tab
table company_financial {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_company_id? {
      table = "master_company"
    }

    text? company_type?="Privately Held" filters=trim
    text? yc_batch? filters=trim
    object funding_total? {
      schema {
        int? value?
        text currency? filters=trim
        int? value_usd?
      }
    }

    int? total_rounds_raised?
    bool is_public?
    date? went_public_on?
    text? stock_label? filters=trim
    text? ticker? filters=trim
    text? primary_exchange? filters=trim
    text? stock_link? filters=trim
    text? acquired_by_permalink? filters=trim
    int? acquirer_master_company_id?=0 {
      table = "master_company"
    }

    int? acquisition_price?
    object revenue? {
      schema {
        int? revenue_usd?
        text? revenue_citation? filters=trim
        text? reveue_citation_url? filters=trim
        text? revenue_display? filters=trim
      }
    }

    bool acquired_triple_added?
    bool rounds_added?
    bool rounds_processing?
    text? funding_summary? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["financial"]
}
```

</Accordion>

<Accordion title="company_funding_round — #201">

```xanoscript
// table of funding for companies - filled by enrichment
table company_funding_round {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text? node_uuid? filters=trim
    text? round_permalink? filters=trim
    int? master_company_id? {
      table = "master_company"
    }

    text funding_type? filters=trim
    object? money_raised? {
      schema {
        int? value?
        text? currency? filters=trim
        int? value_usd?
        text? citation? filters=trim
        int? raised_at_valutaion_usd?
      }
    }

    date? announced_date?
    int? num_investors?
    text? edge_id? filters=trim
    int? data_source_id?=0 {
      table = "data_source"
    }

    text? description? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "funding_type"}]
  tags = ["financial", "depreciated"]
}
```

</Accordion>

<Accordion title="company_investment_partner — #266">

```xanoscript
table company_investment_partner {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    int? company_funding_round_id? {
      table = "company_funding_round"
    }

    int? company_investors_id? {
      table = "company_investor"
    }

    int? vc_master_company_id? {
      table = "master_company"
    }

    text partner_name? filters=trim
    text? partner_crunchbase_url? filters=trim
    text? partner_linkedin_url? filters=trim
    text? triple_to_vc_id? filters=trim
    text? triple_to_round_id? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["financial", "depreciated"]
}
```

</Accordion>

<Accordion title="company_investor — #265">

```xanoscript
table company_investor {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? company_funding_rounds_id? {
      table = "company_funding_round"
    }

    text? investor_name? filters=trim
    bool is_company?
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? investor_crunchbase_url? filters=trim
    text? investor_linkedin_url? filters=trim
    text? investor_domain? filters=trim
    bool is_lead_investor?
    int? amount_invested?
    text? investment_stage? filters=trim
    text? edge_id? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "investor_name"}]
  tags = ["financial", "depreciated"]
}
```

</Accordion>

<Accordion title="company_view — #402">

```xanoscript
// How many times a user views a company
table company_view {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int user_id {
      table = "user"
    }

    int master_company_id {
      table = "master_company"
    }

    int views
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {type: "btree", field: [{name: "user_id", op: "asc"}]}
  ]

  tags = ["view count"]
}
```

</Accordion>

<Accordion title="country — #173">

```xanoscript
// table of country codes, flags, etc
table country {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text country_code? filters=trim
    text flag? filters=trim
    text alpha2? filters=trim
    text? alpha3? filters=trim
    text country_name? filters=trim
    text? description? filters=trim
    text? node_uuid? filters=trim
    json properties?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "country_name"}, {name: "id"}]
  tags = ["geo", "location tables"]
}
```

</Accordion>

<Accordion title="crunchbase_company — #238">

```xanoscript
table crunchbase_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_company_id?=0 {
      table = "master_company"
    }

    text? crunchbase_url? filters=trim
    text? crunch_uuid? filters=trim
    text? crunch_permalink? filters=trim
    text? company_name? filters=trim
    text? company_domain? filters=trim
    text? short_description? filters=trim
    text? overview_description? filters=trim
    text? growth_insight_description? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "company_name"}]
  tags = ["maybe depreciated??"]
}
```

</Accordion>

<Accordion title="date_descriptor — #243">

```xanoscript
// Its a growing list of date descriptors, but user can add custom ones for themselves
table date_descriptor {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text descriptor?
    int? user_id? {
      table = "user"
    }

    // stable machine key (birthday, funding_round, ...); all writer lookups + natural keys reference slug, never id. Null on user_custom rows.
    text? slug?

    // global = built-in vocabulary; system = deterministic/captured descriptors (hidden from custom-descriptor UI); user_custom = user-created
    enum kind?="user_custom" {
      values = ["global", "system", "user_custom"]
    }

    // one row per entity for this descriptor (birthday, death, founded, ipo, acquisition); natural key = slug only
    bool singleton?

    // default for contacts_date.annual_recurring when a date of this kind is created (birthday/anniversary yes, graduation no)
    bool default_recurring?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      name : "descriptor_search"
      lang : "english"
      type : "search"
      field: [{name: "descriptor", op: "A"}]
    }
    {type: "btree|unique", field: [{name: "slug", op: "asc"}]}
  ]

  autocomplete = [{name: "descriptor"}]
}
```

</Accordion>

<Accordion title="discover_context_node — #572">

```xanoscript
table discover_context_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int discover_id? {
      table = "discover"
    }

    text? name?
    text label?
    text edge_relation?
    text? edge_context?
    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid?
    text? film_tv_uuid?
    text? film_tv_award_uuid?
    json paths?
    text? live_event_node_uuid?
    text? music_group_uuid?
    text? music_artist_uuid?
    text? music_recording_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["discover", "refactor"]
}
```

</Accordion>

<Accordion title="discover_question_context_node — #592">

```xanoscript
table discover_question_context_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int discover_question_id? {
      table = "discover_question"
    }

    text name?
    text label?
    text edge_relation?
    text? edge_context?
    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid?
    text? film_tv_uuid?
    text? film_tv_award_uuid?
    json paths?
    text? live_event_node_uuid?
    text? music_group_uuid?
    text? music_artist_uuid?
    text? music_recording_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["discover", "refactor"]
}
```

</Accordion>

<Accordion title="discover_question_response_node — #565">

```xanoscript
table discover_question_response_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int discover_question_id? {
      table = "discover_question"
    }

    text? name?
    text label?
    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid?

    // Unique identifier for a film/TV award.
    text? film_tv_award_uuid?

    // Unique identifier for a film/TV entity.
    text? film_tv_uuid?

    text? live_event_node_uuid?
    json? paths?

    // True while this row's node_path rows are being (re)generated.
    bool paths_processing?

    text? music_group_uuid?
    text? music_artist_uuid?
    text? music_recording_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["discover", "refactor"]
}
```

</Accordion>

<Accordion title="discover_response_node — #567">

```xanoscript
table discover_response_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int discover_id? {
      table = "discover"
    }

    text label?
    text name?
    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid?
    text? film_tv_uuid?
    text? film_tv_award_uuid?
    text? live_event_node_uuid?
    json paths?

    // True while this row's node_path rows are being (re)generated.
    bool paths_processing?

    text? music_group_uuid?
    text? music_artist_uuid?
    text? music_recording_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["discover", "refactor"]
}
```

</Accordion>

<Accordion title="domain_expertise — #459">

```xanoscript
table domain_expertise {
  auth = false

  schema {
    int id

    // Row creation timestamp (added 2026-07-01; legacy rows null)
    timestamp? created_at?=now

    text name?
    int? expertise_category_id?=0 {
      table = "expertise_category"
    }

    text? description?
    text? node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
  ]

  tags = ["system table", "domainof expertise"]
}
```

</Accordion>

<Accordion title="education_experience — #230">

```xanoscript
table education_experience {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int master_person_id? {
      table = "master_person"
    }

    text? school_name? filters=trim
    int? master_company_id? {
      table = "master_company"
    }

    text? linkedin_url? filters=trim
    text? school_domain? filters=trim
    text[] field_of_study? filters=trim
    text[] degree_name? filters=trim
    text[] activities_societies? filters=trim
    int? start_month?
    int? start_year?
    int? end_month?
    int? end_year?
    int data_source_id? {
      table = "data_source"
    }

    text? edge_uuid? filters=trim
    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="email_provider — #175">

```xanoscript
// list of email providers - used to test if email is non-company (personal)
table email_provider {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text provider_domain? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["system table"]
}
```

</Accordion>

<Accordion title="enrich_history_company — #404">

```xanoscript
// record of every enrichment attempt - successful or not
table enrich_history_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    bool enrich_success?
    int? master_company_id?=0 {
      table = "master_company"
    }

    date? last_updated?
    int data_source_id? {
      table = "data_source"
    }

    json data?
    bool processing?
    int? processing_time?
    text source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
  ]

  autocomplete = [{name: "id"}]
  tags = ["enrichment"]
}
```

</Accordion>

<Accordion title="enrich_history_person — #160">

```xanoscript
// record of every enrichment attempt - successful or not
table enrich_history_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    bool enrich_success?
    int? master_person_id? {
      table = "master_person"
    }

    date? last_updated?
    int data_source_id? {
      table = "data_source"
    }

    json data?
    bool processing?
    int? processing_time?
    text source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "id"}]
  tags = ["enrichment"]
}
```

</Accordion>

<Accordion title="enrichment_date — #763">

```xanoscript
// Projection mirror of master_date (Backend tier). READ-ONLY for the app —
// written only by dates/project-master-dates (demo bridge; replaced by the real
// Universe->Backend push at split time). Upsert contract: (master_person_id,
// master_company_id, natural_key). Spec: guides/open-work/important-dates-mem0
table enrichment_date {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    int date_descriptor_id? {
      table = "date_descriptor"
    }

    int? year?
    int? month?
    int? day?
    date? date?
    text? natural_key? filters=trim
    json? metadata?

    // when the projection last touched this row
    timestamp? synced_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [
        {name: "master_person_id", op: "asc"}
        {name: "master_company_id", op: "asc"}
        {name: "natural_key", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="environment_variables — #272">

```xanoscript
// Environment Variables for both environments live and staging
table environment_variables {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    // Name of the Variable
    text name? filters=trim

    // Value of the env variable
    text value? filters=trim

    bool is_nylas?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["system table"]
}
```

</Accordion>

<Accordion title="environmental_variables_falkordb — #508">

```xanoscript
table environmental_variables_falkordb {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text graph_name? filters=trim
    text? user_name? filters=trim
    text password? filters=trim
    text host? filters=trim
    text port? filters=trim
    text db_connection? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["system table"]
}
```

</Accordion>

<Accordion title="error_current_work — #615">

```xanoscript
table error_current_work {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id? {
      table = "master_person"
    }

    text domain? filters=trim
    text profile_url? filters=trim
    text name? filters=trim
    int master_company_id? {
      table = "master_company"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="error_location_nodes — #602">

```xanoscript
table error_location_nodes {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_company_id? {
      table = "master_company"
    }

    text? city? filters=trim
    text region? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["test utilities"]
}
```

</Accordion>

<Accordion title="expertise_identification_log — #660">

```xanoscript
table expertise_identification_log {
  auth = false

  schema {
    int id
    int master_person_id?
    text? person_name?
    text? person_node_uuid?
    int total_expertise?
    int matched?
    int created?
    int skipped?
    json? llm_expertise_raw?
    json? resolve_results?
    json? skipped_items?
    text? model_used?
  }

  index = [{type: "primary", field: [{name: "id"}]}]
  tags = ["log", "expertise", "QA"]
}
```

</Accordion>

<Accordion title="expertise_join — #469">

```xanoscript
table expertise_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? domain_of_expertise_id?=0 {
      table = ""
    }

    int? master_person_id?=0 {
      table = "master_person"
    }

    text? triple_id? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="file_attached_join — #589">

```xanoscript
table file_attached_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int file_id {
      table = "file"
    }

    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? edge_id? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="film_tv_award — #396">

```xanoscript
table film_tv_award {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text? node_uuid? filters=trim
    int? year?
    text? award_type? filters=trim
    text? award_description? filters=trim
    text? nominee_or_winner? filters=trim
    text[]? shared_with? filters=trim
    int? film_tv_id? {
      table = "film_tv"
    }

    text? project_title? filters=trim
    text? project_imdb_url? filters=trim
    int? imdb_person_id? {
      table = "imdb_person"
    }

    text? canonical_event_key? filters=trim
    text? canonical_identity_key? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {type: "btree", field: [{name: "canonical_event_key"}]}
    {type: "btree", field: [{name: "canonical_identity_key"}]}
    {
      type : "btree|unique"
      field: [
        {name: "canonical_identity_key", op: "asc"}
        {name: "imdb_person_id", op: "asc"}
      ]
    }
  ]

  tags = ["imdb"]
}
```

</Accordion>

<Accordion title="founders — #320">

```xanoscript
table founders {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text name? filters=trim
    text? title? filters=trim
    int? master_company_id? {
      table = "master_company"
    }

    int? master_person_id? {
      table = "master_person"
    }

    text? linkedin_url? filters=trim
    text? person_permalink? filters=trim
    text? triple_id? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="fundable_angel_investments — #679">

```xanoscript
table fundable_angel_investments {
  auth = false

  schema {
    // Unique identifier
    uuid id

    // Foreign key to people table
    uuid person_id {
      table = "fundable_people"
    }

    // Foreign key to deals table
    uuid deal_id {
      table = "fundable_deals"
    }

    // Whether this angel was the lead investor
    bool lead_investor?

    // Record creation timestamp
    timestamp created_at?=now

    // Record last update timestamp
    timestamp updated_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "person_id", op: "asc"}]}
    {type: "btree", field: [{name: "deal_id", op: "asc"}]}
  ]

  tags = ["fundable"]
}
```

</Accordion>

<Accordion title="fundable_deals — #671">

```xanoscript
table fundable_deals {
  auth = false

  schema {
    // Unique identifier for the financing
    uuid id

    // Record creation timestamp
    timestamp created_at?=now

    // Foreign key to organizations table
    uuid organization_id {
      table = "fundable_organizations"
    }

    // Groups multiple financings as part of the same deal; links to articles
    uuid canonical_deal_id

    // Financing amount in USD
    decimal size_usd?

    // Financing amount in original currency
    decimal size?

    // Type of financing (e.g., SEED, SERIES_A, EQUITY)
    text? financing_type?

    // Currency code for the financing amount
    text? currency?

    // Whether this is an extension round
    bool extension?

    // Intermediate round indicator (e.g., +, 1, 2, 3)
    text? intermediate?

    // Date of the financing
    timestamp date?

    // Whether this is a pre-Series round
    bool pre?

    // Brief description of the deal
    text? short_description?

    // Detailed description of the deal
    text? long_description?

    // Record last update timestamp
    timestamp updated_at?

    text? funding_round_node_uuid?

    // Set by cascade-deal-participants when this deal has been materialized into the graph (Funding_Round node + edges). Null = needs processing; non-null = skip.
    timestamp? processed_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [{name: "organization_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "canonical_deal_id", op: "asc"}]
    }
  ]

  tags = ["fundable"]
}
```

</Accordion>

<Accordion title="fundable_institutional_investments — #678">

```xanoscript
table fundable_institutional_investments {
  auth = false

  schema {
    // Unique identifier for the institutional investment
    uuid id

    // Foreign key to organizations table (investor)
    uuid organization_id {
      table = "fundable_organizations"
    }

    // Foreign key to deals table
    uuid deal_id {
      table = "fundable_deals"
    }

    // Whether this investor was the lead
    bool lead_investor?

    // Array of personnel names associated with investment
    text[]? personnel?

    // Array of fund names used for the investment
    text[]? fund?

    // Record creation timestamp
    timestamp created_at?=now

    // Record last update timestamp
    timestamp updated_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [{name: "organization_id", op: "asc"}]
    }
    {type: "btree", field: [{name: "deal_id", op: "asc"}]}
  ]

  tags = ["fundable"]
}
```

</Accordion>

<Accordion title="fundable_institutional_investments_person — #681">

```xanoscript
table fundable_institutional_investments_person {
  auth = false

  schema {
    // Unique identifier
    uuid id

    // Foreign key to institutional_investments
    uuid institutional_investment_id {
      table = "fundable_institutional_investments"
    }

    // Foreign key to people table
    uuid person_id {
      table = "fundable_people"
    }

    // Record creation timestamp
    timestamp created_at?=now

    // Record last update timestamp
    timestamp updated_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [{name: "institutional_investment_id", op: "asc"}]
    }
    {type: "btree", field: [{name: "person_id", op: "asc"}]}
  ]

  tags = ["fundable"]
}
```

</Accordion>

<Accordion title="fundable_organization_associations — #673">

```xanoscript
table fundable_organization_associations {
  auth = false

  schema {
    // Unique identifier for the association
    uuid id

    // Foreign key to people table
    uuid person_id {
      table = "fundable_people"
    }

    // Foreign key to organizations table
    uuid organization_id {
      table = "fundable_organizations"
    }

    // Whether the person is a founder
    bool is_founder?

    // Whether the person is the CEO
    bool is_ceo?

    // Whether the person is designated as key personnel
    bool is_key_person?

    // Whether this is a current position
    bool is_current?

    // Job title at the organization
    text? title?

    // Record creation timestamp
    timestamp created_at?=now

    // Record last update timestamp
    timestamp updated_at?

    // True if this is an investor position, False if company
    bool is_investor?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "person_id", op: "asc"}]}
    {
      type : "btree"
      field: [{name: "organization_id", op: "asc"}]
    }
  ]

  tags = ["fundable"]
}
```

</Accordion>

<Accordion title="fundable_organizations — #666">

```xanoscript
table fundable_organizations {
  auth = false

  schema {
    // Unique identifier for the organization
    uuid id

    // Crunchbase permalink
    text? crunchbase?

    // Organization name
    text? name?

    // Legal name of the organization from Crunchbase
    text? legal_name?

    // Full description from Crunchbase
    text? crunchbase_full_description?

    // About section from Crunchbase
    text? crunchbase_about?

    // Current operating status (active, closed, null)
    text? operating_status?

    // Whether the organization has been acquired
    bool acquired?

    // non_profit, for_profit, null
    text? company_type?

    // Brief description of the organization
    text? short_description?

    // Detailed description of the organization
    text? long_description?

    // ISO country code
    text? country_code?

    // Geographic region
    text? region?

    // Full address
    text? address?

    // Date the organization was founded
    timestamp founded_date?

    // IPO status (e.g., private, public, delisted, null)
    text? ipo_status?

    // Organization website URL
    text? website?

    // Website domain
    text? domain?

    // LinkedIn company slug
    text? linkedin?

    // Full LinkedIn company URL
    text? linkedin_url?

    // Full Crunchbase organization URL
    text? crunchbase_url?

    // PitchBook URL
    text? pitchbook_url?

    // Facebook profile URL or handle
    text? facebook?

    // Twitter handle or URL
    text? twitter?

    // Contact email address
    text? contact_email?

    // Contact phone number
    text? contact_phone?

    // Logo or profile image URL
    text? image?

    // Number of employees (stored as string due to ranges)
    text? num_employees?

    // Total number of funding rounds
    int num_funding_rounds?

    // Total number of unique investors
    int num_investors?

    // Total funding raised in USD millions, excludes secondaries
    decimal total_raised?

    // Most recent valuation in USD millions
    decimal latest_valuation_usd?

    // Date of the latest valuation
    timestamp latest_valuation_date?

    // Type of valuation (pre_money, post_money)
    text? latest_valuation_type?

    // Record creation timestamp
    timestamp created_at?=now

    // Record last update timestamp
    timestamp updated_at?

    // True if institutional investor, False if company
    bool is_investor

    // Investment stage focus (for investors only)
    text? investment_stage?

    int? master_company_id? {
      table = "master_company"
    }

    text? master_company_node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "domain", op: "asc"}]}
    {type: "btree", field: [{name: "linkedin_url", op: "asc"}]}
    {type: "btree", field: [{name: "crunchbase_url", op: "asc"}]}
  ]

  tags = ["fundable"]
}
```

</Accordion>

<Accordion title="fundable_people — #667">

```xanoscript
table fundable_people {
  auth = false

  schema {
    // Unique identifier for the person
    uuid id

    // Full name
    text? name?

    // LinkedIn person slug/ID
    text? linkedin_person_id?

    // Full LinkedIn profile URL
    text? linkedin_person_url?

    // Crunchbase person permalink
    text? crunchbase_person_id?

    // Full Crunchbase profile URL
    text? crunchbase_person_url?

    // X slug
    text? x_id?

    // X url
    text? x_url?

    // Tracxn person ID
    text? tracxn_person_id?

    // Full Tracxn profile URL
    text? tracxn_person_url?

    // CB Insights person ID
    text? cb_insights_person_id?

    // Full CB Insights profile URL
    text? cb_insights_person_url?

    // PitchBook person ID
    text? pitchbook_person_id?

    // Full PitchBook profile URL
    text? pitchbook_person_url?

    // Title from Crunchbase
    text? crunchbase_title?

    // Current title from LinkedIn
    text? linkedin_title?

    // Number of LinkedIn followers
    int followers?

    // Number of LinkedIn connections
    int connections?

    // About/bio section from LinkedIn
    text? about?

    // General location
    text? location?

    // ISO country code
    text? country_code?

    // City of residence or work
    text? city?

    // Profile picture URL
    text? profile_image_url?

    // Banner image URL from LinkedIn
    text? banner_image_url?

    // Contact email address
    text? email?

    // Contact phone number
    text? phone?

    // Record creation timestamp
    timestamp created_at?=now

    // Record last update timestamp
    timestamp updated_at?

    int? master_person_id? {
      table = "master_person"
    }

    text? master_person_node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [{name: "linkedin_person_id", op: "asc"}]
    }
    {
      type : "btree|unique"
      field: [{name: "crunchbase_person_id", op: "asc"}]
    }
  ]

  tags = ["fundable"]
}
```

</Accordion>

<Accordion title="honor — #573">

```xanoscript
table honor {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id? {
      table = "master_person"
    }

    text? title? filters=trim
    text? description? filters=trim
    int? issued_on_day?
    int? issued_on_year?
    int? issued_on_month?
    text? issuer_name? filters=trim
    int? master_company_id? {
      table = "master_company"
    }

    text? issuer_domain? filters=trim
    text? issuer_linkedin_url? filters=trim
    text? node_uuid? filters=trim
    text? edge_id? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="human_review_required — #757">

```xanoscript
table human_review_required {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text node_uuid? filters=trim
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? name? filters=trim
    text? reason? filters=trim
    text? reviewer_note? filters=trim
    bool verified?

    // Review category: role_missing (sourced current role absent from SQL) | role_ended (sourced departure of an allowlisted role) | other
    text? kind?

    // Role title in question (stale-SQL signals from deep research)
    text? title?

    // Organization the role claim concerns
    text? organization?

    // Source URLs backing the signal (resolved from deep-research payload citations)
    json? evidence_urls?

    // Detector that wrote the row: llm_repair_evidence_authorized | llm_repair_evidence_ended | researcher_sql_stale_marker | other
    text? source?

    // profile_enrichment_job.id that produced the evidence
    int? source_job_id?=0
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="imdb_award_join — #407">

```xanoscript
table imdb_award_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? imdb_award_id? {
      table = "film_tv_award"
    }

    int? master_person_id?=0 {
      table = "master_person"
    }

    int? imdb_name_id? {
      table = "imdb_person"
    }

    int? imdb_title_id? {
      table = "film_tv"
    }

    text? project_title? filters=trim
    text? edge_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["imdb"]
}
```

</Accordion>

<Accordion title="imdb_credit_join — #397">

```xanoscript
table imdb_credit_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? imdb_person_id? {
      table = "imdb_person"
    }

    text? person_node_uuid? filters=trim
    text? imdb_person_url? filters=trim
    text[] roles? filters=trim
    text? position? filters=trim
    int? film_tv_id? {
      table = "film_tv"
    }

    text? film_tv_node_uuid? filters=trim
    text? imdb_title_url? filters=trim
    bool edge_created?
    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["imdb"]
}
```

</Accordion>

<Accordion title="imdb_person — #394">

```xanoscript
table imdb_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text? node_uuid?
    int? master_person_id? {
      table = "master_person"
    }

    text? imdb_url?
    text? name?
    bool verified?
    text? image_url?
    text? markdown?
    json json?
    text? award_markdown?
    json awards_json?

    // Last full orchestrator scrape; skip re-scrape within 30 days
    timestamp? enriched_at?

    // Depth at which this entity was first enriched
    int? cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "name"}]
  tags = ["imdb"]
}
```

</Accordion>

<Accordion title="industry — #192">

```xanoscript
// table of industries for companies - filled by enrichment
table industry {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text industry? filters=trim
    text type? filters=trim
    text code? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="industry_join — #307">

```xanoscript
// Join between industry, master_company, master_person
table industry_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int industry_id {
      table = "industry"
    }

    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "desc"}]
    }
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="instagram_company — #430">

```xanoscript
table instagram_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_company_id? {
      table = "master_company"
    }

    text? profile_url? filters=trim|lower
    text? user_name? filters=trim|lower
    text? full_name? filters=trim
    text? biography? filters=trim
    object[] bio_links? {
      schema {
        text? title? filters=trim
        text? lynx_url? filters=trim
        text? url? filters=trim
        text? link_type? filters=trim
      }
    }

    text? external_url? filters=trim
    int? followers?
    int? following?
    text? instagram_id? filters=trim
    text? category_name? filters=trim
    text? profile_pic_url_hd? filters=trim
    json related_profiles?
    json data?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree|unique"
      field: [{name: "profile_url", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="instagram_person — #369">

```xanoscript
table instagram_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? profile_url? filters=trim|lower
    text? full_name? filters=trim
    text? biography? filters=trim
    object[] bio_links? {
      schema {
        text? title? filters=trim
        text? lynx_url? filters=trim
        text? url? filters=trim
        text? link_type? filters=trim
      }
    }

    text? external_url? filters=trim
    int? followed_by?
    text username? filters=trim
    text? profile_pic_url_hd? filters=trim
    int? following?
    json data?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree|unique"
      field: [{name: "profile_url", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="interest — #180">

```xanoscript
// used in PROFILES - a growing list of things people are interested in examples: Sports, Technology, Movies, Music, Industrial Design
table interest {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text interest? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="interest_join — #327">

```xanoscript
table interest_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id? {
      table = "master_person"
    }

    int? interested_in_id? {
      table = "interest"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="investment_thesis — #709">

```xanoscript
table investment_thesis {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text? node_uuid? filters=trim
    int? master_person_id?=0 {
      table = "master_person"
    }

    int? master_company_id?=0 {
      table = "master_company"
    }

    text? firm_name? filters=trim
    text? investor_type? filters=trim
    json industries?
    json stage_focus?
    json geography?
    decimal? check_size_min?=0
    decimal? check_size_max?=0
    decimal? check_size_sweet_spot?=0
    int? total_deals_count?=0
    int? lead_deals_count?=0
    decimal? lead_ratio?=0
    json frequent_co_investors?
    json partner_deal_attribution?
    json sector_evolution_timeline?
    json recent_36mo_focus?
    json deal_size_stats?
    json geographic_distribution?
    date? last_lead_date?
    date? last_investment_date?
    text? founder_fit_declared_narrative?
    json founder_fit_declared_vector?
    text? problem_market_declared_narrative?
    json problem_market_declared_vector?
    text? competitive_moat_declared_narrative?
    json competitive_moat_declared_vector?
    text? traction_momentum_declared_narrative?
    json traction_momentum_declared_vector?
    text? business_model_declared_narrative?
    json business_model_declared_vector?
    text? expansion_roadmap_declared_narrative?
    json expansion_roadmap_declared_vector?
    text? founder_fit_derived_narrative?
    json founder_fit_derived_vector?
    text? problem_market_derived_narrative?
    json problem_market_derived_vector?
    text? competitive_moat_derived_narrative?
    json competitive_moat_derived_vector?
    text? traction_momentum_derived_narrative?
    json traction_momentum_derived_vector?
    text? business_model_derived_narrative?
    json business_model_derived_vector?
    text? expansion_roadmap_derived_narrative?
    json expansion_roadmap_derived_vector?
    text? declared_thesis_summary?
    text? derived_thesis_summary?
    json declared_vs_derived_delta?
    json implicit_lenses?
    json thesis_drift_signals?
    json partner_specialization?
    text? syndicate_tier? filters=trim
    json data_sources?
    date? last_validated_date?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="investor_profile_company — #478">

```xanoscript
table investor_profile_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int master_company_id? {
      table = "master_company"
    }

    text? location? filters=trim
    text? exits?=null filters=trim
    text? investment_range? filters=trim
    text? investment_sweet_spot? filters=trim
    text? investment_stages? filters=trim
    text? derived_thesis_summary? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="investor_profile_person — #475">

```xanoscript
table investor_profile_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id?=0 {
      table = "master_person"
    }

    text? exits_summary?=null filters=trim
    text? board_advisory_summary?=null filters=trim
    text investor_type? filters=trim
    text? llm_investor_summary? filters=trim
    text? active_in_sector? filters=trim
    text? sectors_stages? filters=trim
    text? investments? filters=trim
    text? location? filters=trim
    text? investment_range? filters=trim
    text? investment_sweet_spot? filters=trim
    json yc_stats?
    text? derived_thesis_summary? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="keyword_join — #306">

```xanoscript
// Join between keyword ,master company and master person
table keyword_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int keyword_id {
      table = "keyword"
    }

    int? master_company_id? {
      table = "master_company"
    }

    int? master_person_id? {
      table = "master_person"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="language_join — #326">

```xanoscript
table language_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id? {
      table = "master_person"
    }

    int? languages_id? {
      table = "languages"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="languages — #178">

```xanoscript
// a table of languages with flags - used in PROFILES
table languages {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text language? filters=trim
    text flag? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["system table"]
}
```

</Accordion>

<Accordion title="leverage_loop_question_node — #557">

```xanoscript
table leverage_loop_question_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int leverage_loop_question_id? {
      table = "leverage_loop_question"
    }

    text label?
    text? person_node_uuid?
    text company_node_uuid?
    text? funding_round_uuid?
    text? film_tv_uuid?
    text film_tv_award_uuid?
    json paths?
    text? live_event_node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["suggestion : loop", "agent populated", "refactor"]
}
```

</Accordion>

<Accordion title="leverage_loop_trajectory_node — #571">

```xanoscript
table leverage_loop_trajectory_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text label?
    int leverage_loop_trajectory_id? {
      table = "leverage_loop_trajectory"
    }

    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid?
    text? film_tv_uuid?
    text? film_tv_award_uuid?
    json paths?
    text? live_event_node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["suggestion : loop", "agent populated", "refactor"]
}
```

</Accordion>

<Accordion title="link_type — #165">

```xanoscript
// table of "supported link types" used to return icon for the link types
table link_type {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text link_type_label? filters=trim
    text icon? filters=trim
    text link_domain? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["system table"]
}
```

</Accordion>

<Accordion title="linkedin_follower — #495">

```xanoscript
table linkedin_follower {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp updated_at?=now
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id?=0 {
      table = "master_company"
    }

    int follower_count?
    text? linkedin_url? filters=trim
    text linkedin_id? filters=trim
    text universal_name? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="linkedin_profile_company — #499">

```xanoscript
table linkedin_profile_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?=now
    int? master_company_id?=0 {
      table = "master_company"
    }

    text? name? filters=trim
    text url? filters=trim
    text? handle? filters=trim
    text? description? filters=trim
    int? followers?
    text website? filters=trim
    text? tagline? filters=trim
    text? industry? filters=trim
    int? founded?
    text? headquarters? filters=trim
    text[]? specialties? filters=trim
    json funding?
    json employees?
    json location?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="live_event_companies — #704">

```xanoscript
// Join between live_events and master_company — all Company→Event edge types (HOSTS, PRODUCES, SPONSORS, PRESENTED_AT, EXHIBITED_AT)
table live_event_companies {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int live_event_id {
      table = "live_events"
    }

    int master_company_id {
      table = "master_company"
    }

    text? edge_type?
    text? uuid?
    int? weight?=0
    timestamp? valid_from?
    timestamp? valid_to?
    text? host_role?
    text? tier?
    text? amount_usd?
    text? booth_number?
    text? exhibit_type?
    text? presentation_type?
    text? stage?
    timestamp? session_date?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {type: "btree", field: [{name: "live_event_id", op: "asc"}]}
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
    {type: "btree", field: [{name: "edge_type", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "live_event_id", op: "asc"}
        {name: "master_company_id", op: "asc"}
      ]
    }
    {
      type : "btree"
      field: [
        {name: "live_event_id", op: "asc"}
        {name: "edge_type", op: "asc"}
      ]
    }
  ]

  tags = ["live event"]
}
```

</Accordion>

<Accordion title="live_event_participants — #703">

```xanoscript
// Join between live_events and master_person — all Person→Event edge types (SPOKE_AT, MODERATED, EMCEED, JUDGED_AT, MENTORED_AT, PRESENTED_AT, ATTENDED_EVENT, REGISTERED_FOR)
table live_event_participants {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int live_event_id {
      table = "live_events"
    }

    int master_person_id {
      table = "master_person"
    }

    text? edge_type?
    text? uuid?
    int? weight?=0
    timestamp? valid_from?
    timestamp? valid_to?
    text? session_title?
    text? session_topic?
    timestamp? session_date?
    text? role?
    text? judged_category?
    text? expertise_area?
    int? representing_company_id?=0 {
      table = "master_company"
    }

    text? attendance_source?
    text? confidence?
    bool checked_in?
    timestamp? registered_at?
    text? ticket_tier?
    text? registration_status?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {type: "btree", field: [{name: "live_event_id", op: "asc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
    {type: "btree", field: [{name: "edge_type", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "live_event_id", op: "asc"}
        {name: "edge_type", op: "asc"}
      ]
    }
  ]

  tags = ["live event"]
}
```

</Accordion>

<Accordion title="log_agent_tools — #470">

```xanoscript
table log_agent_tools {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text agent_name? filters=trim
    text tool_name? filters=trim
    int? master_company_id?=0 {
      table = "master_company"
    }

    text? status? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="log_crash — #542">

```xanoscript
// global log for crash capture
table log_crash {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    enum? phase? {
      values = [
        "phase-0"
        "phase-1"
        "phase-2"
        "phase-3"
        "phase-4"
        "phase-5"
        "phase-6"
        "phase-7"
        "phase-8"
        "phase-9"
        "phase-10"
        "phase-11"
        "phase-12"
        "load"
        "llm_extract"
        "save_scrape"
        "master_person"
        "bios_node"
        "awards"
        "cascades"
        "tagline"
        "film_tv_node"
        "node_stamp"
        "bios_optional"
        "credit_edges"
        "credit_edges_person"
        "mb_lookup"
        "mb_browse"
        "mb_node"
        "mb_edges"
        "mb_cascade"
        "api_bad_request"
        "api_exhausted"
        "image_fetch"
        "image_upload"
        "node_description"
        "seed_domain"
        "seed_subdomain"
        "drain_orphan"
        "mb_expertise_write"
        "webhook_post_process"
        "dates_upsert"
      ]
    }

    bool qa_passed?
    text? fundable_deal_id?
    text? function_name?
    text? error_message?
    text? note?
    json data?

    // error | telemetry. log_crash carries BOTH: on a healthy Ryan run 307 of 307 rows were telemetry with zero real errors. Consumers must not count rows; filter on this or on non-empty error_message.
    text severity?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["debug log"]
}
```

</Accordion>

<Accordion title="log_enrichment_company — #632">

```xanoscript
table log_enrichment_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? stack_status? filters=trim
    text error_description? filters=trim
    text error_message? filters=trim
    json last_log?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["debug log"]
}
```

</Accordion>

<Accordion title="log_enrichment_person — #579">

```xanoscript
table log_enrichment_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_person_id? {
      table = "master_person"
    }

    text? stack_status? filters=trim
    text error_description? filters=trim
    text error_message? filters=trim
    json last_log?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["debug log"]
}
```

</Accordion>

<Accordion title="marketing_prefix — #174">

```xanoscript
table marketing_prefix {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text prefix? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["system table"]
}
```

</Accordion>

<Accordion title="master_address — #164">

```xanoscript
// master address records
table master_address {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_company_id? {
      table = "master_company"
    }

    int? master_person_id?=0 {
      table = "master_person"
    }

    text? address_label?
    text formatted_address?
    text? unit?
    text? number?
    text? address_line_1?
    text? address_line_2?
    text? city?
    text? state_region?
    text? region_code?
    text? postal_code?
    text country?
    text country_code?
    bool connected_only?
    bool mailing_address?
    int? data_source_id?=0 {
      table = "data_source"
    }

    decimal? longitude?
    decimal? latitude?
    text? timezone?
    bool path_triples_added?
    int? created_by_user_id?=0 {
      table = "user"
    }

    text? source?

    // Tier-prefixed dedupe key (cmp:/fmt:) — computed at write time by mvp/address/build-address-natural-key
    text? natural_key?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "id"}, {name: "formatted_address"}]
}
```

</Accordion>

<Accordion title="master_avatar — #227">

```xanoscript
// master avatars record
table master_avatar {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id? {
      table = "master_person"
    }

    text url? filters=trim
    text? original_source_url? filters=trim
    bool main?
    bool is_placeholder?=true
    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]

  autocomplete = [{name: "id"}, {name: "url"}]
}
```

</Accordion>

<Accordion title="master_banner — #228">

```xanoscript
table master_banner {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id?=0 {
      table = "master_company"
    }

    text url? filters=trim
    bool main?
    int data_source_id? {
      table = "data_source"
    }

    text? source? filters=trim
    bool is_placeholder?
    bool is_uploaded?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "id"}]
}
```

</Accordion>

<Accordion title="master_company — #142">

```xanoscript
// MASTER COMPANY table
table master_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text? node_uuid? filters=trim
    text label?=Company filters=trim
    bool visibility?
    text? company_name? filters=trim
    text? logo?
    text? banner? filters=trim
    text? company_domain? filters=trim
    text[] alternative_domains? filters=trim
    text? linkedin_url? filters=trim
    text? tagline? filters=trim
    text? about? filters=trim
    text? about_500? filters=trim
    text[]? social_insights? filters=trim
    int? founded?
    object? employees? {
      schema {
        text? employee_range? filters=trim
        text? employees_citation? filters=trim
        text? employees_citation_url? filters=trim
      }
    }

    bool is_school?
    bool is_vc?
    bool personal_website?
    int? owner_id? {
      table = "user"
    }

    int? data_source_id? {
      table = "data_source"
    }

    timestamp? last_enrich?
    timestamp? last_about_processed?
    text? fundable_org_id? filters=trim
    timestamp? social_insights_updated_at?
    text[]? aliases? filters=trim
    text? legal_name? filters=trim
    int? seeded_by_user? {
      table = "user"
    }

    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      name : "fuzzy-company"
      lang : "english"
      type : "search"
      field: [{name: "company_name", op: "A"}]
    }
  ]

  autocomplete = [{name: "id"}, {name: "company_name"}]
  tags = ["financial"]
}
```

</Accordion>

<Accordion title="master_date — #762">

```xanoscript
// Canonical enrichment-sourced dated facts for persons AND companies (one polymorphic table).
// Exactly ONE of master_person_id / master_company_id set per row. Rows are dated pointers,
// never a second source of truth (funding_round rows point at fundable_deals via metadata).
// ONLY writer: dates/upsert-master-date. Spec: guides/open-work/important-dates-mem0
table master_date {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp? updated_at?

    // exactly one of person/company set per row
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    int date_descriptor_id? {
      table = "date_descriptor"
    }

    // partial-date parts: year-only = (1967, null, null); month-day = (null, 6, 14)
    int? year?

    int? month?
    int? day?

    // derived: set only when all three parts present
    date? date?

    // slug|yyyy|mm|dd fixed pipe slots; slug-only for singleton descriptors
    text? natural_key? filters=trim

    // descriptor-specific payload: birthplace_location, spouse/is_current, round type/amount + deal ref, acquirer, exchange
    json? metadata?

    int? data_source_id? {
      table = "data_source"
    }

    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [
        {name: "master_person_id", op: "asc"}
        {name: "master_company_id", op: "asc"}
        {name: "natural_key", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="master_email — #155">

```xanoscript
// MASTER EMAIL records
table master_email {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    email email_address? filters=trim|lower
    enum email_type?=work {
      values = ["personal", "work", "other"]
    }

    text full_name? filters=trim
    bool active_status?=true
    bool share_publicly?
    bool connected_only?
    bool orbiter_verified?
    int data_source_id? {
      table = "data_source"
    }

    int? created_by_user_id?=0 {
      table = "user"
    }

    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      name : "email_search"
      lang : "english"
      type : "search"
      field: [
        {name: "email_address", op: "A"}
        {name: "full_name", op: "B"}
      ]
    }
  ]

  autocomplete = [{name: "id"}, {name: "email_address"}]
}
```

</Accordion>

<Accordion title="master_link — #166">

```xanoscript
// MASTER LINK table
table master_link {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text service? filters=trim
    text? service_label? filters=trim
    text icon_url? filters=trim
    text link_url? filters=trim

    // Canonical LinkedIn URL; populated only for LinkedIn links and globally unique across person and company owners.
    text? canonical_linkedin_url? filters=trim

    int data_source_id? {
      table = "data_source"
    }

    int? created_by_user_id?=0 {
      table = "user"
    }

    bool profile?
    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree|unique"
      field: [{name: "canonical_linkedin_url", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]

  view = {
    "descending data": {
      sort: {id: "desc"}
      id  : "6fea6da4-c6c6-4c1c-844a-338df077988e"
    }
  }
}
```

</Accordion>

<Accordion title="master_person — #139">

```xanoscript
// MASTER PERSON table - the "master" record for an identity
table master_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text? node_uuid? filters=trim
    text Label?=Person filters=trim
    bool visibility?
    bool orbiter_user?
    text? name? filters=trim
    text? avatar? filters=trim
    text? linkedin_url? filters=trim
    text? first_name? filters=trim
    text? middle_name? filters=trim
    text? last_name? filters=trim
    text? suffix? filters=trim
    text? nickname? filters=trim
    int? master_company_id?=0 {
      table = "master_company"
    }

    text? current_title? filters=trim
    int? primary_location_id?=0 {
      table = "primary_location"
    }

    bool? profile_published?
    text? bio? filters=trim
    text? bio_500? filters=trim
    text[] deep_bio? filters=trim
    enum status_orbiter?="non_user" {
      values = ["non_user", "user"]
    }

    int? data_source_id?=0 {
      table = "data_source"
    }

    text? source? filters=trim
    enum? sex? {
      values = ["male", "female"]
    }

    text[]? social_insights? filters=trim
    text? social_media_influence? filters=trim
    timestamp? social_insights_updated_at?
    bool is_angel?
    bool is_c_suite?
    bool full_enrich?
    timestamp? last_full_enrich?
    int? seeded_by_user?=0 {
      table = "user"
    }

    text[]? expertise? filters=trim
    text[]? roles? filters=trim
    text[]? skills? filters=trim
    text[]? languages? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "id"}, {name: "name"}]
}
```

</Accordion>

<Accordion title="master_phone — #151">

```xanoscript
// master phone records
table master_phone {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? first_name? filters=trim
    text? last_name? filters=trim
    text phone_number? filters=trim
    text? country_calling_code? filters=trim
    text? local_display_phone? filters=trim
    int? phone_extension?
    enum phone_type?=mobile {
      values = ["mobile", "office", "other", "home", "main", "work"]
    }

    bool connected_only?=true
    bool orbiter_verified?
    int data_source_id? {
      table = "data_source"
    }

    int? created_by_user_id? {
      table = "user"
    }

    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [
    {name: "id"}
    {name: "phone_number"}
    {name: "master_person_id"}
    {name: "first_name"}
  ]
}
```

</Accordion>

<Accordion title="mb_artist — #737">

```xanoscript
// MusicBrainz artist source of truth - raw JSON + enrichment metadata
table mb_artist {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid

    // UUID of the created graph node
    text node_uuid?

    // Artist name
    text name?

    // Person / Group / Orchestra / ... - lets the post-ingest reconciliation pass select person-type artists without parsing json
    text artist_type?

    // Raw lookup payload (full ?inc= response); null until first enrich
    json json?

    // Paginated browse payloads (recording?artist=, release-group?artist=) accumulated across cascade pages
    json browse_json?

    // Last full orchestrator run - the 30-day dedup gate reads this
    timestamp enriched_at?

    // Depth at which first enriched (0, 1, 2)
    int cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?

    // Tombstone - set on 404 (entity deleted/merged away); gates never re-queue a gone row
    bool gone?

    text? music_group_image_url?
    text? gcp_music_group_image_url?
    timestamp? description_generated_at?

    // Stamped by person-resolution when SAME_AS -> master_person Person is written (0 = unreconciled)
    int master_person_id? {
      table = "master_person"
    }

    // Raw serper.dev avatar URL (no-LinkedIn avatar fallback)
    text? person_avatar_url?

    // Public GCS URL for the person avatar; mirrored to the Music_Artist node image_url
    text? gcp_person_avatar_url?

    // Short (<=127 char) LLM node description from create-node-description #13112 v2; the graph node embedding source
    text? description?

    // Immutable person graph UUID linked at bridge time; fences reused numeric person IDs
    text master_person_node_uuid?

    // MusicBrainz lifecycle epoch this artist row was created under
    text lifecycle_epoch_uuid?

    // Required inline person-artist identity materialization completed
    bool core_enrich_success?

    // Timestamp of the latest verified inline artist-core completion
    timestamp? core_enriched_at?

    // Downstream expansion state; pending until cascade continuation completes
    text cascade_status?=pending

    // Timestamp of successful core-plus-cascade completion
    timestamp? cascade_completed_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="merge_audit — #721">

```xanoscript
table merge_audit {
  auth = false

  schema {
    // Unique identifier for the merge audit row
    int id

    // Row creation timestamp
    timestamp created_at?=now

    // 'person' or 'company' — entity type being merged
    text kind

    // master_person.id or master_company.id that was kept (resolve via kind)
    int survivor_id

    // Array of master IDs that collapsed into the survivor
    int[] duplicate_ids

    // 'user', 'cron', or 'admin' — what triggered this merge
    text initiated_by

    // Trigger source for the merge. Values fall into 3 classes — orchestrator skips conflicts_resolved logging only for deterministic-class. Deterministic-class (auto-pick is by definition correct, no conflict review needed): 'deterministic', 'email_exact', 'phone_exact', 'domain_exact', 'linkedin_exact'. LLM-class (Tier 1/2 from detect-suspects, log conflicts to review feed): 'llm_confirmed', 'llm_flash_confirmed', 'llm_pro_confirmed'. User-class (human picked, log conflicts to review feed): 'user_picked', 'manual_inspection'. See merge.mdx for the full taxonomy.
    text match_kind?

    // FK to the merge_suspect row that triggered this merge, if any
    int merge_suspect_id? {
      table = "merge_suspect"
    }

    // 'pending', 'success', 'failed', or 'rolled_back' — merge state machine
    text status?=pending

    // Full JSON of survivor + each duplicate's master_* row plus per-table row counts at start time. Rollback artifact.
    json pre_merge_snapshot?

    // Array of {field, winner_value, winner_source_master_id, loser_values, loser_source_master_ids, rule_applied} — populated when cron auto-resolved high-stakes field conflicts
    json conflicts_resolved?

    // Populated when status='failed'
    text error_message?

    // When the merge began
    timestamp started_at?=now

    // When the merge ended (success or failure)
    timestamp finished_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [{name: "survivor_id", op: "asc"}, {name: "kind", op: "asc"}]
    }
    {type: "btree", field: [{name: "status", op: "asc"}]}
    {type: "btree", field: [{name: "started_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="merge_lock — #722">

```xanoscript
table merge_lock {
  auth = false

  schema {
    // Unique identifier
    int id

    // Row creation timestamp
    timestamp created_at?=now

    // 'person' or 'company' — which kind of master_* row is locked
    text entity_kind filters=trim

    // master_person.id or master_company.id being locked during the in-flight merge
    int master_id

    // FK to the merge_audit row holding this lock
    int merge_audit_id {
      table = "merge_audit"
    }

    // When the lock was taken
    timestamp acquired_at?=now
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [
        {name: "entity_kind", op: "asc"}
        {name: "master_id", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "merge_audit_id", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="merge_suspect — #284">

```xanoscript
// suspected merges
table merge_suspect {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int user_id {
      table = "user"
    }

    int master_person_id? {
      table = "master_person"
    }

    int merge_with_master_person_id? {
      table = "master_person"
    }

    text? merge_context?
    bool merged?
    bool do_not_merge?

    // 'person' or 'company' — discriminator for which pair of FKs is populated. Existing rows backfill to 'person'.
    text kind?=person

    // Set true after a related merge collapses other suspects' inputs; cron picks these up to re-run Tier 1/Tier 2 scoring
    bool needs_rescore?

    // Populated when kind='company'
    int master_company_id? {
      table = "master_company"
    }

    // Populated when kind='company'
    int merge_with_master_company_id? {
      table = "master_company"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {
      type : "btree"
      field: [
        {name: "created_at", op: "desc"}
        {name: "user_id", op: "asc"}
      ]
    }
  ]

  tags = ["merge manager"]
}
```

</Accordion>

<Accordion title="music_reverse_probe — #756">

```xanoscript
// Reverse-bridge idempotency probe: one row per (music person/company) MB search attempt, so refreshes don't re-hit MB. status pending/matched/no_match/review.
table music_reverse_probe {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text? entity_type?
    int? master_person_id?=0
    int? master_company_id?=0
    text? status?
    text? mbid?
    decimal? confidence?=0
    text? match_method?
    timestamp? searched_at?

    // Stable person graph UUID owning this reverse-music probe cache entry.
    uuid? master_person_node_uuid?

    // MusicBrainz lifecycle epoch this probe was written under
    text lifecycle_epoch_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["musicbrainz", "reverse-bridge"]
}
```

</Accordion>

<Accordion title="my_company — #149">

```xanoscript
// User's record of a company
table my_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int user_id? {
      table = "user"
    }

    int master_company_id? {
      table = "master_company"
    }

    text company_name? filters=trim
    text? node_uuid? filters=trim
    text visible_button?=Add filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {
      type : "btree"
      field: [
        {name: "created_at", op: "desc"}
        {name: "user_id", op: "desc"}
        {name: "master_company_id", op: "desc"}
      ]
    }
    {
      name : "fuzzy-company"
      lang : "english"
      type : "search"
      field: [{name: "company_name", op: "A"}]
    }
  ]

  autocomplete = [{name: "id"}, {name: "company_name"}]
}
```

</Accordion>

<Accordion title="my_company_address_join — #314">

```xanoscript
table my_company_address_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int my_company_id {
      table = "my_company"
    }

    int master_address_id {
      table = "master_address"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="my_company_email_join — #315">

```xanoscript
table my_company_email_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int my_company_id {
      table = "my_company"
    }

    int master_email_id {
      table = "master_email"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="my_company_link_join — #317">

```xanoscript
table my_company_link_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int my_company_id {
      table = "my_company"
    }

    int master_link_id {
      table = "master_link"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="my_company_phone_join — #316">

```xanoscript
table my_company_phone_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int my_company_id {
      table = "my_company"
    }

    int master_phone_id {
      table = "master_phone"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="needs_more_info — #249">

```xanoscript
table needs_more_info {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_person_id? {
      table = "master_person"
    }

    int master_company_id? {
      table = "master_company"
    }

    bool company_name_verified?
    bool info_added?
    timestamp? updated_at?
    text crunchbase_url? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="node_registry — #758">

```xanoscript
table node_registry {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text node_uuid filters=trim
    text? entitiy_type? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="organization — #695">

```xanoscript
// Synched Organization data from workos
table organization {
  auth = true

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? user_id? {
      table = "user"
      visibility = "private"
    }

    text workos_organization_id
    text name
    text? domain?
    bool is_personal?
    text? falkor_graph_id?
    int? master_company_id? {
      table = "master_company"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "name"}, {name: "id"}]
}
```

</Accordion>

<Accordion title="outcome_suggestion — #531">

```xanoscript
table outcome_suggestion {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? suggestion_request_id? filters=@:"dbo=529" {
      table = "suggestion_request"
    }

    int? user_id? {
      table = "user"
    }

    text? label_icon?
    text? badge?
    text? panel_description?
    text? suggestion_context?

    // for panel
    text? person_node_uuid? filters=trim

    // for panel display
    text? company_node_uuid? filters=trim

    bool archived?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "panel_description"}, {name: "id"}]
  tags = ["suggestion : outcome", "agent populated", "refactor"]
}
```

</Accordion>

<Accordion title="outcome_suggestion_node — #534">

```xanoscript
table outcome_suggestion_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? outcome_suggestion_id? {
      table = "outcome_suggestion"
    }

    text? person_node_uuid?
    text? company_node_uuid?
    json paths?

    // Unique identifier for a film/TV award.
    text? film_tv_award_uuid?

    // Unique identifier for a film/TV entity.
    text? film_tv_uuid?

    // Unique identifier for a funding round.
    text? funding_round_uuid?

    text? live_event_node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["suggestion : outcome", "agent populated", "refactor"]
}
```

</Accordion>

<Accordion title="outcome_trajectory_node — #561">

```xanoscript
table outcome_trajectory_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int outcome_trajectory_id? {
      table = "outcome_trajectory"
    }

    text name?
    text label?
    text? person_node_uuid?
    text? company_node_uuid?
    text? funding_round_uuid_?
    text? film_tv_uuid_?
    text? film_tv_award_uuid?
    json paths?
    text? live_event_node_uuid?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["suggestion : outcome", "agent populated", "refactor"]
}
```

</Accordion>

<Accordion title="permalink_company — #431">

```xanoscript
table permalink_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? permalink? filters=trim
    json cb_company_page?
    json signal_page?
    json cb_json?
    json signal_json?
    json cb_financials_page?
    text[]? round_urls? filters=trim
    json response?
    json cb_investors?
    bool vc_team?
    bool vc_firm_enriched?
    timestamp? vc_firm_last_enriched_at?
    bool processing?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="permalink_person — #465">

```xanoscript
table permalink_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text? permalink? filters=trim
    int? master_person_id?=0 {
      table = "master_person"
    }

    text? linkedin_url? filters=trim
    json cb_person_page?
    json signal_person?
    json cb_extract?
    json signal_extract?
    timestamp? last_investor_enrich_at?
    bool processing?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="person_alias — #759">

```xanoscript
// Verified alternate names for a master person. Handles and URLs belong in master_link, not this table.
table person_alias {
  auth = false

  schema {
    // Unique alias row identifier
    int id

    // Row creation timestamp
    timestamp created_at?=now

    // Last update timestamp
    timestamp updated_at?=now

    // Person that owns this alias
    int master_person_id {
      table = "master_person"
    }

    // Verified display form of the alternate name
    text alias filters=trim

    // Lowercase punctuation-normalized key used for deduplication
    text normalized_alias filters=trim|lower

    // Classification of the name variant
    enum alias_type? {
      values = [
        "translated_name"
        "reordered_name"
        "abbreviated_name"
        "stage_name"
        "alternate_name"
      ]

    }

    // Source that verified this alias
    text source? filters=trim

    // Identity confidence from 0 to 1
    decimal confidence? filters=min:0|max:1

    // Optional source URL supporting the alias
    text evidence_url? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
    {
      type : "btree|unique"
      field: [
        {name: "master_person_id", op: "asc"}
        {name: "normalized_alias", op: "asc"}
      ]
    }
  ]
}
```

</Accordion>

<Accordion title="person_enrich_data — #500">

```xanoscript
table person_enrich_data {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id?=0 {
      table = "master_person"
    }

    json people_data_labs?
    json exa?
    json scrapecreator_person?
    json fundable?
    json enrich_layer_data?
    json linkedin_profile?
    json raw_linkedin_email?
    json raw_linkedin?
    json contactout_data?
    json email_signature?
    json clado_data?
    json scrapin_data?

    // EnrichLayer /v2/profile/resolve/email response (profile object extracted). Populated by mvp/get-add/master-person v3.10 when PDL 404s and Hunter did not return a linkedin_url. Profile shape mirrors api/enrich_layer/person-linkedin.
    json enrich_layer_email?

    json full_enrich?
    json exa_search?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="person_has_expertise — #711">

```xanoscript
// Relational projection of HAS_EXPERTISE graph edges from FalkorDB. One row per (master_person_id, expertise_node_uuid) pair, mirroring the edge with aggregated evidence. Maintained by mvp/expertise/resolve-person-expertise via dual-write; backfilled once from the graph.
table person_has_expertise {
  auth = false

  schema {
    // Unique identifier for the join row
    int id

    // FK to master_person — the relational anchor
    int master_person_id {
      table = "master_person"
    }

    // FalkorDB Person.uuid; mirrors the resolver's primary input
    text person_node_uuid

    // Which taxonomy table the FK below points to
    enum expertise_kind {
      values = ["domain", "subdomain"]
    }

    // FK to domain_expertise; populated when expertise_kind = domain
    int domain_expertise_id? {
      table = "domain_expertise"
    }

    // FK to sub_domain_expertise; populated when expertise_kind = subdomain
    int sub_domain_expertise_id? {
      table = "sub_domain_expertise"
    }

    // FalkorDB DomainExpertise/SubDomainExpertise.uuid; graph-side identifier
    text expertise_node_uuid

    // Aggregated confidence (max across signals), 0.00–1.00
    decimal confidence

    // Strongest depth seen across signals (creator > expert > practitioner > familiar)
    enum depth {
      values = ["creator", "expert", "practitioner", "familiar"]
    }

    // Most-current recency seen across signals (current > recent > historical)
    enum recency {
      values = ["current", "recent", "historical"]
    }

    // Pipe-concatenated signal types (job_title|skill_mention|achievement…)
    text signal_type

    // Pipe-concatenated evidence snippets (≤100 chars each)
    text evidence

    // Pipe-concatenated original LLM-identified strings
    text identified_as

    // Best (minimum) cosine distance from vector search; lower = better match
    decimal match_score

    // matched = vector-search hit existing node; created = new SubDomainExpertise made
    enum match_type {
      values = ["matched", "created"]
    }

    // Edge weight mirror: min(round(10 + score*80), 50)
    int weight

    // Stamped on every dual-write; backfill sets to now()
    timestamp last_resolved_at?=now

    // Row creation timestamp
    timestamp created_at?=now

    // Last update timestamp
    timestamp updated_at?=now

    // Authoritative versioned expertise semantic decision committed atomically by the reconciler; the table row is authoritative and the graph edge is a derived mirror.
    json semantic_decision?

    // SHA-256 of the canonical normalized semantic input bound to semantic_decision.
    text semantic_input_sha256?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "person_node_uuid", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "domain_expertise_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "sub_domain_expertise_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [{name: "expertise_node_uuid", op: "asc"}]
    }
    {
      type : "btree|unique"
      field: [
        {name: "master_person_id", op: "asc"}
        {name: "expertise_node_uuid", op: "asc"}
      ]
    }
  ]
}
```

</Accordion>

<Accordion title="person_terminal_company_dependency — #760">

```xanoscript
// Durable terminal dependency between a person profile job and its selected current-company enrichment.
table person_terminal_company_dependency {
  auth = false

  schema {
    // Dependency record identifier.
    int id

    // When the dependency was registered before company dispatch.
    timestamp created_at?=now

    // When dependency state was last evaluated.
    timestamp updated_at?=now

    // Person terminal job blocked by this company writer.
    int profile_enrichment_job_id {
      table = "profile_enrichment_job"
    }

    // Person whose terminal current company was selected.
    int master_person_id {
      table = "master_person"
    }

    // Selected current company that must settle before terminal finalization.
    int? master_company_id?=0 {
      table = "master_company"
    }

    // Company last_enrich must be at or after this dispatch-time watermark.
    timestamp required_after

    // pending, success, error, or superseded.
    text status?=pending

    // When the dependency reached a terminal state.
    timestamp completed_at?

    // Terminal company-writer failure detail.
    text error?

    // Last authoritative company/history/readiness evidence.
    json resolver_data?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [{name: "profile_enrichment_job_id", op: "asc"}]
    }
    {
      type : "btree"
      field: [
        {name: "master_company_id", op: "asc"}
        {name: "status", op: "asc"}
      ]
    }
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="person_terminal_stage — #761">

```xanoscript
// Durable, idempotent checkpoint ledger for person enrichment terminal stages.
table person_terminal_stage {
  auth = false

  schema {
    // Primary key.
    int id

    // Receipt creation time.
    timestamp created_at?=now

    // Last stage state update time.
    timestamp updated_at?=now

    // Stable unique key for one run, pipeline, and stage version.
    text idempotency_key filters=trim

    // Stable identifier shared by every stage in one person enrichment run.
    text run_id filters=trim

    // Person being enriched.
    int master_person_id {
      table = "master_person"
    }

    // Provider job associated with the run, when applicable.
    int profile_enrichment_job_id? {
      table = "profile_enrichment_job"
    }

    // Person enrichment history row associated with the run, when applicable.
    int enrich_history_person_id? {
      table = "enrich_history_person"
    }

    // Stage pipeline: orchestrator or callback.
    text pipeline filters=trim|lower

    // Stable machine-readable stage name.
    text stage filters=trim|lower

    // Execution order within the pipeline.
    int stage_order filters=min:0

    // Version of the stage contract.
    int stage_version?=1 filters=min:1

    // Whether terminal readiness requires this receipt to succeed.
    bool is_required?=true

    // pending, running, success, error, or superseded.
    text status?=pending filters=trim|lower

    // Number of execution attempts.
    int attempt_count? filters=min:0

    // Maximum automatic attempts.
    int max_attempts?=3 filters=min:1

    // Earliest permitted retry time.
    timestamp next_attempt_at?

    // Time the active attempt began.
    timestamp started_at?

    // Most recent worker heartbeat.
    timestamp heartbeat_at?

    // Time the stage reached success or terminal error.
    timestamp completed_at?

    // Opaque worker lease token.
    text lease_token? filters=trim

    // Time after which another worker may reclaim the stage.
    timestamp lease_expires_at?

    // Stable failure code.
    text error_code? filters=trim

    // Diagnostic failure message.
    text error_message? filters=trim

    // Small structured stage result.
    json result?

    // Tracing, version, and diagnostic metadata.
    json metadata?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [{name: "idempotency_key", op: "asc"}]
    }
    {
      type : "btree|unique"
      field: [
        {name: "run_id", op: "asc"}
        {name: "pipeline", op: "asc"}
        {name: "stage", op: "asc"}
      ]
    }
    {
      type : "btree"
      field: [
        {name: "profile_enrichment_job_id", op: "asc"}
        {name: "pipeline", op: "asc"}
        {name: "status", op: "asc"}
      ]
    }
    {
      type : "btree"
      field: [
        {name: "master_person_id", op: "asc"}
        {name: "created_at", op: "desc"}
      ]
    }
    {
      type : "btree"
      field: [
        {name: "status", op: "asc"}
        {name: "next_attempt_at", op: "asc"}
      ]
    }
    {
      type : "btree"
      field: [
        {name: "status", op: "asc"}
        {name: "lease_expires_at", op: "asc"}
      ]
    }
  ]
}
```

</Accordion>

<Accordion title="personal_website — #513">

```xanoscript
table personal_website {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text domain? filters=trim
    text markdown? filters=trim
    json extracted_data?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="pipeline_lifecycle_epoch — #767">

```xanoscript
// One row per pipeline: the current lifecycle epoch UUID. Rotated by tool/clear-data-source before truncation; every MusicBrainz writer re-reads it and skips when stale. NEVER truncated.
table pipeline_lifecycle_epoch {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    // Pipeline key, e.g. musicbrainz (one row per pipeline)
    text pipeline

    // When the epoch was last rotated
    timestamp? rotated_at?

    // Function that performed the rotation
    text rotated_by?

    // Current lifecycle epoch
    text epoch_uuid?

    // Lifecycle write gate: ready permits writers; rotating blocks writers until wipe completion.
    text status?=ready
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="primary_location — #385">

```xanoscript
table primary_location {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? city_id? {
      table = "city"
    }

    int? region_id? {
      table = "region"
    }

    int? country_id? {
      table = "country"
    }

    text? formatted_location? filters=trim
    decimal? longitude?
    decimal? latitude?
    text timezone? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["location tables"]
}
```

</Accordion>

<Accordion title="profile_enrichment_job — #686">

```xanoscript
table profile_enrichment_job {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text request_type?
    text job_id?
    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    json data?
    text[]? biography?
    json response?
    bool processing?
    text? error?
    text model?
    int? tokens?
    decimal? temperature?
    text llm_response?

    // Monotonic Cloud Run/provider LLM attempt count for this logical job. Null on legacy rows; new jobs start at 0.
    int? provider_attempt_count?

    // Stable one-per-person automatic Deep Bio reservation key. Preserved after terminal closeout.
    text? logical_job_key?

    // Nullable person-level Deep Bio single-flight key. Cleared only by terminal transition or stale-active recovery.
    text? active_person_key?

    // Launch provenance: automatic, explicit, retry, or operator_replay.
    text? deep_bio_launch_mode?

    // Stable request identifier passed to Cloud Run and callbacks for same-row idempotency.
    text? deep_bio_request_id?

    // Last provider dispatch/callback/heartbeat activity for stale-active recovery.
    timestamp? provider_last_activity_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree|unique"
      field: [{name: "logical_job_key", op: "asc"}]
    }
    {
      type : "btree|unique"
      field: [{name: "active_person_key", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="profile_enrichment_log — #770">

```xanoscript
table profile_enrichment_log {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    json data?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="project — #575">

```xanoscript
table project {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text? node_uuid?
    text? title? filters=trim
    text? description? filters=trim
    object start? {
      schema {
        int? year?
        int? month?
      }
    }

    object end? {
      schema {
        int? year?
        int? month?
      }
    }

    int? master_person_id? {
      table = "master_person"
    }

    int[] contributor_master_person_id? {
      table = "master_person"
    }

    text? edge_uuid? filters=trim
    text? date_range? filters=trim
    json contributors_raw?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="publication — #574">

```xanoscript
table publication {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_person_id? {
      table = "master_person"
    }

    text? node_uuid? filters=trim
    text? name? filters=trim
    text? summary? filters=trim
    text? url? filters=trim
    text? publisher_name? filters=trim
    text? publisher_domain? filters=trim
    int? publisher_master_company_id? {
      table = "master_company"
    }

    int? published_on_day?
    int? published_on_month?
    int? published_on_year?
    text? edge_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_enrich_company — #583">

```xanoscript
table queue_enrich_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_company_id {
      table = "master_company"
    }

    bool processing?
    int count?=1

    // How many hops from the seed entity (0=seed, 1=direct discovery, 2=secondary, etc)
    int cascade_depth?

    // 1=founders/current employer, 2=past employers/VCs, 3=schools/angels, 4=cert issuers/vol orgs/publishers
    int priority_tier?=4

    // Which function queued this entity (e.g. resolve-investors-edges, resolve-edges-work)
    text? source_function?

    // Numeric ID of spawning entity (master_person_id or master_company_id). Use 0 when source is identified by UUID instead — see source_entity_uuid.
    int? source_entity_id?=0

    // UUID/text ID of spawning entity (e.g. fundable_people.id, fundable_orgs.id). Used when the source is not a numeric master_* ID.
    text? source_entity_uuid?

    // Type of source entity: master_person, master_company, fundable_person, fundable_company, etc. Disambiguates which ID field to trust.
    text? source_entity_type?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_enrich_funding — #584">

```xanoscript
table queue_enrich_funding {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_company_id {
      table = "master_company"
    }

    bool processing?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_enrich_person — #582">

```xanoscript
table queue_enrich_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id {
      table = "master_person"
    }

    bool processing?
    bool deep_research?
    int count?=1

    // How many hops from the seed entity (0=seed, 1=direct discovery, 2=secondary, etc)
    int cascade_depth?

    // 1=current employer/founders, 2=past employers/VCs, 3=schools/angels, 4=cert issuers/vol orgs/contributors
    int priority_tier?=4

    // Which function queued this entity (e.g. resolve-edges-work, process-yc-people)
    text? source_function?

    // Numeric ID of spawning entity (master_person_id or master_company_id). Use 0 when source is identified by UUID instead — see source_entity_uuid.
    int? source_entity_id?=0

    // UUID/text ID of spawning entity (e.g. fundable_people.id). Used when the source is not a numeric master_* ID.
    text? source_entity_uuid?

    // Type of source entity: master_person, master_company, fundable_person, fundable_company, etc. Disambiguates which ID field to trust.
    text? source_entity_type?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_enrich_vc_firm — #585">

```xanoscript
table queue_enrich_vc_firm {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_company_id {
      table = "master_company"
    }

    bool processing?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_imdb_award_graph — #764">

```xanoscript
table queue_imdb_award_graph {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?
    int film_tv_award_id
    int imdb_person_id?
    int film_tv_id?
    text idempotency_key filters=trim
    text status?=pending filters=trim
    int priority_tier?=3
    int cascade_depth?
    bool processing?
    int attempt_count?
    int max_attempts?=3
    timestamp next_attempt_at?
    timestamp last_attempt_at?
    timestamp completed_at?
    text lease_token? filters=trim
    timestamp lease_expires_at?
    text last_error?
    uuid award_node_uuid?
    text source_function? filters=trim
    json result?

    // Epoch ms of the last tier promotion; debounces within-run re-adds.
    int last_promoted_at?

    // Times this award has been re-queued. Added 2026-07-25 so duplicate-driven promotion is measurable here.
    int count?=1
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [{name: "film_tv_award_id", op: "asc"}]
    }
    {
      type : "btree|unique"
      field: [{name: "idempotency_key", op: "asc"}]
    }
    {
      type : "btree"
      field: [
        {name: "status", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "created_at", op: "asc"}
      ]
    }
  ]
}
```

</Accordion>

<Accordion title="queue_imdb_title — #707">

```xanoscript
table queue_imdb_title {
  auth = false

  schema {
    // Primary key
    int id

    // When this queue entry was created
    timestamp created_at?=now

    // Normalized canonical IMDB URL (split on /?, rtrim /). Unique key for queue dedup.
    text imdb_url filters=trim

    // Provisional title from discovery context, nullable before stub creation
    text title? filters=trim

    // FK to film_tv once the stub row exists
    int film_tv_id? {
      table = "film_tv"
    }

    // Lock flag - cron worker sets true on pickup
    bool processing?

    // Number of times this URL was referenced; bumped on upsert
    int count?=1 filters=min:0

    // Hops from the seed entity (1 or 2 in practice; 3+ is never queued)
    int cascade_depth filters=min:0

    // 1 = highest priority, 4 = lowest. Upgraded on duplicate discovery.
    int priority_tier?=4 filters=max:4|min:0

    // Which cascade helper queued this (e.g. cascade-credits-from-person)
    text source_function? filters=trim

    // ID of spawning entity (imdb_person.id or film_tv.id)
    int source_entity_id?

    // imdb_person or film_tv
    text source_entity_type? filters=trim

    // Root of this cascade - for lineage/debugging
    int seed_imdb_person_id? {
      table = "imdb_person"
    }

    // Root of this cascade - for lineage/debugging
    int seed_film_tv_id? {
      table = "film_tv"
    }

    // Epoch ms of the last tier promotion. Debounces within-run re-adds (measured 4-60 per title per run).
    int last_promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [{name: "imdb_url", op: "asc"}]
    }
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "film_tv_id", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_mb_artist — #745">

```xanoscript
// Cascade queue for MusicBrainz artists - one row per discovered-but-not-yet-enriched MBID
table queue_mb_artist {
  auth = false

  schema {
    int id
    timestamp created_at?=now

    // Natural key - unique; one row per MBID
    text mbid

    // FK into mb_artist once the stub exists (0 = unset)
    int mb_artist_id? {
      table = "mb_artist"
    }

    // Provisional name from discovery context
    text name?

    // Lock flag - cron sets true on pickup
    bool processing?

    // Incremented on duplicate discovery
    int count?=1

    // 0 only for bootstrap-lane rows; otherwise 1 or 2; 3+ is never queued
    int cascade_depth

    // 0 = bootstrap lane; 1-4 per tier rules; upgraded (lowered) on duplicate discovery
    int priority_tier?=4

    // e.g. cascade-from-artist
    text source_function?

    // MBID of the spawning entity
    text source_entity_mbid?

    // Root of the cascade (lineage); bootstrap rows set it to their own mbid
    text seed_mbid?

    // Retry backoff gate - cron skips the row until now >= next_attempt_at
    timestamp next_attempt_at?

    // Attempts so far; drives the exponential backoff curve
    int retry_count?

    // Lease expiry; reclaimer may recover the row after this moment
    timestamp? lease_expires_at?

    // When the most recent processing attempt claimed this row
    timestamp? last_attempt_at?

    // Most recent failure reason (lease_expired for interrupted attempts)
    text last_error?

    // Times an expired lease on this row was reclaimed
    int lease_reclaim_count?

    // MusicBrainz lifecycle epoch fence — writes carrying a stale epoch are skipped
    text lifecycle_epoch_uuid?

    // Worker lease token; only the holder may finalize or fail this row
    text lease_token?

    // Immutable person graph UUID this root-artist row belongs to; fences reused numeric person IDs
    text master_person_node_uuid?

    // full for normal/legacy work; cascade_only after verified inline root core
    text execution_scope?=full

    // Explicit sanctioned promoter provenance; duplicate discovery cannot promote tier
    text promotion_source?

    // Timestamp of explicit sanctioned promotion into a claimable tier
    timestamp? promoted_at?

    // Immutable MusicBrainz catalog plan persisted once under the root artist lease; retries verify against this exact MBID set.
    json? planned_catalog?

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: enrichers loop over track/credit/relation collections that repeat the same entity, and unthrottled that collapses every tier to 0.
    int last_promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "mb_artist_id", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="region — #331">

```xanoscript
// states and regions
table region {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text region? filters=trim
    text? node_uuid? filters=trim
    text? region_code? filters=trim
    int? country_id? {
      table = "country"
    }

    text? description? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["location tables"]
}
```

</Accordion>

<Accordion title="role — #179">

```xanoscript
// Used in PROFILES - growing list created by users
table role {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text role? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "role"}]
}
```

</Accordion>

<Accordion title="role_join — #329">

```xanoscript
table role_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_person_id? {
      table = "master_person"
    }

    int? roles_id? {
      table = "role"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="runtime_metadata — #733">

```xanoscript
// Per-data-source runtime metadata. Each data source (live, staging, sandbox)
// holds its own copy of this table — Xano data sources are isolated databases,
// so a row written here in live is not visible from sandbox and vice versa.
// Used by callers of async webhook-based vendors (e.g. FullEnrich via #13036)
// to thread the running data source into the dispatch so callbacks route
// back to the correct DB via db.set_datasource.
table runtime_metadata {
  auth = false

  schema {
    // Primary key
    int id

    // Metadata key, e.g. 'data_source_name'
    text key filters=trim

    // Metadata value. For key=data_source_name: 'live' | 'staging' | 'sandbox' | branch name
    text value filters=trim

    // Last write to this key
    timestamp updated_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "key", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="serendipity_suggestion_node — #537">

```xanoscript
table serendipity_suggestion_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int serendipity_suggestion_id {
      table = "serendipity_suggestion"
    }

    text? name? filters=trim
    text? person_node_uuid
    text? company_node_uuid filters=trim
    json paths

    // Unique identifier for a film/TV award.
    text film_tv_award_uuid filters=trim

    // Unique identifier for a film/TV entity.
    text film_tv_uuid filters=trim

    // Unique identifier for a funding round.
    text funding_round_uuid filters=trim

    text? live_events_node_uuid filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["suggestion : serendipity", "refactor"]
}
```

</Accordion>

<Accordion title="skills — #130">

```xanoscript
// used in PROFILES - growing list created by users
table skills {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text skill? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "skill"}]
}
```

</Accordion>

<Accordion title="skills_join — #325">

```xanoscript
table skills_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int master_person_id? {
      table = "master_person"
    }

    int skill_id? {
      table = "skills"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="speciality_join — #456">

```xanoscript
table speciality_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id?=0 {
      table = "master_company"
    }

    int? specialty_id?=0 {
      table = "specialty"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_company_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="specialty — #455">

```xanoscript
table specialty {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text? specialty? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="sub_domain_expertise — #654">

```xanoscript
table sub_domain_expertise {
  auth = false

  schema {
    int id

    // Row creation timestamp (added 2026-07-01; legacy rows null)
    timestamp? created_at?=now

    text? node_uuid?
    int domain_expertise_id? {
      table = "domain_expertise"
    }

    text name?
    text? description?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
  ]

  tags = ["system table", "domainof expertise"]
}
```

</Accordion>

<Accordion title="team_question_node — #663">

```xanoscript
// Has all the data referenced by a collection question
table team_question_node {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int added_by_user_id? {
      table = "user"
    }

    int team_question_id? {
      table = "team_question"
    }

    int? team_question_node_id? {
      table = "team_question_node"
    }

    text label? filters=trim
    text? person_node_uuid? filters=trim
    text? company_node_uuid? filters=trim
    text? funding_round_uuid_? filters=trim
    text? film_tv_uuid? filters=trim
    text film_tv_award_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="twitter_company — #429">

```xanoscript
table twitter_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_company_id?=0 {
      table = "master_company"
    }

    text? profile_url? filters=trim|lower
    text? name? filters=trim
    text? screen_name? filters=trim|lower
    text? description? filters=trim
    text? location? filters=trim
    text? profile_banner_url? filters=trim
    text? profile_image_url_https? filters=trim
    int? followers_count?
    int? statuses_count?
    json data?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree|unique"
      field: [{name: "profile_url", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="twitter_join — #497">

```xanoscript
table twitter_join {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    text twitter_handle? filters=trim
    int? master_person_id?=0 {
      table = "master_person"
    }

    int master_company_id? {
      table = "master_company"
    }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="twitter_person — #368">

```xanoscript
table twitter_person {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_person_id? {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text twitter_handle? filters=trim
    text? avatar? filters=trim
    text? name? filters=trim
    text description? filters=trim
    json entities?
    text? profile_banner_url? filters=trim
    int? followers_count?
    json data?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="vc_company — #277">

```xanoscript
table vc_company {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id? {
      table = "master_company"
    }

    text? permalink? filters=trim
    text? domain? filters=trim
    bool crunch_scraped?
    text[] firecrawl_id? filters=trim
    int team_scrape_attempts?
    int? source_company_id? {
      table = "master_company"
    }

    timestamp? last_team_scrape?
    bool team_scraped?
    bool team_extracted?
    bool team_resolved?
    json firecrawl_api?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  autocomplete = [{name: "master_company_id"}]
}
```

</Accordion>


<Accordion title="volunteering — #577">

```xanoscript
table volunteering {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_person_id? {
      table = "master_person"
    }

    text? title? filters=trim
    text? description? filters=trim
    int? end_year?
    int? end_month?
    int? start_year?
    int? start_month?
    text? company_name? filters=trim
    text? company_linkedin_url? filters=trim
    int? master_company_id? {
      table = "master_company"
    }

    text? edge_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="work_experience — #147">

```xanoscript
// table of work experiences - populated via enrichment and also users when they build their profiles
table work_experience {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    timestamp? triples_created_at?
    int? master_person_id?=0 {
      table = "master_person"
    }

    int? master_company_id? {
      table = "master_company"
    }

    // depreciated??
    bool main?

    text? person_permalink? filters=trim
    text first_name? filters=trim
    text last_name? filters=trim
    text? title? filters=trim
    text? company_name? filters=trim
    text? company_domain? filters=trim
    text? organization_permalink? filters=trim
    text? company_linkedin_url? filters=trim

    // depreciated!!
    text? linkedin_company_id? filters=trim

    text? start_day? filters=trim
    text? start_month? filters=trim
    text? start_year? filters=trim
    text? end_day? filters=trim
    text? end_month? filters=trim
    text? end_year? filters=trim
    text? description? filters=trim
    text? location? filters=trim
    bool current?
    text? edge_uuid? filters=trim
    int? data_source_id?=0 {
      table = "data_source"
    }

    text? source? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {
      type : "btree"
      field: [{name: "master_person_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="youtube — #441">

```xanoscript
table youtube {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    int? master_company_id? {
      table = "master_company"
    }

    int master_person_id? {
      table = "master_person"
    }

    text? channel_id? filters=trim
    text? channel_url? filters=trim
    text? handle? filters=trim
    text? name? filters=trim
    text? description? filters=trim
    int? subscriber_count?
    text? subscriber_count_text? filters=trim
    text? video_count_text? filters=trim
    int? video_count?
    text? view_count_text? filters=trim
    int? view_count?
    text? joined_date_text? filters=trim
    text[]? tags? filters=trim
    text[]? links? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["social"]
}
```

</Accordion>

## IMDB & Music Enrichment

These **17 tables** can be deferred until the IMDB and Music enrichment waterfalls are deployed.

### Unique table inventory

| Table | Xano ID | Waterfall access |
| --- | ---: | --- |
| `film_tv` | **#395** | Read + write |
| `mb_event` | **#740** | Read + write |
| `mb_label` | **#739** | Read + write |
| `mb_place` | **#743** | Read + write |
| `mb_rate_limit` | **#736** | Read + write |
| `mb_recording` | **#738** | Read + write |
| `mb_release` | **#744** | Read + write |
| `mb_release_group` | **#741** | Read + write |
| `mb_role_expertise_map` | **#755** | Read |
| `mb_work` | **#742** | Read + write |
| `queue_imdb_person` | **#706** | Read + write |
| `queue_mb_event` | **#748** | Read + write |
| `queue_mb_label` | **#747** | Read + write |
| `queue_mb_recording` | **#746** | Read + write |
| `queue_mb_release_group` | **#749** | Read + write |
| `queue_mb_work` | **#750** | Read + write |
| `queue_person_resolution` | **#751** | Read + write |

### Copy/paste schema reference

<Accordion title="film_tv — #395">

```xanoscript
table film_tv {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    text? node_uuid?
    text? image_url?
    text? title?
    text? imdb_url?
    text type?
    text? tagline?
    text? description?
    text[] genres?
    text? year?
    text? imdb_rating?
    text? running_time?
    text? mpaa_rating?
    json writers?
    text[]? countries?
    text[]? languages?
    json official_site?
    json directors?
    text? markdown?
    json json?
    text? source?

    // Last full orchestrator scrape; skip re-scrape within 30 days
    timestamp? enriched_at?

    // Depth at which this entity was first enriched
    int? cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "gin", field: [{name: "xdo", op: "jsonb_path_op"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["imdb"]
}
```

</Accordion>

<Accordion title="mb_event — #740">

```xanoscript
// MusicBrainz event source of truth - raw JSON + enrichment metadata
table mb_event {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid filters=trim

    // UUID of the created graph node
    text node_uuid? filters=trim

    // Event name
    text name? filters=trim

    // Raw lookup payload (full ?inc= response); null until first enrich
    json json?

    // Last full orchestrator run - the 30-day dedup gate reads this
    timestamp enriched_at?

    // Depth at which first enriched (0, 1, 2)
    int cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?

    // Tombstone - set on 404 (entity deleted/merged away); gates never re-queue a gone row
    bool gone?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="mb_label — #739">

```xanoscript
// MusicBrainz label source of truth - raw JSON + enrichment metadata
table mb_label {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid filters=trim

    // Label name
    text name? filters=trim

    // Raw lookup payload (full ?inc= response); null until first enrich
    json json?

    // Paginated release?label= browse payloads - the full pressing catalog lives here, per the canonical-release decision
    json browse_json?

    // Stamped by the post-ingest reconciliation pass when SAME_AS -> Company is written (0 = unreconciled)
    int master_company_id? {
      table = "master_company"
    }

    // Last full orchestrator run - the 30-day dedup gate reads this
    timestamp enriched_at?

    // Depth at which first enriched (0, 1, 2)
    int cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?

    // Tombstone - set on 404 (entity deleted/merged away); gates never re-queue a gone row
    bool gone?

    // UUID of the created graph node
    text node_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="mb_place — #743">

```xanoscript
// MusicBrainz place source of truth - filled inline from embedded place-rels (no queue, no orchestrator)
table mb_place {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid filters=trim

    // Place name (venue or studio)
    text name? filters=trim

    // The embedded place-rels object (name, type, address, coordinates, area) - NOT a lookup response; places are materialized inline
    json json?

    // When inline-materialized during a recording/event enrich
    timestamp enriched_at?

    // Depth of the enrich that embedded it
    int cascade_depth?

    // Set true when the Place node write succeeds
    bool enrich_success?

    // Unused for places (always false - never individually looked up, so a 404 cannot occur); kept for shared-base uniformity
    bool gone?

    // UUID of the created Place node
    text node_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="mb_rate_limit — #736">

```xanoscript
// Singleton token-bucket state for the global MusicBrainz 1 req/s limit
table mb_rate_limit {
  auth = false

  schema {
    // Singleton primary key - exactly one row, id = 1
    int id

    // Stamped on every granted call; acquire defers if now - last_call_at < 1000ms
    timestamp last_call_at?=now

    // Token bucket - binary (0 or 1) at the 1 req/s ceiling; int so the bucket can widen if MusicBrainz raises the limit
    int tokens?=1
  }

  index = [{type: "primary", field: [{name: "id"}]}]
}
```

</Accordion>

<Accordion title="mb_recording — #738">

```xanoscript
// MusicBrainz recording source of truth - raw JSON + enrichment metadata
table mb_recording {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid

    // Recording title
    text title?

    // Raw lookup payload (full ?inc= response); null until first enrich
    json json?

    // Last full orchestrator run - the 30-day dedup gate reads this
    timestamp enriched_at?

    // Depth at which first enriched (0, 1, 2)
    int cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?

    // Tombstone - set on 404 (entity deleted/merged away); gates never re-queue a gone row
    bool gone?

    // UUID of the created graph node
    text node_uuid?

    text? cover_art_url?
    text? gcp_cover_art_url?
    timestamp? description_generated_at?

    // Short (<=127 char) LLM node description from create-node-description #13112 v2; the graph node embedding source
    text? description?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="mb_release — #744">

```xanoscript
// MusicBrainz release source of truth - ONE row per release-group (the canonical Official edition only)
table mb_release {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid filters=trim

    // Release title
    text title? filters=trim

    // FK to the release-group this canonical release belongs to (0 = unset)
    int mb_release_group_id? {
      table = "mb_release_group"
    }

    // Raw payload for the canonical release; all other pressings stay in mb_release_group.json / mb_label.browse_json
    json json?

    // Last full orchestrator run - the 30-day dedup gate reads this
    timestamp enriched_at?

    // Depth at which first enriched (0, 1, 2)
    int cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?

    // Tombstone - set on 404 (entity deleted/merged away); gates never re-queue a gone row
    bool gone?

    // UUID of the created graph node
    text node_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [{name: "mb_release_group_id", op: "asc"}]
    }
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="mb_release_group — #741">

```xanoscript
// MusicBrainz release-group source of truth - raw JSON + enrichment metadata
table mb_release_group {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid filters=trim

    // Release-group title (the abstract album/single/EP)
    text title? filters=trim

    // Raw lookup payload (full ?inc= response, including all pressings); null until first enrich
    json json?

    // Stamped when the canonical release is selected (status = Official, earliest date) - makes the canonical-release decision auditable
    text canonical_release_mbid? filters=trim

    // Last full orchestrator run - the 30-day dedup gate reads this
    timestamp enriched_at?

    // Depth at which first enriched (0, 1, 2)
    int cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?

    // Tombstone - set on 404 (entity deleted/merged away); gates never re-queue a gone row
    bool gone?

    // UUID of the created graph node
    text node_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="mb_role_expertise_map — #755">

```xanoscript
// Deterministic MusicBrainz credit-role -> expertise taxonomy map. Keys on OUR graph edge vocabulary (PERFORMED/PLAYED_ON/PRODUCED/ENGINEERED from mb/run-base-recording-enrich; COMPOSED/WROTE_LYRICS/WROTE from mb/run-base-work-enrich), not raw MB rel names. Rows are matched per credit aggregate in priority order (lower first); attr_contains is a lowercase substring test against the edge role/instruments property, empty = default row for that edge_type. sub_domain_name is the wipe-stable canonical key (sandbox ids change on re-seed; re-resolve ids from names after wipes).
table mb_role_expertise_map {
  auth = false

  schema {
    // Unique identifier for the mapping row
    int id

    // Row creation timestamp
    timestamp created_at?=now

    // Graph edge label this row matches: PERFORMED | PLAYED_ON | PRODUCED | ENGINEERED | COMPOSED | WROTE_LYRICS | WROTE
    text edge_type filters=trim

    // Which edge property attr_contains is tested against; none = edge type alone decides
    enum attr_field {
      values = ["none", "role", "instruments"]
    }

    // Lowercase substring to look for in the edge property (e.g. vocal, mix, master); empty = default/catch-all row for this edge_type
    text attr_contains? filters=trim|lower

    // Match order within an edge_type; lower checked first, default rows use 100
    int priority?=100

    // Whether this row maps to a top-level domain or a curated sub-domain
    enum target_kind {
      values = ["domain", "subdomain"]
    }

    // FK to domain_expertise; populated when target_kind = domain
    int domain_expertise_id? {
      table = "domain_expertise"
    }

    // FK to sub_domain_expertise; populated when target_kind = subdomain
    int sub_domain_expertise_id? {
      table = "sub_domain_expertise"
    }

    // Canonical curated sub-domain name (wipe-stable key; ids are re-resolved from this after sandbox re-seeds)
    text sub_domain_name? filters=trim

    // Depth hint for person_has_expertise emission
    enum depth {
      values = ["creator", "expert", "practitioner", "familiar"]
    }

    // Confidence hint; MB credits are deterministic evidence so default is high
    decimal confidence?="0.95" filters=min:0|max:1

    // HAS_EXPERTISE edge-weight hint
    int weight?=35 filters=min:0|max:50

    // Minimum distinct credited works/recordings in this role before expertise is emitted
    int min_credits?=3

    // Row toggle; inactive rows are ignored by the emitter
    bool active?=true

    // Curator notes (source MB rel types, edge-vocabulary gaps, rationale)
    text notes? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree"
      field: [
        {name: "edge_type", op: "asc"}
        {name: "priority", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "active", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="mb_work — #742">

```xanoscript
// MusicBrainz work source of truth - raw JSON + enrichment metadata
table mb_work {
  auth = false

  schema {
    int id
    timestamp created_at?=now
    timestamp updated_at?

    // MusicBrainz ID - MERGE key, unique
    text mbid filters=trim

    // Work title (the underlying composition)
    text title? filters=trim

    // Raw lookup payload (full ?inc= response); null until first enrich
    json json?

    // Last full orchestrator run - the 30-day dedup gate reads this
    timestamp enriched_at?

    // Depth at which first enriched (0, 1, 2)
    int cascade_depth?

    // Flipped true on successful orchestrator completion
    bool enrich_success?

    // Tombstone - set on 404 (entity deleted/merged away); gates never re-queue a gone row
    bool gone?

    // UUID of the created graph node
    text node_uuid? filters=trim
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_imdb_person — #706">

```xanoscript
table queue_imdb_person {
  auth = false

  schema {
    // Primary key
    int id

    // When this queue entry was created
    timestamp created_at?=now

    // Normalized canonical IMDB URL (split on /?, rtrim /). Unique key for queue dedup.
    text imdb_url

    // Provisional name from discovery context, nullable before stub creation
    text name?

    // FK to imdb_person once the stub row exists
    int imdb_person_id? {
      table = "imdb_person"
    }

    // Lock flag - cron worker sets true on pickup
    bool processing?

    // Number of times this URL was referenced; bumped on upsert
    int count?=1

    // Hops from the seed entity (1 or 2 in practice; 3+ is never queued)
    int cascade_depth

    // 0 = bootstrap lane (processed now); 1-4 per tier rules; upgraded (lowered) on duplicate discovery
    int priority_tier?=4

    // Which cascade helper queued this (e.g. cascade-crew-from-title)
    text source_function?

    // ID of spawning entity (imdb_person.id or film_tv.id)
    int source_entity_id?

    // imdb_person or film_tv
    text source_entity_type?

    // Root of this cascade - for lineage/debugging
    int seed_imdb_person_id? {
      table = "imdb_person"
    }

    // Root of this cascade - for lineage/debugging
    int seed_film_tv_id? {
      table = "film_tv"
    }

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: a title is re-upserted 4-60x per run mechanically, which floored every tier to 0.
    int last_promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {
      type : "btree|unique"
      field: [{name: "imdb_url", op: "asc"}]
    }
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "imdb_person_id", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_mb_event — #748">

```xanoscript
// Cascade queue for MusicBrainz events - one row per discovered-but-not-yet-enriched MBID
table queue_mb_event {
  auth = false

  schema {
    int id
    timestamp created_at?=now

    // Natural key - unique; one row per MBID
    text mbid filters=trim

    // Provisional name from discovery context
    text name? filters=trim

    // FK into mb_event once the stub exists (0 = unset)
    int mb_event_id? {
      table = "mb_event"
    }

    // Lock flag - cron sets true on pickup
    bool processing?

    // Incremented on duplicate discovery
    int count?=1 filters=min:0

    // 0 only for bootstrap-lane rows; otherwise 1 or 2; 3+ is never queued
    int cascade_depth filters=min:0

    // 0 = bootstrap lane; 1-4 per tier rules; upgraded (lowered) on duplicate discovery
    int priority_tier?=4 filters=min:0|max:4

    // e.g. cascade-from-artist
    text source_function? filters=trim

    // MBID of the spawning entity
    text source_entity_mbid? filters=trim

    // Root of the cascade (lineage); bootstrap rows set it to their own mbid
    text seed_mbid? filters=trim

    // Retry backoff gate - cron skips the row until now >= next_attempt_at
    timestamp next_attempt_at?

    // Attempts so far; drives the exponential backoff curve
    int retry_count? filters=min:0

    // Lease expiry; reclaimer may recover the row after this moment
    timestamp? lease_expires_at?

    // When the most recent processing attempt claimed this row
    timestamp? last_attempt_at?

    // Most recent failure reason (lease_expired for interrupted attempts)
    text last_error?

    // Times an expired lease on this row was reclaimed
    int lease_reclaim_count?

    // MusicBrainz lifecycle epoch fence — writes carrying a stale epoch are skipped
    text lifecycle_epoch_uuid?

    // Worker lease token; only the holder may finalize or fail this row
    text lease_token?

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: enrichers loop over track/credit/relation collections that repeat the same entity, and unthrottled that collapses every tier to 0.
    int last_promoted_at?

    // Why the tier last moved: duplicate_discovery | award_equalization | explicit.
    text promotion_source?

    // When the tier last moved. Mirrors queue_mb_artist.
    timestamp promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "mb_event_id", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_mb_label — #747">

```xanoscript
// Cascade queue for MusicBrainz labels - one row per discovered-but-not-yet-enriched MBID
table queue_mb_label {
  auth = false

  schema {
    int id
    timestamp created_at?=now

    // Natural key - unique; one row per MBID
    text mbid filters=trim

    // Provisional name from discovery context
    text name? filters=trim

    // FK into mb_label once the stub exists (0 = unset)
    int mb_label_id? {
      table = "mb_label"
    }

    // Lock flag - cron sets true on pickup
    bool processing?

    // Incremented on duplicate discovery
    int count?=1 filters=min:0

    // 0 only for bootstrap-lane rows; otherwise 1 or 2; 3+ is never queued
    int cascade_depth filters=min:0

    // 0 = bootstrap lane; 1-4 per tier rules; upgraded (lowered) on duplicate discovery
    int priority_tier?=4 filters=min:0|max:4

    // e.g. cascade-from-label
    text source_function? filters=trim

    // MBID of the spawning entity
    text source_entity_mbid? filters=trim

    // Root of the cascade (lineage); bootstrap rows set it to their own mbid
    text seed_mbid? filters=trim

    // Retry backoff gate - cron skips the row until now >= next_attempt_at
    timestamp next_attempt_at?

    // Attempts so far; drives the exponential backoff curve
    int retry_count? filters=min:0

    // Lease expiry; reclaimer may recover the row after this moment
    timestamp? lease_expires_at?

    // When the most recent processing attempt claimed this row
    timestamp? last_attempt_at?

    // Most recent failure reason (lease_expired for interrupted attempts)
    text last_error?

    // Times an expired lease on this row was reclaimed
    int lease_reclaim_count?

    // MusicBrainz lifecycle epoch fence — writes carrying a stale epoch are skipped
    text lifecycle_epoch_uuid?

    // Worker lease token; only the holder may finalize or fail this row
    text lease_token?

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: enrichers loop over track/credit/relation collections that repeat the same entity, and unthrottled that collapses every tier to 0.
    int last_promoted_at?

    // Why the tier last moved: duplicate_discovery | award_equalization | explicit.
    text promotion_source?

    // When the tier last moved. Mirrors queue_mb_artist.
    timestamp promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "mb_label_id", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_mb_recording — #746">

```xanoscript
// Cascade queue for MusicBrainz recordings - one row per discovered-but-not-yet-enriched MBID
table queue_mb_recording {
  auth = false

  schema {
    int id
    timestamp created_at?=now

    // Natural key - unique; one row per MBID
    text mbid filters=trim

    // Provisional title from discovery context
    text title? filters=trim

    // FK into mb_recording once the stub exists (0 = unset)
    int mb_recording_id? {
      table = "mb_recording"
    }

    // Lock flag - cron sets true on pickup
    bool processing?

    // Incremented on duplicate discovery
    int count?=1 filters=min:0

    // 0 only for bootstrap-lane rows; otherwise 1 or 2; 3+ is never queued
    int cascade_depth filters=min:0

    // 0 = bootstrap lane; 1-4 per tier rules; upgraded (lowered) on duplicate discovery
    int priority_tier?=4 filters=min:0|max:4

    // e.g. cascade-from-artist
    text source_function? filters=trim

    // MBID of the spawning entity
    text source_entity_mbid? filters=trim

    // Root of the cascade (lineage); bootstrap rows set it to their own mbid
    text seed_mbid? filters=trim

    // Retry backoff gate - cron skips the row until now >= next_attempt_at
    timestamp next_attempt_at?

    // Attempts so far; drives the exponential backoff curve
    int retry_count? filters=min:0

    // Lease expiry; reclaimer may recover the row after this moment
    timestamp? lease_expires_at?

    // When the most recent processing attempt claimed this row
    timestamp? last_attempt_at?

    // Most recent failure reason (lease_expired for interrupted attempts)
    text last_error?

    // Times an expired lease on this row was reclaimed
    int lease_reclaim_count?

    // MusicBrainz lifecycle epoch fence — writes carrying a stale epoch are skipped
    text lifecycle_epoch_uuid?

    // Worker lease token; only the holder may finalize or fail this row
    text lease_token?

    // full for legacy work; catalog_preview is bounded seed-only materialization
    text execution_scope?=full

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: enrichers loop over track/credit/relation collections that repeat the same entity, and unthrottled that collapses every tier to 0.
    int last_promoted_at?

    // Why the tier last moved: duplicate_discovery | award_equalization | explicit.
    text promotion_source?

    // When the tier last moved. Mirrors queue_mb_artist.
    timestamp promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {
      type : "btree"
      field: [{name: "mb_recording_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="queue_mb_release_group — #749">

```xanoscript
// Cascade queue for MusicBrainz release-groups - one row per discovered-but-not-yet-enriched MBID
table queue_mb_release_group {
  auth = false

  schema {
    int id
    timestamp created_at?=now

    // Natural key - unique; one row per MBID
    text mbid filters=trim

    // Provisional title from discovery context
    text title? filters=trim

    // FK into mb_release_group once the stub exists (0 = unset)
    int mb_release_group_id? {
      table = "mb_release_group"
    }

    // Lock flag - cron sets true on pickup
    bool processing?

    // Incremented on duplicate discovery
    int count?=1 filters=min:0

    // 0 only for bootstrap-lane rows; otherwise 1 or 2; 3+ is never queued
    int cascade_depth filters=min:0

    // 0 = bootstrap lane; 1-4 per tier rules; upgraded (lowered) on duplicate discovery
    int priority_tier?=4 filters=min:0|max:4

    // e.g. cascade-from-artist
    text source_function? filters=trim

    // MBID of the spawning entity
    text source_entity_mbid? filters=trim

    // Root of the cascade (lineage); bootstrap rows set it to their own mbid
    text seed_mbid? filters=trim

    // Retry backoff gate - cron skips the row until now >= next_attempt_at
    timestamp next_attempt_at?

    // Attempts so far; drives the exponential backoff curve
    int retry_count? filters=min:0

    // Lease expiry; reclaimer may recover the row after this moment
    timestamp? lease_expires_at?

    // When the most recent processing attempt claimed this row
    timestamp? last_attempt_at?

    // Most recent failure reason (lease_expired for interrupted attempts)
    text last_error?

    // Times an expired lease on this row was reclaimed
    int lease_reclaim_count?

    // MusicBrainz lifecycle epoch fence — writes carrying a stale epoch are skipped
    text lifecycle_epoch_uuid?

    // Worker lease token; only the holder may finalize or fail this row
    text lease_token?

    // full for legacy work; catalog_preview is bounded seed-only materialization
    text execution_scope?=full

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: enrichers loop over track/credit/relation collections that repeat the same entity, and unthrottled that collapses every tier to 0.
    int last_promoted_at?

    // Why the tier last moved: duplicate_discovery | award_equalization | explicit.
    text promotion_source?

    // When the tier last moved. Mirrors queue_mb_artist.
    timestamp promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {
      type : "btree"
      field: [{name: "mb_release_group_id", op: "asc"}]
    }
  ]
}
```

</Accordion>

<Accordion title="queue_mb_work — #750">

```xanoscript
// Cascade queue for MusicBrainz works - one row per discovered-but-not-yet-enriched MBID
table queue_mb_work {
  auth = false

  schema {
    int id
    timestamp created_at?=now

    // Natural key - unique; one row per MBID
    text mbid filters=trim

    // Provisional title from discovery context
    text title? filters=trim

    // FK into mb_work once the stub exists (0 = unset)
    int mb_work_id? {
      table = "mb_work"
    }

    // Lock flag - cron sets true on pickup
    bool processing?

    // Incremented on duplicate discovery
    int count?=1 filters=min:0

    // 0 only for bootstrap-lane rows; otherwise 1 or 2; 3+ is never queued
    int cascade_depth filters=min:0

    // 0 = bootstrap lane; 1-4 per tier rules; upgraded (lowered) on duplicate discovery
    int priority_tier?=4 filters=min:0|max:4

    // e.g. cascade-from-recording
    text source_function? filters=trim

    // MBID of the spawning entity
    text source_entity_mbid? filters=trim

    // Root of the cascade (lineage); bootstrap rows set it to their own mbid
    text seed_mbid? filters=trim

    // Retry backoff gate - cron skips the row until now >= next_attempt_at
    timestamp next_attempt_at?

    // Attempts so far; drives the exponential backoff curve
    int retry_count? filters=min:0

    // Lease expiry; reclaimer may recover the row after this moment
    timestamp? lease_expires_at?

    // When the most recent processing attempt claimed this row
    timestamp? last_attempt_at?

    // Most recent failure reason (lease_expired for interrupted attempts)
    text last_error?

    // Times an expired lease on this row was reclaimed
    int lease_reclaim_count?

    // MusicBrainz lifecycle epoch fence — writes carrying a stale epoch are skipped
    text lifecycle_epoch_uuid?

    // Worker lease token; only the holder may finalize or fail this row
    text lease_token?

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: enrichers loop over track/credit/relation collections that repeat the same entity, and unthrottled that collapses every tier to 0.
    int last_promoted_at?

    // Why the tier last moved: duplicate_discovery | award_equalization | explicit.
    text promotion_source?

    // When the tier last moved. Mirrors queue_mb_artist.
    timestamp promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "mb_work_id", op: "asc"}]}
  ]
}
```

</Accordion>

<Accordion title="queue_person_resolution — #751">

```xanoscript
// Person-resolution queue - one row per Person:Music_Artist awaiting Exa social resolution
table queue_person_resolution {
  auth = false

  schema {
    int id
    timestamp created_at?=now

    // Natural key - unique; one row per artist MBID
    text mbid filters=trim

    // Artist name from the enrichment context
    text name? filters=trim

    // FK into mb_artist (0 = unset)
    int mb_artist_id? {
      table = "mb_artist"
    }

    // Lock flag - cron sets true on pickup
    bool processing?

    // Incremented on duplicate enqueue
    int count?=1 filters=min:0

    // Cascade depth of the source artist (0 = seed)
    int cascade_depth filters=min:0

    // 0 = bootstrap lane; 1-4 per tier rules; lowered on duplicate enqueue
    int priority_tier?=4 filters=min:0|max:4

    // e.g. mb/run-base-artist-enrich
    text source_function? filters=trim

    // MBID of the spawning entity
    text source_entity_mbid? filters=trim

    // Root of the cascade (lineage)
    text seed_mbid? filters=trim

    // Retry backoff gate - cron skips the row until now >= next_attempt_at
    timestamp next_attempt_at?

    // Attempts so far; drives the exponential backoff curve
    int retry_count? filters=min:0

    // MusicBrainz lifecycle epoch fence — writes carrying a stale epoch are skipped
    text lifecycle_epoch_uuid?

    // Epoch ms of the last tier promotion. Debounces within-run re-adds: enrichers loop over track/credit/relation collections that repeat the same entity, and unthrottled that collapses every tier to 0.
    int last_promoted_at?

    // Why the tier last moved: duplicate_discovery | award_equalization | explicit.
    text promotion_source?

    // When the tier last moved. Mirrors queue_mb_artist.
    timestamp promoted_at?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree|unique", field: [{name: "mbid", op: "asc"}]}
    {
      type : "btree"
      field: [
        {name: "processing", op: "asc"}
        {name: "priority_tier", op: "asc"}
        {name: "cascade_depth", op: "asc"}
      ]
    }
    {type: "btree", field: [{name: "mb_artist_id", op: "asc"}]}
  ]
}
```

</Accordion>
