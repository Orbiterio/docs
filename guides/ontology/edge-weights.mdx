---
title: "Edge Weights"
description: "The master reference for edge weights in Orbiter.io."
---

**Graph Database Edge Weights** — the master reference for every weighted relationship in the Orbiter graph, and the canonical scale for assigning weights to new edges.

<Note>
  **Lower weight = stronger / more important connection.** Weights are relative ranking values for graph traversal, not literal distances. Use the [Weight scale](#weight-scale) below when adding a new edge.
</Note>

<Note>
  **Last reviewed & updated: 2026-07-09** — write-side weights re-verified against the live edge-creator XanoScript (workspace 3, branch `v1`).
</Note>

<Note>
  **2026-09-05 — Planned inventory extended.** The Deep Bio relationship ledger's edges and the Enrichment Engine v1.2 lane edges are now specified on [Edge Types](/guides/ontology/edges#person-relationship-edges) and banded below. Edge names in <span style={{ color: "#16a34a", fontWeight: 600 }}>green</span> are in the spec with no live writer yet.
</Note>

## Weight scale

Pick the band that matches how strong a signal the relationship is for connecting two entities, then fine-tune within the band.

| Band | Weight | Signal strength | Examples |
| --- | --- | --- | --- |
| **Defining** | 1–5 | Identity-defining bonds | `COFOUNDED_WITH`, `FOUNDED`, `IDENTICAL_TWINS`, `CONNECTED_ON_ORBITER` |
| **Very strong** | 6–15 | Active senior roles, close ties, direct money | `BOARD_MEMBER_OF`, `MENTOR`, `INVESTED_IN` (direct), `RAISED` |
| **Strong** | 16–25 | Standing roles & partnerships | `MEMBER_OF`, `INVESTMENT_PARTNER_AT`, co-investing |
| **Moderate** | 26–40 | Past roles, deal participation, education, expertise | `WORKS_AT` (past), `HAS_EXPERTISE`, `ATTENDED` |
| **Weak** | 41–65 | Acquaintance, co-attendance, co-location, platform | `KNOWS`, `REGISTERED_FOR`, geography |
| **Negligible** | 100 | Platform-only, zero interaction (normalized) | `MUTUALLY_FOLLOWS`, `CONNECTED_ON_LINKEDIN` |

**Principles**

- Direct creation (founding / co-founding) and immediate family are the strongest — nothing should out-rank them.
- Current relationships beat past ones; senior roles beat junior; active interaction beats passive association.
- **Hub nodes** (cities, awards, expertise, events, publications) connect huge numbers of entities. Their edges must sit in the **Moderate band or below — never stronger than 30** (`WON_AWARD` 30 is the ceiling, set by the §3.6 awards audit; most hub edges land in Weak: `LOCATED_IN` 70/80, `POLITICAL_DONOR_TO` 60, `CO_MENTIONED_WITH` 55), regardless of how "specific" they feel — a low weight on a hub edge makes every co-member spuriously close. *(Wording aligned with [Edges → v1.2 design rules](/guides/ontology/edges#enrichment-engine-v12-edges) 2026-09-06; this page previously said "Weak or below", which its own awards audit below contradicts.)*
- Never use `0` or sub-`1` weights — they create near-free traversal and distort every path that touches them. The floor for a real edge is `1`.

## Deployed

Audited against the live FalkorDB graph on **2026-05-31**. Counts are summed across node sub-label variants (e.g. `Person` / `Person:C_Suite` / `Person:Angel` collapse to **Person**; `Company:VC_Firm` / `Company:School` / `Company:Organization` collapse to **Company**). Weights are the live `r.weight` values.

<Note>
  **Fix-forward changed the *write* values on 2026-05-31 (§3.6 audit).** The weights in the tables immediately below are the **live-graph snapshot as audited** — several are now superseded by the canonical values in [Weight audit — applied adjustments](#weight-audit--applied-adjustments). New edges are already written at the canonical values (fix-forward landed on branch `v1`, shared across data sources); the existing rows shown here are reweighted by the held live backfill `tool/backfill-edge-weights` (#13063). Snapshot rows that change after backfill: `LOCATED_IN` (10/50 → 70/80), `LEAD_INVESTED_IN` (30 → 15), `FOLLOW_ON_INVESTED_IN` (25 → 20), `WORKED_ON` / `HAS_AWARD` / `WON_AWARD` / `NOMINATED_FOR` (sub-1 → 30–40), `HAS_SUBDOMAIN` (`null` → 100 plus required metadata envelope), and `WAS_FOUNDED` (being removed).
</Note>

<Check>
  **Write-side weights re-verified against live creator Cypher on 2026-06-01.** Every weight below was read directly from its edge-creator function (not only the graph snapshot). All §3.6 canonical values match live. Rows added in this pass — `ATTENDED` / `ATTENDED_TOGETHER` / `STUDIED_UNDER` (moved up from *Planned* — they are written by `create-education-edges` #1898), the certification / honor / project edges, and `SPECIALIZES_IN` — are marked **`—`** under *Live edges* because they are written by an active creator but were not part of the 2026-05-31 graph count audit. `HAS_SUBDOMAIN` was re-verified again on **2026-07-04** after the metadata-envelope fix across #12663 / #13142 / #13161.
</Check>

<Note>
MusicBrainz rows are write-side canonical weights from the music waterfall functions, not part of the 2026-05-31 live graph count audit; they are marked `—` under *Live edges* for that reason.
</Note>

### Person → Person

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:KNOWS]` | `(Person)→(Person)` | 569 | 60 *(default)* |
| `[:MENTOR]` | `(Person)→(Person)` | 11 | 14 |

### Person → Company / Organization

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:WORKS_AT]` | `(Person)→(Company)` | 8,001 | 5–40 *(see notes)* |
| `[:FOUNDED]` | `(Person)→(Company)` | 882 | 5 |
| `[:BOARD_MEMBER_OF]` | `(Person)→(Company)` | 1,318 | 8 |
| `[:ADVISOR_TO]` | `(Person)→(Company)` | 423 | 12 |
| `[:MEMBER_OF]` | `(Person)→(Company / Organization)` | 85 | 20 |
| `[:INVESTED_IN]` | `(Person)→(Company)` | 808 | 15 |
| `[:VOLUNTEERED_AT]` | `(Person)→(Organization)` | 80 | 38 / 50 |

### Person → VC Firm

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:INVESTMENT_PARTNER_AT]` | `(Person)→(VC_Firm)` | 1,342 | 20 |

### Investing → Funding Round

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:INVESTED_IN]` | `(VC_Firm)→(Funding_Round)` | 3,949 | 20 |
| `[:INVESTED_IN]` | `(Person / Angel)→(Funding_Round)` | 1,442 | 25 |
| `[:LEAD_INVESTED_IN]` | `(VC_Firm / Person)→(Funding_Round)` | 2,039 | 30 |
| `[:FOLLOW_ON_INVESTED_IN]` | `(VC_Firm)→(Funding_Round)` | 1,324 | 25 |
| `[:INVESTMENT_PARTNER_IN]` | `(Person)→(Funding_Round)` | 1,903 | 15 |
| `[:RAISED]` | `(Company)→(Funding_Round)` | 1,968 | 10 |

### Expertise

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:HAS_EXPERTISE]` | `(Person)→(Domain / SubDomainExpertise)` | 16,695 | 10–50 *(formula; `#12926`)* |
| `[:SPECIALIZES_IN]` | `(Company)→(SubDomainExpertise)` | — | 10–50 *(formula; `#12746`)* |
| `[:HAS_SUBDOMAIN]` | `(DomainExpertise)→(SubDomainExpertise)` | 170 | **100** *(taxonomy hub damping; `#12663` / `#13142` / `#13161`)* |

`HAS_EXPERTISE` and `SPECIALIZES_IN` share the same locked-spec formula: **matched** edges use `weight = min(round(10 + match_score × 160), 50)` — a distance gradient that floors at 10 and caps at 50 — and edges to **newly-created** `SubDomainExpertise` nodes get a fixed weight `10` (a new node perfectly represents its domain). Writers: `resolve-person-expertise-v2` #12926 v2.8 (person; the legacy #12668 is uncalled) and `resolve-company-specialties` #12746 v1.8 (company) — see [Person Edges](/guides/enrichment/waterfall/person-edges) \+ [Company Edges](/guides/enrichment/waterfall/company-edges). Both edge types are refreshed idempotently (existing edges are deleted and rewritten per run). _Do not copy the stale `× 80` comment that survives in #12925's input description — the live formula is `× 160`._

`HAS_SUBDOMAIN` is always **100** because it is a taxonomy backbone edge, not an affinity signal. Since 2026-07-04, every live writer also stamps the standard metadata envelope: `uuid`, `created_at`, `updated_at`, `description`, and `description_generated_at`.

### Geography

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:LOCATED_IN]` | `(Company / Person)→(City / State / Region / Country)` | 12,298 | 10 / 50 |
| `[:IN_REGION]` | `(City)→(Region / State)` | 1,969 | 25 |
| `[:IN_COUNTRY]` | `(Region / State / City)→(Country)` | 440 | 50 |

### Film / TV & Awards

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:WORKED_ON]` | `(Person)→(Film_TV)` | 158 | 0.5 |
| `[:HAS_AWARD]` | `(Film_TV)→(Film_TV_Award)` | 2,692 | 0.5 |
| `[:NOMINATED_FOR]` | `(Person)→(Film_TV_Award:Nominee)` | 776 | 0.7 |
| `[:WON_AWARD]` | `(Person)→(Film_TV_Award:Winner)` | 455 | 0.3 |

### MusicBrainz

`Artist` below means either `Person:Music_Artist` or `Music_Group`, except for person-only credit edges. Genres are properties on music nodes, not graph edges.

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:SAME_AS]` | `(Company:Music_Label)→(Company)` | — | 1 *(`#13091` / reconciliation pass)* |
| `[:MEMBER_OF]` | `(Person:Music_Artist)→(Music_Group)` | — | 5 current / 12 past *(`#13094`)* |
| `[:COMPOSED]` | `(Artist)→(Work)` | — | 8 *(`#13101`)* |
| `[:WROTE]` | `(Artist)→(Work)` | — | 10 *(`#13101`)* |
| `[:PERFORMED]` | `(Artist)→(Recording)` | — | 10 *(`#13100`)* |
| `[:RELEASED]` | `(Artist)→(Release_Group)` | — | 10 *(`#13099`)* |
| `[:WROTE_LYRICS]` | `(Artist)→(Work)` | — | 12 *(`#13101`)* |
| `[:COLLABORATED_WITH]` | `(Artist)↔(Artist)` | — | 15 |
| `[:SUBSIDIARY_OF]` | `(Company:Music_Label)→(Company:Music_Label)` | — | 15 *(`#13091`)* |
| `[:OWNED_BY]` | `(Company:Music_Label)→(Company)` | — | 15 *(`#13133`)* |
| `[:SUPPORTING_MUSICIAN_FOR]` | `(Person:Music_Artist)→(Artist)` | — | 18 |
| `[:PRODUCED]` | `(Person:Music_Artist)→(Recording)` | — | 18 *(`#13100`)* |
| `[:PLAYED_ON]` | `(Person:Music_Artist)→(Recording)` | — | 20 *(`#13100`)* |
| `[:ENGINEERED]` | `(Person:Music_Artist)→(Recording)` | — | 22 *(`#13100`)* |
| `[:RECORDING_OF]` | `(Recording)→(Work)` | — | 30 *(`#13100`)* |
| `[:RELEASE_OF]` | `(Release)→(Release_Group)` | — | 30 *(`#13099`)* |
| `[:HELD_AT]` | `(Event:Music_*)→(Place)` | — | 30 *(`#13105`)* |
| `[:HEADLINED]` | `(Artist)→(Event:Music_*)` | — | 35 *(`#13105`)* |
| `[:RECORDED_AT]` | `(Recording)→(Place)` | — | 35 *(`#13100`)* |
| `[:TRIBUTE_TO]` | `(Artist)→(Artist)` | — | 40 |
| `[:APPEARS_ON]` | `(Recording)→(Release)` | — | 45 *(`#13099`)* |
| `[:PERFORMED_AT]` | `(Artist)→(Event:Music_*)` | — | 45 *(`#13105`)* |
| `[:RELEASED_ON]` | `(Release)→(Company:Music_Label)` | — | 50 *(`#13099`)* |
| `[:ON_RECORD_LABEL]` | `(Recording)→(Company:Music_Label)` | — | 55 *(`#13131`, derived)* |
| `[:FROM_AREA]` | `(Artist / Company:Music_Label / Place)→(Country / Region / City)` | — | 70 *(`#13103`)* |

Music label bridges use the shared company model: `Company:Music_Label` can link to a canonical `Company` by `SAME_AS`, and to its owner by `OWNED_BY`. `ON_RECORD_LABEL` is intentionally weaker than `RELEASED_ON` because it is a derived recording-to-label shortcut from the canonical release path.

### Education

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:ATTENDED]` | `(Person)→(School)` | — | 40 *(`#1898`)* |
| `[:ATTENDED_TOGETHER]` | `(Person)↔(Person)` | — | 35 / 70 *(`#1898`)* |
| `[:STUDIED_UNDER]` | `(Person)→(Person instructor)` | — | 10 *(`#1898`)* |

`ATTENDED_TOGETHER` is graded by overlap in two tiers: **35** same school \+ same `field_of_study` (`relationship_strength = 3`), **70** time-overlap only (`= 1`) — the live Cypher compares `field_of_study` only, so the old 50 middle tier no longer exists on new writes. `create-education-edges` #1898 reads a `TAUGHT_AT` edge to derive `STUDIED_UNDER`, but no creator writes `TAUGHT_AT` yet — it stays in [Planned](#person--school).

### Credentials & achievements

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:HAS_CERTIFICATION]` | `(Person)→(Certification)` | — | 20 *(15 for `Series …` certs; `#12578`)* |
| `[:CERTIFIED_BY]` | `(Person)→(Organization)` | — | 25 *(`#12578`)* |
| `[:GRANTED_BY]` | `(Certification)→(Organization)` | — | 10 *(`#12578`)* |
| `[:RECEIVED_HONOR]` | `(Person)→(Honor)` | — | 20 *(`#4574`)* |
| `[:ISSUED_BY]` | `(Honor)→(Organization)` | — | 25 *(`#4574`)* |

### Content, work products & events

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:AUTHORED]` | `(Person)→(Work:Publication)` | 380 | 25 / 22 / 18 / 15 *(recency; `#12579`)* |
| `[:PUBLISHED_BY]` | `(Work:Publication)→(Company)` | — | 20 *(`#12579`)* |
| `[:CONTRIBUTED_TO]` | `(Person)→(Project)` | — | 15–40 *(recency; `#12579`)* |
| `[:REGISTERED_FOR]` | `(Person)→(Event:Live_*)` | 1,982 | 65 |

Both work-product edges are **recency-graded** (newer work = stronger = lower weight). `CONTRIBUTED_TO`: **40** current/ongoing, **35 / 30 / 25 / 20** for ending ≤1 / ≤2 / ≤3 / ≤4 years ago, **15** older. `AUTHORED`: **25** current-year, **22** ≤2y, **18** ≤4y, **15** older.

### Other / artifacts

| Edge | Pattern | Live edges | Weight |
| --- | --- | --- | --- |
| `[:WAS_FOUNDED]` | `(contextFounded)→(Company)` | 6 | 0 |

**Notes on weights**

- `[:WORKS_AT]` spans **5–40** across 14 distinct weights — it folds seniority *and* current-vs-past into one edge. There is no separate `WORKED_AT`; **past roles live here at weight ~40** (the 40 bucket, 2,385 edges, is the largest).
- `[:HAS_EXPERTISE]` spans **22–50** (a relevance / confidence gradient) and is the highest-volume edge by far (16,695).
- `[:INVESTED_IN]` weight is pattern-dependent: **15** Person→Company (work-derived — a defunct #2800 branch since v2.5; legacy edges linger), **25** Person→Company (the live direct angel-portfolio edge, `#12830`), **20** VC_Firm→Funding_Round, **25** Person/Angel→Funding_Round.
- `[:LOCATED_IN]` snapshot is mostly **10** (11,925 edges); 373 carry **50** — both now superseded by the §3.6 write values **70 / 80** (creators `#4528` / `#4690`). `VOLUNTEERED_AT` is **38** ongoing / **50** ended (`#4575`). `AUTHORED` is **15–25**, recency-graded — see [Content, work products & events](#content-work-products--events).

### Weight audit — applied adjustments

<Check>
  **✅ Applied as fix-forward (2026-05-31).** Every edge creator below now writes the canonical value. Fix-forward code lives on branch `v1` and is shared across data sources, so **new** edges are already written at the corrected weights for both sandbox and live enrichment. The **existing live-graph data** is reweighted by the data-source-aware backfill `tool/backfill-edge-weights` (#13063) — sandbox-first, with the live run held. See [edge-audit §3.6](/guides/robert-mark/edge-audit) for the backfill runbook and tool cards.
</Check>

<Warning>
  **Why this mattered: hub-node edges were the strongest weights in the graph, which inverted relationship ranking.** The lowest weights all sat on high-degree hub nodes — awards (`0.3`–`0.7`) and city via `LOCATED_IN` (`10`) — making any two people who share an award or a city *more strongly connected than co-founders* (`FOUNDED` = 5). The adjustments below move every hub edge into the Weak band (or below) so structural bonds out-rank incidental co-attribute ties.
</Warning>

| Edge(s) | Live (old) | Canonical (new) | Fix-forward | Backfill |
| --- | --- | --- | --- | --- |
| `HAS_AWARD` | 0.5 | **40** | ✅ `#2334` | `#13063` |
| `WORKED_ON` | 0.5 | **30** | ✅ `#2334` / `#13013` / `#13015` | `#13063` |
| `WON_AWARD` | 0.3 | **30** | ✅ `#2334` | `#13063` |
| `NOMINATED_FOR` | 0.7 | **35** | ✅ `#2334` | `#13063` |
| `LEAD_INVESTED_IN` | 30 | **15** | ✅ `#12702` | `#13063` |
| `FOLLOW_ON_INVESTED_IN` | 25 | **20** | ✅ `#12702` | `#13063` |
| `LOCATED_IN` (→ City / Region / State) | 10 | **70** | ✅ `#4528` / `#4690` | `#13063` |
| `LOCATED_IN` (→ Country, direct) | 50 | **80** | ✅ `#4528` / `#4690` | `#13063` |
| `HAS_SUBDOMAIN` | `null` | **100** + metadata envelope | ✅ `#12663` / `#13142` / `#13161` | `#13063` for legacy weight; one-time sandbox metadata repair on 2026-07-04 |
| `WAS_FOUNDED` | 0 | **1** *(accepted by design)* | ✅ `#12700` v3.2 (2026-07-02) | superseded — hub kept per Mark; old w0 hubs self-heal on re-enrichment |
| `KNOWS` | 60 (flat) | 60 default → ~8 floor | ✅ creator already writes 60 (`#12778`); decay cron = follow-up | n/a |
| `WORKS_AT` | 5–40 | unchanged — [buckets documented](#works_at-weight-buckets) | ✅ already canonical (`#2800`) | n/a |

**Rationale for the resolved values (the recommendations gave ranges):**

- **Awards / credits → Moderate band (30–40).** Sized so co-credit lands alongside `WORKS_AT` (past) / `HAS_EXPERTISE` / `ATTENDED` — present but never out-ranking founding or family. `WON_AWARD` (30) is slightly stronger than `NOMINATED_FOR` (35) — winning is a tighter tie than a nomination — and `HAS_AWARD` (40, the Film→Award hub edge) is the weakest of the set. `WORKED_ON` = 30 also fixes the 0.5 that was an outright error vs its spec intent.
- **Investment-round monotonicity restored.** Conviction now orders correctly: `LEAD_INVESTED_IN` (15) < `INVESTED_IN` (20) ≤ `FOLLOW_ON_INVESTED_IN` (20) — leading a round is the strongest signal (lowest weight) and a follow-on is no weaker than the initial check. Angel `INVESTED_IN` → Funding_Round stays **25**; Person → Company `INVESTED_IN` stays **15**.
- **`LOCATED_IN` demoted to Weak.** City is a massive hub; at 10 everyone in a city was near-maximally linked. Now **70** (city / region / state) and **80** (direct Person → Country, an even bigger hub = weaker tie). The `IN_REGION` (25) / `IN_COUNTRY` (50) taxonomy backbone is unchanged — those are structural geography edges, not affinity signals.
- **`HAS_SUBDOMAIN` = 100.** Taxonomy edges should never shorten a path between two people; an explicit Negligible weight removes the `null`-in-path-math ambiguity without marking the edge non-traversable. The 2026-07-04 follow-up also standardized its graph-QA metadata envelope across the relational sync, forward-path attach, and batch seeder.

<Note>
  **`contextFounded` is accepted by design (Mark, 2026-07-02).** The YC multi-founder branch of `process-yc-people` (#12700, seed-only via phase-3 #12799) deliberately writes a founding-event hub — `(Person)-[:FOUNDED w5]->(contextFounded)-[:WAS_FOUNDED w1]->(Company)` — because the hub node carries the LLM founding narrative **with a `name_embedding`**, making the founding story vector-searchable (direct edges would lose that; edge descriptions aren't indexed). This section's original purge premise ("no active creator") was wrong. The only defect was the sub-1 weight: **#12700 v3.2 sets `WAS_FOUNDED` to `1`** (identity-strength floor — founder→company path distance 5\+1, effectively matching a direct `FOUNDED` w5). Existing w0 hubs self-heal on re-enrichment via the Section-7 delete-then-recreate; `tool/backfill-purge-was-founded` (#13064) is **superseded — do not run it**. See [Company Edges](/guides/enrichment/waterfall/company-edges) \+ [Company Nodes](/guides/enrichment/waterfall/company-nodes).
</Note>

### KNOWS weight lifecycle

`KNOWS` is **evidence-driven**: it is created at a weak default and only strengthens (weight *lowers*) as interaction data accumulates. Weights ratchet **downward** from the default — they never start strong.

1. **Created at the default.** When Orbiter first detects a relationship with no interaction history, the edge is written at the **default weight `60`** (the weakest end of the band). This is why every live `KNOWS` is currently 60 — none have been decayed yet. **Confirmed in the creator** `known/ingest/create_knows_edge` (#12778), which writes `k.weight = 60` on `ON CREATE` and leaves the scoring fields null for the re-scoring cron to fill — so the §3.6 audit required **no fix-forward change** to the KNOWS creator.
2. **Crons lower it over time.** Background jobs re-score the edge as evidence lands — 1:1 emails, calendar events, reply rates, recency — and reduce the weight toward the floor. **Status:** the re-scoring cron that ratchets the weight down is a tracked **follow-up** — not yet built. Until it ships, every `KNOWS` stays at the 60 default (correct creation state, just not yet decayed).
3. **Floor ~8.** Even a maximally-active `KNOWS` stops around `BOARD_MEMBER_OF` / `MENTOR` strength, staying **below** the structural bonds (`FOUNDED` 5, family 1–10).

| Accumulated signal | Weight |
| --- | --- |
| No interaction data yet *(creation default)* | 60 |
| Minimal contact | 50 |
| Light contact (occasional email, or 1 event) | 40 |
| Regular contact (frequent email, or repeat events) | 25 |
| Close (high 1:1 email **and** multiple events) | 8–10 |

<Note>
  Filter automated / group / newsletter mail before it counts toward strengthening — raw volume over-inflates the signal. This **default-then-decay** pattern applies to any evidence-scored edge, not just `KNOWS`: create at the weakest default, let crons earn the weight down.
</Note>

### WORKS_AT weight buckets

`WORKS_AT` is the one professional edge whose weight is **computed, not a constant** — it folds *seniority* and *current-vs-past tenure* into a single value, which is why the live graph shows 14 distinct weights across the 5–40 range. The §3.6 audit asked for this mapping to be published rather than changed; the values are already canonical (no fix-forward, no backfill).

The formula in `mvp/edges/create-work-edges` (#2800) is:

```
weight = seniority_rank + (any role at this company is current ? 0 : 20)
```

`seniority_rank` is derived from the best (most senior, current-preferred) role title:

| Seniority (best role title) | rank | Current weight | Past weight (+20) |
| --- | --- | --- | --- |
| C-Suite — CEO/CTO/CFO/COO/CMO/CRO/CPO/CIO/CISO, or "Chief … Officer" | 5 | **5** | **25** |
| Founder / Co-Founder | 8 | **8** | **28** |
| Operating Partner (general / managing partner — *not* Limited Partner) | 8 | **8** | **28** |
| President (not Vice President) | 8 | **8** | **28** |
| SVP / EVP | 10 | **10** | **30** |
| VP / Vice President | 12 | **12** | **32** |
| Director / Head of | 15 | **15** | **35** |
| Manager / Lead / Principal | 18 | **18** | **38** |
| Individual Contributor (default) | 20 | **20** | **40** |

So a current C-suite role is the strongest `WORKS_AT` (5) and a past individual-contributor role is the weakest (40, the largest live bucket). The classifier also routes Founder titles to a parallel `FOUNDED` edge (weight 5), and Limited Partner / Angel / Advisor / Board / union-member titles to their specialized edges instead of `WORKS_AT` — see [edge-audit §3.3 / §3.7](/guides/robert-mark/edge-audit).

<Note>
  **Known quirk (out of scope for §3.6):** a *current* C-suite `WORKS_AT` lands at weight 5, tying `FOUNDED` in the **Defining** band. A future refinement could floor non-founder employment at the Very-strong band so founding stays strictly strongest; deferred so this pass changes only the values the audit flagged.
</Note>

## Planned

Designed edges that are **not yet built** in the live graph. Edges that turned out to be deployed under a different name, and obsolete / competing designs, have been removed. Names in <span style={{ color: "#16a34a", fontWeight: 600 }}>green</span> have a full specification (pattern, properties, `description` template, worked example) on [Edge Types](/guides/ontology/edges#person-relationship-edges); plain names are banded here only.

### Person → Person

#### Social & platform

| Edge | Weight | Description |
| --- | --- | --- |
| `[:INTRODUCED]` | 25 | Tracking warm introductions |
| `[:MUTUALLY_FOLLOWS]` | 100 *(normalized)* | Person A mutually follows Person B on a social platform like X |
| `[:CONNECTED_ON_LINKEDIN]` | 100 *(normalized)* | Person A is connected to Person B on LinkedIn |
| `[:CONNECTED_ON_ORBITER]` | 1 | Person A is connected to Person B on Orbiter (strongest platform connection) |

#### Family

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:PARENT_OF]</span> | 2 | Person A is parent of Person B — [spec](/guides/ontology/edges#parent_of); Deep Bio ledger `parent` / `child`, gated on `public_professional_context` |
| `[:SIBLING]` | 3 | Person A is sister / brother of Person B — two shared parents |
| `[:STEP_SIBLING]` | 5 | Person A is sister / brother of Person B — one shared parent |
| `[:RELATED_TO]` | 8 | Person A is related to Person B — uncle, cousin, etc. |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:MARRIED_TO]</span> | 4 | Person A is married to Person B — [spec](/guides/ontology/edges#married_to); Deep Bio ledger `spouse`, gated on `public_professional_context` |
| `[:PREVIOUSLY_MARRIED_TO]` | 10 | Person A was previously married to Person B |
| `[:IDENTICAL_TWINS]` | 1 | Identical twins (strongest possible family bond) |

#### Business & investment

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:COFOUNDED_WITH]</span> | 1 | Person A & B co-founded a company (extremely strong professional bond) — [spec](/guides/ontology/edges#cofounded_with); Deep Bio ledger `co_founder` |
| `[:BUSINESS_PARTNERS]` | 5 | Person A & B are partners for financial gain (NOT co-founders); includes co-owning a property, etc. |
| `[:STRATEGIC_PARTNERS]` | 10 | Person A & B actively refer business to each other |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:COLLABORATORS]</span> | 15 | Person A & B work together on a project (e.g. worked on a film, co-wrote a book, co-authored a white paper) — [spec](/guides/ontology/edges#collaborators); Deep Bio ledger `collaborator` |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:CO_INVESTORS]</span> | 20 | Person A & B invested in same company — same funding round attested — [spec](/guides/ontology/edges#co_investors) |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:CO_INVESTORS]</span> | 25 | Person A & B invested in same company — round unknown or different (the ledger default) |

#### Working relationships (Deep Bio ledger)

Attested ties only — a source names the two people together. Structural co-tenure (two `WORKS_AT` at one company) stays derived through the shared node at score time. Values set with Mark 2026-09-05; **pending §3.6 ratification** with Denis, like `DONOR_TO`.

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:REPORTED_TO]</span> | 15 / 25 | Person A reported to Person B at an organization — current / past. One step behind `MENTOR` (14), ahead of a peer colleague (20) — [spec](/guides/ontology/edges#reported_to) |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:COLLEAGUES_AT]</span> | 16 / 24 | Person A & B serve on the same board — current / past. 16 = the derived co-board path (`BOARD_MEMBER_OF` 8 \+ 8), so the attested edge never beats the structural one — [spec](/guides/ontology/edges#colleagues_at) |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:COLLEAGUES_AT]</span> | 20 / 28 | Person A & B named as working together at the same organization — current / past. The instructors-at-one-school case is this tier with a `School` as the shared node |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:SUCCEEDED_BY]</span> | 30 | Person A was succeeded by Person B in a named seat. Shared seat, often no overlap — `WORKED_ON`-grade — [spec](/guides/ontology/edges#succeeded_by) |

#### Representation

| Edge | Weight | Description |
| --- | --- | --- |
| `[:REPRESENTED_BY]` | 18 | Person A is represented by Person B — lawyer, agent, manager, broker |

#### Competitive & recognition

| Edge | Weight | Description |
| --- | --- | --- |
| `[:CO_NOMINATED_WITH]` | 40 | Shared recognition / nomination |

### Person → Company

| Edge | Weight | Description |
| --- | --- | --- |
| `[:REPRESENTED_BY]` | 10 | Person A is represented by Company (law firm, agency) |

### Person → School

| Edge | Weight | Description |
| --- | --- | --- |
| `[:TAUGHT_AT]` | 25 | Person taught / teaches at school (title, department, courses properties). Read by #1898 to derive `STUDIED_UNDER`, but no creator writes it yet. |

<Note>`[:ATTENDED]` (40) is **deployed** by `create-education-edges` #1898 — see [Education](#education).</Note>

### Person → Person (derived school relationships)

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:COLLEAGUES_AT]</span> | 20 | Two instructors taught at same school with overlapping years — now the `colleague` tier of the general edge above (28 once either tenure ends) |

<Note>`[:ATTENDED_TOGETHER]` (35 / 50 / 70) and `[:STUDIED_UNDER]` (10) are **deployed** by #1898 — see [Education](#education).</Note>

### Person / Company → Institution (philanthropy)

Designed in the [Donors](/guides/open-work/roadmap/donors) build plan (Enrichment Engine v1.2); weights derived 2026-08-25 against this page's scale, **pending §3.6 ratification**. Full property spec: [DONOR_TO](/guides/ontology/edges#donor_to). Recipient institutions are thousand-donor hubs, so every variant sits in the Moderate band or below — the awards-audit envelope — and none competes with `INVESTED_IN` (15), whose targets are small, selective nodes.

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:DONOR_TO]</span> (Person, direct gift) | 30 / 33 / 36 | Amount-graded: ≥\$10M / ≥\$1M / ≥\$100K floor. Ceiling = `WON_AWARD` (30), the strongest hub-target edge. |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:DONOR_TO]</span> (Foundation → institution) | 30, or 40 hub-damped | 40 when the foundation exceeds a grant-count threshold (mega-grantmakers are donor-side hubs — the `LOCATED_IN` bigger-hub-weaker-tie precedent). |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:DONOR_TO]</span> (Person via foundation, derived) | 38, or 48 damped | Path-neutral shortcut: `BOARD_MEMBER_OF` (8) \+ foundation edge — never beats its canonical path (the `ON_RECORD_LABEL` principle). |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:DONOR_TO]</span> (Company, corporate donor) | 35 | Diffuse, PR-driven giving — weaker than a personal gift. |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:DONOR_TO]</span> (honor-roll-only) | 50 *(reserved)* | No edges written in v1; band reserved ≈ `VOLUNTEERED_AT` ended. |

### Person / Company → Stage production (Broadway lane)

Set at the 2026-08-21 cross-lane §3.6 sitting; folded from the [Broadway co-producers](/guides/open-work/roadmap/broadway-co-producers) plan. Co-production pairs are never written — they derive through the production node from two credit edges.

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:PRODUCED]</span> (stage) | 12 / 16 / 22 / 28 / 35 | Producer credit on a Broadway / West End production, by billing tier: lead producer / producer / co-producer / associate or executive producer / in association. Pattern-dependent with the music `PRODUCED` (18) — [spec](/guides/ontology/edges#produced-stage) |

### Person → MediaAppearance / Event (media lane)

Set 2026-08-21; folded from [Media co-occurrence](/guides/open-work/roadmap/media-co-occurrence). Interview pairs are `COLLABORATORS`-class; conferences are hubs.

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:APPEARED_IN]</span> | 15 / 20 / 45 | Person appeared in a podcast episode, video or session — interview host / guest 15, co-panelist 20, conference speaker 45 — [spec](/guides/ontology/edges#appeared_in). Reconciled 2026-09-05: speaking is 15 below the speaker-count hub threshold (default 50) and 45 at or above it, for both `APPEARED_IN {role: speaker}` and the Live Events `SPOKE_AT` — the `LOCATED_IN` / `DONOR_TO` hub-damping idiom |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:PART_OF]</span> | 30 | `MediaAppearance` → `Event:Live_*` containment; the `RECORDING_OF` analogue — [spec](/guides/ontology/edges#part_of) |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:CO_MENTIONED_WITH]</span> | 55 *(evidence layer)* | Person A & B co-appear in news coverage; `excluded_from_scoring: true` — [spec](/guides/ontology/edges#co_mentioned_with) |

### Person → Company (federal filings lane)

Set 2026-08-21; folded from [990 / FEC / Form D](/guides/open-work/roadmap/990-fec-form-d). Nonprofit trustees and directors reuse the live `BOARD_MEMBER_OF` (8); co-officer and co-donor pairs derive through the shared node.

| Edge | Weight | Description |
| --- | --- | --- |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:OFFICER_OF]</span> | 14 / 20 | Officer / key employee of a nonprofit, from Form 990 Part VII — [spec](/guides/ontology/edges#officer_of) |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:RELATED_PERSON_OF]</span> | 15 / 20 | Executive officer or director / promoter named on an issuer's Form D — [spec](/guides/ontology/edges#related_person_of) |
| <span style={{ color: "#16a34a", fontWeight: 600 }}>[:POLITICAL_DONOR_TO]</span> | 60 *(evidence layer)* | Aggregated campaign contributions to a committee per cycle; committees are thousand-donor hubs; `excluded_from_scoring: true`. Renamed 2026-09-05 from the lane's `CONTRIBUTED_TO`, which collided with the live work-product edge (15–40) — [spec](/guides/ontology/edges#political_donor_to) |

### Person → VC Firm

| Edge | Weight | Description |
| --- | --- | --- |
| `[:LP_AT]` | 20 | Limited Partner relationship |
| `[:VENTURE_PARTNER_AT]` | 30 | Scout / venture partner role |

### Person → Funding Round

| Edge | Weight | Description |
| --- | --- | --- |
| `[:SOURCED]` | 25 | Person who sourced the deal |

### Person → Introduction

| Edge | Weight | Description |
| --- | --- | --- |
| `[:MADE_INTRO]` | 20 | The connector who made the introduction |
| `[:RECEIVED_INTRO]` | 100 *(normalized)* | The person requesting / receiving the introduction |
| `[:INTRODUCED_TO]` | 15 | The target of the introduction |

### Company → VC Firm

| Edge | Weight | Description |
| --- | --- | --- |
| `[:PORTFOLIO_OF]` | 20 | Portfolio company relationship (established after investment) |
| `[:PITCHED_TO]` | 60 | Pitch tracking (date, outcome: "passed" \| "termsheet" \| "pending") |

### Funding Round → Funding Round

| Edge | Weight | Description |
| --- | --- | --- |
| `[:PRECEDED]` | 50 | Chronological ordering (Series A preceded Series B) |
| `[:BRIDGED_TO]` | 30 | Bridge / extension rounds |
