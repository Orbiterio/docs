---
title: "Enrichment Crons"
description: "Deployment policy for load-bearing enrichment CRONs and the definitive queue-table drain build list."
icon: "clock-rotate-left"
---

<Note>
  **Task inventory reviewed: 2026-07-09. Deployment policy updated: 2026-07-27.** Task states, schedules, and stacks were audited against live Xano (workspace 3, branch `v1`); the deployment classification below comes from a follow-up audit of all 49 tasks.
</Note>

This page defines which enrichment CRONs must be deployed now and which entity-queue drain CRONs still need to be built.

<Warning>
  **Deployment decision: Only load-bearing CRONs should be deployed now—not entity queue drains.**

  Deploy the control-plane tasks required for a run to terminate or verify successfully. Keep person, company, IMDB entity, and MusicBrainz entity throughput drains inactive. Those drains only change how quickly a backlog moves; the load-bearing tasks below determine whether terminal receipts, parity readbacks, and sandbox wipe verification can ever complete.
</Warning>

## Load-bearing deployment set

The distinction is **control plane versus throughput**. Most queue drains only advance entity backlogs. The following tasks are different: without them, a run can remain pending or verifying forever.

| Task | Data source | Frequency | Why it is required |
| --- | --- | ---: | --- |
| **#192** `sweep-person-non-bio-terminal-retries` | `live` | 60 s | **Critical.** `enrich-master-person` reserves a `person_terminal_stage` receipt before phase 7, but nothing else executes it. Without #192, every person's terminal receipt remains pending forever. Since #13260 v1.33 (2026-07-30) each cycle also runs the stranded-leaf watchdog #13318: C-suite leaves have no receipt by design, so a leaf killed after the safety close is re-dispatched once per person per 24 h and parks loud on a second stranding. |
| **#191** `process-queue-imdb-award-graph` | `live` | 60 s | Required for terminal readback, not throughput. #13194 requires exact relational-table↔graph parity for every award with a `node_uuid`, with no queue tolerance on that branch. #13246 v1.6 reopens the row when a key changes; only #191 services it. |
| **#193 / #195** | `sandbox` | 360 s / 60 s | Sandbox twins of live Tasks #192 and #191. |
| **#194** `process-queue-musicbrainz-sandbox` | `sandbox` | 60 s | Sandbox QA only. It also acts as the wipe certifier; without it, wipes remain stuck at `verifying` forever. |

<Note>
  **Deploy now:** live Tasks **#192 and #191**. Where the sandbox QA environment is deployed, also deploy **#193, #195, and #194**. Task #191 has `queue` in its name, but it is a terminal parity/readback worker—not an entity-throughput drain.
</Note>

## Build Drain Crons

Build a drain CRON for every queue table below. Entity-throughput drain CRONs must ship **disabled** until a separate go-live decision. The immediate production exception is `queue_imdb_award_graph`, whose existing Task #191 is load-bearing and must be deployed now.

| Waterfall | Queue table | Xano ID | Deployment class |
| --- | --- | ---: | --- |
| Person | `queue_enrich_person` | **#582** | Entity throughput — build, deploy disabled |
| Company | `queue_enrich_company` | **#583** | Entity throughput — build, deploy disabled |
| Company | `queue_enrich_funding` | **#584** | Entity throughput — build, deploy disabled |
| Company | `queue_enrich_vc_firm` | **#585** | Entity throughput — build, deploy disabled |
| IMDB | `queue_imdb_person` | **#706** | Entity throughput — build, deploy disabled |
| IMDB | `queue_imdb_title` | **#707** | Entity throughput — build, deploy disabled |
| IMDB | `queue_imdb_award_graph` | **#764** | Load-bearing terminal readback — deploy Task #191 now |
| MusicBrainz | `queue_mb_artist` | **#745** | Entity throughput — build, deploy disabled |
| MusicBrainz | `queue_mb_recording` | **#746** | Entity throughput — build, deploy disabled |
| MusicBrainz | `queue_mb_label` | **#747** | Entity throughput — build, deploy disabled |
| MusicBrainz | `queue_mb_event` | **#748** | Entity throughput — build, deploy disabled |
| MusicBrainz | `queue_mb_release_group` | **#749** | Entity throughput — build, deploy disabled |
| MusicBrainz | `queue_mb_work` | **#750** | Entity throughput — build, deploy disabled |
| MusicBrainz / Person bridge | `queue_person_resolution` | **#751** | Entity throughput — build, deploy disabled |

Exact live schemas for all 14 tables are available on [Required Tables for Enrichment](/guides/archive/required-tables-for-enrichment).
